<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>一个 90 万阅读的技术帖，藏着 Agentic Engineering 的 22 条心法</title>
      <link href="/2026/06/08/agentic-engineering-hacks/"/>
      <url>/2026/06/08/agentic-engineering-hacks/</url>
      
        <content type="html"><![CDATA[<p>三个月前，Matt Van Horn 的《我知道的所有 Claude Code 技巧》在 X 上爆火。就在上周，他发了新帖《Every Agentic Engineering Hack I Know》，很快被 90 万人看过。</p><p>Matt 是谁？last30days（2.7 万星）、Printing Press（4000+ 星）、Agent Cookie 的作者。今年他成了 Python、Go、OpenClaw 等顶级开源项目的核心贡献者。他说自己从高中之后就没发布过别人觉得有价值的软件，直到今年。</p><p>帖子分享了 22 条心法，我按主题整理了一下，挑了几条最有感触的聊一聊。</p><hr><h2 id="一、先规划，再动手"><a href="#一、先规划，再动手" class="headerlink" title="一、先规划，再动手"></a>一、先规划，再动手</h2><p>Matt 的第一条规则是：有想法立刻写 plan.md，不是”让我想想”，不是”先写代码”。</p><p>但他紧接着说了第二条：<strong>别读 plan.md。</strong></p><p>这两条放在一起很有意思。他的逻辑是：强制写 plan 让 Agent 不偷懒——它会去研究、承诺方案、写下验收标准，然后真的去达成。有计划的 Agent 交付完成的工作，没有计划的 Agent 抄近路、提前停。</p><p>他原话是：<strong>“The plan is the leash.”</strong></p><p><strong>胖虎说：</strong></p><p>这个思路和我之前写的 Agent Spec 有点类似，但本质不同。Agent Spec 是把”行为的定义”从代码里抽出来单独管理，更像一份合同。而 Matt 的 plan.md 是工作流里的一个 checkpoint——<code>/ce-plan</code> 写完，<code>/ce-work</code> 执行，session 挂了也不怕，plan 还在。</p><p>他的观点更激进：<strong>plan 是给 Agent 看的，不是给你看的。</strong> 你只需要问一句 TLDR，然后让它去干活。这个”信任但验证”的度，值得琢磨。</p><blockquote><p>其他相关条目：非工程工作（战略文档、产品 spec、竞品分析）也可以用同一条 loop；先让 Agent 规划怎么规划，再做正事。</p></blockquote><hr><h2 id="二、怎么把活高效喂给-Agent"><a href="#二、怎么把活高效喂给-Agent" class="headerlink" title="二、怎么把活高效喂给 Agent"></a>二、怎么把活高效喂给 Agent</h2><p>Matt 的做法是同时开 4-6 个 cmux 会话，每个跑不同任务：一个写 plan、一个 build、一个做研究、一个修 bug。一个窗口在研究时，切到另一个窗口执行，变成多线程调度 Agent。</p><p>他还提到一个细节：<strong>新终端标签页默认直达 Claude Code</strong>，不需要 cd、不需要敲命令。当启动一个新 session 的成本降到一次快捷键，你会开更多 session。</p><p><strong>胖虎说：</strong></p><p>这个”降低摩擦”的思路值得借鉴。很多时候我们不是不想用工具，是启动成本太高。把默认入口改了、把权限跳过了、把远程控制打开了——这些小事累积起来，就是效率差距。</p><blockquote><p>其他相关条目：语音做主输入（Monologue&#x2F;Wispr Flow）、跳过权限确认（”YOLO，GitHub 会帮我兜底”）、远程控制 + 邮箱入口（给 Agent 一个邮箱地址，发邮件就是开任务）。</p></blockquote><hr><h2 id="三、Agent-强不强，看你喂了多少上下文"><a href="#三、Agent-强不强，看你喂了多少上下文" class="headerlink" title="三、Agent 强不强，看你喂了多少上下文"></a>三、Agent 强不强，看你喂了多少上下文</h2><p>Matt 提到一个很实用的做法：<strong>跑 <code>/ce-plan</code> 之前，先跑 <code>/last30days</code> 做研究。</strong></p><p>这是他自己的开源项目。举个例子：在 Vercel 的 agent-browser 和 Playwright 之间做选择，他没先读文档，直接跑：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">/last30days Vercel agent browser vs Playwright</span><br></pre></td></tr></table></figure><p>几分钟内 Reddit、X、YouTube、HN 等平台的讨论被并行抓回来。结果是 agent-browser 每次调用吃掉的上下文更少，而 Playwright 光工具定义就灌进去几千 token。再把这批结果喂给 <code>/ce-plan</code>，做出来的 plan 直接站在社区最近 30 天的真实经验上，而不是模型六个月前的训练数据。</p><p>他的另一个做法是：<strong>会议别自己总结，原始 transcript 直接扔进去。</strong></p><p>用 Granola 全程录音，结束后不整理，直接把完整 raw transcript 丢给 Claude Code。原始对话里的跑题、停顿、插科打诨，模型会自己判断哪些留下、哪些忽略——不需要你先替它总结一遍。</p><p>他还说：<strong>同时跑多个 Agent 时，你负责给信号。</strong></p><p>原文标题叫 Human Signal。Agent 提供产量，人提供品味、方向和判断。你要做的是不断给出反馈——“第二版更接近了，但把第一版的语言拿回来””先处理最大的风险””这个方向不对”。</p><p>原话是：<strong>“Agents supply volume, you supply taste.”</strong></p><blockquote><p>其他相关条目：笔记做成 Agent 的知识库（找一个带 CLI 的笔记系统，让 Agent 能读进去）。</p></blockquote><hr><h2 id="四、让-Agent-走出终端"><a href="#四、让-Agent-走出终端" class="headerlink" title="四、让 Agent 走出终端"></a>四、让 Agent 走出终端</h2><p>Matt 的两个项目很有意思：</p><p><strong>Printing Press</strong> 把真实世界的服务包装成 CLI，让 Agent 能直接调用——比如给 Tesla 预热、在 Instacart 下单。配套的 <strong>Agent Cookie</strong> 把浏览器 session 交给 CLI，让 Agent 带着你的登录态行动。</p><p>他的判断是：<strong>工作方式会不会变，很大程度取决于能不能把自己每天在用的服务印成 Agent 可调用的接口。</strong></p><p>他还提到一个很实用的协作方式：<strong>plan.md 给 Agent 看，Proof 给同事看。</strong></p><p>plan.md 在终端里好用，直接拿给同事看不顺手。把 plan 或 spec 丢进 Proof，生成一个链接，同事像看文档一样阅读和 inline 评论，评论还能再流回 Agent 的工作流。比把 Markdown 粘到 Slack 里强。</p><blockquote><p>其他相关条目：任何做超过两次的事写成 skill；开源贡献也走同一条 loop。</p></blockquote><hr><h2 id="五、最后一条提醒"><a href="#五、最后一条提醒" class="headerlink" title="五、最后一条提醒"></a>五、最后一条提醒</h2><p>帖子最后，Matt 坦诚地说：<strong>AI Psychosis 是真实存在的。</strong></p><p>Agent 本应帮我们干活，但每个朋友都比以前更拼命。他把这套 loop 形容成反馈极快的视频游戏——你说一句，东西就长出来；改一句，结果又更好。</p><p><strong>“Building with agents is the greatest video game ever made.”</strong></p><p>风险不是做出来的东西没人用，而是沉浸在 build 的兴奋里，把身边的人和真正重要的关系弄丢了。</p><p><strong>胖虎说：</strong></p><p>22 条”怎么更高效”的 hack 之后，最后一条是”小心别把自己烧没了”。这种坦诚很难得。</p><p>效率提升的尽头不是”做更多”，而是”想清楚做什么”。这个道理在 AI 时代反而更值得记住。</p><hr><h2 id="写在最后"><a href="#写在最后" class="headerlink" title="写在最后"></a>写在最后</h2><p>看完这 22 条，我觉得最值得记住的是这几点：</p><ol><li><strong>Plan-first 不是新东西，但 Agent 让它变得真正可行</strong>——以前 plan 和 code 是脱节的，现在 plan 直接驱动执行</li><li><strong>拉开差距的往往是上下文</strong>——截图、issue、会议录音、过去的笔记，这些东西能稳定流进 Agent，效果完全不一样</li><li><strong>人的位置在上移</strong>——你做的越来越像调度：给信号、给判断、给品味、给取舍</li><li><strong>越能 build，越要小心 build 带来的成瘾感</strong></li></ol><hr><p>原文地址：<a href="https://x.com/mvanhorn/status/2061877533885473181">https://x.com/mvanhorn/status/2061877533885473181</a></p>]]></content>
      
      
      <categories>
          
          <category> AI </category>
          
      </categories>
      
      
        <tags>
            
            <tag> AI </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>如何让 AI 快速接入业务系统？</title>
      <link href="/2026/05/26/mcp-gateway-v2/"/>
      <url>/2026/05/26/mcp-gateway-v2/</url>
      
        <content type="html"><![CDATA[<h2 id="一、背景：AI-融入业务的”最后一公里”"><a href="#一、背景：AI-融入业务的”最后一公里”" class="headerlink" title="一、背景：AI 融入业务的”最后一公里”"></a>一、背景：AI 融入业务的”最后一公里”</h2><p>当我们把 AI 引入业务系统时，绕不开一个问题：<strong>AI 怎么和现有系统对话？</strong></p><p>目前最主流的答案是 MCP（Model Context Protocol）。AI 通过调用 MCP 工具来完成业务操作，这个方向是对的。但把 AI 引入业务系统，有两道坎绕不开。</p><p><strong>第一道是接入成本：</strong> 业务系统的接口成百上千，但如果每个接口都要单独做 MCP 改造，不仅工作量巨大，还要动业务代码，推进几乎不可能。</p><p><strong>第二道是调用安全：</strong> Agent 绕开了所有前端页面的逻辑控制，直接触达后端接口。接入之后，AI 会在什么时机、以谁的身份、调用哪些接口——如果这些问题没有答案，生产环境根本不敢上。</p><p>效率问题决定”能不能做”，安全问题决定”敢不敢上”。</p><p>我们的解法是：<strong>在业务网关层统一做 MCP 适配，并在同一层建立三层递进的鉴权体系。</strong> 接下来详细拆解：</p><hr><h2 id="二、为什么选择网关层"><a href="#二、为什么选择网关层" class="headerlink" title="二、为什么选择网关层"></a>二、为什么选择网关层</h2><p>网关天然是所有业务请求的必经之路，它已经维护了完整的服务注册信息和接口路由规则。在网关层做 MCP 适配，意味着：</p><ul><li><strong>零额外部署</strong>：不需要新服务，在现有网关上加插件</li><li><strong>复用现有基础设施</strong>：路由、负载均衡、熔断、监控全部复用</li><li><strong>一次接入，全局覆盖</strong>：所有已注册到网关的接口，理论上都可以转换成 MCP 工具</li><li><strong>鉴权体系统一</strong>：在同一个入口做统一鉴权，而不是每个业务系统各自把关</li></ul><p>网关层的代价是插件复杂度上升，但相比分散建设的维护成本，这是值得的。</p><hr><h2 id="三、核心设计：网关-MCP-插件"><a href="#三、核心设计：网关-MCP-插件" class="headerlink" title="三、核心设计：网关 MCP 插件"></a>三、核心设计：网关 MCP 插件</h2><h3 id="3-1-MCP-服务注册"><a href="#3-1-MCP-服务注册" class="headerlink" title="3.1 MCP 服务注册"></a>3.1 MCP 服务注册</h3><p>首先，我们在网关的管理服务中建设了 <strong>MCP 工具管理能力</strong>，用于定义 MCP 服务的元信息：</p><ul><li><strong>MCP 服务基础信息</strong>：供外部 Client 访问的 URL、服务介绍</li><li><strong>工具定义</strong>：每个工具的名称、参数定义、对应的业务接口</li></ul><p>这些工具都映射到已经注册在网关上的业务接口。配置完成后，一个 MCP 服务就”存在”了，不需要额外部署任何东西。</p><h3 id="3-2-请求转换流程"><a href="#3-2-请求转换流程" class="headerlink" title="3.2 请求转换流程"></a>3.2 请求转换流程</h3><p>网关中新增一个 <strong>MCP 插件</strong>，处理所有 MCP 协议请求。完整的处理链路如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">MCP 请求 /xxx/mcp</span><br><span class="line">    ↓</span><br><span class="line">根据 URL path 找到对应的 MCP 服务</span><br><span class="line">    ↓</span><br><span class="line">加载该 MCP 服务的注册信息（工具列表）</span><br><span class="line">    ↓</span><br><span class="line">解析 MCP 请求 body，获取要调用的工具名称</span><br><span class="line">    ↓</span><br><span class="line">根据工具名称找到对应的业务接口和业务服务</span><br><span class="line">    ↓</span><br><span class="line">将请求 path 替换为工具对应的接口 path</span><br><span class="line">    ↓</span><br><span class="line">将 MCP 请求 body 转换为业务接口所需的参数格式</span><br><span class="line">    ↓</span><br><span class="line">将转发目标从 MCP Service 替换为业务 Service</span><br><span class="line">    ↓</span><br><span class="line">完成请求转发，接收业务响应</span><br><span class="line">    ↓</span><br><span class="line">将业务响应转换为 MCP Response 返回给 Client</span><br></pre></td></tr></table></figure><p>整个过程对业务系统完全透明——业务服务收到的就是普通的 HTTP&#x2F;RPC 请求，感知不到 MCP 的存在。这使得任何已接入网关的接口，无需修改即可成为 MCP 工具。</p><hr><h2 id="四、工具质量：不只是”能跑”"><a href="#四、工具质量：不只是”能跑”" class="headerlink" title="四、工具质量：不只是”能跑”"></a>四、工具质量：不只是”能跑”</h2><p>接口能通是基础，<strong>工具能被 AI 正确理解和调用</strong>才是关键。MCP 工具的质量核心在于两点：工具描述的语义准确性，以及工具粒度的合理性。</p><h3 id="4-1-工具描述自动生成：从-Proto-到-MCP-工具-Schema"><a href="#4-1-工具描述自动生成：从-Proto-到-MCP-工具-Schema" class="headerlink" title="4.1 工具描述自动生成：从 Proto 到 MCP 工具 Schema"></a>4.1 工具描述自动生成：从 Proto 到 MCP 工具 Schema</h3><p>手工填写工具描述费时且容易与接口定义脱节。我们利用现有的 Protobuf 定义，实现了从 Proto 文件到 MCP 工具的自动推导。</p><p><strong>整体思路是两层分离、最终合并：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">Proto 文件</span><br><span class="line">    ├── 类型层（结构信息）  →  字段类型、嵌套关系、数组/枚举</span><br><span class="line">    └── 语义层（注释信息）  →  描述、是否必填、枚举值含义、约束条件</span><br><span class="line">                ↓</span><br><span class="line">        合并生成 JSON Schema</span><br><span class="line">                ↓</span><br><span class="line">        符合 MCP 协议规范的工具参数定义</span><br></pre></td></tr></table></figure><p><strong>第一步：RPC 方法 → MCP 工具</strong></p><p>解析 Proto 文件中的 RPC 方法定义，每个 RPC 方法映射为一个 MCP 工具：</p><ul><li><code>tool.name</code> ← 方法名（如 <code>UpdateAuthorInfo</code>）</li><li><code>tool.description</code> ← 方法注释（如 <code>// 更新作者基础信息</code>）</li></ul><p><strong>第二步：Request Message → 参数 Schema（类型层）</strong></p><p>递归遍历请求消息的所有字段，从 Proto 类型描述符自动推导 JSON Schema 类型：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">Proto 类型        →    JSON Schema 类型</span><br><span class="line">─────────────────────────────────────</span><br><span class="line">string/bytes      →    &quot;type&quot;: &quot;string&quot;</span><br><span class="line">int32/int64       →    &quot;type&quot;: &quot;integer&quot;</span><br><span class="line">float/double      →    &quot;type&quot;: &quot;number&quot;</span><br><span class="line">bool              →    &quot;type&quot;: &quot;boolean&quot;</span><br><span class="line">message（嵌套）   →    &quot;type&quot;: &quot;object&quot;, &quot;properties&quot;: &#123; ... &#125;</span><br><span class="line">repeated          →    &quot;type&quot;: &quot;array&quot;,  &quot;items&quot;: &#123; ... &#125;</span><br><span class="line">enum              →    &quot;type&quot;: &quot;string&quot;, &quot;enum&quot;: [&quot;VAL_A&quot;, &quot;VAL_B&quot;]</span><br></pre></td></tr></table></figure><p>嵌套 message 和 <code>repeated</code> 字段会递归处理，保留完整的层级结构。</p><p><strong>第三步：字段注释 → 语义信息（语义层）</strong></p><p>仅靠类型信息不够，AI 需要语义信息才能正确填参。我们通过正则匹配从字段注释中提取：</p><ul><li><strong>字段描述</strong>：注释正文直接作为 <code>description</code></li><li><strong>是否必填</strong>：识别注释中的 <code>@required</code> 标记</li><li><strong>枚举值含义</strong>：提取枚举常量的注释，补充每个枚举值的语义说明</li><li><strong>约束条件</strong>：识别 <code>@min</code>、<code>@max</code>、<code>@maxLength</code> 等标记，映射为 JSON Schema 的对应约束字段</li></ul><p>注释中未提供的语义信息，由 Proto 结构本身兜底补全。</p><p><strong>一个完整示例：</strong></p><figure class="highlight protobuf"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 更新作者等级</span></span><br><span class="line"><span class="comment">// @required author_id, level</span></span><br><span class="line"><span class="function"><span class="keyword">rpc</span> UpdateAuthorLevel(UpdateAuthorLevelRequest) <span class="keyword">returns</span> (Response)</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">message </span><span class="title class_">UpdateAuthorLevelRequest</span> &#123;</span><br><span class="line">  <span class="comment">// 作者 ID</span></span><br><span class="line">  <span class="type">string</span> author_id = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 目标等级 @enum JUNIOR=初级 SENIOR=高级 EXPERT=专家</span></span><br><span class="line">  AuthorLevel level = <span class="number">2</span>;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 变更原因 @maxLength 200</span></span><br><span class="line">  <span class="type">string</span> reason = <span class="number">3</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>自动生成的 MCP 工具参数 Schema：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;UpdateAuthorLevel&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;更新作者等级&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;inputSchema&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;object&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;required&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;author_id&quot;</span><span class="punctuation">,</span> <span class="string">&quot;level&quot;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;properties&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;author_id&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;string&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;作者 ID&quot;</span></span><br><span class="line">      <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;level&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;string&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;enum&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;JUNIOR&quot;</span><span class="punctuation">,</span> <span class="string">&quot;SENIOR&quot;</span><span class="punctuation">,</span> <span class="string">&quot;EXPERT&quot;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;目标等级：JUNIOR=初级, SENIOR=高级, EXPERT=专家&quot;</span></span><br><span class="line">      <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;reason&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;string&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;变更原因&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;maxLength&quot;</span><span class="punctuation">:</span> <span class="number">200</span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p><strong>人工修正兜底</strong></p><p>自动生成解决了”有没有”的问题，但部分场景仍需人工介入：</p><ul><li><strong><code>oneof</code> 字段</strong>：多个互斥参数对 AI 语义不清晰，建议人工补充”三选一”的逻辑说明</li><li><strong>深层嵌套结构</strong>：超过两层的嵌套参数，AI 理解准确率会明显下降，可以考虑将常用子字段”打平”描述</li><li><strong>隐含业务约束</strong>：比如”author_id 必须是当前登录用户名下的作者”这类约束无法从 Proto 推导，需要人工在 description 中显式说明</li></ul><p>系统支持对自动生成的工具进行人工二次修改，修改后的版本优先于自动生成结果，下次 Proto 更新重新导入时会提示冲突，由人工决策是否覆盖。</p><h3 id="4-2-工具粒度设计"><a href="#4-2-工具粒度设计" class="headerlink" title="4.2 工具粒度设计"></a>4.2 工具粒度设计</h3><p>实践中我们遇到一个典型问题：<strong>接口的技术边界和工具的语义边界不一致</strong>。</p><p>最常见的情况是：一个”大而全”的修改接口，可以修改几十个字段，对应十几种不同的业务场景。如果把它直接暴露为一个 MCP 工具，AI 面对复杂的参数列表往往不知道在具体场景下该填什么。</p><p>我们的解法是：<strong>按场景将一个接口拆分成多个垂直工具</strong>，每个工具只暴露当前场景相关的字段，但底层仍调用同一个接口。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">/update_author（底层接口）</span><br><span class="line">    ├── update_author_contact   ← 修改联系方式</span><br><span class="line">    ├── update_author_bio       ← 修改个人简介</span><br><span class="line">    └── update_author_status    ← 修改状态</span><br></pre></td></tr></table></figure><p>好处显而易见：工具意图明确、参数简单、AI 调用准确率更高，鉴权也可以细化到每个工具维度。</p><p>对于需要多接口编排的复杂场景，我们通过<strong>原生 MCP 工具封装</strong>或 <strong>Skill</strong> 的方式来解决，而不是强行用单接口映射来覆盖。</p><h3 id="4-3-工具组织：以场景为单位，而非以服务为单位"><a href="#4-3-工具组织：以场景为单位，而非以服务为单位" class="headerlink" title="4.3 工具组织：以场景为单位，而非以服务为单位"></a>4.3 工具组织：以场景为单位，而非以服务为单位</h3><p>一个常见误区是：一个后端服务对应一个 MCP 服务。</p><p>我们的设计打破了这个限制——<strong>一个 MCP 服务的工具可以来自不同的后端业务服务</strong>。<br>网关本身就维护所有服务的路由信息，工具对应的后端服务在转发时动态解析，<br>MCP 服务的边界与后端服务的边界完全解耦。</p><p>这样就可以以「Agent 的任务域」为单位来组织 MCP 服务，而不是受制于后端服务的划分：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">传统做法（服务视角）：</span><br><span class="line">  作者服务    →  MCP服务A</span><br><span class="line">  内容服务    →  MCP服务B      ← Agent 需要同时接入三个</span><br><span class="line">  权限服务    →  MCP服务C</span><br><span class="line"></span><br><span class="line">我们的做法（场景视角）：</span><br><span class="line">  作者服务  ┐</span><br><span class="line">  内容服务  ├→  「作者管理」MCP服务  ← Agent 只需接入一个</span><br><span class="line">  权限服务  ┘</span><br></pre></td></tr></table></figure><p>一个处理「作者管理」任务的 Agent，只需接入一个 MCP 服务，就能获取到跨多个后端服务的所有工具，工具组织的逻辑从「我有哪些服务」变成了「Agent 需要完成什么任务」。</p><p>结合前面提到的纵向拆分，完整的工具组织设计有两个维度：</p><ul><li><strong>纵向</strong>：大接口 → 按场景拆成多个垂直工具（一接口 → 多个垂直工具）</li><li><strong>横向</strong>：多个后端服务 → 按任务域聚合进一个 MCP 服务（多个后端服务 → 一个MCP 服务）</li></ul><p>两个维度结合，才能构建出真正以 Agent 为中心的工具体系。</p><hr><h2 id="五、鉴权体系：企业级-AI-不能没有安全护栏"><a href="#五、鉴权体系：企业级-AI-不能没有安全护栏" class="headerlink" title="五、鉴权体系：企业级 AI 不能没有安全护栏"></a>五、鉴权体系：企业级 AI 不能没有安全护栏</h2><h3 id="为什么-AI-调用接口的风险更高"><a href="#为什么-AI-调用接口的风险更高" class="headerlink" title="为什么 AI 调用接口的风险更高"></a>为什么 AI 调用接口的风险更高</h3><p>人工操作有天然的摩擦力——用户需要登录、找到页面、填写表单、点击确认，每一步都是隐性的权限过滤。</p><p>但 Agent 没有这些，它绕开了传统前端页面的所有逻辑控制，直接触达后端接口。它可以在一次对话里连续调用十个接口，操作速度和规模远超人工，而且不会因为”这个操作看起来不对劲”而停下来。</p><p>这意味着传统的”界面即权限边界”假设在 AI 时代完全失效。</p><p>对此我们设计了三层递进的鉴权体系，并引入 ABAC 来处理复杂的业务级权限判断，以及 Human-in-the-Loop 机制来对高风险操作兜底。</p><h3 id="L1：Agent-身份验证"><a href="#L1：Agent-身份验证" class="headerlink" title="L1：Agent 身份验证"></a>L1：Agent 身份验证</h3><p>为每个 Agent 分配唯一 Token，确保请求来源合法，防止未授权的 AI 接入。这是最基础的一道门。</p><h3 id="L2：用户身份-接口级-ACL（RBAC）"><a href="#L2：用户身份-接口级-ACL（RBAC）" class="headerlink" title="L2：用户身份 + 接口级 ACL（RBAC）"></a>L2：用户身份 + 接口级 ACL（RBAC）</h3><p>调用 MCP 工具时必须携带用户的身份认证信息。由于工具与接口一一对应，我们复用现有的 RBAC 体系——只有对该接口有访问权限的用户，才能正常调用对应工具。</p><p>RBAC 解决的是”<strong>能不能进门</strong>“的问题：操作人的角色是否有权调用这个功能接口。</p><h3 id="L3：ABAC-细粒度鉴权"><a href="#L3：ABAC-细粒度鉴权" class="headerlink" title="L3：ABAC 细粒度鉴权"></a>L3：ABAC 细粒度鉴权</h3><p>RBAC 回答的是”这个角色能不能调这个接口”——是静态的、粗粒度的控制。RBAC 能过滤掉大多数非法调用，但有一类场景它无能为力：</p><blockquote><p>“只有某个作者的经纪人，或者经纪人的行政上级，才能修改其信息。”</p></blockquote><p>这类权限依赖于请求中具体对象的属性，无法用静态角色描述——RBAC 在这里触到了天花板，这正是 ABAC 的用武之地。ABAC 解决的是”<strong>进门后能不能动这张桌子</strong>“的问题。</p><p><strong>系统组件设计</strong></p><p>我们 ABAC 鉴权体系划分了四个角色：</p><table><thead><tr><th>组件</th><th>角色</th><th>核心职责</th></tr></thead><tbody><tr><td>PEP（网关）</td><td>策略执行点</td><td>拦截请求、提取身份、封装鉴权请求包、执行最终放行或拦截</td></tr><tr><td>PDP（鉴权服务）</td><td>策略决策点</td><td>解析 Policy、调度属性获取、执行规则判定、返回 Allow&#x2F;Deny</td></tr><tr><td>PIP（业务服务）</td><td>策略信息点</td><td>提供标准化属性查询接口，只吐数据，不判断权限</td></tr><tr><td>PAP（管理后台）</td><td>策略管理点</td><td>配置和分发 YAML 格式的鉴权策略</td></tr></tbody></table><p><strong>声明式属性依赖：一次请求，收齐所有属性</strong></p><p>ABAC 最容易出现的性能陷阱是：鉴权时反复向业务服务请求属性，每获取一个属性发一次请求，形成串行调用链。</p><p>我们的解法是<strong>声明式属性依赖</strong>：在 Policy 中预先声明这条策略需要哪些属性、从哪里获取，鉴权服务在执行前<strong>一次性并行拉取</strong>所有依赖属性，不做级联请求。</p><p>Policy 示例：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">policy_id:</span> <span class="string">&quot;author_sensitive_data_policy&quot;</span></span><br><span class="line"><span class="attr">target:</span></span><br><span class="line">  <span class="attr">resource_type:</span> <span class="string">&quot;author&quot;</span></span><br><span class="line">  <span class="attr">actions:</span> [<span class="string">&quot;view_resume&quot;</span>]</span><br><span class="line"></span><br><span class="line"><span class="comment"># 显式声明属性依赖，鉴权服务据此并行拉取</span></span><br><span class="line"><span class="attr">data_requirements:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">attribute:</span> <span class="string">&quot;resource.director_id&quot;</span></span><br><span class="line">    <span class="attr">provider:</span> <span class="string">&quot;AuthorService&quot;</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">attribute:</span> <span class="string">&quot;resource.director_id.manager_chain&quot;</span></span><br><span class="line">    <span class="attr">provider:</span> <span class="string">&quot;HRService&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="attr">rules:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">id:</span> <span class="string">&quot;is_director_self&quot;</span></span><br><span class="line">    <span class="attr">description:</span> <span class="string">&quot;操作人是该作者的经纪人&quot;</span></span><br><span class="line">    <span class="attr">condition:</span></span><br><span class="line">      <span class="attr">operator:</span> <span class="string">&quot;equals&quot;</span></span><br><span class="line">      <span class="attr">args:</span> [<span class="string">&quot;$.subject.id&quot;</span>, <span class="string">&quot;$.resource.director_id&quot;</span>]</span><br><span class="line">    <span class="attr">effect:</span> <span class="string">&quot;ALLOW&quot;</span></span><br><span class="line"></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">id:</span> <span class="string">&quot;is_director_manager&quot;</span></span><br><span class="line">    <span class="attr">description:</span> <span class="string">&quot;操作人是经纪人的行政上级&quot;</span></span><br><span class="line">    <span class="attr">condition:</span></span><br><span class="line">      <span class="attr">operator:</span> <span class="string">&quot;in&quot;</span></span><br><span class="line">      <span class="attr">args:</span> [<span class="string">&quot;$.subject.id&quot;</span>, <span class="string">&quot;$.resource.director_id.manager_chain&quot;</span>]</span><br><span class="line">    <span class="attr">effect:</span> <span class="string">&quot;ALLOW&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="attr">default_effect:</span> <span class="string">&quot;DENY&quot;</span></span><br></pre></td></tr></table></figure><p><strong>完整请求链路</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">① Agent 发起 MCP 请求（携带用户身份）</span><br><span class="line">         ↓</span><br><span class="line">② 网关 MCP 插件</span><br><span class="line">   - 根据 tool_name 查找映射（resource_type、action、resource_id_path）</span><br><span class="line">   - 用 JSONPath 从请求 body 提取 resource_id</span><br><span class="line">   - 封装标准鉴权请求包：&#123;subject_id, action, resource_type, resource_id&#125;</span><br><span class="line">         ↓</span><br><span class="line">③ RBAC 预校验（静态鉴权）</span><br><span class="line">   - 检查用户角色是否有权调用该接口</span><br><span class="line">   - 不通过 → 直接 403，终止</span><br><span class="line">         ↓</span><br><span class="line">④ ABAC 动态鉴权</span><br><span class="line">   - 鉴权服务读取 Policy 的 data_requirements</span><br><span class="line">   - 并行向各 PIP（业务服务）拉取所需属性</span><br><span class="line">   - 任一核心属性获取失败 → Fail-Close，直接拒绝</span><br><span class="line">   - 构建 Evaluation Context，执行规则判定</span><br><span class="line">         ↓</span><br><span class="line">⑤ 返回结果</span><br><span class="line">   - ALLOW → 放行请求至业务接口</span><br><span class="line">   - DENY → 返回策略名称（不暴露具体原因）</span><br></pre></td></tr></table></figure><p><strong>业务零侵入：PIP 只吐数据，不判逻辑</strong></p><p>业务服务只需实现一个通用属性查询接口：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">POST /v1/resource/attributes</span><br><span class="line">入参：resource_id, required_attributes[]</span><br><span class="line">出参：attributes: &#123; key: value &#125;</span><br></pre></td></tr></table></figure><p>业务系统完全不需要理解鉴权逻辑。权限规则的变更（例如：扩展到 GM 也可以操作），只需修改 Policy，无需修改任何业务代码。</p><p><strong>新增工具不需要改代码</strong></p><p>当需要上线新的 MCP 工具时，流程是：</p><ol><li>在 MCP 管理服务配置映射：<code>audit_article → {resource_type: article, action: audit}</code></li><li>在鉴权服务写一条 Policy</li><li>网关自动生效</li></ol><p>全程不需要修改网关代码，也不需要修改 Agent 的 Prompt。</p><p><strong>错误反馈设计</strong></p><p>L3 鉴权失败时，返回的是不通过的<strong>策略名称</strong>（如 <code>author_sensitive_data_policy</code>），而不是具体失败原因。这样 AI 能从名称推断出”这个操作需要特定权限”，做出合理响应，同时不暴露”你不是 XXX 的对接人”这类业务信息。</p><h3 id="工具风险控制"><a href="#工具风险控制" class="headerlink" title="工具风险控制"></a>工具风险控制</h3><p>三层鉴权解决了”有没有权限”的问题，但对于高风险操作，”有权限”不等于”应该直接执行”。</p><p>因此在鉴权体系之外，我们还设计了工具风险分级机制：在注册 MCP 工具时声明风险等级（low &#x2F; medium &#x2F; high）和是否需要人工确认，对高风险工具在执行前暂停 Agent、向审批人发起确认，审批通过后再继续，把人工确认嵌入 Agent 的执行链路进一步降低安全风险</p><hr><h2 id="六、踩过的坑，说给后来者"><a href="#六、踩过的坑，说给后来者" class="headerlink" title="六、踩过的坑，说给后来者"></a>六、踩过的坑，说给后来者</h2><p><strong>工具描述质量决定 AI 调用准确率</strong></p><p>从 PB 自动生成的描述解决了”有没有”的问题，但”好不好用”仍然需要人工打磨。特别是 <code>oneof</code> 字段、深层嵌套结构，自动生成的描述往往不够语义化，AI 容易理解偏差。工具上线前建议用真实的 Agent 调用测试一轮，发现问题及时补充描述。</p><p><strong>接口粒度 ≠ 工具粒度</strong></p><p>这是最容易忽视的问题。按接口 1:1 创建工具，短期省事，长期会导致工具参数复杂、AI 调用不稳定。从一开始就按语义场景拆分工具，收益远大于成本。</p><p><strong>鉴权不能事后补</strong></p><p>AI 调用业务接口的安全风险比人工调用更高——AI 可能在你意想不到的情况下触发敏感操作。三层鉴权体系需要在设计阶段就考虑清楚，而不是出了问题再打补丁。</p><p><strong>RBAC 和 ABAC 要分层，不要混在一起设计</strong></p><p>RBAC 控面（能不能调这个接口），ABAC 控点（能不能操作这个具体对象）。两者职责清晰，独立演进，互不干扰。如果把复杂的业务属性判断全塞入 RBAC 的角色定义里，维护成本会失控。</p><hr><h2 id="七、总结与展望"><a href="#七、总结与展望" class="headerlink" title="七、总结与展望"></a>七、总结与展望</h2><p>网关层 MCP 适配的核心价值在于：<strong>用基础设施的方式解决接入成本问题</strong>。</p><p>业务团队不需要关心 MCP 协议细节，不需要修改现有代码，只需要在管理界面配置工具映射，就能让 AI 访问业务能力。这种”一次建设，全局受益”的模式，是 AI 在企业场景大规模落地的基础之一。</p>]]></content>
      
      
      <categories>
          
          <category> AI </category>
          
      </categories>
      
      
        <tags>
            
            <tag> AI </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>ADK Go：Go 开发者终于等来了自己的 AI 智能体框架</title>
      <link href="/2026/05/11/ADK-v2/"/>
      <url>/2026/05/11/ADK-v2/</url>
      
        <content type="html"><![CDATA[<h2 id="一、引言"><a href="#一、引言" class="headerlink" title="一、引言"></a>一、引言</h2><p>过去一年，AI 智能体的热潮席卷全球。但如果你是 Go 开发者，心里多半是既兴奋又有点不是滋味。</p><p>兴奋的是，AI 的能力确实让人大开眼界；不是滋味的是，想用 Go 构建一个真正能上生产的 AI 应用，却发现满世界都是 Python 的炼丹教程。调 prompt 像念咒，结果时好时坏。单元测试怎么写？版本怎么管理？怎么和现有 Go 微服务优雅集成？一堆问题让人头大。</p><p>这种感觉，就像一个习惯了精密图纸和标准化流程的机械工程师，突然被扔进了满是玄学咒语的炼丹房。</p><p>直到 Google 开源了 <strong>ADK-Go（Agent Development Kit for Go）</strong>，并且迅速霸榜 GitHub Go 趋势榜一周多，我才意识到：转机来了。</p><hr><h2 id="二、为什么-Go-开发者需要-ADK？"><a href="#二、为什么-Go-开发者需要-ADK？" class="headerlink" title="二、为什么 Go 开发者需要 ADK？"></a>二、为什么 Go 开发者需要 ADK？</h2><p>先说 Go 本身的优势，这不是废话——它直接决定了 ADK Go 的价值天花板：</p><ul><li><strong>高并发</strong>：Goroutine 仅需几千字节内存，可处理数千个并发工具调用</li><li><strong>类型安全</strong>：静态类型消除了 LLM 输出解析的运行时错误</li><li><strong>云原生</strong>：天然适配容器化和微服务部署</li></ul><p>但更重要的是 ADK Go 的<strong>设计理念</strong>：它选择了「代码优先」，而不是拖拽配置、YAML 低代码那套。</p><p>所有逻辑用 Go 代码定义，版本控制、代码审查、单元测试一应俱全，和现有 Go 项目无缝集成。这才是 Gopher 应该有的工作方式。</p><hr><h2 id="三、核心概念：5-分钟搞懂架构"><a href="#三、核心概念：5-分钟搞懂架构" class="headerlink" title="三、核心概念：5 分钟搞懂架构"></a>三、核心概念：5 分钟搞懂架构</h2><p>ADK Go 采用<strong>事件驱动的运行时架构</strong>，一句话说清楚：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">用户请求 → Runner → 事件循环 → 模型调用 → 工具执行 → 返回结果</span><br></pre></td></tr></table></figure><p><strong>四个核心组件</strong>：</p><table><thead><tr><th>组件</th><th>是什么</th></tr></thead><tbody><tr><td><strong>Runner</strong></td><td>系统入口，管理整个执行生命周期</td></tr><tr><td><strong>Agent</strong></td><td>执行特定任务的智能体</td></tr><tr><td><strong>Tool</strong></td><td>智能体能调用的外部能力（函数、API 等）</td></tr><tr><td><strong>Session</strong></td><td>管理对话上下文和状态</td></tr></tbody></table><p><strong>三种智能体类型</strong>：</p><ul><li><strong>LLM Agent</strong>：最常用，背后是大语言模型</li><li><strong>Workflow Agent</strong>：控制执行流程（顺序 &#x2F; 并行 &#x2F; 循环）</li><li><strong>Multi-Agent</strong>：多个智能体协作，各司其职</li></ul><hr><h2 id="四、动手写第一个智能体"><a href="#四、动手写第一个智能体" class="headerlink" title="四、动手写第一个智能体"></a>四、动手写第一个智能体</h2><p>理论说够了，直接上代码。以「天气查询智能体」为例：</p><h3 id="环境准备"><a href="#环境准备" class="headerlink" title="环境准备"></a>环境准备</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> my_agent &amp;&amp; <span class="built_in">cd</span> my_agent</span><br><span class="line">go mod init my_agent</span><br><span class="line">go get google.golang.org/adk</span><br><span class="line"><span class="built_in">export</span> GOOGLE_API_KEY=<span class="string">&quot;your_api_key&quot;</span></span><br></pre></td></tr></table></figure><h3 id="核心代码"><a href="#核心代码" class="headerlink" title="核心代码"></a>核心代码</h3><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">    <span class="string">&quot;context&quot;</span></span><br><span class="line">    <span class="string">&quot;fmt&quot;</span></span><br><span class="line">    <span class="string">&quot;log&quot;</span></span><br><span class="line">    <span class="string">&quot;os&quot;</span></span><br><span class="line"></span><br><span class="line">    <span class="string">&quot;google.golang.org/adk/agent/llmagent&quot;</span></span><br><span class="line">    <span class="string">&quot;google.golang.org/adk/model/gemini&quot;</span></span><br><span class="line">    <span class="string">&quot;google.golang.org/adk/runner&quot;</span></span><br><span class="line">    <span class="string">&quot;google.golang.org/adk/tool&quot;</span></span><br><span class="line">    <span class="string">&quot;google.golang.org/adk/tool/geminitool&quot;</span></span><br><span class="line">    <span class="string">&quot;google.golang.org/genai&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    ctx := context.Background()</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 1. 初始化模型</span></span><br><span class="line">    model, err := gemini.NewModel(ctx, <span class="string">&quot;gemini-2.5-flash&quot;</span>, &amp;genai.ClientConfig&#123;</span><br><span class="line">        APIKey: os.Getenv(<span class="string">&quot;GOOGLE_API_KEY&quot;</span>),</span><br><span class="line">    &#125;)</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        log.Fatalf(<span class="string">&quot;Failed to create model: %v&quot;</span>, err)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 2. 创建智能体</span></span><br><span class="line">    agent, err := llmagent.New(llmagent.Config&#123;</span><br><span class="line">        Name:        <span class="string">&quot;weather_agent&quot;</span>,</span><br><span class="line">        Model:       model,</span><br><span class="line">        Description: <span class="string">&quot;回答城市天气问题的助手&quot;</span>,</span><br><span class="line">        Instruction: <span class="string">&quot;你是一个天气助手，使用 Google Search 获取实时天气信息。&quot;</span>,</span><br><span class="line">        Tools:       []tool.Tool&#123;geminitool.GoogleSearch&#123;&#125;&#125;,</span><br><span class="line">    &#125;)</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        log.Fatalf(<span class="string">&quot;Failed to create agent: %v&quot;</span>, err)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 3. 创建运行器并执行</span></span><br><span class="line">    r, err := runner.New(runner.Config&#123;Agent: agent&#125;)</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        log.Fatalf(<span class="string">&quot;Failed to create runner: %v&quot;</span>, err)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 4. 发起对话</span></span><br><span class="line">    result, err := r.Run(ctx, <span class="string">&quot;北京今天天气怎么样？&quot;</span>)</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        log.Fatalf(<span class="string">&quot;Failed to run: %v&quot;</span>, err)</span><br><span class="line">    &#125;</span><br><span class="line">    fmt.Println(result)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>三步：初始化模型 → 创建智能体 → 跑起来。和写普通 Go 代码没什么两样。</p><hr><h2 id="五、生产级特性：它凭什么说自己不是玩具？"><a href="#五、生产级特性：它凭什么说自己不是玩具？" class="headerlink" title="五、生产级特性：它凭什么说自己不是玩具？"></a>五、生产级特性：它凭什么说自己不是玩具？</h2><p>光能跑 demo 不算本事，ADK Go 真正有价值的是这几个生产级能力：</p><p><strong>① 原生 OpenTelemetry 追踪</strong></p><p>每个模型调用、每次工具执行都有结构化追踪，可以可视化智能体的”思维链”，调试复杂逻辑不再靠 print 大法。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">telemetryProviders, _ := telemetry.New(ctx, telemetry.WithOtelToCloud(<span class="literal">true</span>))</span><br><span class="line"><span class="keyword">defer</span> telemetryProviders.Shutdown(ctx)</span><br></pre></td></tr></table></figure><p><strong>② Retry and Reflect 插件</strong></p><p>工具调用失败？不用手动处理，插件会把错误反馈给模型，让它自动调整参数重试。某种意义上是”自愈”能力。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">r, _ := runner.New(runner.Config&#123;</span><br><span class="line">    Agent: myAgent,</span><br><span class="line">    PluginConfig: runner.PluginConfig&#123;</span><br><span class="line">        Plugins: []*plugin.Plugin&#123;</span><br><span class="line">            retryandreflect.MustNew(retryandreflect.WithMaxRetries(<span class="number">3</span>)),</span><br><span class="line">        &#125;,</span><br><span class="line">    &#125;,</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p><strong>③ Human-in-the-Loop</strong></p><p>敏感操作加一行 <code>RequireConfirmation: true</code>，执行前强制等人工确认，生产环境的救命稻草。</p><hr><h2 id="六、说实话：它的局限在哪？"><a href="#六、说实话：它的局限在哪？" class="headerlink" title="六、说实话：它的局限在哪？"></a>六、说实话：它的局限在哪？</h2><p>我不想写成一篇广告软文，所以必须把坑也说清楚：</p><ul><li><strong>生态还嫩</strong>：Python 版本的第三方工具丰富度碾压 Go 版本，遇到冷门需求大概率要自己造轮子</li><li><strong>主要优化 Gemini</strong>：虽然号称”模型无关”，但用其他模型可能踩坑，建议先验证兼容性</li><li><strong>学习曲线存在</strong>：代码优先的理念对 Go 开发者友好，但对 Python 背景的 AI 玩家反而增加了门槛</li><li><strong>Google 绑定风险</strong>：深度集成 GCP，如果你有多云需求，提前想好迁移成本</li></ul><hr><h2 id="七、什么人应该用它？"><a href="#七、什么人应该用它？" class="headerlink" title="七、什么人应该用它？"></a>七、什么人应该用它？</h2><p><strong>适合你，如果：</strong></p><ul><li>团队技术栈是 Go，想少踩迁移坑</li><li>要构建高并发的生产级 AI 服务</li><li>需要与现有 Go 微服务集成</li></ul><p><strong>暂时不适合，如果：</strong></p><ul><li>快速验证想法（Python 原型更快）</li><li>需要大量第三方工具集成（LangChain 生态更丰富）</li><li>团队完全不懂 Go</li></ul><hr><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>ADK Go 不是银弹，但对 Go 开发者来说，它是目前最工程化、最有生产诚意的 AI 智能体框架。</p><p>炼丹房的大门终于向 Gopher 打开了，而且这次进去，你会发现里面装了空调、贴了施工图纸，还备了灭火器。</p><p><strong>本周行动建议</strong>：花 30 分钟跑通上面那个天气 demo，感受一下「用写普通 Go 代码的方式写 AI 应用」是什么感觉。</p><hr><p><strong>参考资源：</strong></p><ul><li>官方文档：<a href="https://adk.dev/">https://adk.dev/</a></li><li>GitHub 仓库：<a href="https://github.com/google/adk-go">https://github.com/google/adk-go</a></li><li>Go 示例：<a href="https://github.com/google/adk-samples/tree/main/go">https://github.com/google/adk-samples/tree/main/go</a></li><li>Codelabs 教程：<a href="https://codelabs.developers.google.com/codelabs/agent-starter-pack-golang">https://codelabs.developers.google.com/codelabs/agent-starter-pack-golang</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> AI </category>
          
      </categories>
      
      
        <tags>
            
            <tag> AI </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>当用户说&quot;不对&quot;的时候，你的 Multi-Agent 系统会怎么做？</title>
      <link href="/2026/05/09/coordinator/"/>
      <url>/2026/05/09/coordinator/</url>
      
        <content type="html"><![CDATA[<blockquote><p>在多智能体协作系统中，Coordinator 是离用户最近、却最容易被低估的角色。它不执行任何具体任务，但整个系统的用户体验上限，取决于它。</p></blockquote><hr><h2 id="一、你需要一个-Coordinator"><a href="#一、你需要一个-Coordinator" class="headerlink" title="一、你需要一个 Coordinator"></a>一、你需要一个 Coordinator</h2><p>当你的 AI 系统从”一个模型打天下”演进到 Multi-Agent 架构时，一个新角色自然浮现——<strong>Coordinator</strong>。</p><p>想象一个大型医院的导诊台。患者走进来说”我头疼”，导诊护士不会自己做诊断，她的工作是：理解患者的诉求，判断应该挂哪个科室，然后把患者引导过去。如果她判断错了——把偏头痛送去了骨科——患者的就医体验直接崩盘，哪怕骨科的医生再厉害也没用。</p><p>Coordinator 就是 Multi-Agent 系统里的那个导诊台。</p><p>它的职责很简单：<strong>理解用户在说什么，找到最合适的 Agent 去处理</strong>。它不写文章、不分析数据、不生成报告。但如果它理解错了、派发错了，整个系统的能力就等于零。</p><p>这引出了 Coordinator 设计中最核心的一条原则：</p><p><strong>路由准确率，是 Coordinator 唯一的 KPI。</strong></p><hr><h2 id="二、我们的-Coordinator-架构"><a href="#二、我们的-Coordinator-架构" class="headerlink" title="二、我们的 Coordinator 架构"></a>二、我们的 Coordinator 架构</h2><p>在我们的实践中，Coordinator 与各个专业 Agent 之间通过 A2A（Agent-to-Agent）协议进行通信，以 Team 的形式组织协作。每个专业 Agent 独立部署、独立演进，可能由不同的团队甚至不同的组织开发。Coordinator 通过标准化的能力描述（Agent Card）来认识它们，通过统一的通信协议来调用它们。</p><p>这意味着 Coordinator 面对的是一个<strong>开放、异构、不完全可控</strong>的 Agent 生态。这是它与传统的”函数调用路由”最本质的区别。</p><p>为了在这样的生态中做出准确的路由决策，Coordinator 需要依赖的远不只是对当前这句话的理解。它需要<strong>感知用户所处的工作场景</strong>——用户是在热点工作台上，还是在内容分析页面？它需要<strong>追踪对话状态</strong>——上一轮任务派发给了哪个 Agent、那个 Agent 的回答中是否包含了对用户的追问？它需要<strong>记录路由历史</strong>——同一个话题是否已经连续两次被用户否定、已经尝试过哪些 Agent？</p><p>这些场景感知能力、状态追踪机制和路由记忆，共同构成了 Coordinator 的决策基础：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">┌─────────────────────────────────────────────────┐</span><br><span class="line">│                   用户交互层                      │</span><br><span class="line">│       自然语言理解、上下文管理、响应呈现             │</span><br><span class="line">├─────────────────────────────────────────────────┤</span><br><span class="line">│                   决策引擎层                      │</span><br><span class="line">│    意图识别、路由决策、消歧判断、纠错恢复            │</span><br><span class="line">├─────────────────────────────────────────────────┤</span><br><span class="line">│                  Agent 编排层                     │</span><br><span class="line">│    Agent 注册与发现、能力描述管理、通信协议           │</span><br><span class="line">├─────────────────────────────────────────────────┤</span><br><span class="line">│                 状态与记忆层                      │</span><br><span class="line">│  场景感知、委派历史追踪、对话状态、失败计数          │</span><br><span class="line">└─────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure><p>每一层都在为路由准确率服务。缺少任何一层，Coordinator 的决策质量都会显著下降。</p><hr><h2 id="三、第一道难关：对话不只有一轮"><a href="#三、第一道难关：对话不只有一轮" class="headerlink" title="三、第一道难关：对话不只有一轮"></a>三、第一道难关：对话不只有一轮</h2><p>如果每次对话都是独立的一句话，Coordinator 的工作就很简单——分析意图、匹配 Agent、派发任务。但现实中，用户的对话是连续的、有上下文的，充满了指代、省略和隐含信息。</p><h3 id="3-1-多轮对话中的身份危机"><a href="#3-1-多轮对话中的身份危机" class="headerlink" title="3.1 多轮对话中的身份危机"></a>3.1 多轮对话中的身份危机</h3><p>用户说了一个字——“是”。</p><p>这个”是”可能是：</p><ul><li>对上一轮 Agent 提问”是否需要详细分析？”的确认</li><li>对某个选项列表的选择</li><li>一个莫名其妙的首条消息</li></ul><p>Coordinator 必须在瞬间判断：<strong>这句话是给谁的？是延续上一轮对话，还是开启新话题？</strong></p><p>在单 Agent 系统中这不是问题——所有消息都给同一个 Agent 处理。但在 Multi-Agent 架构中，每一轮对话都是一次路由决策。判断错了，用户的确认被送给了一个完全不相关的 Agent，体验直接崩坏。</p><h3 id="3-2-我们的解法：输入分类优先级树"><a href="#3-2-我们的解法：输入分类优先级树" class="headerlink" title="3.2 我们的解法：输入分类优先级树"></a>3.2 我们的解法：输入分类优先级树</h3><p>经过大量真实对话的分析，我们设计了一个分层的输入分类框架。用户的每一次输入，Coordinator 会按优先级从高到低依次判断：</p><p><strong>第一优先级：纠错信号检测</strong><br>用户是否在表达”上一轮回答不对”？这个判断的优先级最高——因为它直接影响后续是”继续交给同一个 Agent”还是”需要换人”。一旦检测到纠错意图，必须立即进入纠错流程，而不是简单地延续上一轮对话。</p><p><strong>第二优先级：简短回复识别</strong><br>用户输入是否是一个简短的确认、否定或选择（”好的”、”1和3”、”取消”）？这类输入几乎必然是对上一轮对话的回应，Coordinator 应该保持对话的连贯性。</p><p><strong>第三优先级：延续与新话题判断</strong><br>当以上两层都未命中时，再通过话题关联性、指代信号、动作延续性等维度来判断是延续还是新话题。</p><p>为什么要分层？因为<strong>模糊情况下的默认行为不同</strong>。简短回复的默认行为是”继续给上一个 Agent”，而纠错信号的默认行为恰恰相反——“不要继续给上一个 Agent”。如果不分层，这两种相反的策略会在边界 case 中打架。</p><h3 id="3-3-指代消解：Coordinator-该做到哪一步"><a href="#3-3-指代消解：Coordinator-该做到哪一步" class="headerlink" title="3.3 指代消解：Coordinator 该做到哪一步"></a>3.3 指代消解：Coordinator 该做到哪一步</h3><p>用户说”帮我看看它的评论情况”——“它”是什么？只有 Coordinator 知道，因为只有它持有完整的对话历史。</p><p>但问题是，如果 Coordinator 做了太多的理解和改写工作，它就不再”薄”了。它可能改错、改多、改变了用户的原始意图。</p><p>我们的原则是：<strong>Coordinator 只做常识性的补全，领域判断留给专业 Agent。</strong></p><p>具体来说，Coordinator 负责：</p><ul><li>把代词替换成实体——“它”→”《春节运营复盘》”</li><li>补全省略的主语——“再分析一下”→”再分析一下昨天的爆款数据”</li><li>继承显式约束——上文确定的时间范围、筛选条件</li></ul><p>Coordinator 不负责：</p><ul><li>判断用户说的”效果不好”具体指哪个指标</li><li>推断用户想要什么粒度的分析</li></ul><p>这条线划得很清楚：<strong>人人都能做的常识性补全 → Coordinator；需要领域知识的解读 → 专业 Agent。</strong></p><p>以上这些，都建立在一个前提上：<strong>Coordinator 知道该把任务给谁</strong>。如果系统里只有寥寥几个 Agent、各司其职、边界清晰，路由决策并不难。但当 Agent 生态逐渐丰富，能力开始出现重叠，Coordinator 会遭遇第二道难关。</p><hr><h2 id="四、第二道难关：相似的-Agent，不同的场景"><a href="#四、第二道难关：相似的-Agent，不同的场景" class="headerlink" title="四、第二道难关：相似的 Agent，不同的场景"></a>四、第二道难关：相似的 Agent，不同的场景</h2><p>当你的 Agent 生态足够丰富时，一定会遇到这个问题：<strong>两个 Agent 看起来能做同一件事，但适用场景不同</strong>。</p><h3 id="4-1-真实案例"><a href="#4-1-真实案例" class="headerlink" title="4.1 真实案例"></a>4.1 真实案例</h3><p>在我们的系统中，有两个 Agent 都能对热点事件进行”拆解”：</p><ul><li><strong>Agent A</strong>：对指定热点事件做深度角度拆解，产出多个写作切入点</li><li><strong>Agent B</strong>：对热点事件做潜力评估，分析事件热度趋势和跟进价值</li></ul><p>用户说”帮我拆解一下这个热点”，该给谁？</p><h3 id="4-2-错误做法：硬编码默认值"><a href="#4-2-错误做法：硬编码默认值" class="headerlink" title="4.2 错误做法：硬编码默认值"></a>4.2 错误做法：硬编码默认值</h3><p>最容易想到的方案：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> 确定在场景 X:</span><br><span class="line">    route_to(Agent_A)</span><br><span class="line"><span class="keyword">else</span>:</span><br><span class="line">    route_to(Agent_B)  <span class="comment"># 默认兜底</span></span><br></pre></td></tr></table></figure><p>这个方案的问题在于：<strong>“没有明确在场景 X”不等于”用户想要 Agent B 的服务”</strong>。默认值是对用户意图的粗暴假设。</p><h3 id="4-3-我们的做法：多层消歧-场景感知"><a href="#4-3-我们的做法：多层消歧-场景感知" class="headerlink" title="4.3 我们的做法：多层消歧 + 场景感知"></a>4.3 我们的做法：多层消歧 + 场景感知</h3><p>我们的 Agent 系统具备<strong>用户场景感知能力</strong>——在用户发起请求时，系统会感知用户当前所处的工作场景（例如在哪个工作台、浏览什么页面），并将这个场景信息作为上下文提供给 Coordinator。</p><p>有了场景感知后，消歧规则变成了一个多层判断：</p><p><strong>第一层：确定性信号。</strong> 场景感知提供了明确的环境标识——用户就在某个特定工作台上，这是最强的路由信号，可以直接决策。</p><p><strong>第二层：表述特征。</strong> 当没有环境信号时，从用户的表述中提取意图特征。比如”有什么值得跟的热点”是探索性表述，倾向于潜力评估；”帮我把这个事件拆出几个写作角度”是明确的角度拆解需求。</p><p><strong>第三层：承认不知道。</strong> 当前两层都无法给出确定性判断时，Coordinator 不应该猜测——而是向用户追问。</p><p>这个设计背后有一个重要的认知：<strong>场景感知不是一个”可选的优化”，而是 Multi-Agent 路由系统的核心基础设施</strong>。当你的 Agent 生态越丰富，能力重叠越多，纯靠 NLU（自然语言理解）做路由的天花板就越低。场景信号是另一个维度的信息，它和语言理解互补，共同提升路由准确率。</p><h3 id="4-4-通用的消歧设计模式"><a href="#4-4-通用的消歧设计模式" class="headerlink" title="4.4 通用的消歧设计模式"></a>4.4 通用的消歧设计模式</h3><p>从这个案例中我们抽象出了一个通用的消歧处理模式：</p><ol><li><strong>识别冲突组</strong>：哪些 Agent 的能力存在重叠？</li><li><strong>定义区分特征</strong>：这些 Agent 在什么维度上不同？（场景、用户表述、操作对象……）</li><li><strong>建立有序规则</strong>：按信号强度从高到低排列判断条件</li><li><strong>设定兜底行为</strong>：所有规则都无法命中时，追问而非猜测</li></ol><p>每一对可能混淆的 Agent 都应该有自己的消歧规则。这些规则不应该散落在 Coordinator 的各处，而应该作为结构化的路由配置来维护——当 Agent 增减时，只需要更新配置，不需要重写 Coordinator 的核心逻辑。</p><hr><h2 id="五、第三道难关：派错了怎么办"><a href="#五、第三道难关：派错了怎么办" class="headerlink" title="五、第三道难关：派错了怎么办"></a>五、第三道难关：派错了怎么办</h2><p>在理想世界里，Coordinator 每次都能把任务派给对的 Agent，Agent 每次都能给出正确的回答。但现实是，错误不可避免。</p><p>更棘手的是，在我们的架构中，<strong>Coordinator 在 Agent 返回结果后没有”审阅”的机会</strong>。</p><h3 id="5-1-为什么选择”透传”"><a href="#5-1-为什么选择”透传”" class="headerlink" title="5.1 为什么选择”透传”"></a>5.1 为什么选择”透传”</h3><p>我们的系统选择了将专业 Agent 的输出直接呈现给用户，而不是经过 Coordinator 的二次加工。这个决策基于两个考量：</p><p><strong>保真度。</strong> 专业 Agent 精心组织的回答——表格、数据、格式化建议——如果经过 Coordinator 的”总结”和”转述”，信息必然会丢失或变形。Coordinator 的语言能力不一定比专业 Agent 强，但它”翻译”一遍一定会引入噪声。</p><p><strong>响应速度。</strong> 每多一次 LLM 推理就意味着额外的延迟和成本。在用户体感上，等待时间从 3 秒变成 6 秒，满意度会显著下降。</p><p>但透传的代价是：<strong>Coordinator 失去了”兜底”的机会</strong>。Agent 答错了、答偏了、甚至说了”我处理不了”——这些内容会原封不动地到达用户面前。</p><h3 id="5-2-如果不透传：Coordinator-后处理模式"><a href="#5-2-如果不透传：Coordinator-后处理模式" class="headerlink" title="5.2 如果不透传：Coordinator 后处理模式"></a>5.2 如果不透传：Coordinator 后处理模式</h3><p>另一种架构选择是让 Coordinator 在 Agent 返回后保留一次”审阅”的机会。在这种模式下，Agent 的输出先回到 Coordinator，Coordinator 可以：</p><ul><li><strong>结果审阅</strong>：快速判断 Agent 的回答是否与用户问题对得上——是不是答非所问、是否包含明显的拒绝或错误信号</li><li><strong>失败拦截</strong>：如果发现 Agent 返回了”抱歉我无法处理”或者回答明显偏离用户需求，Coordinator 可以拦截这个结果，悄悄重新路由到另一个 Agent，用户感知上只是”等了久一点”而不是”被告知处理不了”</li><li><strong>补充引导</strong>：在 Agent 回答的末尾追加一些引导语，帮助用户进行后续探索</li></ul><p>这种模式的纠错能力显著更强——Coordinator 可以在用户无感知的情况下完成一次自动重试。但它的代价也很明确：每次都多一轮 LLM 推理，增加延迟和成本；Coordinator 的”总结”可能扭曲 Agent 的原始输出；而且审阅本身也可能误判——把正确的回答当成了错误的。</p><p><strong>两种模式没有绝对的优劣，取决于你的系统更在意什么。</strong> 如果 Agent 的输出质量已经比较稳定、路由准确率较高，透传模式的高效和保真优势更大；如果 Agent 生态还在早期、质量参差不齐，后处理模式的纠错能力可能更重要。也可以折中——对高置信度路由走透传，对低置信度路由保留后处理。</p><p>我们选择了透传，因此需要在其他环节补上失去的纠错能力。</p><h3 id="5-3-透传模式下的失败检测：用户的下一句话"><a href="#5-3-透传模式下的失败检测：用户的下一句话" class="headerlink" title="5.3 透传模式下的失败检测：用户的下一句话"></a>5.3 透传模式下的失败检测：用户的下一句话</h3><p>既然 Coordinator 无法在 Agent 返回后审阅，那么失败检测只能发生在<strong>用户的下一轮输入</strong>。</p><p>这要求 Coordinator 对用户的”纠错信号”高度敏感。我们把纠错信号分为三类：</p><ul><li><strong>显式否定</strong>：”不对”、”错了”、”不是我想要的”——用户直接告诉你上一轮有问题</li><li><strong>隐式纠正</strong>：”我的意思是…”、”不是 XX，是 YY”——用户通过重新描述来纠正方向</li><li><strong>情绪反馈</strong>：”这不靠谱”、”没用”——用户对质量表达不满</li></ul><p>检测到纠错信号后，Coordinator 需要进一步判断：<strong>是”派错了人”，还是”找对了人但理解偏了”？</strong></p><p>这两种失败的补救策略完全不同：</p><ul><li><strong>派错了人</strong> → 重新路由到正确的 Agent，并在任务信息中附加纠错上下文</li><li><strong>理解偏了</strong> → 仍然交给同一个 Agent，但用纠正后的需求描述替换原始 query</li></ul><h3 id="5-4-防死循环机制"><a href="#5-4-防死循环机制" class="headerlink" title="5.4 防死循环机制"></a>5.4 防死循环机制</h3><p>纠错重试必须有边界。如果同一个话题连续两轮被用户否定，Coordinator 不应该继续自动路由——这时候大概率是 Coordinator 对用户意图的理解存在根本性偏差。正确的做法是停下来，明确追问用户想要什么，给出具体的选项供选择。</p><p>同时，Coordinator 需要记录已经尝试过的 Agent，避免在同一个话题中反复派给同一个失败的 Agent——这是死循环的常见来源。</p><h3 id="5-5-更根本的解法：减少出错"><a href="#5-5-更根本的解法：减少出错" class="headerlink" title="5.5 更根本的解法：减少出错"></a>5.5 更根本的解法：减少出错</h3><p>事后纠错终归是代价高昂的——它需要用户多说一轮话，需要 Coordinator 重新推理，需要另一个 Agent 重新处理。</p><p>与其把精力放在”错了怎么补”，不如把更多精力放在”怎么不出错”：</p><ul><li><strong>提高首次路由准确率</strong>：每一对容易混淆的 Agent 都建立消歧规则</li><li><strong>前置信息完整性检查</strong>：在派发之前确认信息是否充分，不够就先问</li><li><strong>保证 Query 质量</strong>：传递给 Agent 的请求要完整、无歧义、自洽</li><li><strong>宁可多问一句，不要派错一次</strong></li></ul><hr><h2 id="六、Coordinator-的设计原则"><a href="#六、Coordinator-的设计原则" class="headerlink" title="六、Coordinator 的设计原则"></a>六、Coordinator 的设计原则</h2><p>基于以上实践，我们沉淀了几条 Coordinator 的设计原则：</p><h3 id="原则一：Coordinator-要足够”薄”"><a href="#原则一：Coordinator-要足够”薄”" class="headerlink" title="原则一：Coordinator 要足够”薄”"></a>原则一：Coordinator 要足够”薄”</h3><p>Coordinator 只做两件事——<strong>理解</strong>和<strong>路由</strong>。所有超出这两件事的工作，都应该下放给专业 Agent。</p><p>一个常见的陷阱是往 Coordinator 里堆越来越多的逻辑：实体抽取、信息补全、格式化输出、质量校验……每加一项能力，Coordinator 的复杂度就上升一个台阶，它在每一项上的表现就被稀释一分。</p><p><strong>专注产生准确，臃肿制造混乱。</strong> 还记得 Coordinator 唯一的 KPI 吗？路由准确率。一个臃肿的 Coordinator，能力越多，路由越不准——这不是悖论，这是分心的必然代价。</p><h3 id="原则二：质量把控前置"><a href="#原则二：质量把控前置" class="headerlink" title="原则二：质量把控前置"></a>原则二：质量把控前置</h3><p>如果你的架构允许 Coordinator 在 Agent 返回后做审阅和补救，那是最好的。但如果像我们一样选择了透传模式，那么所有的质量把控都必须在派发之前完成。</p><p>这意味着：</p><ul><li>信息够不够？不够就追问，不要让 Agent 去猜</li><li>Query 清不清楚？有歧义就改写，不要把模糊的需求扔给 Agent</li><li>路由有没有把握？没把握就确认，不要赌</li></ul><p><strong>一次高质量的派发，胜过三次补救。</strong></p><h3 id="原则三：为失败设计"><a href="#原则三：为失败设计" class="headerlink" title="原则三：为失败设计"></a>原则三：为失败设计</h3><p>不要问”怎么保证不出错”——这在 LLM 系统中是不可能的。要问”出错后怎么最快恢复”。</p><p>这要求 Coordinator 在每一轮对话中都保持一种”可能上一轮是错的”的警觉。纠错信号的优先级要高于延续对话的判断——因为如果用户在说”不对”，而你还在继续往下走，那就是错上加错。</p><h3 id="原则四：Agent-的能力描述决定路由的上限"><a href="#原则四：Agent-的能力描述决定路由的上限" class="headerlink" title="原则四：Agent 的能力描述决定路由的上限"></a>原则四：Agent 的能力描述决定路由的上限</h3><p>Coordinator 做路由决策时，依赖的核心信息是每个 Agent 的能力描述。如果能力描述写得模糊、有重叠、边界不清，再聪明的 Coordinator 也做不出正确判断。</p><p>好的 Agent 能力描述应该包含：</p><ul><li><strong>能做什么</strong>：核心能力和擅长场景</li><li><strong>不能做什么</strong>：明确的边界和限制</li><li><strong>容易混淆的场景</strong>：与其他 Agent 的能力重叠区域和区分方法</li></ul><p><strong>投入在 Agent 能力描述上的精力，回报率远高于调优 Coordinator 的 Prompt。</strong></p><h3 id="原则五：决策逻辑与-Agent-知识分离"><a href="#原则五：决策逻辑与-Agent-知识分离" class="headerlink" title="原则五：决策逻辑与 Agent 知识分离"></a>原则五：决策逻辑与 Agent 知识分离</h3><p>Coordinator 的核心 Prompt 应该定义”如何做决策”——输入分类框架、路由优先级、纠错流程——这些是稳定的方法论，不随 Agent 的增减而变化。</p><p>而”认识哪些 Agent”、”它们的能力是什么”、”谁和谁容易混淆”——这些是动态的数据，应该随 Agent 列表一起注入上下文，而不是硬编码在 Prompt 中。</p><p>这种分离确保了：加入新 Agent 时，只需要补充能力描述和消歧规则，而不需要重写 Coordinator 的决策逻辑。</p><hr><h2 id="七、几点反思"><a href="#七、几点反思" class="headerlink" title="七、几点反思"></a>七、几点反思</h2><p>做了这么久 Coordinator，有几个认知值得分享：</p><p><strong>第一，Coordinator 是整个 Multi-Agent 系统中最需要”产品思维”的组件。</strong></p><p>它不是一个纯技术问题。理解用户在说什么、判断用户想要什么、决定在什么时候追问而不是猜测——这些本质上是产品设计决策。好的 Coordinator 设计者，需要同时具备 NLP 的技术功底和产品经理的用户同理心。</p><p><strong>第二，完美的路由不存在，但 80 分的路由加上好的纠错机制可以接近 95 分的体验。</strong></p><p>不要追求一次做对。要追求的是：做对的时候足够快，做错的时候用户能用最少的成本纠正你。</p><p><strong>第三，Coordinator 的 Prompt 不是越详细越好。</strong></p><p>我们经历过从 200 行到 500 行再精简回 250 行的过程。更长的 Prompt 不会带来更好的决策——反而会稀释 LLM 的注意力，让它在边界 case 上表现更差。<strong>每一条规则只应该出现一次，用引用而非重述。</strong></p><p><strong>第四，Multi-Agent 系统的用户体验瓶颈，几乎总是在 Coordinator。</strong></p><p>用户不会说”这个 Agent 不行”——他们会说”这个 AI 助手不行”。因为在用户眼里，他们只和一个角色对话。Coordinator 的任何失误——理解错了、派发错了、没有追问该追问的问题——都会直接伤害用户对整个系统的信任。</p><p>善待你的 Coordinator。它是用户和你精心构建的 Agent 生态之间，唯一的桥梁。</p><p>回到最初那句话：<strong>路由准确率，是 Coordinator 唯一的 KPI。</strong> 所有的架构设计、所有的原则取舍，最终都在回答同一个问题——用户的这句话，有没有送到对的地方。</p>]]></content>
      
      
      <categories>
          
          <category> AI </category>
          
      </categories>
      
      
        <tags>
            
            <tag> AI </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>在构建了这么多 Agent 之后，我发现了一个共同之处</title>
      <link href="/2026/04/09/agent-spec/"/>
      <url>/2026/04/09/agent-spec/</url>
      
        <content type="html"><![CDATA[<p>每次开始构建一个新的 Agent，我都会跟自己说：这次要设计得优雅一点。</p><p>然后大概写到第三个的时候，开始感觉不对劲。</p><p>不是功能不对，是结构开始重复——模型初始化的代码复制过来改一改，Prompt 直接硬编码在代码里，工具列表在构造函数里一个个手动注册。每个 Agent 看起来都挺独立，但你一眼看过去，这些代码基本上都长一个样。</p><p>更难受的是，你想改个 Prompt 试试效果，得改代码，然后重新部署。你想给某个 Agent 加一个新工具，也得改代码，再重新部署。Agent 还在跑着，但你没有办法在不停服的情况下更新它。</p><p>做过的人大概都有这种感受：<strong>明明每个 Agent 干的事情不一样，但构建和维护它们的痛苦却惊人的相似。</strong></p><hr><h2 id="这不只是工程问题，是认知问题"><a href="#这不只是工程问题，是认知问题" class="headerlink" title="这不只是工程问题，是认知问题"></a>这不只是工程问题，是认知问题</h2><p>在我真正意识到问题之前，我以为这只是「代码写烂了」的问题——多抽象几层，写个基类，加个接口，就解决了。</p><p>但后来我发现，这个判断本身就是错的。</p><p>因为我一直在<strong>用管理代码的方式去管理 Agent</strong>，而 Agent 根本不是普通的代码。</p><p>普通的代码，你写完它，它就确定了。但 Agent 是动态的：你每次调优 Prompt，Agent 的行为就变了；你给它换个模型，它的能力边界就变了；你改一组工具的组合方式，它能处理的任务范围就变了。<strong>Agent 的「身份」是可以独立于代码演进的</strong>——但只要 Agent 的定义还藏在代码里，这件事就做不到。</p><p>这才是真正的问题所在：我们把「Agent 是什么」和「Agent 怎么跑」混在了一起。</p><hr><h2 id="重复是因为我们没想清楚”变的”和”不变的”"><a href="#重复是因为我们没想清楚”变的”和”不变的”" class="headerlink" title="重复是因为我们没想清楚”变的”和”不变的”"></a>重复是因为我们没想清楚”变的”和”不变的”</h2><p>回头看这些重复，根源其实很清晰。</p><p>每个 Agent 里，有些东西是<strong>不变的</strong>：推理引擎、工具调用的执行逻辑、记忆的读写、上下文的组装方式——这些是通用的基础能力，每个 Agent 都需要，但每个 Agent 写一遍，是在重复造同一台发动机。</p><p>有些东西是<strong>变的</strong>：这个 Agent 用哪个模型、Prompt 是什么、挂了哪些工具、可以调用哪些子 Agent——这些是这个 Agent 区别于其他 Agent 的真正原因。</p><p>把变的和不变的混在一起写代码，是让 Agent 管理越来越难的根本原因。</p><p>但更关键的一步，是把这两类东西的<strong>生命周期也分开</strong>。</p><p>「不变的」那部分，走代码 review、工程测试、版本发布——节奏慢、成本高、风险低。「变的」那部分，应该走完全不同的通道：快速迭代、灰度验证、随时回滚——甚至不需要工程师介入，业务侧自己就能操作。</p><p>你不可能既要这种灵活性，又让两件事混在同一个代码库里。</p><hr><h2 id="解法：Agent-Spec-执行引擎"><a href="#解法：Agent-Spec-执行引擎" class="headerlink" title="解法：Agent Spec + 执行引擎"></a>解法：Agent Spec + 执行引擎</h2><p>这里借鉴一个大家都熟悉的思路。</p><p>k8s 里你不会直接去操作某个容器，而是写一份 YAML，告诉 k8s「我要什么」：用哪个镜像、要几个副本、资源限制是多少。具体怎么拉镜像、怎么调度、挂掉了怎么重启——你不用管，控制面帮你搞定。</p><p>你描述的是<strong>意图</strong>，不是过程。</p><p>Agent 完全可以照这个思路来。</p><p>给每个 Agent 写一份 <strong>Agent Spec</strong>——一份声明式的配置文件，描述这个 Agent 是谁。</p><p>举个例子，假设你在做一个电商客服系统，里面有一个专门处理售后问题的 Agent：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 售后客服 Agent 的 Spec</span></span><br><span class="line"><span class="attr">promptKey:</span> <span class="string">after-sales-assistant</span>     <span class="comment"># 用哪个 Prompt</span></span><br><span class="line"><span class="attr">model:</span> <span class="string">gpt-4o</span>                        <span class="comment"># 用哪个模型</span></span><br><span class="line"><span class="attr">mcpServers:</span>                          <span class="comment"># 挂哪些工具</span></span><br><span class="line">  <span class="bullet">-</span> <span class="string">order-query-service</span>              <span class="comment"># 订单查询</span></span><br><span class="line">  <span class="bullet">-</span> <span class="string">refund-system</span>                    <span class="comment"># 退款系统</span></span><br><span class="line">  <span class="bullet">-</span> <span class="string">logistics-tracker</span>                <span class="comment"># 物流追踪</span></span><br><span class="line"><span class="attr">partnerAgents:</span>                       <span class="comment"># 可以转交给哪些兄弟 Agent</span></span><br><span class="line">  <span class="bullet">-</span> <span class="string">complaint-escalation-agent</span>       <span class="comment"># 投诉升级 Agent</span></span><br><span class="line">  <span class="bullet">-</span> <span class="string">vip-service-agent</span>                <span class="comment"># VIP 专属服务 Agent</span></span><br><span class="line"><span class="attr">memory:</span></span><br><span class="line">  <span class="attr">storageType:</span> <span class="string">redis</span></span><br><span class="line"><span class="attr">maxIterations:</span> <span class="number">8</span></span><br></pre></td></tr></table></figure><p>一个通用的 <strong>Agent 执行引擎</strong>读取这份 Spec，动态组装出完整的运行时。这里有三个配套组件是让 Spec 真正能活起来的基础：</p><ul><li><strong>Prompt Center</strong>：Spec 里的 <code>promptKey</code> 只是一个指针，真正的内容在 Prompt Center 里管理——版本控制、灰度发布、效果对比都在那边，Prompt 能独立迭代的基础就在这里。</li><li><strong>MCP Registry</strong>：Spec 里的 <code>mcpServers</code> 只是工具名字，执行引擎通过 Registry 发现实际服务地址和协议——这是工具能标准化、可插拔的基础。</li><li><strong>Agent Registry</strong>：Spec 里的 <code>partnerAgents</code> 也只是名字，Agent 间能互相发现和调用，靠的是这层注册——多 Agent 协作能动态组合的基础。</li></ul><p><strong>Spec 是声明，三个 Registry 是实现。</strong> 没有这三个组件，Spec 就只是一份格式好看的配置文件；有了它们，Spec 才能成为真正驱动 Agent 运行的”意图描述”。</p><p>这套机制建立之后，日常的变更就变得极其简单：</p><p>你要新建一个「物流咨询 Agent」？照着写一份新的 Spec，换几个工具和 Prompt，启动引擎，完成。</p><p>你想针对春节大促优化一下售后话术？改 Prompt Center 里的版本，配个灰度比例，执行引擎监听到变更，热加载，不停服。</p><p>退款流程加了一个新的审批工具？在 Spec 里加一行工具名，MCP Registry 里注册一下，重载配置，生效。</p><p><strong>整个过程，没有改一行执行逻辑的代码。</strong></p><hr><h2 id="Spec-化带来的另一个推论：Agent-可以弹性扩缩容"><a href="#Spec-化带来的另一个推论：Agent-可以弹性扩缩容" class="headerlink" title="Spec 化带来的另一个推论：Agent 可以弹性扩缩容"></a>Spec 化带来的另一个推论：Agent 可以弹性扩缩容</h2><p>这是很多人没想到的一个推论，但它其实是 Spec 化最重要的工程价值之一。</p><p>逻辑链是这样的：</p><p><strong>Agent Spec 定义了 Agent 是谁 → 执行引擎动态组装运行时 → Agent 实例本身不需要持有任何状态。</strong></p><p>这里的关键是<strong>状态外置</strong>。对话历史、用户上下文、中间推理结果——这些全部存入独立的 Session&#x2F;Memory 服务，Agent 实例本身只负责”给我一个 Spec 和一条消息，我返回一个结果”。</p><p>一旦 Agent 变成无状态的，很多事情就变得可能：</p><ul><li>某个 Agent 实例挂了？立刻启动一个新实例，从 Session 服务里恢复上下文，用户无感知。</li><li>某个 Agent 流量突然暴增？横向扩出更多实例，负载均衡，就像 k8s 扩 Pod 一样。</li><li>低峰期流量下降？缩减实例，节省资源。</li></ul><p>这和 k8s 管容器的逻辑是完全一致的——容器之所以能弹性扩缩，前提就是应用本身是无状态的，状态外置到数据库或缓存服务。Agent 的 Session&#x2F;Memory 服务扮演的就是这个角色。</p><p>反过来也说明了一件事：<strong>很多团队现在构建的 Agent 天然无法扩缩</strong>——因为对话历史存在进程内存里，实例挂了状态就没了，更没法横向扩展。这不是 Agent 框架的问题，是架构选择的问题。Spec 化 + 状态外置，才是解决这件事的正确方式。</p><hr><h2 id="这套架构真正解放的，是迭代速度"><a href="#这套架构真正解放的，是迭代速度" class="headerlink" title="这套架构真正解放的，是迭代速度"></a>这套架构真正解放的，是迭代速度</h2><p>光说「代码更整洁了」有点低估这件事的价值。</p><p>这套思路实际上是在<strong>把 AI 能力的迭代，从软件工程的交付节奏里解放出来</strong>。</p><p>以前，业务侧发现某个 Agent 的回答在一个场景下答非所问，想调整一下 Prompt，正常流程是：提需求 → 排期 → 工程师改代码 → 测试 → 上线，可能要一两周。Prompt 这件事本质上是业务认知问题，跟工程师的软件能力毫无关系，但它被工程交付流程卡住了。Prompt Center 解决的就是这个问题——业务侧直接操作，24 小时内完成一个完整的调优循环。</p><p>工具层同理。MCP 协议把工具能力标准化成可发现、可插拔的服务，能力组合可以动态调整，不用每次改代码。</p><p>执行引擎的升级也是如此。想给所有 Agent 加统一的重试机制？引擎升级，所有 Agent 自动获益，和任何一个 Agent 的业务逻辑改了完全不相干，两件事可以并行发生。</p><p>当 Prompt、工具、引擎三件事都能独立迭代，整个 Agent 系统的演进速度是不一样的。</p><hr><h2 id="真正难的部分：语义对齐"><a href="#真正难的部分：语义对齐" class="headerlink" title="真正难的部分：语义对齐"></a>真正难的部分：语义对齐</h2><p>工程架构能解决的，只是其中相对容易的那部分。</p><p>Spec 化之后，Agent 间的发现和连接问题是解决了。但多个 Agent 协作时，真正的难题不是「找到彼此」，而是「理解彼此」。</p><p>一个 Agent 输出的结果，另一个 Agent 能不能正确解读和使用？上游 Agent 的意图，能不能准确传递给下游？在一个复杂的多 Agent 系统里，一个中间 Agent 的判断偏差，会在整条链路上产生多大的放大效应？</p><p>这些问题，在 Spec 里写几行配置是解决不了的。</p><p>它需要的是：精心设计的 Prompt 接口约定、明确的输入输出格式规范、在关键节点上设置人工审核的介入点、大量的真实场景测试。说白了，是<strong>把软件工程里接口设计的严谨性，带入到 Prompt 设计里来</strong>。</p><p>而目前大多数团队做 Agent 系统，在这件事上的投入还远远不够。</p><hr><h2 id="基础设施之下的那个问题"><a href="#基础设施之下的那个问题" class="headerlink" title="基础设施之下的那个问题"></a>基础设施之下的那个问题</h2><p>还有一件事值得单独说。</p><p>这套架构的前提，是每个 Agent 本身的能力是可靠的。</p><p>Spec 再标准、执行引擎再完善、Registry 体系再健全，如果你塞进去的 Agent 在核心任务上经常出错、推理经常偏轨、工具调用经常失败——用户最终感受到的，还是一个不好用的系统。</p><p>基础设施能让你管理 Agent 的规模，但它不会自动提升单个 Agent 的质量。这两件事是正交的，都需要投入，都不能偷懒。</p><hr><h2 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h2><p>这套思路，其实是把云原生工程领域里一个非常成熟的判断——<strong>「定义与执行分离」</strong>——用到了 Agent 的构建上。</p><p>容器时代，我们花了很多年才想清楚「应用的运行环境」和「应用本身」要分开管理，才有了今天 k8s 的弹性扩缩、滚动升级、故障自愈。Agent 时代，「执行引擎」和「Agent 的业务身份」，大概也值得被同等认真地分开对待。</p><p>但有一点不完全一样：容器里跑的应用是静态代码，但 Agent 的「业务身份」里包含 Prompt——<strong>Prompt 是会说话的，它直接决定 AI 的行为边界</strong>。这意味着 Agent Spec 不只是工程配置文件，它更接近于一份「行为合同」——定义了这个 Agent 能做什么、应该怎么做、不应该做什么。</p><p>把这份合同从代码里分离出来，单独管理、单独版本控制、单独审计，可能比你想象的更加重要。</p><p>如果你也在构建越来越多的 Agent，不妨回头看看：你每次重复的那些代码，是不是其实都可以变成一份配置文件？</p><p>也许真正该被重新设计的，不是某一个 Agent，而是你管理所有 Agent 的方式。</p>]]></content>
      
      
      <categories>
          
          <category> AI </category>
          
      </categories>
      
      
        <tags>
            
            <tag> AI </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>你还在为 MCP 占用巨大的上下文窗口而烦恼吗？</title>
      <link href="/2026/03/25/mcp-cli-context/"/>
      <url>/2026/03/25/mcp-cli-context/</url>
      
        <content type="html"><![CDATA[<p>最近我越来越强烈地感觉到一件事：</p><p>很多 Agent 系统到了后面，真正先撑不住的，不是模型能力，也不是工具数量，而是<strong>工具描述本身</strong>。</p><p>这话听起来有点绕。明明我们接 MCP，就是为了让模型更好地用工具。结果工具一接多，schema 一铺开，最先被吃掉的反而是上下文窗口。你本来想给 Agent 加能力，最后干成了给它塞说明书。</p><p>这就有点像什么呢？</p><p>本来你是想给新同事配一套顺手的工具箱，结果你先给了他 300 页设备手册。理论上没毛病，工程上已经开始让人皱眉了。</p><p>所以这篇文章我不准备聊 MCP 对不对。MCP 当然是对的，它解决的是<strong>标准化工具暴露</strong>的问题。我要聊的是另一个更现实的问题：</p><p><strong>当工具越来越多时，怎么让 Agent 既看得见能力，又不被工具描述本身拖死？</strong></p><h2 id="一、MCP-为什么会占用大量上下文窗口？"><a href="#一、MCP-为什么会占用大量上下文窗口？" class="headerlink" title="一、MCP 为什么会占用大量上下文窗口？"></a>一、MCP 为什么会占用大量上下文窗口？</h2><p>这个问题其实不用讲太复杂。</p><p>MCP 本质上就是把工具的名字、描述、参数、返回结构这些信息，以一种统一的方式暴露给模型。工具少的时候，这套机制非常优雅；但工具一多，问题就来了：</p><ul><li>每个工具都带一段描述</li><li>每个参数都带 schema</li><li>工具数量一上来，描述体积就会迅速膨胀</li></ul><p>于是，大规模工具暴露下，<strong>Token 成本会快速上升</strong>。</p><p>而且这还不只是钱的问题。更麻烦的是，业务上下文、用户问题、历史对话，本来就要抢窗口；现在工具描述再来分一大块，模型真正能拿来思考问题的空间就更紧了。</p><p>说白了，MCP 的问题不是协议不好，而是：</p><blockquote><p><strong>当工具规模上来之后，直接把整份能力描述裸喂给模型，成本会越来越高。</strong></p></blockquote><p>这时候，就得想办法换一种暴露方式了。</p><h2 id="二、OpenClaw-的解法：用-mcporter-把-MCP-变成-CLI"><a href="#二、OpenClaw-的解法：用-mcporter-把-MCP-变成-CLI" class="headerlink" title="二、OpenClaw 的解法：用 mcporter 把 MCP 变成 CLI"></a>二、OpenClaw 的解法：用 mcporter 把 MCP 变成 CLI</h2><p>OpenClaw 这边给出的一个很有意思的思路，就是 <code>mcporter generate-cli</code>。</p><p>它干的事情，简单说就是：</p><p><strong>把原本给 MCP 用的工具描述，转换成一套 CLI 命令面。</strong></p><p>也就是说，Agent 不再一开始就吃下整份庞大的 schema，而是先看到一个顶层命令入口。然后它可以像人类一样：</p><ul><li>先看顶层 <code>help</code></li><li>再进入相关子命令</li><li>再看具体参数说明</li><li>最后执行命令</li></ul><p>这个变化的关键在于，它把原来的“全量注入”变成了“按需探索”。</p><p>以前是：</p><blockquote><p>先把所有工具说明都塞进上下文，再让模型决定用哪个。</p></blockquote><p>现在更像是：</p><blockquote><p>先告诉模型这里有一棵命令树，需要的时候自己沿着树往下看。</p></blockquote><p>这样做的好处非常直接。</p><h3 id="1）省上下文"><a href="#1）省上下文" class="headerlink" title="1）省上下文"></a>1）省上下文</h3><p>最明显的收益，就是不用把所有工具 schema 一次性放进模型窗口里。</p><p>你只需要暴露一个 CLI 入口，剩下的信息让 Agent 在运行时通过 <code>help</code> 渐进式获取。这样上下文里保留的是当前任务真正相关的那一小部分，而不是一整片工具海。</p><h3 id="2）保留-MCP-的完整能力面"><a href="#2）保留-MCP-的完整能力面" class="headerlink" title="2）保留 MCP 的完整能力面"></a>2）保留 MCP 的完整能力面</h3><p>mcporter 的价值不在于“重新发明工具协议”，而在于它站在 MCP 之上做了一个更适合 Agent 消费的投影层。</p><p>底层还是 MCP server，能力还是那套能力，只是对 Agent 来说，消费方式从“读 schema”变成了“用 CLI”。</p><h3 id="3）工程接入成本低"><a href="#3）工程接入成本低" class="headerlink" title="3）工程接入成本低"></a>3）工程接入成本低</h3><p>这一点很关键。</p><p>如果你已经有 MCP server，那么用 <code>generate-cli</code> 相当于可以很快获得一个可探索、可调用的命令面。你不用再手工为每个工具重写一层 CLI，也不用额外维护一套和后端可能逐渐脱节的命令文档。</p><p>所以 mcporter 主要省的，其实是<strong>工程接入成本</strong>：</p><ul><li>已有 MCP server，马上变 CLI</li><li>少写一层胶水</li><li>快速扩大可用能力面</li></ul><p>当然，这套方案也不是没有代价。</p><p>mcporter 生成出来的 CLI，往往是<strong>能用，但不一定特别好用</strong>。因为它本质上还是从 schema 自动映射出来的，所以很容易继承底层工具定义的“机器味儿”：</p><ul><li>参数名偏技术化</li><li>某些命令结构不够自然</li><li>复杂参数仍然可能需要 JSON</li><li>从调用准确性看，CLI 有时甚至会比原生 MCP 更脆，尤其是在 shell quoting、复杂嵌套参数这些地方</li></ul><p>所以如果你问我，mcporter 解决了什么？</p><p>我会说，它解决的是：</p><blockquote><p><strong>如何以最低成本，把 MCP 从“模型上下文里的工具描述”变成“运行时可探索的工具入口”。</strong></p></blockquote><p>这已经很有价值了。</p><p>不过，如果再往前走一步，你会发现还有一条更“产品化”的路。</p><p><strong>让我们看看 Google 是怎么进一步处理这个问题的。</strong></p><h2 id="三、Google-Workspace-CLI-的解法：把能力树做成更适合-Agent-探索的产品层"><a href="#三、Google-Workspace-CLI-的解法：把能力树做成更适合-Agent-探索的产品层" class="headerlink" title="三、Google Workspace CLI 的解法：把能力树做成更适合 Agent 探索的产品层"></a>三、Google Workspace CLI 的解法：把能力树做成更适合 Agent 探索的产品层</h2><p>Google Workspace CLI 这条路，和 mcporter 有相似之处，但味道明显不一样。</p><p>它的核心思路不是把所有接口说明一股脑给模型，而是先暴露一个顶层 CLI，然后让 Agent 通过 <code>help</code> 逐步进入能力树：</p><ul><li>先暴露一个顶层 CLI</li><li>Agent 通过 <code>help</code></li><li>再进入子命令</li><li>再看参数说明</li><li>按需逐步探索能力树</li></ul><p>这套方式最有意思的地方在于，它不是简单换个壳，而是在<strong>命令空间组织方式</strong>上更照顾 Agent 的认知习惯。</p><p>换句话说，它更像是专门为 Agent 使用体验设计的一层 CLI 产品层。</p><h3 id="1）从-token-视角看，为什么它和-mcporter-都省？"><a href="#1）从-token-视角看，为什么它和-mcporter-都省？" class="headerlink" title="1）从 token 视角看，为什么它和 mcporter 都省？"></a>1）从 token 视角看，为什么它和 mcporter 都省？</h3><p>因为两者本质上都在做同一件事：</p><blockquote><p><strong>把一次性注入整份接口描述，改成运行时按需拉取局部说明。</strong></p></blockquote><p>这就像原来你要先把整本字典背下来再说话，现在变成“需要哪个词再翻哪一页”。</p><p>对于上下文窗口来说，这种差别是非常实在的：</p><ul><li>不再全量暴露能力描述</li><li>只在当前任务需要时获取局部 help</li><li>避免模型在超大的平面工具列表里乱看一圈</li></ul><p>所以，<strong>mcporter 和 Google Workspace CLI 在 token 视角下其实是同路人</strong>。</p><h3 id="2）但-mcporter-的问题也很明显：自动生成的-CLI，往往“能用，但不一定好用”"><a href="#2）但-mcporter-的问题也很明显：自动生成的-CLI，往往“能用，但不一定好用”" class="headerlink" title="2）但 mcporter 的问题也很明显：自动生成的 CLI，往往“能用，但不一定好用”"></a>2）但 mcporter 的问题也很明显：自动生成的 CLI，往往“能用，但不一定好用”</h3><p>这点前面已经提了一下，这里单独拎出来说，是因为它正好衬托出 Google 这套方案的价值。</p><p>自动生成 CLI 最大的优点是快，但副作用也很明显：</p><ul><li>它忠实映射后端，不一定忠实映射“用户意图”</li><li>它描述的是能力，不一定描述的是任务</li><li>它适合机器完整暴露，不一定适合模型高效调用</li></ul><p>尤其在复杂参数场景下，CLI 这层有时会比原生 MCP 更脆。因为原生 MCP 本来就是结构化调用，到了 shell 世界里，转义、嵌套、引号这些东西都开始变得很“工程现实主义”。</p><h3 id="3）Google-Workspace-CLI-的优势：更适合“渐进式发现”"><a href="#3）Google-Workspace-CLI-的优势：更适合“渐进式发现”" class="headerlink" title="3）Google Workspace CLI 的优势：更适合“渐进式发现”"></a>3）Google Workspace CLI 的优势：更适合“渐进式发现”</h3><p>这一点我觉得特别关键。</p><p>LLM 有一个很现实的特征：</p><blockquote><p><strong>它更擅长沿着层级树逐步探索，不擅长一开始就理解一个超大的工具平面列表。</strong></p></blockquote><p>Google Workspace CLI 恰好顺着这个特点来设计。</p><p>它做的不是“把所有方法罗列出来”，而是把接口能力组织成一棵更适合探索的树。这样 Agent 可以先确定大方向，再不断缩小范围，而不是一开始就面对一大片同级工具名。</p><h3 id="4）它还可以被人为优化成“任务语言”"><a href="#4）它还可以被人为优化成“任务语言”" class="headerlink" title="4）它还可以被人为优化成“任务语言”"></a>4）它还可以被人为优化成“任务语言”</h3><p>这一步比自动生成更进一步。</p><p>Google Workspace CLI 不只是动态发现服务，它还加入了一些人工设计的辅助命令，比如：</p><ul><li><code>+send</code></li><li><code>+reply</code></li><li><code>+agenda</code></li><li><code>+meeting-prep</code></li></ul><p>这些命令的意义，不只是“简化参数”，而是把底层 API 的表达方式，重新包装成更接近任务意图的语言。</p><p>比如你让 Agent 去“准备会议”，和让它自己从一堆底层接口里推导出：先查 calendar、再查参与人、再查相关文档，这完全不是一个认知负担。</p><p>前者像是在给它任务按钮，后者像是在让它自己读 API 地图。</p><h3 id="5）help-也可以写得非常-agent-friendly"><a href="#5）help-也可以写得非常-agent-friendly" class="headerlink" title="5）help 也可以写得非常 agent-friendly"></a>5）help 也可以写得非常 agent-friendly</h3><p>这是 Google Workspace CLI 这类方案的另一个优势。</p><p>自动生成的 schema 更像“能力定义”，而人工优化过的 help 可以更像“行动指引”：</p><ul><li>命令更短</li><li>示例更贴近任务</li><li>说明更贴近意图</li><li>默认路径更明确</li></ul><p>这对 Agent 特别友好，因为它更接近“我要做什么”，而不是“底层接口长什么样”。</p><p>如果要用一句话概括这一章，我会这么说：</p><blockquote><p><strong>mcporter 更像 MCP 的 CLI 投影层；Google Workspace CLI 更像动态 CLI 平台之上，再加了一层面向 Agent 体验优化的产品层。</strong></p></blockquote><h2 id="四、如果站在-Agent-系统设计角度，我会怎么选？"><a href="#四、如果站在-Agent-系统设计角度，我会怎么选？" class="headerlink" title="四、如果站在 Agent 系统设计角度，我会怎么选？"></a>四、如果站在 Agent 系统设计角度，我会怎么选？</h2><p>这里有一个特别容易被忽略，但我觉得很关键的点：</p><p><strong>两者省的，其实不是同一种成本。</strong></p><p>这也是为什么你很难简单地下结论，说谁绝对更优。</p><h3 id="mcporter-主要省的是工程接入成本"><a href="#mcporter-主要省的是工程接入成本" class="headerlink" title="mcporter 主要省的是工程接入成本"></a>mcporter 主要省的是工程接入成本</h3><p>它的优势非常朴素，也非常实用：</p><ul><li>已有 MCP server，马上变 CLI</li><li>少写一层胶水</li><li>快速扩大可用能力面</li></ul><p>如果你已经接了一堆 MCP 服务，或者你正在搭一个通用工具平台，那 mcporter 这种方案的价值就非常大。因为它让你不用每来一个工具域，就重新做一轮 CLI 产品设计。</p><h3 id="Google-Workspace-CLI-主要省的是模型认知成本"><a href="#Google-Workspace-CLI-主要省的是模型认知成本" class="headerlink" title="Google Workspace CLI 主要省的是模型认知成本"></a>Google Workspace CLI 主要省的是模型认知成本</h3><p>它更强调的是另一件事：</p><ul><li>更好的命令组织</li><li>更少一次性描述</li><li>更符合 Agent 探索习惯</li><li>更少“理解后端接口”的负担</li></ul><p>它不是先追求“所有能力都快点暴露出来”，而是更在意“暴露出来之后，Agent 好不好理解、好不好走通”。</p><h3 id="所以哪个更优？"><a href="#所以哪个更优？" class="headerlink" title="所以哪个更优？"></a>所以哪个更优？</h3><p>答案并不是单一的，而是：</p><blockquote><p><strong>看你想优化哪一种成本。</strong></p></blockquote><p>如果你现在最缺的是“接入效率”，那 mcporter 很有吸引力。</p><p>如果你现在最缺的是“Agent 调用体验”和“高频任务的成功率”，那 Google Workspace CLI 这种 help 渐进式发现 + 人工辅助命令的思路，会更值得学。</p><h3 id="如果让我来设计，我会怎么做？"><a href="#如果让我来设计，我会怎么做？" class="headerlink" title="如果让我来设计，我会怎么做？"></a>如果让我来设计，我会怎么做？</h3><p>我的选择其实不是二选一，而是分层。</p><h4 id="第一层：MCP-API-作为标准能力层"><a href="#第一层：MCP-API-作为标准能力层" class="headerlink" title="第一层：MCP &#x2F; API 作为标准能力层"></a>第一层：MCP &#x2F; API 作为标准能力层</h4><p>先把工具能力标准化，保证覆盖完整性。这一层是基础，没有它，后面所有优化都容易变成散装脚本。</p><h4 id="第二层：自动生成-CLI，作为低-token-接入层"><a href="#第二层：自动生成-CLI，作为低-token-接入层" class="headerlink" title="第二层：自动生成 CLI，作为低 token 接入层"></a>第二层：自动生成 CLI，作为低 token 接入层</h4><p>这里可以用 mcporter 这样的方案，快速把能力投影成一个 Agent 可探索的 CLI 面。</p><h4 id="第三层：对高频场景做人工优化-helper"><a href="#第三层：对高频场景做人工优化-helper" class="headerlink" title="第三层：对高频场景做人工优化 helper"></a>第三层：对高频场景做人工优化 helper</h4><p>当某些任务使用频率足够高，比如发消息、查日程、做摘要、准备会议，那就值得再往上做一层“任务语言”封装，把它们从底层接口提升成明确动作。</p><p>说得直白一点：</p><ul><li><strong>MCP 保证完整性</strong></li><li><strong>生成式 CLI 保证低成本接入</strong></li><li><strong>人工 helper 保证高频体验</strong></li></ul><p>这三层放在一起，我觉得才是一个更成熟的 Agent 工具体系。</p><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>所以回到文章标题。</p><p>如果你也在为 MCP 占用巨大的上下文窗口而烦恼，我的判断是：</p><p>问题不在 MCP 本身，而在于我们不能一直把它当成“直接给模型看的最终形态”。</p><p>当工具规模上来之后，协议层标准化是一回事，<strong>让 Agent 低成本理解这些能力</strong>又是另一回事。</p><p>mcporter 给出的答案是：先把 MCP 变成 CLI；Google Workspace CLI 进一步给出的答案是：不仅要变成 CLI，还要把 CLI 设计成更适合 Agent 渐进探索和任务调用的形态。</p><p>从这个角度看，CLI 化不是倒退，反而像是一种很现实的重新分层。</p><p>毕竟工程世界里，真正好用的系统，往往都不是“把所有能力都摊开”，而是让使用者在正确的时间，只看到他需要看到的那一小部分。</p><p>而 Agent，大概也一样。</p>]]></content>
      
      
      <categories>
          
          <category> AI </category>
          
      </categories>
      
      
        <tags>
            
            <tag> AI </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>你的 Agent 用着用着就&quot;叛变&quot;了？OpenClaw 终于把这个问题讲清楚了</title>
      <link href="/2026/03/14/openclaw-context-engine/"/>
      <url>/2026/03/14/openclaw-context-engine/</url>
      
        <content type="html"><![CDATA[<p>最近有个 OpenClaw 核心 maintainer 说了一句很扎心的话：</p><blockquote><p><strong>“your OpenClaw agents work perfectly for 20 minutes, then it silently forgets your instructions and goes rogue.”</strong></p></blockquote><p>我第一次看到这句话的时候，笑了一下，然后有点难受。</p><p>因为这不是在说某个人写的 Agent 有 bug，这是在说：<strong>这个问题是系统性的</strong>，而且它是”静默”发生的——Agent 不会报错，不会提示你它忘了什么，它只是悄悄开始按自己的理解行动。</p><hr><h2 id="先说清楚，这不是”记忆模块”的问题"><a href="#先说清楚，这不是”记忆模块”的问题" class="headerlink" title="先说清楚，这不是”记忆模块”的问题"></a>先说清楚，这不是”记忆模块”的问题</h2><p>这里有个很容易混淆的地方，值得单独说一下。</p><p>很多人看到”上下文管理”，第一反应是：</p><blockquote><p>“哦，就是那个 MEMORY.md？跨 session 的记忆那层？”</p></blockquote><p>不是的。</p><p>OpenClaw 的记忆其实分两层，这两层干的是完全不同的事：</p><p><strong>第一层：长期记忆</strong>（之前就有，这次没动）</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">MEMORY.md、memory/YYYY-MM-DD.md</span><br><span class="line">跨 session 持久化，重启还在</span><br><span class="line">解决的问题：我上周说了什么</span><br></pre></td></tr></table></figure><p><strong>第二层：上下文管理</strong>（这次更新的核心）</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">当前这个 session 里，token 窗口怎么组织</span><br><span class="line">旧消息超出窗口后怎么压缩</span><br><span class="line">压缩后的内容怎么还原</span><br><span class="line">解决的问题：这个 session 里 20 分钟前我说了什么</span><br></pre></td></tr></table></figure><p>那个”20 分钟后叛变”的问题，出在第二层。</p><hr><h2 id="滑动窗口：一个”有效但有毒”的方案"><a href="#滑动窗口：一个”有效但有毒”的方案" class="headerlink" title="滑动窗口：一个”有效但有毒”的方案"></a>滑动窗口：一个”有效但有毒”的方案</h2><p>在 2026.3.7 之前，OpenClaw 处理长对话的方式是<strong>滑动窗口压缩</strong>：</p><p>对话太长了 → 把老消息总结成摘要 → 摘要顶替原文 → 腾出空间给新消息</p><p>这个方案在大部分情况下是够用的。但它有一个致命的问题：<strong>压缩是有损的，而且是不可逆的</strong>。</p><p>“请用 TypeScript 写”被摘要成”用户要写代码”。<br>“这个函数不允许有副作用”被摘要成”用户对代码质量有要求”。</p><p>Agent 从那一刻起，就在靠一个失真的摘要工作。</p><p>更麻烦的是，这个失真是<strong>渐进的</strong>——不是某一刻突然全忘了，而是每次压缩都丢一点，丢到某个临界点，Agent 的行为开始偏，然后你发现你在跟一个已经不太记得你要什么的东西对话。</p><p>这就是那个 maintainer 说的”goes rogue”。</p><hr><h2 id="2026-3-7-做了什么：把上下文管理从硬编码变成接口"><a href="#2026-3-7-做了什么：把上下文管理从硬编码变成接口" class="headerlink" title="2026.3.7 做了什么：把上下文管理从硬编码变成接口"></a>2026.3.7 做了什么：把上下文管理从硬编码变成接口</h2><p>很多人看这次更新，把它理解成”做了一个更好的压缩算法”。</p><p>这个理解是错的，而且错得有点离谱。</p><p>这次更新做的事是：<strong>把整个上下文管理逻辑从核心里挖出来，抽成一个可插拔的接口</strong>。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">之前：</span><br><span class="line">OpenClaw Core 里写死了&quot;如何组织上下文&quot;</span><br><span class="line">→ 想换策略？改源码，或者 fork</span><br><span class="line"></span><br><span class="line">现在：</span><br><span class="line">contextEngine = 一个插件槽（slot）</span><br><span class="line">→ 接口定义好，实现可以换</span><br><span class="line">→ 没配置插件？用原来的行为，零变化</span><br></pre></td></tr></table></figure><p>接口提供了 7 个生命周期钩子，从引擎启动、消息到来、组装上下文、触发压缩，一直到子 Agent 派生和回收——整条链路全部开放。</p><p>其中最核心的一个钩子是 <code>assemble(budget)</code>：</p><blockquote><p>每次模型调用之前，OpenClaw 把 token 预算交给你，问你：<strong>这次调用，给模型看什么？</strong></p></blockquote><p>你返回什么，模型就看到什么。这一行为之前是系统决定的，现在是你的插件决定的。</p><hr><h2 id="lossless-claw：第一个旗舰实现"><a href="#lossless-claw：第一个旗舰实现" class="headerlink" title="lossless-claw：第一个旗舰实现"></a>lossless-claw：第一个旗舰实现</h2><p>接口开放之后，第一个值得认真看的实现就是 lossless-claw，来自 Martian Engineering 的 Josh Lehman。</p><p>思路来自学术论文《LCM: Lossless Context Management》，但落地非常工程化。</p><p>核心逻辑是：</p><ol><li><strong>每条消息永久存入 SQLite</strong>，原文不删</li><li><strong>旧消息分块，用 LLM 生成摘要</strong></li><li><strong>摘要聚合成 DAG（有向无环图）</strong>，形成层级摘要树</li><li><strong>每轮对话：高层摘要 + 最近原文 → 组成上下文</strong></li><li><strong>Agent 有工具可以主动钻回原文</strong>（<code>lcm_grep</code>、<code>lcm_expand</code>）</li></ol><p>DAG 的层级结构长这样：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">Level 2 摘要（高层）</span><br><span class="line">├── Level 1 摘要（中层）</span><br><span class="line">│       ├── 消息块 1-20 的摘要</span><br><span class="line">│       └── 消息块 21-40 的摘要（←→ 原始消息保留在 SQLite）</span><br><span class="line">└── Level 1 摘要</span><br><span class="line">        ├── 消息块 41-60 的摘要</span><br><span class="line">        └── 消息块 61-80 的摘要</span><br></pre></td></tr></table></figure><p>原始消息永远不删，摘要从下往上聚合，越高层越抽象。模型每次看到的是顶层摘要加上最近的原始消息。而 Agent 随时可以用工具从任意摘要节点钻回原文——<strong>没有悬空的摘要，没有找不回去的细节</strong>。</p><p>关键在第 5 点：<strong>Agent 可以主动回溯</strong>。</p><p>当它遇到需要确认早期指令的场景，不是依赖一个可能失真的摘要，而是真的能查回去。这把上下文管理从”被动截断”升级成”主动检索”。</p><p>实测数据：OOLONG benchmark（专门测长上下文理解能力）里，lossless-claw 得分 74.8，Claude Code 70.3，而且上下文越长，差距越大。</p><p>Josh Lehman 本人跑了一周之后说的评价是：</p><blockquote><p><em>“说它表现很好，都算是低估了。”</em></p></blockquote><hr><h2 id="降温一下：这不是魔法"><a href="#降温一下：这不是魔法" class="headerlink" title="降温一下：这不是魔法"></a>降温一下：这不是魔法</h2><p>工程界出现过太多”这次彻底解决了”的说法。</p><p>接口&#x2F;插件&#x2F;钩子这套思想不新鲜。Eclipse 插件体系、OSGi、SOA，20 年前就这么玩了。在 AI 时代，它依然有效，但也不会因为加了 AI 就变成魔法。</p><p>有几个问题还值得观察：</p><ul><li>SQLite + DAG 的方案在大规模并发子 Agent 场景下，一致性怎么保证？</li><li>摘要本身是 LLM 生成的，摘要的质量依赖你用的模型，这个成本有没有算进去？</li><li><code>lcm_expand</code> 这类工具是暴露给 Agent 主动调用的，但 Agent 知道什么时候该用吗？</li></ul><p>这些不是否定，而是说：<strong>方向是对的，但这只是起点</strong>。</p><hr><h2 id="真正值得关注的是这个"><a href="#真正值得关注的是这个" class="headerlink" title="真正值得关注的是这个"></a>真正值得关注的是这个</h2><p>从工程视角来看，这次更新的本质不是”做了一个更好的记忆系统”，而是：</p><p><strong>OpenClaw 承认了”上下文管理”是 Agent 架构中一个需要被认真设计的独立关注点。</strong></p><p>之前这层逻辑被埋在核心里，用户只能接受，无法定制，也没有标准接口去衡量它做得好不好。</p><p>现在接口在这里了，意味着：</p><ul><li>不同场景可以有不同引擎（RAG 引擎、任务图引擎、省 token 引擎……）</li><li>上下文质量可以被独立测试和优化</li><li>生态可以在这一层上真正生长</li></ul><p>这件事本身，比 lossless-claw 更重要。</p><hr><h2 id="最后想说的"><a href="#最后想说的" class="headerlink" title="最后想说的"></a>最后想说的</h2><p>如果你现在还在用原来的 OpenClaw，不装任何 context engine 插件，行为不会有任何变化——这次更新对你完全透明。</p><p>但如果你在跑长时间、多轮次的 Agent 任务，比如持续几小时的 coding session，或者需要跨越几十轮对话保持一致行为的场景，那这层终于可以认真设计了。</p><p>那个”20 分钟后叛变”的问题，有解了。</p><p>至少，现在你有机会自己决定它怎么解。</p><hr><blockquote><p>下一个问题可能是：当上下文管理可以被完全自定义之后，Agent 的”人格一致性”该怎么保证？这是 memory 和 context 都解决不了的另一层问题。</p></blockquote>]]></content>
      
      
      <categories>
          
          <category> AI </category>
          
      </categories>
      
      
        <tags>
            
            <tag> AI </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>别再拿上下文窗口不够大当借口了！</title>
      <link href="/2026/03/10/context-window/"/>
      <url>/2026/03/10/context-window/</url>
      
        <content type="html"><![CDATA[<p>最近大家好像都得了一种”窗口焦虑症”。</p><p>128K 嫌小，1M 刚够，恨不得把整个数据库都塞进 Prompt 里。</p><p>那种感觉就像是你家里的杂物堆不下了，你第一反应不是去请个整理师，而是想把房子的门拓宽成城门——觉得只要”带宽”足够大，再乱的东西也能一股脑挤进去。</p><p>但我相信，真正写过 Agent 业务代码的人，深夜里肯定都骂过娘：明明我把那条规则塞进上下文了，为什么它转头就给忘了？</p><h2 id="一、窗口越大，Agent-越像在”坐牢”"><a href="#一、窗口越大，Agent-越像在”坐牢”" class="headerlink" title="一、窗口越大，Agent 越像在”坐牢”"></a>一、窗口越大，Agent 越像在”坐牢”</h2><p>很多团队觉得 Agent 执行长任务拉胯是因为窗口不够长。这事儿咱得摸着良心说实话：窗口不是越大越好，尤其是当你的 Agent 已经开始”答非所问”的时候。</p><p>大模型本质上是个注意力分配机器。你给它 100 万 Token，这不叫”给它博学”，这叫”把它扔进了一个吵死人的菜市场”。</p><ul><li><strong>注意力稀释：</strong> 背景噪音（冗余上下文）一多，模型捕捉关键约束的信噪比就直线下降。</li><li><strong>Lost in the Middle：</strong> 这是一个写代码时最容易撞上的鬼故事。核心逻辑明明在第 10 页写着，模型却只记得开头和结尾。</li></ul><p>窗口是带宽，但带宽从来不等于智力。</p><p>如果你非要让 Agent 每一轮都去扫描几十万 Token 的 KV Cache，那不是在用模型，那是在浪费预算买它的”注意力疲劳”。</p><h2 id="二、记忆不是”压缩”，而是”内存管理单元”"><a href="#二、记忆不是”压缩”，而是”内存管理单元”" class="headerlink" title="二、记忆不是”压缩”，而是”内存管理单元”"></a>二、记忆不是”压缩”，而是”内存管理单元”</h2><p>我看到很多技术文章把”压缩（Compression）”和”记忆（Memory）”拆开说。甚至有人觉得，压缩只是为了省那点 Token 钱。</p><p>这完全是把路走窄了。</p><p>如果把上下文窗口比作带宽，那 Agent 的记忆系统其实更像是计算机里的 <strong>MMU（存储管理单元）</strong>。</p><p>性能瓶颈从来不在总线有多宽，而在于你那套调度算法。一个合格的 Agent 记忆管理，起码得把这四件事闭环了，少一步都得踩坑：</p><ul><li><strong>资产化存储（不是聊天记录）：</strong> 别只会存 Raw Data。要把见过的信息提取成”资产”——哪些是用户的怪癖，哪些是死命令。</li><li><strong>逻辑重构（不是单纯变小）：</strong> 压缩不是为了扔垃圾，而是为了让”工作台（L1 Cache）”保持清爽。</li><li><strong>精准置换（Page Fault）：</strong> 在任务拐弯的时候，系统得知道什么时候该把”换出到硬盘”的旧 Page 捞回来。</li><li><strong>状态更新：</strong> 记忆是活的。用户昨天说爱吃辣，今天说戒了，你的系统要是搜出两个互相打架的答案，Agent 当场就能宕机给你看。</li></ul><p>没有更新机制的记忆叫”堆填区”，没有调度逻辑的压缩叫”残次品”。</p><h2 id="三、别被理论正确给骗了"><a href="#三、别被理论正确给骗了" class="headerlink" title="三、别被理论正确给骗了"></a>三、别被理论正确给骗了</h2><p>在实验室里，RAG 或者长窗口跑个 Benchmark 确实很漂亮。但在工程现场，理论正确往往等于不好用。</p><p>我们要解决的不是”怎么让它记住更多”，而是**”怎么控制它只看该看的”**。</p><p>这听起来很反直觉：我们辛辛苦苦开发长窗口，结果工程上的最高境界竟然是**”克制”**。但没办法，现实就是这么残酷——如果你不能精准地控制 Agent 的”视界”，那它在处理第 50 轮逻辑时的崩坏，就是命中注定的。</p><h2 id="四、最后留个思考"><a href="#四、最后留个思考" class="headerlink" title="四、最后留个思考"></a>四、最后留个思考</h2><p>当然，我不是在唱衰长窗口技术，它确实提高了上限。但我更想说的是，别把技术的进步当成工程偷懒的借口。</p><p>如果你现在的 Agent 表现不佳，先别急着去申请更高的模型配额。</p><p>不如先问问自己：如果你是这个 Agent，被塞了 5 万行毫无章法的历史记录，你还能记得最初用户让你”别用 Python 写代码”的要求吗？</p><p>下回咱聊聊，怎么在工程上给 Agent 做一个不打架的”分层内存”。</p>]]></content>
      
      
      <categories>
          
          <category> AI </category>
          
      </categories>
      
      
        <tags>
            
            <tag> AI </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>为什么你的 Multi-Agent 越拆越乱？</title>
      <link href="/2026/02/28/multi-agent-when-and-how/"/>
      <url>/2026/02/28/multi-agent-when-and-how/</url>
      
        <content type="html"><![CDATA[<p>最近在重构一个 Agent 系统，从单智能体改成了多智能体架构。改完之后效果确实好了，但回头一看成本：Token 消耗翻了快 10 倍，代码复杂度也上去了。</p><p>这让我开始反思：什么时候真的需要多 Agent？什么时候其实是过度设计？</p><p>这篇文章就聊聊这个。不是理论分析，就是踩过的坑和一些实践经验。</p><hr><h2 id="一、我为什么会想拆-Agent"><a href="#一、我为什么会想拆-Agent" class="headerlink" title="一、我为什么会想拆 Agent"></a>一、我为什么会想拆 Agent</h2><p>先说问题从哪来的。</p><p>项目最开始是单 Agent 架构，跑了一段时间后发现一个很明显的问题：<strong>对话越长，Agent 越容易”失忆”。</strong></p><p>具体表现：</p><ul><li>重复之前已经失败的操作</li><li>忽略用户早期提到的约束条件</li><li>在一堆中间日志里迷失方向，找不到重点</li></ul><p>最离谱的一次，我让它修复一个 bug，它前面试了 3 种方案都失败了，结果第 4 次又回到第 1 种方案重试，就好像完全不记得之前发生了什么。</p><p>这时候你会想：<strong>是不是该拆成多个 Agent，每个负责一个干净的子任务？</strong></p><p>但问题来了——怎么拆？拆完之后会不会引入新问题？</p><hr><h2 id="二、多-Agent-到底解决了什么问题"><a href="#二、多-Agent-到底解决了什么问题" class="headerlink" title="二、多 Agent 到底解决了什么问题"></a>二、多 Agent 到底解决了什么问题</h2><p>在真正动手拆之前，我先梳理了一下：到底哪些场景是多 Agent 能解决、单 Agent 很难搞定的？</p><h3 id="1-上下文污染"><a href="#1-上下文污染" class="headerlink" title="1. 上下文污染"></a>1. 上下文污染</h3><p>这是我遇到的核心问题。</p><p>单 Agent 在执行任务时，上下文会不断累积。有些信息是有用的（用户需求、关键决策），但更多是噪声（中间步骤的原始输出、失败的尝试、调试日志）。</p><p><strong>举个例子：</strong></p><p>你让 Agent 分析一个用户的行为数据。它会：</p><ol><li>查数据库，拉回来 3000 tokens 的原始记录</li><li>做清洗，又产生 1000 tokens 的中间结果</li><li>生成分析报告，500 tokens</li></ol><p>到第 3 步时，上下文里已经塞满了前面的原始数据和中间结果。如果后续还要做决策（比如推荐下一步行动），这些历史数据都是噪声。</p><p><strong>多 Agent 怎么解决？</strong></p><p>拆一个 DataFetchAgent，让它在独立的上下文里完成数据查询和清洗，只返回一个干净的摘要（比如 “用户最近 7 天活跃度下降 30%，主要流失在功能 A”）。</p><p>主 Agent 只看到这个摘要，上下文保持干净。</p><p><strong>但要注意：</strong> 这不是说所有查询都要拆 Agent。如果查询结果本身就很简洁（比如 “订单状态：已发货”），拆 Agent 反而多此一举。</p><hr><h3 id="2-并行探索"><a href="#2-并行探索" class="headerlink" title="2. 并行探索"></a>2. 并行探索</h3><p>有些任务天然适合并行，串行执行就是浪费时间。</p><p><strong>我的场景：</strong></p><p>需要对一个技术方案做可行性分析，包括：</p><ul><li>技术栈 A 的成熟度调研</li><li>技术栈 B 的成熟度调研</li><li>竞品是怎么做的</li></ul><p>这三条路径完全独立，互不依赖。单 Agent 只能一个一个来，多 Agent 可以同时开工。</p><p><strong>但有个隐藏成本：</strong> 并行不等于更快。</p><p>你虽然同时跑了 3 个 Agent，但每个 Agent 都要有完整的上下文初始化，总 Token 消耗可能是单 Agent 的 5-10 倍。省的是时间，花的是钱。</p><p>所以我的判断标准是：<strong>如果这个任务时效性很强（比如实时分析），并行值得；如果不急，单 Agent 串行更经济。</strong></p><hr><h3 id="3-工具选择过载"><a href="#3-工具选择过载" class="headerlink" title="3. 工具选择过载"></a>3. 工具选择过载</h3><p>这是我后来才意识到的问题。</p><p>最开始我的 Agent 有 30+ 个工具（文件操作、数据库、API 调用、代码执行……）。跑了一段时间发现：<strong>工具越多，选错的概率越高。</strong></p><p>不是模型能力不行，而是”选择过多”本身就是个认知负担。</p><p><strong>类比一下：</strong> 就像你去餐厅，菜单上有 10 道菜，你很快就能决定；但如果有 100 道菜，你反而不知道该点什么，最后可能随便选一个。</p><p>后来我拆成了几个专业化的 Agent：</p><ul><li>FileAgent：只有文件相关的 10 个工具</li><li>DBAgent：只有数据库相关的 8 个工具</li><li>APIAgent：只有外部 API 调用的 12 个工具</li></ul><p>每个 Agent 只看到自己领域的工具，选择准确率明显上去了。</p><p><strong>但要注意：</strong> 这个方案只适用于工具集本身有明确领域边界的场景。如果工具之间关联度很高（比如”读文件”和”解析 JSON”经常一起用），强行拆开反而增加通信成本。</p><hr><h2 id="三、但它的代价比你想的大"><a href="#三、但它的代价比你想的大" class="headerlink" title="三、但它的代价比你想的大"></a>三、但它的代价比你想的大</h2><p>说完好处，得说说代价。这是我在实际落地时踩的坑。</p><h3 id="1-Token-成本暴涨"><a href="#1-Token-成本暴涨" class="headerlink" title="1. Token 成本暴涨"></a>1. Token 成本暴涨</h3><p>这是最直观的代价。</p><p>我做过一个对比：</p><ul><li>单 Agent 完成一个任务：平均 15k tokens</li><li>多 Agent 完成同样任务：平均 80k tokens</li></ul><p>为什么差这么多？因为：</p><ul><li>每个 Sub-agent 都要初始化一遍系统提示词</li><li>Agent 之间传递消息也要花 tokens</li><li>结果要总结才能传给下一个 Agent</li></ul><p><strong>这还不算最坑的。</strong> 最坑的是：你花了更多 Token，但很多都用在”通信”上，并没有真正用在推理上。</p><p>我把这个叫 <strong>“通信税”</strong>——就像分布式系统的网络开销，单机不需要考虑延迟，但一旦拆成微服务，网络通信本身就成了新的瓶颈。</p><p><strong>所以我的经验是：</strong> 拆完之后，算一下信息传递消耗的 Token 占总 Token 的比例。如果超过 40-50%，说明你可能在错误的地方画了线。</p><hr><h3 id="2-信息在传递中损耗"><a href="#2-信息在传递中损耗" class="headerlink" title="2. 信息在传递中损耗"></a>2. 信息在传递中损耗</h3><p>这是更隐蔽的代价。</p><p><strong>举个例子：</strong></p><p>我让一个 Sub-agent 搜索相关代码文件。它找到了 5 个候选文件，经过分析排除了 4 个，最终选择了 file_a.py。</p><p>但返回给主 Agent 时，它只能说：”找到了 file_a.py，它处理用户认证逻辑。”</p><p>那它为什么排除了另外 4 个文件？这个决策过程丢了。</p><p>如果后续主 Agent 需要理解为什么选择 file_a 而不是 file_b，它只能重新推理一遍，或者干脆盲目信任 Sub-agent 的选择。</p><p><strong>这就是信息损耗的本质：</strong> 探索过程中的上下文线索被压缩掉了，只保留了结论。</p><hr><h3 id="3-隐式决策链断裂"><a href="#3-隐式决策链断裂" class="headerlink" title="3. 隐式决策链断裂"></a>3. 隐式决策链断裂</h3><p>这是我最近读到的一个观点，来自 Cognition 的博客（做 Devin 的那家公司），我觉得特别准确。</p><p>他们提出：<strong>Actions carry implicit decisions</strong>（每个行动都暗含决策）。</p><p><strong>什么意思？</strong></p><p>比如，一个 Agent 选择修改 file_a.py 而不是 file_b.py，这背后隐含了它对架构的某种理解。但这个”隐式决策”没有被显式写下来，只是体现在行为序列中。</p><p>当你拆成多个 Agent 并行工作时，这些隐式决策就断了。</p><p><strong>我遇到的实际问题：</strong></p><p>Agent A 在处理前端逻辑时，选择了某种数据结构（基于它对后端 API 的理解）。Agent B 在处理后端逻辑时，不知情地引入了不兼容的依赖。</p><p>冲突的根源不是”代码写错了”，而是<strong>决策上下文碎片化</strong>——两个 Agent 各自做了”局部最优”的选择，但放在一起就不兼容了。</p><p><strong>类比一下：</strong> 就像两个人在不同房间各自装修，都觉得自己设计得很合理，但一合起来发现风格完全不搭。</p><hr><h2 id="四、所以，什么时候该拆？"><a href="#四、所以，什么时候该拆？" class="headerlink" title="四、所以，什么时候该拆？"></a>四、所以，什么时候该拆？</h2><p>说了这么多问题，你可能会想：那是不是别用多 Agent 了？</p><p>也不是。关键是要<strong>找对边界</strong>。</p><p>我自己总结的判断标准是：<strong>按上下文耦合度拆分，而不是按功能阶段拆分。</strong></p><h3 id="错误的拆法：按工作流程"><a href="#错误的拆法：按工作流程" class="headerlink" title="错误的拆法：按工作流程"></a>错误的拆法：按工作流程</h3><p>很多人（包括我一开始）会这么想：</p><ul><li>Agent A 负责需求分析</li><li>Agent B 负责编码</li><li>Agent C 负责测试</li><li>Agent D 负责审查</li></ul><p>看起来很合理，就像敏捷开发的分工。</p><p><strong>但问题是：这恰好在上下文耦合度最高的地方画了线。</strong></p><p>编码的 Agent 不知道需求分析时为何排除了某些方案。测试的 Agent 不知道编码时遇到了哪些权衡。审查的 Agent 更不知道前面发生了什么。</p><p>结果就是典型的”电话游戏”——信息在每次传递中被简化、误解，到最后一环已经面目全非。</p><h3 id="正确的拆法：按上下文耦合度"><a href="#正确的拆法：按上下文耦合度" class="headerlink" title="正确的拆法：按上下文耦合度"></a>正确的拆法：按上下文耦合度</h3><p><strong>什么叫上下文耦合度？</strong></p><p>就是两个子任务需要共享多少上下文才能各自正确完成。</p><p><strong>低耦合的例子：</strong></p><ul><li>并行搜索不同市场的趋势（亚洲 vs 欧洲）</li><li>黑盒测试最终产物（不需要知道实现细节）</li><li>独立的数据清洗任务（输入输出明确）</li></ul><p><strong>高耦合的例子：</strong></p><ul><li>编写功能和编写测试（需要理解设计意图）</li><li>前后端并行开发（API 变更时需要频繁同步）</li><li>需求分析和架构设计（决策高度关联）</li></ul><p><strong>我的判断标准：</strong></p><ul><li><strong>低耦合（&lt;20% 上下文重叠）</strong> → 可以拆</li><li><strong>高耦合（&gt;80% 上下文重叠）</strong> → 不要拆</li><li><strong>中等耦合（20-80%）</strong> → 谨慎评估，<strong>默认不拆</strong></li></ul><hr><h3 id="一个几乎不会错的模式"><a href="#一个几乎不会错的模式" class="headerlink" title="一个几乎不会错的模式"></a>一个几乎不会错的模式</h3><p>如果你实在不确定该不该拆，有一个模式几乎不会踩坑：<strong>Verification Subagent</strong>（验证子智能体）。</p><p>为什么它这么稳？因为<strong>验证 Agent 不需要知道主 Agent 是如何构建输出的，它只需要黑盒测试最终产物。</strong></p><p><strong>工作流程：</strong></p><ol><li>主 Agent 完成一个功能（比如写了一段代码）</li><li>生成一个验证 Agent，给它：<ul><li>待验证的产物（代码）</li><li>验收标准（”所有单元测试通过” + “性能指标达标”）</li></ul></li><li>验证 Agent 跑测试，报告结果</li><li>如果不通过，主 Agent 根据反馈修改</li></ol><p><strong>为什么不会踩坑？</strong></p><p>需要传递的上下文非常少——只有产物和标准，没有实现细节，没有探索过程。信息损耗降到最低。</p><p><strong>类比一下：</strong> 就像代码审查时，Reviewer 不需要知道你是怎么写出这段代码的，只需要判断”它是否符合规范”。</p><p>Anthropic 的博客里也提到这个模式是他们实践下来最稳定的一种，我自己用下来确实如此。</p><hr><h2 id="五、我的建议"><a href="#五、我的建议" class="headerlink" title="五、我的建议"></a>五、我的建议</h2><p>总结一下，如果你在考虑多 Agent，可以按这个流程走：</p><h3 id="1-先穷尽单-Agent-的优化空间"><a href="#1-先穷尽单-Agent-的优化空间" class="headerlink" title="1. 先穷尽单 Agent 的优化空间"></a>1. 先穷尽单 Agent 的优化空间</h3><p>别急着拆。先看看：</p><ul><li>Prompt 是不是写得够清晰？</li><li>上下文有没有做压缩？（只保留关键信息，丢弃中间步骤）</li><li>工具描述是否准确？</li><li>有没有用 Tool Use 的最佳实践？</li></ul><p><strong>我的经验：</strong> 很多看似需要多 Agent 的问题，实际上是单 Agent 的上下文管理没做好。</p><p>比如，你可以在每个阶段结束时，让 Agent 自己总结关键信息，丢弃无用的中间结果。这样就能在单 Agent 内部实现”上下文压缩”。</p><hr><h3 id="2-识别明确的瓶颈信号"><a href="#2-识别明确的瓶颈信号" class="headerlink" title="2. 识别明确的瓶颈信号"></a>2. 识别明确的瓶颈信号</h3><p>只有当你看到这些信号时，才考虑拆分：</p><ul><li><strong>上下文窗口接近极限</strong> → 表现为幻觉率上升、忽略早期指令</li><li><strong>工具选择频繁出错</strong> → 工具集过大（20+ 个）</li><li><strong>需要并行覆盖大信息空间</strong> → 单 Agent 串行太慢</li></ul><p>如果没有这些信号，<strong>别拆</strong>。</p><hr><h3 id="3-按上下文耦合度画线"><a href="#3-按上下文耦合度画线" class="headerlink" title="3. 按上下文耦合度画线"></a>3. 按上下文耦合度画线</h3><p>拆的时候，问自己：</p><ul><li>这两个子任务需要共享多少上下文？</li><li>如果拆开，信息传递的成本有多高？</li><li>拆开后，会不会出现”决策上下文碎片化”？</li></ul><p>如果答案是”需要共享很多上下文”，那就别拆。</p><hr><h3 id="4-监控”通信税”"><a href="#4-监控”通信税”" class="headerlink" title="4. 监控”通信税”"></a>4. 监控”通信税”</h3><p>拆完之后，跑几个任务，算一下：</p><p><strong>通信税 &#x3D; 信息传递消耗的 Token &#x2F; 总 Token</strong></p><p>如果超过 50%，说明你的拆分方案有问题，需要重新调整边界。</p><hr><h2 id="六、最后想说的"><a href="#六、最后想说的" class="headerlink" title="六、最后想说的"></a>六、最后想说的</h2><p>多 Agent 不是银弹。</p><p>它解决的是特定问题：<strong>上下文隔离、并行探索、工具专业化</strong>。如果你的场景不符合这三种，单 Agent + 好好写 Prompt 可能更靠谱。</p><p>我很喜欢 Anthropic 博客里的一句话：<strong>“Start with the simplest approach that works, and add complexity only when evidence supports it.”</strong></p><p>从最简单的方案开始，只在有证据支持时才增加复杂度。</p><p>那接下来问题就来了：怎么量化”上下文耦合度”？20% 算低，80% 算高，中间那段怎么办？</p><p>也许真正该做的不是定义精确的阈值，而是建立一套<strong>成本收益评估机制</strong>——投入多少 Token，得到多少性能提升，这笔账是否划算？</p><p>这可能是下一个值得深入的方向。</p><hr><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li>Anthropic: Building multi-agent systems: When and how to use them</li><li>Cognition: Don’t Build Multi-Agents</li></ul><hr><p><strong>如果你也在搞 Agent 系统，欢迎留言聊聊你遇到的坑。</strong></p>]]></content>
      
      
      <categories>
          
          <category> AI </category>
          
      </categories>
      
      
        <tags>
            
            <tag> AI </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>从规划到翻车，聊聊 Agent 背后的真相</title>
      <link href="/2025/07/02/agent/"/>
      <url>/2025/07/02/agent/</url>
      
        <content type="html"><![CDATA[<p>你是否也在各类社交平台刷到过这样的标题：<code>AI 自己干活，不用你教</code></p><p>什么“感知、规划、记忆、执行”，“能自己拆任务、选工具、还会复盘”——这些听起来像是<strong>终极打工 AI</strong>的功能，全都被打包进一个词：<strong>AI Agent</strong>。看完我只想说一句：模型你这么能，工资卡要不要也一起拿去？</p><p>最近，胖虎也在工作中尝试落地 AI Agent，调研了不少构建方式，过程嘛……说不上痛苦，但确实走也遇到了一些问题。胖虎想用 Agent 干活，它却规划好了怎么划水。玩笑话了，言归正传，Agent 并非镜中花水中月——它确实在试图回答一个值得技术人认真思考的问题：</p><p><code>当我们不再让 AI 只输出一句话，而是完成一整件事，它该怎么思考，如何做？</code></p><p>这篇文章把过程中的观察、踩过的坑，以及一些不成熟的想法整理出来。不是“教你怎么用 Agent”，而是“我踩过的坑，给你照个路”。希望能帮你节省一点 debug 时间，哪怕不多。</p><h2 id="什么是-AI-Agent：一句话说不清，但我说说看"><a href="#什么是-AI-Agent：一句话说不清，但我说说看" class="headerlink" title="什么是 AI Agent：一句话说不清，但我说说看"></a>什么是 AI Agent：一句话说不清，但我说说看</h2><h3 id="你心中的-AI-Agent-是什么样？"><a href="#你心中的-AI-Agent-是什么样？" class="headerlink" title="你心中的 AI Agent 是什么样？"></a>你心中的 AI Agent 是什么样？</h3><p>曾经，一切内嵌了大模型的系统都被称为 Agent：</p><ul><li>聊天机器人？Agent。</li><li>工作流加个 LLM？Agent。</li><li>能自己调 API、还知道拆任务的？真·Agent。</li></ul><p>在这篇文章中，我们讨论的 AI Agent，指的是：一个 <strong>基于大模型具备感知环境、自主决策、执行任务能力的智能系统。</strong> 也就是说，它不只是被调用，还要 <strong>自己想清楚要干嘛，然后去干。</strong> 其核心特性： <strong>感知 + 规划 + 记忆 + 执行。</strong> Anthropic 给出的定义也很贴切：</p><blockquote><p>Agent 通常是在一个循环中，根据环境反馈使用工具的大模型。</p></blockquote><p>听起来就像我们自己上班干活：一边观察需求，一边想怎么做，一边忘掉上周做过什么。</p><h3 id="为啥叫“智能体”，看起来却不智能"><a href="#为啥叫“智能体”，看起来却不智能" class="headerlink" title="为啥叫“智能体”，看起来却不智能"></a>为啥叫“智能体”，看起来却不智能</h3><p>你可能也遇到过这种情况：</p><ul><li>Agent 明明理解了任务，却执行得乱七八糟；</li><li>你写了一大段 prompt，它还是“想当然”；</li><li>模型输出完一段话，甚至忘了自己刚才说了什么。</li></ul><p>这是因为，<strong>Agent 的“智能”建立在模型本身的能力之上</strong>。但问题在于：</p><ul><li>大模型是<strong>概率模型</strong>，输出不确定</li><li>是<strong>无状态</strong>的，记不住历史</li><li><strong>每次调用都可能“重新做人”</strong></li></ul><p>所以当 Agent 需要多轮决策、工具调用和状态记忆时，就容易出错：出错一次是模型的锅，出错三次就是你 Agent 架构的问题。做一个<strong>能跑起来的 Agent</strong>并不难，但做一个<strong>靠谱、稳定、能复用的 Agent</strong>，就真得下功夫了。所以 Barry（Anthropic）才说：</p><blockquote><p>Agent 最适合用在那些任务复杂、有价值、可行性高，但对失败容忍度也高的场景。</p></blockquote><p>换句话说：<strong>重要但不致命的活，Agent 才能放心接。</strong> 有时候我看着 Agent 输出的内容都想劝它一句：您别干了，您歇会儿，活我自己来。</p><h2 id="从单Agent-solo-到多-Agent-乐队"><a href="#从单Agent-solo-到多-Agent-乐队" class="headerlink" title="从单Agent solo 到多 Agent 乐队"></a>从单Agent solo 到多 Agent 乐队</h2><p>既然 Agent 一不留神就翻车，那业内当然要出招解决。于是，<strong>各种 Agent 架构和“派系”纷纷出山，八仙过海，各显神通。</strong></p><h3 id="workflow-派-：你别瞎干，我帮你安排好流程"><a href="#workflow-派-：你别瞎干，我帮你安排好流程" class="headerlink" title="workflow 派 ：你别瞎干，我帮你安排好流程"></a>workflow 派 <strong>：你别瞎干，我帮你安排好流程</strong></h3><p>Agent 总是乱来？那我们就固定运行流程，让 AI 仅在需要的环节发挥作用就好。Agent 与 workflow 最大的区别在于大模型能否在其中起到自主规划的作用。目前workflow更多的是对业务逻辑的描述和封装，AI仅作为其中的一个节点&#x2F;组件服务于特定环节。说白了就是：<strong>你别琢磨怎么决策，你就把第三步的 API 调了就行。</strong></p><p>我们依然借用 Anthropic 对二者区别的描述：</p><blockquote><p>Workflows are systems where LLMs and tools are orchestrated through predefined code paths</p><p>Agents, on the other hand, are systems where LLMs dynamically direct their own processes and tool usage, maintaining control over how they accomplish tasks.</p></blockquote><p>虽然 workflow 难以支撑复杂场景的端到端智能化，但是它可以作为Agent 的工具，成为 Agent 的一部分</p><h3 id="Beyond-Agent-派：别再“走一步看一步”了"><a href="#Beyond-Agent-派：别再“走一步看一步”了" class="headerlink" title="Beyond Agent 派：别再“走一步看一步”了"></a>Beyond Agent 派：<strong>别再“走一步看一步”了</strong></h3><p>在前面我们提到 Agent 通常是在循环中根据环境反馈进行决策和⼯具使用，虽然这种 ReAct 模式是当下主流的 Agent 行动框架但它也存在显著的问题：走一步看一步，缺少全局规划；工具调用结果追加到上下文中导致 token 爆掉，最终任务执行失败。</p><p>于是更“聪明”的结构来了：</p><p><strong>✅ Plan &amp; Execute</strong></p><ul><li>全局规划：根据任务生成一个全局的多步骤的详细执行计划</li><li>任务执行：按照计划表进行执行，返回结果</li><li>重规划：根据执行结果动态调整计划</li></ul><p>Manus 是一种典型借鉴这种思路的Agent，通过生成全局规划并按照任务清单进行执行，避免出现“走一步看一步，渐行渐远”的经典 Agent 翻车场景。</p><p><strong>✅ ReWOO（Reasoning Without Observation）</strong></p><p>相较于 Plan&amp;Execute ReWOO 在规划器和执行器之外增加了一个求解器(Solver)，Solver专门负责综合规划和工具执行结果生成最终答案，极端一点：大模型只管思考，调用逻辑交给外部系统。</p><p><strong>✅ LLMCompiler</strong></p><p>把任务提前编译成 DAG，所有调用异步并发，解决 ReAct 顺序调用工具导致的延迟高、成本大、准确率受限等问题。</p><h3 id="多-Agent-乐队"><a href="#多-Agent-乐队" class="headerlink" title="多 Agent 乐队"></a>多 Agent 乐队</h3><p>当单 Agent 在处理复杂任务遇到上限时，我们开始尝试让 Agent 组成团队，用类似人类协同的方式来提升整体的性能。单个 Agent 能力有限，干脆来点“集体智慧”。于是我们看到多 Agent 架构开始流行。几种典型组织方式如下：</p><table><thead><tr><th><strong>架构</strong></th><th><strong>特点</strong></th></tr></thead><tbody><tr><td>网状结构</td><td>自由对话，但容易吵成一团</td></tr><tr><td>监督者模式</td><td>有个“老板 Agent”，所有人听它的</td></tr><tr><td>层级结构</td><td>类似人类组织：经理-主管-执行者</td></tr></tbody></table><p><strong>框架选型也不少：</strong></p><ul><li>LangGraph &#x2F; LangChain：早期代表，需要你<strong>手动编排流程</strong>，复杂但灵活</li><li>CrewAI &#x2F; AutoGen：更偏向<strong>多 Agent 协作编排</strong>，结构清晰，功能封装好</li></ul><p>现在的趋势是：<strong>越来越多框架都在做“Agent 编排”</strong> ，即通过多 Agent 协作完成复杂任务，而不是让一个 Agent 背锅到底。甚至 Google、OpenAI 都推出了自己的 Agent SDK，说明这个方向正走向标准化。但多 Agent 架构也不全是美好：多打一把反而打成绣花针，协调成本甚至超过了问题本身。沟通同步、任务冲突、上下文割裂、角色扮演错乱……这不是搞 AI，是搞一场全息职场模拟。</p><p>所以也有人说：</p><blockquote><p>Don’t build multi-Agents.</p></blockquote><p>你看人类都天天内耗了，Agent 组队翻车也不过分吧？🤣</p><h1 id="一些思考和感悟"><a href="#一些思考和感悟" class="headerlink" title="一些思考和感悟"></a>一些思考和感悟</h1><h2 id="落地AI的“五味瓶”"><a href="#落地AI的“五味瓶”" class="headerlink" title="落地AI的“五味瓶”"></a>落地AI的“五味瓶”</h2><p>在经过与大模型相爱相杀的一番折腾之后无论是技术同学还是为此买单的老板，几乎都会经过以下的心理路程：</p><p>1、AI 什么都能做</p><p>2、AI 什么都不能做</p><p>3、好像还是能做</p><p>4、好像还是不能做</p><p>5、貌似换个姿势也能做</p><p>简单总结：从信仰到破灭，再到将信将疑的实用主义。AI 到底能不能行，仍需要在具体的场景中贴紧业务上下求索。</p><h2 id="Agent-开发：还是门复杂的工程活"><a href="#Agent-开发：还是门复杂的工程活" class="headerlink" title="Agent 开发：还是门复杂的工程活"></a>Agent 开发：还是门复杂的工程活</h2><p>你想让 Agent 不翻车，至少得解决这些问题：</p><ul><li>提示词怎么写才对齐意图？</li><li>工具描述怎么结构化才不被模型误解？</li><li>怎么容错？怎么回滚？怎么重试？</li><li>怎么观察系统表现？怎么自动化测试？</li></ul><p>看起来像是“AI 编排”，本质上还是**开发一套能自证正确的分布式系统，**这仍然是一项复杂的工程挑战。</p><h3 id="未来的-Agent-会变成什么样"><a href="#未来的-Agent-会变成什么样" class="headerlink" title="未来的 Agent 会变成什么样"></a>未来的 Agent 会变成什么样</h3><p>从 <strong><code>通过提示词使模型输出工具调用指令</code></strong> 到 <strong><code>OpenAI 将 Function Call 的能力训练到模型内部</code></strong></p><p>从 <strong><code>提示词引导模型进行思维链模式的思考</code></strong> 到 <strong><code>OpenAI O 系列模型/DeepSeek R1 推理模型开始登场</code></strong></p><p>从<code>纯工程 Agent</code> 到<code>模型微调 Agent</code> 再到<code>端到端 Agent</code></p><p>模型的能力正在一步步增强，它们逐渐能够直接完成复杂任务从而减少甚至消除对传统意义上独立于模型之外的Agent开发工作的需求。</p><p>当下正有一种“主流”观点：<strong><code>在未来模型即服务、模型即产品</code>。</strong> 该观点认为Agent 的关键在于模型内在智能的提升，而不是通过预先设定好的、由提示词和工具调用路径构成的工作流，AI Agent 发展的下一个阶段是将传统Agent所追求的“自主性”（如自主规划、决策、执行能力）直接训练到模型内部，<strong>Agent 消失，智能内生。</strong></p><p><strong>如此以来工程复杂度没了，效果还更好了。如果真有那么一天，可能唯一还需要我的地方就是：给它插上电源，静静地看它自己上线、自己调用、自己复盘……然后顺手开我一张发票。✌️</strong></p><h1 id="尾声：你也在搞-Agent-吗？"><a href="#尾声：你也在搞-Agent-吗？" class="headerlink" title="尾声：你也在搞 Agent 吗？"></a><strong>尾声：你也在搞 Agent 吗？</strong></h1><p>你如果也在做 Agent，欢迎来聊聊你遇到过最迷惑的调用行为，或者你最想对 Agent 说的一句话。</p><p>评论区见，我们继续 debug。</p>]]></content>
      
      
      <categories>
          
          <category> AI </category>
          
      </categories>
      
      
        <tags>
            
            <tag> AI </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>工具我都接上了，模型你倒是调用啊！</title>
      <link href="/2025/06/20/mcp/"/>
      <url>/2025/06/20/mcp/</url>
      
        <content type="html"><![CDATA[<p>当下，AI 领域最炙手可热的概念莫过于 MCP。随着全球开发者对 Agent 技术的热情空前高涨，MCP 一跃成为 AI 生态中的“底层标准”，也逐渐成为业内讨论的焦点之一。 </p><p>最近我也开始尝试将业务中的一些能力通过 MCP Server 的方式暴露出来，供大模型调用。初步实践下来效果还不错，甚至让我一度动了“重构网关，把所有服务都暴露成 MCP”的念头。毕竟谁能拒绝一个“全能助理”在你业务上自由驰骋的画面呢？ </p><p>不过很快我发现：你以为自己是在对接 API，实际可能是在和大模型谈恋爱 —— 你说的，它未必懂；它懂了，也未必照做。 </p><p>所以在真正行动之前，我想结合自己的开发体验，聊聊 MCP 和 function call 到底有什么不同，大模型是如何知道 MCP Tool 是干什么的，以及目前 MCP 生态中存在的一些“不成熟”现象。</p><h2 id="一些关于-MCP-的一些体验与思考"><a href="#一些关于-MCP-的一些体验与思考" class="headerlink" title="一些关于 MCP 的一些体验与思考"></a>一些关于 MCP 的一些体验与思考</h2><h2 id="1、function-call-与-MCP-的联系与区别"><a href="#1、function-call-与-MCP-的联系与区别" class="headerlink" title="1、function call 与 MCP 的联系与区别"></a>1、function call 与 MCP 的联系与区别</h2><p>MCP 推出之后，不少人误以为它是对传统 function call 机制的一种替代。但实际上，function call 和 MCP 是两个不同层面的概念，它们并不是此消彼长的关系，而是彼此协作、紧密配合的。</p><p>Function Call 是大模型与外部工具或 API 交互的“能力本体”。它让模型能判断是否需要使用工具、该用哪个工具，并正确地调用 —— 也就是说，它解决的是“工具怎么调用”的问题。</p><p>MCP 更像是一个“工具生态的标准化容器”：它定义了如何管理工具、如何暴露服务、如何实现复用，提供的是一套统一的接口规范。它解决的是“工具如何被组织、共享和复用”的问题。 </p><p>在底层，MCP 实际仍然依赖 function call 来完成工具调用。可以这么理解：function call 是能力，MCP 是协议与生态。一个不支持 function call 的模型，是无法使用 MCP 的；反过来，有了 function call，不一定就具备了 MCP 的工具管理能力。</p><p>所以与其说 MCP 是“取代”了 function call，不如说它是在 function call 之上，搭建了一个更通用、可复用的交互标准。它把原本“手工打 API 工具包”的工作，变成了标准化集成开发的方式。类比来说，<strong>function call 是插座接口，而 MCP</strong> <strong>是整套电力布线和电器管理系统</strong>。插座能用，不代表电网有序；MCP 想运转，也得靠插座供电。</p><h2 id="2、MCP-Host-到底是如何将工具告诉模型的？"><a href="#2、MCP-Host-到底是如何将工具告诉模型的？" class="headerlink" title="2、MCP Host 到底是如何将工具告诉模型的？"></a><strong>2、MCP Host 到底是如何将工具告诉模型的？</strong></h2><p>在 MCP 协议中，MCP Host（例如 Claude Desktop）扮演着一个关键角色：它既要通过 MCP Client 从 MCP Server 获取 Tool 的定义，又要将这些工具“介绍”给大模型使用；而在大模型发出调用指令时，它还需要负责请求对应的 MCP Server 执行实际调用。 </p><p>但问题来了 —— Host 是如何把这些工具的“使用说明书”交给大模型的？ 遗憾的是，MCP 协议本身并没有对此做出明确规定。 </p><p>实际上，根据当前各平台的实现，常见有两种方式：</p><p>✅ 方式一：通过 Tools 参数传递（主流、结构化） </p><p>目前主流的大模型 API（如 OpenAI ChatGPT、Anthropic Claude、阿里 Qwen 等）都支持一个专门的 <code>tools</code> 参数。MCP Host 会将从 MCP Server 获取到的 Tool 描述，转换成各模型 API 所要求的格式（例如 OpenAI 的 JSON Schema、Claude 的 function signature 等），然后通过该参数传递给模型。</p><p>这种方式的优势在于：</p><ul><li>结构清晰，格式规范</li><li>控制粒度高，易于调试</li><li>更符合模型设计初衷，调用更稳定</li></ul><p>简单来说，这是目前<strong>最通用、最可靠的方式</strong>。 </p><p>⚠️ 方式二：通过 Prompt 注入（非主流、风险高） </p><p>在一些特殊场景下，工具描述也可能被直接拼接进 Prompt 中传递给模型 —— 比如社区用户抓包分析 cline 请求时就发现，某些工具定义是以纯文本形式“注入”到 prompt 中的。 </p><p>出现这种做法的常见原因有： </p><ul><li>当前模型 API 不支持独立的 tools 参数（如某些早期模型或自研大模型）</li><li>想补充一些额外的上下文信息（如示例用法、调用期望等）</li></ul><p>但这种方式存在明显的安全隐患，比如可能被恶意构造的 prompt 破坏调用逻辑，触发所谓的“Prompt 注入攻击”。同时它也增加了 prompt 长度成本，并降低了调用结果的稳定性。 </p><p>因此可以看出，虽然 MCP 协议没有强制 Host 采用哪种传递方式，但在实际落地中，结构化工具参数 + 明确工具 schema 的方式，已成为主流 Host 的标准实现路径。</p><h2 id="3、通过-Tools-参数和-Prompt-注入两种方式的本质区别与联系"><a href="#3、通过-Tools-参数和-Prompt-注入两种方式的本质区别与联系" class="headerlink" title="3、通过 Tools 参数和 Prompt 注入两种方式的本质区别与联系"></a>3、通过 Tools 参数和 Prompt 注入两种方式的本质区别与联系</h2><p>不管是通过 <code>tools</code> 参数，还是通过 prompt 注入，<strong>从模型底层的角度看，最终的处理方式其实殊途同归</strong> —— 都会被转换成一串文本输入给大模型。因为，<strong>大语言模型的本质就是一个“根据输入文本预测输出文本</strong> <strong>”的神经网络系统</strong>：</p><ul><li>输入：被分词器（tokenizer）编码后的文本序列</li><li>处理：通过 Transformer 架构做序列建模和 token 预测</li><li>输出：下一个 token 的概率分布，并选择高概率 token 输出</li></ul><p>所以，无论我们给它的是结构化的 <code>tools</code> 参数，还是拼接好的 prompt 字符串，<strong>它最终都要“读成文本”来理解工具内容</strong>。<code>tools</code> 参数的优势更多体现在接口设计上：它是为了让开发者方便组织和提交工具信息，结构更清晰、机器更容易处理。实际上，很多模型厂商（如 OpenAI 和 Anthropic）内部会把 <code>tools</code> 参数转换成紧凑的内部表示，再嵌入到模型输入中，并可能在训练阶段对工具格式做过针对性优化。</p><p>因此从 <strong>推理成本</strong> 来看，二者并无本质差别，甚至结构化方式还有可能更节省 token。</p><p>但无论使用哪种方式，<strong>对 MCP Tool 开发者而言，最重要的事情始终是：写好工具的描述。</strong></p><p>一个好的工具描述，应该清晰传达工具的用途、输入输出参数、使用场景等关键信息。模型需要依赖这些内容来判断是否使用工具、使用哪个工具、传什么参数。</p><p>正如 Anthropic 在一篇关于开发多智能体系统的研究中所强调的：<strong>糟糕的工具描述不仅会让模型产生误解，甚至可能导致整****个智能体行为方向跑偏。</strong></p><p>同样，对 Agent 开发者来说也有一个容易忽视的陷阱：<strong>工具越多，未必越好。</strong></p><p>因为每个 Tool 的描述都会被编码进模型的输入序列，工具越多，输入文本就越长，而 Transformer 架构的注意力机制是“有长度限制”的 —— 当输入超出模型的关注范围时，它可能根本没读到后面的 Tool 定义，或者只记住了开头几个，导致工具调用能力下降、理解失误频发。</p><p>所以，合理控制 MCP Tool 的数量、长度与精度，是提升智能体效果的关键一环。</p><h2 id="4、大厂为什么给-Tools-描述“白嫖”额度？"><a href="#4、大厂为什么给-Tools-描述“白嫖”额度？" class="headerlink" title="4、大厂为什么给 Tools 描述“白嫖”额度？"></a>4、大厂为什么给 Tools 描述“白嫖”额度？</h2><p>在使用大模型 API 的过程中，有个细节常常被开发者忽略 —— 当我们通过 <code>tools</code> 参数传递 MCP Server 的工具描述时，<strong>这些内容有时并不会计入 token 费用，或者只按****照更低的费率计算。</strong></p><p>那么问题来了：<strong>大模型处理这些内容明明也是“读文本”，为什么这部分****不怎么收费？</strong></p><p>其实这里面的考量，既有技术实现上的差异，也有平台方精心设计过的商业策略：</p><p>✅ <strong>1. 布局生态：推动标准化工具体系</strong></p><p>各大模型厂商（如 OpenAI、Anthropic）都在构建以自身为中心的“Agent 工具生态”。</p><p>为了吸引开发者使用官方标准（比如 tools 参数、MCP 协议），而不是自定义 prompt hacking 方式，<strong>自然会在计费策略上给予倾斜</strong>。</p><p>这种鼓励政策可以促使更多开发者以“平台认同的方式”构建能力，从而让整个工具生态更加统一、规范、可控。</p><p>✅ <strong>2. 降低门槛：提升工具功能的使用率</strong></p><p>工具功能是大模型从“聊天机器人”向“智能体”演进的关键一环。</p><p>如果每试一个工具就要为冗长的描述付出高昂 token 成本，那么开发者的使用意愿无疑会大打折扣。</p><p>因此，不计费或低费率，是为了<strong>让开发者更自由地实验工具组合、试错设计、快速迭代</strong>。</p><p>尤其是 MCP 模式下，一个服务往往会包含多个工具，如果每个都计费，很容易劝退开发者。</p><p>✅ <strong>3. 实际成本低：标准化数据处理更高效</strong></p><p>从模型运行层面看，tools 描述往往采用统一结构，<strong>在模型的底层输入处理中可以使用优化后的编码方式</strong>，比自由文本 prompt 更易压缩、更快处理。</p><p>换句话说：即使模型看起来在“读工具说明书”，其实它的“阅读成本”远低于处理你随手写的 prompt。</p><p>对平台来说，这部分的边际成本并不高，完全可以承担。</p><p>✅ <strong>4. 数据价值大：反向用于模型优化</strong></p><p>最后还有一点经常被忽略：<strong>这些工具描述本身也是训练数据的宝库。</strong></p><p>平台可以通过观察开发者如何组织 tool、模型如何理解 tool、调用是否成功等信息，持续优化其模型行为与工具系统。</p><p>在这个角度看，平台甚至愿意“贴钱买数据”。</p><p><strong>看似慷慨的计费政策，背后其实是一次精密的生态运营与****商业博弈。</strong> 越来越多 MCP Server 被挂载上线，不仅丰富了模型能力，也推动了 Agent 生态的标准化与专业化。</p><h2 id="5、MCP-还不成熟，但未来可期"><a href="#5、MCP-还不成熟，但未来可期" class="headerlink" title="5、MCP 还不成熟，但未来可期"></a>5、MCP 还不成熟，但未来可期</h2><h4 id="🛠-后端开发者的“鸡肋时刻”"><a href="#🛠-后端开发者的“鸡肋时刻”" class="headerlink" title="🛠 后端开发者的“鸡肋时刻”"></a>🛠 后端开发者的“鸡肋时刻”</h4><p>MCP 的起源可以追溯到 Anthropic 的 Claude Desktop，它最初只是为了让桌面 AI 能调用本地文件、应用等功能而设计的接口协议。从客户端视角看，它无疑极大扩展了智能体的可操作边界 —— 真正让“AI 动手”成为可能。</p><p>但问题来了：<strong>对后端开发者来说，这协议属实有点反人类</strong>。</p><p>当前 MCP 协议广泛采用双通道通信机制，即同时维护一个 <strong>双向请求通道</strong> 和一个 <strong>SSE（Server-Sent Events）长链****接</strong>。服务端需要：</p><ul><li><p>创建一个临时 SSE 通道；</p></li><li><p>接收模型发来的工具调用请求；</p></li><li><p>将执行结果通过 SSE 通道返回；</p></li><li><p>再手动关闭链接。</p></li></ul><p>整个流程既不优雅也不高效，更别提在复杂业务下的部署、连接数控制和稳定性维护了。</p><p>好消息是：新版本的 MCP 协议正在尝试引入<strong>无状态调用支持</strong>，未来可能对一次性请求&#x2F;响应类的服务更加友好。但现阶段，MCP Client 仍然让不少后端开发者“直呼上头”。</p><h4 id="🧩-工具生态：热闹是热闹，就是不太能用"><a href="#🧩-工具生态：热闹是热闹，就是不太能用" class="headerlink" title="🧩 工具生态：热闹是热闹，就是不太能用"></a>🧩 工具生态：热闹是热闹，就是不太能用</h4><p>自从 Claude 率先开放 MCP 工具接入后，整个 AI 圈就像打开了潘多拉的盒子：<strong>短短几个月内，已有上千个 MCP 工具接入平台</strong>，一度形成 “谁还没个工具挂在 Claude 上？” 的热潮。</p><p>但热闹归热闹，真正有用的却寥寥无几。</p><p>根据我的观察，目前 MCP 工具中：</p><ul><li><p>有效可复用的不足 20%；</p></li><li><p>大量工具功能重复、描述模糊、质量参差；</p></li><li><p>缺乏系统性的评分体系，Agent 无法判断哪个更好用。</p></li></ul><p>这直接导致一个尴尬局面：</p><blockquote><p>Agent 面前横着一堆工具 —— 重名的、近义的、毫无描述差异的，谁也不认识谁，只能挨个试。</p><p>模型懵，token 飞，开发者心在滴血。</p></blockquote><p>某种程度上讲，当前 Agent 的性能差异往往并不来自大模型本身，而是<strong>是否用了靠谱的 MCP 工具集</strong>。比如被广泛讨论的 Manus，就通过精心构建和定制化打磨 MCP 工具，大幅提升了 Agent 的任务完成率。</p><h2 id="📌-结语：我们还在山脚下，但至少看清了方向"><a href="#📌-结语：我们还在山脚下，但至少看清了方向" class="headerlink" title="📌 结语：我们还在山脚下，但至少看清了方向"></a>📌 结语：我们还在山脚下，但至少看清了方向</h2><p>MCP 协议作为模型与工具之间的桥梁，确实带来了范式的改变 —— 从“让模型看世界”到“让模型操作世界”。</p><p>尽管它现在还不够成熟，后端机制有待简化，生态仍需清洗和筛选，但它所代表的方向是清晰的：<strong>一个可调用、可协作、可复用的 AI 能力生态系统。</strong></p><p>如果你也在开发 MCP Server，欢迎一起来交流踩坑经验 ——</p><p>顺便说说你写过最 <strong>离谱</strong>、但最终却最 <strong>有效</strong> 的工具描述，那些模型“懂了也不一定照做”的瞬间。</p>]]></content>
      
      
      <categories>
          
          <category> AI </category>
          
      </categories>
      
      
        <tags>
            
            <tag> AI </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>LSM-Tree vs B-Tree：NewSQL为何偏爱LSM-Tree？</title>
      <link href="/2024/04/14/why-newsql-use-lsmTree/"/>
      <url>/2024/04/14/why-newsql-use-lsmTree/</url>
      
        <content type="html"><![CDATA[<h1 id="问题由来"><a href="#问题由来" class="headerlink" title="问题由来"></a>问题由来</h1><p>最近在梳理业务中所使用的存储时脑子里突然冒出一个问题：为什么大部分 NewSQL 的践行者们在选择底层的存储模型时不约而同的选择了 LSM-Tree？在数据存储领域除了 LSM-Tree 之外另一个常用的存储模型就是 B-Tree（这里将 B+Tree 看做是 B-Tree 的一种），相比于 B-Tree，LSM-Tree 有哪些优劣之处？</p><h2 id="什么是-NewSQL，它要解决的问题是什么"><a href="#什么是-NewSQL，它要解决的问题是什么" class="headerlink" title="什么是 NewSQL，它要解决的问题是什么"></a>什么是 NewSQL，它要解决的问题是什么</h2><p>要回答这个问题首先要弄清楚什么是 NewSQL 以及它要解决的问题是什么。在<a href="https://451research.com/analyst-team/analyst/Matt+Aslett">Matthew Aslett</a> 2011 年的一篇<a href="http://cs.brown.edu/courses/cs227/archives/2012/papers/newsql/aslett-newsql.pdf">论文</a>中第一次提出 NewSQL 的概念，它用来指代提供类似 NoSQL 高吞吐、高可用支持，同时仍然保持 ACID 特性的新一代数据库系统。旨在解决传统关系型数据库在大规模数据处理、高并发和分布式环境下的性能瓶颈。与 NoSQL 一样都是为了解决传统关系数据库的瓶颈问题，但NewSQL更注重于保持数据的一致性和可靠性，而NoSQL则更注重于可扩展性和高性能。</p><p>从上述描述可以看出 NewSQL 所要解决的问题之一就是高并发下的性能瓶颈，对于关系型数据库的高并发读取业内已经有比较成熟的解决方案，而在数据写入上需要寻找更好的解决方案。</p><p>明白了 NewSQL 的核心目标后，我们接下来将深入探讨 LSM-Tree 的特性以及它如何成为解决高并发写入瓶颈的关键。至于分布式场景下的一些其他问题，例如：分布式事务、全局时间戳、高可用等问题就不在本次讨论的范围之内</p><h1 id="LSM-Tree-的写入优势"><a href="#LSM-Tree-的写入优势" class="headerlink" title="LSM-Tree 的写入优势"></a>LSM-Tree 的写入优势</h1><p>写友好可以说是 LSM-Tree 身上最明显的一个标签，我们通过一张关于 LSM-Tree 经典的图来看看在写入过程中都做了哪些操作，并跟 B-Tree 做个对比：</p><figure class="image-bubble">                <div class="img-lightbox">                    <div class="overlay"></div>                    <img src="/img/lsm-tree.png" alt="" title="">                </div>                <div class="image-caption"></div>            </figure><h2 id="LSM-Tree-和-B-Tree-的数据写入过程对比"><a href="#LSM-Tree-和-B-Tree-的数据写入过程对比" class="headerlink" title="LSM-Tree 和 B-Tree 的数据写入过程对比"></a>LSM-Tree 和 B-Tree 的数据写入过程对比</h2><h3 id="第一步：写-WAL-日志"><a href="#第一步：写-WAL-日志" class="headerlink" title="第一步：写 WAL 日志"></a>第一步：写 WAL 日志</h3><p>WAL(Write Ahead Log)日志在各类数据库中都存在，只是名字有所不同，例如：InnoDB 中的Redo Log。他们作用都是一样：为数据库提供 crash-safe 的能力，在数据库宕机之后通过该日志可以保证数据的恢复。</p><p>通过WAL的名字可以看出该日志是以顺序写入的方式不断追加，因此 LSM-Tree 和 B-Tree 在这一步中性能开销是比较接近的。</p><h3 id="第二步：树结构操作"><a href="#第二步：树结构操作" class="headerlink" title="第二步：树结构操作"></a>第二步：树结构操作</h3><p>在这一步中 LSM-Tree 主要是将数据写入 Memtable 中，而 B-Tree 是要将数据插入到合适的位置。这一步成为了二者在数据写入方面性能差异的关键：</p><ol><li><p>首先数据的写入都是内存操作，有 WAL 的加持两种存储结构都不需要数据落入磁盘之后才算写入成功</p></li><li><p>LSM-Tree 可以看做是在内存中进行顺序追加，而 B-Tree 则是从索引根节点开始找到记录行应该存储的数据页，写入该数据。如果在此过程中任何页不在缓存中则需要发生磁盘 IO 操作，如果发生页分裂则可能会产生更多的磁盘 IO。由此来看 B-Tree 的维护就慢了，尽管像 MySQL 等数据库通过 buffer pool 的方式来减少磁盘的 IO 但整体而言其速度还是低于 LSM-Tree。其原因之一是 B-Tree 为了提升查询的速度在数据写入时便进行了数据的规整，正如一条谚语所说：如果你把东西整理的井井有条，下次就不用查找了。但也因此降低了写入性能。</p></li><li><p>原地更新于异位更新间的差别。原地更新是指直接覆盖旧记录来存储新更新，例如：B-Tree，但因为更新通常会带来随机 IO 因此牺牲了写入的性能，而且可以发现 B-Tree 实际是一种读后写。异位更新是指总是将更新存储到新的位置而不是覆盖旧的条目，例如LSM-Tree，它通过顺序 IO 操作来提升了写入的性能。</p></li></ol><p>通过上面的分析可以看出 LSM-Tree 在写入方面存在一定的优势，但同时细心的你也会发现一些问题，这些问题也会成为技术选型过程中的一些担忧点：</p><ol><li><p>异位更新会导致同一条数据存在多个文件中的任意一个或多个，获取数据时需要读取多个地方，读取性能是否会受到影响？</p></li><li><p>LSM-Tree 通过追加写的方式代替读后写，那么在需要对已有数据做检查才能判定能否写入&#x2F;更新的场景该如何解决，性能又如何？</p></li></ol><p>以上两个问题我们会在后面会继续讨论</p><h3 id="第三步：脏数据落盘"><a href="#第三步：脏数据落盘" class="headerlink" title="第三步：脏数据落盘"></a>第三步：脏数据落盘</h3><p>无论是 LSM-Tree 还是 B-Tree 这一步都是异步执行，本身不会对写入性能造成太大影响。但也正因为是异步操作，所以给数据库提供性能优化空间，比如：InnoDB 的 inset merge、LSM-Tree 的 compact 等</p><h1 id="三、关于-LSM-Tree-的读取性能"><a href="#三、关于-LSM-Tree-的读取性能" class="headerlink" title="三、关于 LSM-Tree 的读取性能"></a>三、关于 LSM-Tree 的读取性能</h1><p>从上面的图可以看出从 read 这个方块出发的线有多条，带来的直观感受就是 LSM-Tree 的读取性能肯定不行。读多次性能肯定是受损的，但是损失有多大还需要具体来看。</p><ol><li><p>首先，如果读取的数据命中 cache 或者刚写入 memtable 那么读取不涉及到磁盘操作，速度肯定是快的。</p></li><li><p>另外，前面我们说过在异步的流程中 LSM-Tree 会做数据的 compact，在 compact 的过程中除了有多版本数据去重、老数据删除等为了防止数据量不断增长的清理机制外，compact 的过程中会将多个文件中的数据按KEY排序写入新的文件中。也就是说SSTable 内部是排序的，每个 SSTable 文件中除了存放数据外还有该文件中 KEY 的起始值，以及数据对应的索引，因此即便是从文件中读取也并非是简单的按块扫描。</p></li><li><p>除此之外，每个 SSTable 中都有一个 bloom filter 用来判断要查找的 KEY 是否在文件中，避免无效的数据读取</p></li></ol><p>由此看见 LSM-Tree 并未完全放弃读性能，相反在异步操作的流程里暗暗的优化读性能，通过将数据分成很多小的顺序分组并配合 bloom filter 可以快速找到数据在磁盘中的位置，大大减少磁盘 IO，在大范围的的随机查找时还可以使用 SSTable 中的索引提升查找速度。</p><p>但这并不是说 LSM-Tree 整体的读取性能要比 B-Tree 要好，而是通过一些其他的优化方法缓解读取性能的损耗，使其处于一个可接受的范围内。</p><h1 id="四、关于读后写的问题"><a href="#四、关于读后写的问题" class="headerlink" title="四、关于读后写的问题"></a>四、关于读后写的问题</h1><p>说完 LSM-Tree 的读性能，我们再来看下关于读后写的问题，在实际的开发过程中经常会遇到根据现有数据做逻辑运算后更新数据的情况，例如使用<code>select value from table where id = 1 for update</code>语句进行当前读。在这种情况下仍然要对比的是 B-Tree 和 LSM-Tree 的读取性能，而且使用该语句本身就是考虑到并发冲突，因此该数据应该是一个更新比较频繁的数据，在这种情况下 SSTable 的读取数量会比较多，一定程度上削弱了 LSM-Tree 的优势。</p><h1 id="五、关于存储模型的思考"><a href="#五、关于存储模型的思考" class="headerlink" title="五、关于存储模型的思考"></a>五、关于存储模型的思考</h1><p>其实 LSM-Tree 的出现并不完，为何近些年才备受大家的关注呢？个人猜测是跟行业的发展有关，在信息科技发展初期数据产生的量小、变更少，但会被多次读取，是读远大于写，对于读友好的 B-Tree 结构刚好能够满足需求。但随着时代的发展，数据生成速度和量级远超过去，因此对于写友好的 LSM-Tree 逐步的走向更多大众的视野。</p><p>实际开发中到底是选择 B-Tree 还是 LSM-Tree 需要将业务场景代入之后进行判断，比如：你的系统数据写入规模如何，是相对稳定还是快速膨胀的？写入之后会被频繁读取吗？如果读取较少那么即便它读取时要分别读取 N 多个文件对你的系统又有什么影响呢，反正你也不怎么读它，对吧？</p><h1 id="六、总结"><a href="#六、总结" class="headerlink" title="六、总结"></a>六、总结</h1><p>回到文章最开始的那个问题，相比于 B-Tree LSM-Tree 在数据写入方面带来质的提升，并且对于近期写入数据的查询性能也有不错的表现，虽然在读性能方面受到了一定的影响，但是数据库的读写性能平衡性变得更为合理。</p><p>从某种角度讲，使用 LSM-Tree 是对数据库写入进行了一种 scale up 的提升，另外通过分布式集群等 scale out 的方式来解决读的问题总是比解决写的问题更容易，这就是 NewSQL 在解决大规模数据问题时选择 LSM-Tree 的底层逻辑。</p>]]></content>
      
      
      <categories>
          
          <category> Database </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>一场因 json null 搭配 interface 使用而引发的 panic</title>
      <link href="/2023/07/30/json-null-cause-nil-panic/"/>
      <url>/2023/07/30/json-null-cause-nil-panic/</url>
      
        <content type="html"><![CDATA[<p>上周因同事休假暂时帮忙维护一个服务，周五下午本以为要顺利度过本周的时候线上出了问题，在进行数据修复的时候发现之前一个工具代码的隐藏 bug，该 bug 会在进行数据修复时触发 panic。剥离具体业务逻辑之后代码逻辑如下：</p><figure class="highlight golang"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"><span class="comment">// 从数据库中查询数据，a 为具体数据的指针，当数据没找到时 a=nil</span></span><br><span class="line">a := findFromDB()</span><br><span class="line">m, _ := StructToMap(a)</span><br><span class="line"><span class="comment">// 通过 builder 构建数据</span></span><br><span class="line">b := NewJSONBuilder()</span><br><span class="line">b.SetData(m)</span><br><span class="line"><span class="comment">// 省略部分逻辑</span></span><br><span class="line">....</span><br><span class="line">b.AddValueWithPath(mask.DstPath, v)</span><br><span class="line">....</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br></pre></td></tr></table></figure><p><code>main</code> 函数逻辑很简单：从数据库中查询某个数据，将该数据转换为<code>map[string]interface{}</code>，再通过 <code>JsonBuilder</code> 根据<code>JsonPath</code>对数据进行修改，其中依赖的其他函数如下：</p><figure class="highlight golang"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">StructToMap</span><span class="params">(v <span class="keyword">interface</span>&#123;&#125;)</span></span> (<span class="keyword">map</span>[<span class="type">string</span>]<span class="keyword">interface</span>&#123;&#125;, <span class="type">error</span>) &#123;</span><br><span class="line">bytes, err := json.Marshal(v)</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line"><span class="keyword">return</span> <span class="literal">nil</span>, err</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> m <span class="keyword">map</span>[<span class="type">string</span>]<span class="keyword">interface</span>&#123;&#125;</span><br><span class="line">err = json.Unmarshal(bytes, &amp;m)</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line"><span class="keyword">return</span> <span class="literal">nil</span>, err</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> m, <span class="literal">nil</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> JSONBuilder <span class="keyword">struct</span> &#123;</span><br><span class="line">data <span class="keyword">interface</span>&#123;&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(b *JSONBuilder)</span></span> SetData(data <span class="keyword">interface</span>&#123;&#125;) <span class="type">error</span> &#123;</span><br><span class="line"><span class="keyword">if</span> b == <span class="literal">nil</span> &#123;</span><br><span class="line"><span class="keyword">return</span> ErrNilBuilder</span><br><span class="line">&#125;</span><br><span class="line">b.data = data</span><br><span class="line"><span class="keyword">return</span> <span class="literal">nil</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(b *JSONBuilder)</span></span> AddValueWithPath(path <span class="type">string</span>, value <span class="keyword">interface</span>&#123;&#125;) (*JSONBuilder, <span class="type">error</span>) &#123;</span><br><span class="line"><span class="comment">// 省略部分逻辑</span></span><br><span class="line">....</span><br><span class="line"><span class="keyword">if</span> b.data == <span class="literal">nil</span> &#123;</span><br><span class="line">b.data = <span class="built_in">make</span>(<span class="keyword">map</span>[<span class="type">string</span>]<span class="keyword">interface</span>&#123;&#125;)</span><br><span class="line">&#125;</span><br><span class="line">data = b.data</span><br><span class="line"><span class="keyword">for</span> i:=<span class="number">0</span>; i&lt;<span class="built_in">len</span>(keys); i++ &#123;</span><br><span class="line"><span class="comment">// 省略部分逻辑</span></span><br><span class="line">....</span><br><span class="line"></span><br><span class="line">data[keys[i]]=value <span class="comment">// panic！</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 省略部分逻辑</span></span><br><span class="line">.....</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line">....</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>在讨论为何会引发 panic 之前，有几个背景知识需要交代清楚：</p><ol><li><code>json.Marshal(v any) ([]byte, error)</code> 函数如果入参为 nil 则返回的 bytes 内容为<code>null</code></li><li><code>json.Unmarshal(data []byte, v any)</code> 函数如果入参 <code>data</code>为<code>null</code>则函数执行结束之后 V 的值为 nil</li><li>golang 的 interface 的内部实现两个字段：<code>type</code> 和 <code>data</code>，只有两个字段都为 nil 时<code>interface == nil</code>才为 <code>true</code></li></ol><p>这里引发 panic 的主要原因是<code>StructToMap</code>返回的 map 为 nil，在后续将其赋值给了<code>builder.data</code>， 而 <code>data</code> 为<code>interface</code> 类型，因此该<code>interface</code>的<code>type</code>为<code>map[string]interface{}</code>，而该<code>interface</code>的<code>data</code>为 <code>nil</code>，最终导致<code>if b.data == nil </code>的判断语句失效，进而没有进行数据初始化引发了 panic。</p><p>这里只需将<code>if b.data == nil </code>修改为<code>if b.data == nil || reflect.ValueOf(b.data).IsNil()</code> 即可准确的判断出 <code>interface</code>是否为 nil，从而完成数据初始化避免 panic</p><p>golang 中的 inteface 是否为 nil 的判断已经是老生常谈的问题了，但在实际开发过程中还是经常被忽略掉，再搭配到 json 包一起使用，一不小心还是很容易踩坑。</p>]]></content>
      
      
      <categories>
          
          <category> Go </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>你可能并不需要显式的加分布式锁</title>
      <link href="/2023/03/18/not-need-lock/"/>
      <url>/2023/03/18/not-need-lock/</url>
      
        <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>在不同的系统或者同一个系统的不同实例间共享某个临界资源，为防止互相影响往往需要通过加锁的方式来将二者互斥开来。分布式锁就是在分布式系统中将多个同时对临界资源进行的操作互斥开的一种方式。</p><p>当我们遇到并发的对某个资源进行操作的场景时经常第一时间想到的是利用Redis、ZK等中间件加分布式锁来保证数据的准确性和一致性，但是在一些场景下并不一定需要显式的加锁来解决这个问题。</p><h2 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h2><p>这天亚瑟和胖虎遇到如下的一个业务场景：</p><p>假设一条记录中包含两个字段：A和B，同时存在两个业务逻辑：</p><p>A逻辑：更新A字段值为1，同时判断B字段值是否为1，如果是则通知下游删除数据</p><p>B逻辑：更新B字段值为1，同时判断A字段值是否为1，如果是则同步数据给下游时携带删除标记</p><p>思考一分钟，一般会怎么做呢？</p><h2 id="几种解决办法"><a href="#几种解决办法" class="headerlink" title="几种解决办法"></a>几种解决办法</h2><h3 id="Option-1"><a href="#Option-1" class="headerlink" title="Option 1"></a>Option 1</h3><p>不假思索的亚瑟先提出了一个大胆的想法：执行逻辑前先查一下数据，在执行逻辑时判断对方的字段是否有更新，操作流程如下：</p><figure class="image-bubble">                <div class="img-lightbox">                    <div class="overlay"></div>                    <img src="/img/lock-option-1.png" alt="" title="">                </div>                <div class="image-caption"></div>            </figure><p>但是当两个操作并发执行的时候A、B的两次读取都恰好读到对方字段没有发生变更，最终下游没有接到任何的删除通知，导致逻辑出错。</p><h3 id="Option-2"><a href="#Option-2" class="headerlink" title="Option 2"></a>Option 2</h3><p>为了补救上一种解法存在的问题，亚瑟提出一个更大胆的想法：在处理完自身逻辑通知下游前再查一次数据，这样如果是在处理逻辑时刚好对方字段有更新那就可以查到最新的数据，然后根据最新的数据判断是否要通知下游进行删除。</p><p>可真是个小机灵，问题解决，代码关闭，王者荣耀启动，让我康康今天谁还能阻止亚瑟成为峡谷中的王者！</p><p>但仔细一想虽然在通知下游前查询了一次最新数据，但依然没有解决问题，有可能会出现通知下游前的那次查询查到的还是旧数据，具体流程如下：</p><figure class="image-bubble">                <div class="img-lightbox">                    <div class="overlay"></div>                    <img src="/img/lock-option-2.png" alt="" title="">                </div>                <div class="image-caption"></div>            </figure><p>那怎么办呢，总不能在那里一直轮询吧，要轮询多久呢？况且轮询常有但并发不常有，万一没遇上并发岂不是白轮询了？着急去驰骋峡谷的亚瑟说道：实在不行加锁吧？手里的Redis已经跃跃欲试了，此时略加思索后的胖虎拍着亚瑟的肩膀说：我知道你急，但你先别急，且往下看。</p><h3 id="Option-3"><a href="#Option-3" class="headerlink" title="Option 3"></a>Option 3</h3><p>实际上我们将业务逻辑执行顺序稍作调整就可以解决这个问题，如下图所示：</p><figure class="image-bubble">                <div class="img-lightbox">                    <div class="overlay"></div>                    <img src="/img/lock-option-3.png" alt="" title="">                </div>                <div class="image-caption"></div>            </figure><p>我们将数据的更新操作放在二次查询之前，更新成功之后再查询最新数据并执行后面的通知逻辑。</p><p>为什么这样做可以避免上面的问题呢？</p><p>应了那句<del>废</del>老话：现在的关键问题是要找到问题的关键😂</p><p>问题的关键是在并发的时候需要将两个操作互斥开来，而又因为操作的是同一条记录那数据库的行锁恰好可以利用起来：</p><p>当逻辑A和逻辑B同时更新记录时先获取到行锁的会先执行，没有获取到锁的需要等待（通常只更新一条记录等待锁的时间不会很长），这个获取锁的逻辑将两个并发更新变成了串行，又因为更新后再查询了一次最新数据所以可以保证至少有一方逻辑是能获取到对方最新的更新记录，从而解决了这个问题。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>看完之后亚瑟说：掰扯了半天这不就是一句<code>select for update</code>可以解决的事吗？</p><p>哈哈，说的对但也不完全对。</p><p>使用<code>select for update</code>确实可以解决问题，但同时要考虑到这种在第一次查询就加锁的操作如果在业务逻辑复杂的场景下会使得事务变长、持锁的时间变长，增加锁的等待时间进而带来资源的损耗。另外，如果使用的存储并非关系型数据库也不提供<code>for update</code>的语义接口怎么办呢？此时使用上面Option 3的方法只要存储有行锁机制就可以达到<code>select for update</code>的效果。</p><p>当然这里有个大前提：两个操作更新的是同一个存储里的同一条数据。那如果是不同数据库里的不同数据呢，亚瑟问到。胖虎笑嘻嘻的说：你腰间不是还别着一把锁呢么~</p><p>亚瑟听完开启一技能兴冲冲的奔向了峡谷</p><p>望着他远去的背影胖虎脑子里又浮现出另一个topic：分布式系统中数据读写的线性一致性问题</p><p>未完待续……</p>]]></content>
      
      
      <categories>
          
          <category> 系统设计 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>说说那些“鬼见愁”的项目-Part2</title>
      <link href="/2022/12/03/the-dammit-mistaks-part2/"/>
      <url>/2022/12/03/the-dammit-mistaks-part2/</url>
      
        <content type="html"><![CDATA[<p>书接上回，继续聊聊“鬼见愁”项目中的那些神操作</p><h1 id="数据库相关"><a href="#数据库相关" class="headerlink" title="数据库相关"></a>数据库相关</h1><h2 id="1-ES索引过大导致集群分布不均衡"><a href="#1-ES索引过大导致集群分布不均衡" class="headerlink" title="1. ES索引过大导致集群分布不均衡"></a>1. ES索引过大导致集群分布不均衡</h2><p><strong>神操作：</strong> ES单索引分片数据过大导致集群中数据倾斜，历史数据不归档</p><p>单索引分片数据过大或者索引分片数与集群实例数相差较多容易导致数据倾斜，使得集群某个实例负载较高而其他实例负载较低，在集群整体负载不高的情况下出现部分响应耗时较长的现象，也容易出现因为某个实例的存储水位过高导致整个集群不能写入数据的情况。</p><p><strong>建议：</strong> 提前规划好集群规模与索引分片的大小，避免出现数据倾斜的问题</p><h2 id="2-历史数据不归档，数据无限增长"><a href="#2-历史数据不归档，数据无限增长" class="headerlink" title="2. 历史数据不归档，数据无限增长"></a>2. 历史数据不归档，数据无限增长</h2><p><strong>神操作：</strong> 存储资源申请好之后就一直往里写，对于不用的历史数据不删除、不归档</p><p>当存储中有较多的历史数据时，随着数据量的增长系统的响应时长增加，若还想要保证一定的性能，那么就得…得加钱！</p><p><strong>建议：</strong> 在初期要对数据存储规划好，某些场景数据可按时间分片存储，对于历史数据可及时的归档到成本更低的存储中，降低在线存储的压力</p><h2 id="3-数据库连接数过高"><a href="#3-数据库连接数过高" class="headerlink" title="3. 数据库连接数过高"></a>3. 数据库连接数过高</h2><p><strong>神操作：</strong> 一个物理数据库实例中创建了几十个逻辑数据库，在服务扩容的时候产生数据库连接数过高的问题</p><p>一个物理数据库中有M个逻辑数据库，一个服务部署了N个容器，一个容器中有K个进程，那么对于一个数据库实例来说有M×N×K个连接，连接数过高会增加资源的消耗对数据库来说也是比较危险。如果有Y个服务，那真的是要炸了，绝了嘿！到了这个地步已经是一个比较棘手的问题，服务也不能随便扩容，解决成本很高，在数据库前面加一层proxy可以缓解这个问题但要根本解决问题需要调整整个系统的设计</p><p><strong>建议：</strong> 单个数据库实例中不应放太多的逻辑数据库，对数据库的连接数量应做好控制。如果是因为性能问题而在一个容器中部署多个进程，应从系统性能测角度去解决问题，不应无脑的在一个容器中开启多个进程</p><h2 id="4-缺乏数据库连接生命周期管理"><a href="#4-缺乏数据库连接生命周期管理" class="headerlink" title="4. 缺乏数据库连接生命周期管理"></a>4. 缺乏数据库连接生命周期管理</h2><p><strong>神操作：</strong> 没有数据库连接的管理，执行过程中可能会使用到无效的连接</p><p>场景一：通过名字服务解析数据库地址失败导致获取不到IP，将空字符串作为IP传入DBManager导致在真正创建连接去执行SQL时失败</p><p>场景二：创建好的连接作为全局变量，当数据库proxy实例扩缩容时连接可能已经失效，在使用保存到全局变量里的连接时也不判断连接是否可用导致操作数据库失败</p><p><strong>建议：</strong> 尽可能的采用标准库或成熟库实现连接和连接池的管理，不能简单的通过全局变量来保存连接</p><h2 id="5-单表数据量过大"><a href="#5-单表数据量过大" class="headerlink" title="5. 单表数据量过大"></a>5. 单表数据量过大</h2><p><strong>神操作：</strong> 单表存放过亿条数据且存在较大的字段，存储大小过T，导致某些查询时间过长</p><p><strong>建议：</strong> 对表容量大小做好规划，及时归档历史数据或做拆表处理，对于较大字段可考虑用子表存储并进行压缩处理</p><h2 id="6-采用采用GBK、latin1编码，不支持特殊字符"><a href="#6-采用采用GBK、latin1编码，不支持特殊字符" class="headerlink" title="6. 采用采用GBK、latin1编码，不支持特殊字符"></a>6. 采用采用GBK、latin1编码，不支持特殊字符</h2><p><strong>神操作：</strong> 数据库编码使用latin1，写入一些特殊字符时会出现乱码</p><p>推测是历史问题，早期并没有考虑到字符编码的问题</p><p><strong>建议：</strong> MySQL使用utf8mb4编码，PostgreSQL使用UTF8编码</p><h2 id="7-Redis-key不设置过期时间，Redis内存爆炸"><a href="#7-Redis-key不设置过期时间，Redis内存爆炸" class="headerlink" title="7. Redis key不设置过期时间，Redis内存爆炸"></a>7. Redis key不设置过期时间，Redis内存爆炸</h2><p><strong>神操作：</strong> 使用Redis做缓存，有很多key没有设置过期时间且还有新的数据不断的写入，导致Redis内存爆炸</p><p><strong>建议：</strong> 对Redis中的缓存数据设置有效的过期时间，对于不期望过期的数据应对数据的大小及数据量随着时间的增长做到心中有数，重视存储的监控报警！</p><h1 id="监控报警相关"><a href="#监控报警相关" class="headerlink" title="监控报警相关"></a>监控报警相关</h1><h2 id="1-监控系统老旧，关键链路监控缺失"><a href="#1-监控系统老旧，关键链路监控缺失" class="headerlink" title="1. 监控系统老旧，关键链路监控缺失"></a>1. 监控系统老旧，关键链路监控缺失</h2><p><strong>神操作：</strong> 系统使用旧的监控平台，随着旧平台的遗弃系统的监控数据开始出现缺失</p><p>监控不完善会使得我们对业务的运行情况缺乏掌控力，甚至对外暴露的接口都由谁在调用都不是很清楚，出现线上问题不能快速定位问题。</p><p><strong>建议：</strong> 完善系统监控，包括基础的系统监控和关键的业务指标监控</p><h2 id="2-关键监控项没有报警或者报警被屏蔽"><a href="#2-关键监控项没有报警或者报警被屏蔽" class="headerlink" title="2. 关键监控项没有报警或者报警被屏蔽"></a>2. 关键监控项没有报警或者报警被屏蔽</h2><p><strong>神操作：</strong> 系统中的关键的监控项没有设置报警，甚至部分报警被屏蔽</p><p>监控项没有报警则监控作用丧失了一半，不可能天天人肉盯着监控表盘。如果因报警被消息太多而主动屏蔽应该考虑报警阈值设置的准确性和有效性，而不应该直接将其屏蔽，以免错过重要的报警信息。</p><p><strong>建议：</strong> 报警不应被屏蔽，并且应随着业务的发展要对报警阈值做合适的调整</p><h2 id="3-日志打印Unicode编码的中文"><a href="#3-日志打印Unicode编码的中文" class="headerlink" title="3. 日志打印Unicode编码的中文"></a>3. 日志打印Unicode编码的中文</h2><p><strong>神操作：</strong> 日志中打印出的中文为Unicode编码，不能一眼明确日志内容，需要将日志进行Unicode转中文影响工作效率</p><p><strong>建议：</strong> 日志中的信息如果能用英文描述清楚尽量用英文，如果使用中文则需要注意字符编码问题，日志本是用来排查问题，不应再给排查问题增加其他的负担</p><h1 id="系统设计相关"><a href="#系统设计相关" class="headerlink" title="系统设计相关"></a>系统设计相关</h1><h2 id="1-链路不统一，数据一致性难保证"><a href="#1-链路不统一，数据一致性难保证" class="headerlink" title="1. 链路不统一，数据一致性难保证"></a>1. 链路不统一，数据一致性难保证</h2><p><strong>神操作：</strong> 涉及到与下游交互的同一业务逻辑有两条不同的链路且都在生效，当下游接到的数据不符合预期时不知是哪条链路出现了问题</p><p>在某次case排查的过程中，原本以为只有一条生效链路，在这条链路上与下游一起排查了好久，最后发现还有另外一条链路，并且是另外一条链路bug引发的问题，坑爹至极。</p><p><strong>建议：</strong> 对于同一个逻辑不应存在多种生效的路径，如果是要对旧的路径做替换应及时下线旧的路径</p><h2 id="2-复杂的业务流程，缺乏补偿机制"><a href="#2-复杂的业务流程，缺乏补偿机制" class="headerlink" title="2. 复杂的业务流程，缺乏补偿机制"></a>2. 复杂的业务流程，缺乏补偿机制</h2><p><strong>神操作：</strong> 对于复杂且流程很长的业务逻辑缺少补偿机制，当在流程中某个环节出现失败时则这个内容将彻底无法继续下去，只能通过人工修数据的方式去解决，低效且易出错</p><p><strong>建议：</strong> 提供自动&#x2F;手动补偿的机制，以免出现故障时修数据时手忙脚乱</p><h2 id="3-系统重构烂尾，系统间关系混乱"><a href="#3-系统重构烂尾，系统间关系混乱" class="headerlink" title="3. 系统重构烂尾，系统间关系混乱"></a>3. 系统重构烂尾，系统间关系混乱</h2><p><strong>神操作：</strong> 系统重构到一半烂尾，一部分逻辑在新系统，一部分逻辑仍然在旧系统中，使得原本就不清晰的调用关系变的更复杂，系统的熵不减反增</p><p><strong>建议：</strong> 系统重构前应做好技术方案难度、可行性及人力评估，避免出现项目烂尾</p><h2 id="4-依赖的基础设施陈旧"><a href="#4-依赖的基础设施陈旧" class="headerlink" title="4. 依赖的基础设施陈旧"></a>4. 依赖的基础设施陈旧</h2><p><strong>神操作：</strong> 系统仍在使用陈旧的基础设施，而这些基础设施都已濒临下线或处于无人维护的状态</p><p><strong>建议：</strong> 跟随公司基础设施的迭代及时升级，使用陈旧的基础设施将使得系统稳定性更加难以保障</p><h2 id="5-不再使用的功能不维护却也不下线"><a href="#5-不再使用的功能不维护却也不下线" class="headerlink" title="5. 不再使用的功能不维护却也不下线"></a>5. 不再使用的功能不维护却也不下线</h2><p><strong>神操作：</strong> 对于一些不再使用的功能不进行下线处理，在意想不到的地方仍会触发旧功能逻辑引发bug</p><p><strong>建议：</strong> 及时清理下线不使用的功能代码，提升系统的稳定性</p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>在文章中所列举的问题一部分是在开发过程中不细心，没有养成良好的开发习惯所致；一部分是缺乏对系统的维护、缺乏对线上的敬畏之心；甚至还有一部分是非技术原因导致的。</p><p>系统中所表现出的问题都是表象，实际背后都是人的问题。对系统中所犯下的错误要引以为戒，以免在后续的工作中犯下相同的错误。</p>]]></content>
      
      
      <categories>
          
          <category> 系统设计 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Architecture &amp; Design </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>说说那些“鬼见愁”的项目-Part1</title>
      <link href="/2022/11/27/the-dammit-mistakes-part1/"/>
      <url>/2022/11/27/the-dammit-mistakes-part1/</url>
      
        <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>聊一聊之前接手的一个核心老系统，该系统主要负责内容的生产和加工，内容包括：图文、视频、直播等，加工生产主要指内容的发布、修改、特征计算和处理。内容在经过加工处理后送给下游进行个性化分发。 </p><p>在这个系统中见识到了许许多多令人震惊的神操作，这里面一部分是有历史原因还有相当一部分是因为不好的开发习惯以及项目缺乏维护而引起。</p><h1 id="代码相关"><a href="#代码相关" class="headerlink" title="代码相关"></a>代码相关</h1><h2 id="1-调用其他服务接口不判断响应结果"><a href="#1-调用其他服务接口不判断响应结果" class="headerlink" title="1. 调用其他服务接口不判断响应结果"></a>1. 调用其他服务接口不判断响应结果</h2><p><strong>神操作：</strong> 在调用其他服务接口得到response后不判断结果是否符合预期径直执行后续的逻辑或者将response直接返回给上层调用方</p><p>通常我们会跟对方约定好接口返回结构及状态码，调用方可以根据不同的状态码判断是否该重试、执行逻辑分支或者提前退出业务逻辑等，这种不判断返回的做法等于默认对其他服务调用成功而实际上是存在调用失败或者对方内部出错的情况，在这种情况下不应该再继续执行业务逻辑，否则会带来数据不正确&#x2F;不一致等问题。</p><p><strong>建议：</strong> 对于其他服务的接口调用应及时判断接口的返回结果，并根据结果执行对应的业务逻辑。</p><h2 id="2-好异步却不知Trace为何物"><a href="#2-好异步却不知Trace为何物" class="headerlink" title="2. 好异步却不知Trace为何物"></a>2. 好异步却不知Trace为何物</h2><p><strong>神操作：</strong> 代码里通过发送event的方式生成了很多异步执行的job，但缺少可以进行链路追踪的trace信息导致问题排查困难</p><p>项目中将一些逻辑复杂且不需要即时响应的工作通过异步的方式来处理是一种常见做法，当异步job及触发异步job的事件变多之后，因缺少trace信息在排查问题时将变得异常困难，仅通过打印日志的时间无法判断一次异常的操作(例如：数据库更新)是由哪条链路的哪个event触发而产生的，尤其在job内还会触发别的job的情况下事情变得更为复杂</p><p><strong>建议：</strong>  对每次请求都分配一个唯一的TraceID并将traceID在整个调用链(无论是异步job还是跨服务调用)中传递下去，配合日志及监控打点可以使得整条调用链有比较好的可观测性。对于服务的可观测性业内已有较为成熟的方案，可以借鉴并引入到项目中，将大大提升问题排查的效率</p><h2 id="3-滥用事件且监听随意"><a href="#3-滥用事件且监听随意" class="headerlink" title="3. 滥用事件且监听随意"></a>3. 滥用事件且监听随意</h2><p><strong>神操作：</strong> event名称与实际用途不一致，同一个event在多个不同场景下都会触发且随意增加监听event的listener进而导致逻辑混乱</p><p>异步虽好但不能贪杯。如果不对异步job进行管理和维护则容易产生滥用。例如：在某个新场景下需要触发一个异步任务，因为已经有个类似的job则偷懒在原job基础上添加了新逻辑并通过发送对应的event来触发产生异步job，但殊不知这个已有类型的event还被别的listener所监听，进而也触发了别的异步job执行带来了意想不到的结果</p><p><strong>建议：</strong> 明确event的作用范围避免随意复用。这更多的是在项目迭代过程中需要注意维护好event和job的对应关系，但这却常常被忽视</p><h2 id="4-日志不全、吞异常不打日志"><a href="#4-日志不全、吞异常不打日志" class="headerlink" title="4. 日志不全、吞异常不打日志"></a>4. 日志不全、吞异常不打日志</h2><p><strong>神操作：</strong> 日志信息记录不全甚至关键地方不打印日志</p><p>一些关键日志对于问题的定位和排查是十分重要的，然而却经常遇到某些关键链路上没有日志或者只有一句：<code>failed to update db</code>，你说没打日志吗？确实打了，但又跟没打一样。</p><p>还有一些场景逻辑执行产生了错误&#x2F;异常并将错误&#x2F;异常catch住后没有记录任何关于该异常的日志，并且返回了一个空值给上游，再加上调用方在接到空值后没有进行判断进而产生了一连串的逻辑错误。</p><p>每次查问题都要先补日志，这简直是天坑</p><p><strong>建议：</strong> 在一些关键路径以及产生异常错误时应尽量打印完整且有用的日志信息，包括：业务信息、trace信息、有效的错误堆栈等。打印日志的时候思考几个问题：这行日志是可不打的吗？看到这个日志能获取什么信息以及可以做什么？每个字段都是必须打印的吗？出现问题时能不能提高排查效率？</p><h2 id="5-数据同步消息发送时机混乱导致读取到旧数据"><a href="#5-数据同步消息发送时机混乱导致读取到旧数据" class="headerlink" title="5. 数据同步消息发送时机混乱导致读取到旧数据"></a>5. 数据同步消息发送时机混乱导致读取到旧数据</h2><p><strong>神操作：</strong> 两个系统之间通过kakfa消息传递信息，但因消息发送时机不对导致数据同步混乱</p><p>在某个场景中内容在系统A中发生变更，系统A发送消息通知系统B内容发生了变更，系统B接到消息后调用系统A的接口获取最新数据，然而系统A却在内容变更落库前先发送消息，导致系统B调用A的接口获取的仍然是旧数据</p><p>严格来说这是个bug</p><p><strong>建议：</strong> 明确消息的发送时机，确保获取到的数据有效性。</p><h2 id="6-方法名词不达意、随意复用方法引发bug"><a href="#6-方法名词不达意、随意复用方法引发bug" class="headerlink" title="6. 方法名词不达意、随意复用方法引发bug"></a>6. 方法名词不达意、随意复用方法引发bug</h2><p><strong>神操作：</strong> 方法名与实际逻辑不符，随意复用引发bug</p><p>在一个从方法名来看是做上报用户操作日志的方法中还连带发送了触发其他job的event，在其他地方复用该方法时触发了不该触发的job。</p><p><strong>建议：</strong> 好好写代码，别瞎搞</p><h2 id="7-过长的函数"><a href="#7-过长的函数" class="headerlink" title="7. 过长的函数"></a>7. 过长的函数</h2><p><strong>神操作：</strong> 所有逻辑在一个方法函数里完成，一个函数上千行，不易读懂和维护</p><p>一个过长的函数就像是一篇没有分段的作文，层次结构不分明，想到哪写到哪。如果一个函数的职责范围不够明确，后续有新的功能似乎都可以塞到这个函数里，长此以往这个函数变得难以读懂，不可维护。</p><p><strong>建议：</strong> 对于较长的业务流程可以按逻辑分块，拆分到不同的函数中并且明确函数的职责范围</p><h2 id="8-嵌套过深的if语句"><a href="#8-嵌套过深的if语句" class="headerlink" title="8. 嵌套过深的if语句"></a>8. 嵌套过深的if语句</h2><p><strong>神操作：</strong>  if语句嵌套过深，远观代码整体像一个向右的大箭头</p><p>嵌套过深的if语句理解起来比较费劲，后续再添加新的条件时容易出错</p><p><strong>建议：</strong>  可以采用不满足条件提前退出的方式减少if的嵌套。或者采用责任链模式来完成数据的处理，亦或是通过查表的方式完成不同数据状态的处理</p><h2 id="9-数据库事务与数据唯一性混淆"><a href="#9-数据库事务与数据唯一性混淆" class="headerlink" title="9. 数据库事务与数据唯一性混淆"></a>9. 数据库事务与数据唯一性混淆</h2><p><strong>神操作：</strong> 通过数据库事务来保证数据的唯一性</p><p>期望通过数据库来保证表中数据的唯一性，但未添加唯一索引。在出现重复插入的数据时一脸疑惑，以为事务失效了</p><p><strong>建议：</strong> 摸清事务的特性，如果要通过数据库保证唯一性可以添加唯一索引</p><h2 id="10-异步任务共用队列，不区分优先级"><a href="#10-异步任务共用队列，不区分优先级" class="headerlink" title="10. 异步任务共用队列，不区分优先级"></a>10. 异步任务共用队列，不区分优先级</h2><p><strong>神操作：</strong> 多种类型的任务共用一条队列，当突发产生大量低优先级的任务时，因消费积压导致高优先级的任务一同被淹没，不能及时得到处理</p><p><strong>建议：</strong> 按照任务优先级送入不同的队列，保证高优先级的任务能够得到及时的处理。同理，系统中数据流与控制流也应分开避免相互影响</p><h2 id="未完待续。。。"><a href="#未完待续。。。" class="headerlink" title="未完待续。。。"></a>未完待续。。。</h2>]]></content>
      
      
      <categories>
          
          <category> 系统设计 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Architecture &amp; Design </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>计算机发展历史</title>
      <link href="/2022/05/03/computer-history/"/>
      <url>/2022/05/03/computer-history/</url>
      
        <content type="html"><![CDATA[<h2 id="从算盘开始说起"><a href="#从算盘开始说起" class="headerlink" title="从算盘开始说起"></a>从算盘开始说起</h2><p>科学史专家们将中国的算盘作为人类历史上最早出现的计算机，由此算来计算机从出现至今已有2000年的历史。古希腊的算盘比中国的算盘早出现近7个世纪，为何中国算盘却被认为是人类历史上最早的计算机呢？</p><p>古希腊的算盘与中国的算盘外形十分相似，但是古希腊的算盘只是用来辅助计算，算盘上的铜珠在计算过程中用来计数而计算的过程是靠人动脑筋来完成的，因此它只是一个辅助计算工具。中国的算盘是通过一套珠算口诀来控制操作而不是通过心算来完成，打算盘的人只是在执行珠算口诀而已。例如：“三下五除二”，这其实是一句珠算口诀，它的意思是：加3时先把算盘上半部分代表5的珠子拨下来，再从下面的珠子中减掉2个，是“加3”的操作指令。打算盘的人不需要懂数学运算，只要背下这些珠算口诀按照口诀拨动算珠即可完成运算。由此可见古希腊算盘是靠人动脑筋完成计算，而使用中国算盘的过程中人只是提供机械动能，而非脑中的运算能力。正是这个区别使得中国算盘被认为是计算机的原因。</p><p>对此类机器的一个较为严谨的描述为：一个具有运算能力、可能还有一定存储能力的机器设备，受指令控制，包括但不限于：计算机、服务器、平板电脑、程控交换机等。对比一下算盘和计算机会发现二者的相似之处：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">算盘：珠算口诀、算珠、人</span><br><span class="line">计算机：CPU指令集、电子器件、电力</span><br></pre></td></tr></table></figure><h2 id="布尔代数与开关电路"><a href="#布尔代数与开关电路" class="headerlink" title="布尔代数与开关电路"></a>布尔代数与开关电路</h2><p>1642年法国数学家布莱兹·帕斯卡发明了最早的机械计算机，可以完成加法和减法运算。</p><p>1822年英国科学家查尔斯·巴贝奇研制出简单的差分机，可以完成简单的微积分运算且具有6位小数的精度。随后他试图制造一台精度为20位小数的差分机，但是巴贝奇耗费了自己大部分精力和财富直到去世仍未完成这个庞大的机器。巴贝奇陷入了一个误区：用复杂的方法解决复杂的问题。要完成的计算越复杂，机械计算机的设计就越精巧，内部的结构就越复杂。要实现更复杂的功能就需要设计和制造出更复杂的机械，机械复杂到一定程度就无法造出来了。</p><p>1854年英国数学家乔治·布尔出版的《思维规律的研究》第一次向人们展示了如何用数学的方法解决逻辑问题。</p><p>1937年美国科学家克劳德·香农完成其硕士论文《继电器和开关电路的符号分析》，香农发现很多现象和布尔代数的逻辑是对应的，例如：电路连接和断开、高低电压、数学的0和1等。他提出基于布尔代数的逻辑电路来控制分析仪的运转，进一步发现加减乘除各种运算都可以用多个基本的逻辑电路“搭”出来。香农通过逻辑电路在布尔代数和算术之间搭起来一座桥梁：布尔代数—&gt;逻辑电路—&gt;算术运算。香农电路设计的核心思想总结为：模块化和等价性。模块化指用少量简单的模块搭建出各种复杂的功能。等价性指再复杂的计算都可以等价成很多的加减乘除运算，进而等价成开关电路的逻辑运算</p><p>德国工程师康拉德·楚泽凭借着自己深厚的数学功底在二战时期研制出人类第一台可编程计算机，直到二战结束楚泽都不知道图灵和香农等人的理论，他完全是从一个工程师的经验出发发现了和香农开关电路类似的规律，采用简单的方法实现了复杂的计算功能。经过后面的多次改进在其第三个版本的计算机上实现了图灵所描绘的功能，而此时他依然不知道图灵的工作。（真是一个牛逼的工程师，数学功底深厚，知道布尔代数，懂得用二进制实现运算和控制机械以及进制间的转换）</p><h2 id="计算的本质的机械运动"><a href="#计算的本质的机械运动" class="headerlink" title="计算的本质的机械运动"></a>计算的本质的机械运动</h2><p>1936年图灵提出了可计算性理论，从数学上奠定了可计算问题的理论基础，提出了计算机的数学模型——图灵机。</p><p>图灵在20世纪30年代中期开始思考三个非常根本的问题：</p><ol><li>数学问题是否都有答案</li><li>如果有明确的答案是否可以通过有限步的计算得到答案</li><li>对于有可能在有限步计算出来的数学问题，能否有一种假象的机器，它不断的运动，最后当它停下来的时候，那个数学问题就解决了</li></ol><p>由此可见大师级的人物思考问题都是很直接，图灵抛开具体技术从计算的本质来寻找计算机的极限。更进一步他设计了一种后来被称为图灵机的数学模型：</p><ol><li>要有一条无限长的纸带，纸带被分成一个个格子，每个格子里记录着符号或者数字</li><li>有一个读写头在纸带上左右移动，它停在那里就可以改变哪里的数字或者符号</li><li>要有一套规则表，根据图灵机当前的状态和读写头所指格子中的符号或者数字查表后就知道下一步该做什么，完成这步操作后图灵机也进入下一个状态</li><li>图灵机需要有寄存器来记录图灵机的状态，图灵机的状态是有限的，其中一个特殊的状态是停机，一旦进入这个状态则表示计算完成</li></ol><p>图灵机对计算机科学的发展产生了深远的意义：</p><ol><li><p>它将数学问题分为两类：一类是可以用图灵机在有限步骤内完成计算的，另一类是不可以。今天在计算机科学中说一个问题可不可以计算不是指数学上能否计算，而是指能否用图灵机这样一个简单的逻辑来计算。由此可见能用图灵机进行计算的问题只是可计算数学问题的很小一部分</p></li><li><p>图灵机提出的存储地址、计算机状态、规则表（今天的指令集）、当前位置读写头四个重要概念至今影响这计算机的发展</p></li><li><p>图灵机将求解数学问题的过程和机械运动对应起来</p></li></ol><h2 id="人工智能的边界"><a href="#人工智能的边界" class="headerlink" title="人工智能的边界"></a>人工智能的边界</h2><p>近些年人工智能的发展让人们担忧人工智能将取代人类，但了解了图灵的可计算理论之后就会发现这种想法有些过于担忧，人工智能所能解决问题的范畴仍未超越图灵机的范畴。如果给给所有问题进行集合划分的话，其大致如下：</p><p>所有问题集合⊇数学问题⊇可判定问题⊇有答案的问题⊇可计算问题⊇工程可解问题⊇人工智能可解问题</p><p>近几年人工智能的发展只是将人工智能可解问题的范畴扩大了一些，但无论怎么扩大都不可能超出可计算这个范畴，况且世间好多问题不是数学问题，例如：情感问题、企业管理问题等，这也说明理论对工程的影响有多么深远。</p><h3 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h3><p>《计算之魂》——吴军博士著</p>]]></content>
      
      
      <categories>
          
          <category> 系统设计 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Architecture &amp; Design </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>利用Protocol buffers FieldMask和Wrapper Message设计API</title>
      <link href="/2021/10/23/protobuf-tips/"/>
      <url>/2021/10/23/protobuf-tips/</url>
      
        <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>在做API设计的时候经常遇到两个问题：</p><p>1、更新接口参数是否可以只传要修改的字段。</p><p>2、protobuf3 Go generated code中没有HasField()方法之后，如何判断某个字段是否被赋值。</p><h2 id="实践"><a href="#实践" class="headerlink" title="实践"></a>实践</h2><h3 id="FieldMask"><a href="#FieldMask" class="headerlink" title="FieldMask"></a>FieldMask</h3><p>对于前面提到的第一个问题可以利用FieldMask来解决，看下FieldMask官方介绍：</p><blockquote><p><code>FieldMask</code> represents a set of symbolic field paths. Field masks are used to specify a subset of fields that should be returned by a get operation (a <em>projection</em>), or modified by an update operation.</p></blockquote><p>FieldMask是一组字段的路径，它可以用来指定返回哪些字段或更新哪些字段。例如：我们有个一个更新用户信息的接口，在更新接口中我们可以通过FieldMask指定更新哪些字段，这样就不必为每个字段的更新设计一个接口。</p><p>Protobuf 定义如下：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> <span class="string">&quot;google/protobuf/field_mask.proto&quot;</span>;</span><br><span class="line"><span class="keyword">import</span> <span class="string">&quot;google/protobuf/wrappers.proto&quot;</span>;</span><br><span class="line">message User &#123;</span><br><span class="line">  <span class="type">int64</span> id = <span class="number">1</span>;</span><br><span class="line">  <span class="type">string</span> name = <span class="number">2</span>;</span><br><span class="line">  <span class="type">string</span> gender = <span class="number">4</span>;</span><br><span class="line">  <span class="type">string</span> birthday = <span class="number">5</span>;</span><br><span class="line">  google.protobuf.StringValue email = <span class="number">6</span>;</span><br><span class="line">  google.protobuf.DoubleValue money = <span class="number">7</span>;</span><br><span class="line">  google.protobuf.Int32Value size = <span class="number">8</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">message UpdateUserRequest &#123;</span><br><span class="line">  User user = <span class="number">1</span>;</span><br><span class="line">  google.protobuf.FieldMask field_mask = <span class="number">2</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">message UserReply &#123;</span><br><span class="line">  User user = <span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">service UserService &#123;</span><br><span class="line">  rpc UpdateUser(UpdateUserRequest) returns (UserReply);</span><br><span class="line">  rpc FindUser(FindUserRequest) returns (UserReply);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Client端在请求时指定要更新的字段，未被指定的字段不会被更新。代码如下：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">updateUser</span><span class="params">(client pb.UserServiceClient)</span></span> &#123;</span><br><span class="line">ctx, cancel := context.WithTimeout(context.Background(), time.Second)</span><br><span class="line"><span class="keyword">defer</span> cancel()</span><br><span class="line"></span><br><span class="line">user := &amp;pb.User&#123;</span><br><span class="line">Id:       <span class="number">1001</span>,</span><br><span class="line">Name:     <span class="string">&quot;Jerry&quot;</span>,</span><br><span class="line">Gender:   <span class="string">&quot;female&quot;</span>,</span><br><span class="line">Birthday: <span class="string">&quot;1991-01-01&quot;</span>,</span><br><span class="line">Money:    &amp;wrappers.DoubleValue&#123;Value: <span class="number">123243.43</span>&#125;,</span><br><span class="line">&#125;</span><br><span class="line">req := &amp;pb.UpdateUserRequest&#123;</span><br><span class="line">User: user,</span><br><span class="line">FieldMask: &amp;field_mask.FieldMask&#123;</span><br><span class="line">Paths: []<span class="type">string</span>&#123;<span class="string">&quot;user.birthday&quot;</span>, <span class="string">&quot;user.money&quot;</span>&#125;,</span><br><span class="line">&#125;,</span><br><span class="line">&#125;</span><br><span class="line">reply, err := client.UpdateUser(ctx, req)</span><br><span class="line">log.Printf(<span class="string">&quot;update user reply: %v, err: %v\n&quot;</span>, reply, err)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Server 端根据请求中的FieldMask来更新响应字段。代码如下：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(s *server)</span></span> UpdateUser(ctx context.Context, req *pb.UpdateUserRequest) (*pb.UserReply, <span class="type">error</span>) &#123;</span><br><span class="line"><span class="comment">// assume is found by req.GetUser().GetId() from db</span></span><br><span class="line">dst := &amp;pb.User&#123;</span><br><span class="line">Id:       <span class="number">1001</span>,</span><br><span class="line">Name:     <span class="string">&quot;mary&quot;</span>,</span><br><span class="line">Gender:   <span class="string">&quot;female&quot;</span>,</span><br><span class="line">Birthday: <span class="string">&quot;1996-12-12&quot;</span>,</span><br><span class="line">Email:    &amp;wrappers.StringValue&#123;Value: <span class="string">&quot;xxx@xx.com&quot;</span>&#125;,</span><br><span class="line">Money:    &amp;wrappers.DoubleValue&#123;Value: <span class="number">12.34</span>&#125;,</span><br><span class="line">Size:     &amp;wrappers.Int32Value&#123;Value: <span class="number">12</span>&#125;,</span><br><span class="line">&#125;</span><br><span class="line">log.Printf(<span class="string">&quot;user find from db: %v\n&quot;</span>, dst)</span><br><span class="line">fmutils.Filter(req, req.GetFieldMask().GetPaths())</span><br><span class="line">src := req.GetUser()</span><br><span class="line">log.Printf(<span class="string">&quot;user to be merged: %v\n&quot;</span>, src)</span><br><span class="line">proto.Merge(dst, src)</span><br><span class="line">log.Printf(<span class="string">&quot;user to update: %v\n&quot;</span>, dst)</span><br><span class="line"><span class="comment">// omit save to db</span></span><br><span class="line"><span class="keyword">return</span> &amp;pb.UserReply&#123;</span><br><span class="line">User: dst,</span><br><span class="line">&#125;, <span class="literal">nil</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Client端请求日志：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">2021/10/30 15:41:21 update user reply: user:&#123;id:1001  name:&quot;mary&quot;  gender:&quot;female&quot;  birthday:&quot;1991-01-01&quot;  email:&#123;value:&quot;xxx@xx.com&quot;&#125;  money:&#123;value:123243.43&#125;  size:&#123;value:12&#125;&#125;, err: &lt;nil&gt;</span><br></pre></td></tr></table></figure><p>Server端日志：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">2021/10/30 15:41:21 user find from db: id:1001  name:&quot;mary&quot;  gender:&quot;female&quot;  birthday:&quot;1996-12-12&quot;  email:&#123;value:&quot;xxx@xx.com&quot;&#125;  money:&#123;value:12.34&#125;  size:&#123;value:12&#125;</span><br><span class="line">2021/10/30 15:41:21 user to be merged: birthday:&quot;1991-01-01&quot;  money:&#123;value:123243.43&#125;</span><br><span class="line">2021/10/30 15:41:21 user to update: id:1001  name:&quot;mary&quot;  gender:&quot;female&quot;  birthday:&quot;1991-01-01&quot;  email:&#123;value:&quot;xxx@xx.com&quot;&#125;  money:&#123;value:123243.43&#125;  size:&#123;value:12&#125;</span><br></pre></td></tr></table></figure><p>可以看到通过FieldMask只更新了用户的birthday和money两个字段，其他字段未被更新。这样做的好处是所有字段的更新可以统一到一个接口中，不需要根据不同的字段定义不同的接口，例如：UpdateUserBirthDay、UpdateUserMoney等。</p><p>也可以使用FieldMask来指定查找接口里的返回字段：</p><p>Client端代码：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">conn, err := grpc.Dial(<span class="string">&quot;127.0.0.1:8888&quot;</span>, grpc.WithInsecure(), grpc.WithBlock())</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">log.Fatalf(<span class="string">&quot;did not connect: %v&quot;</span>, err)</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">defer</span> conn.Close()</span><br><span class="line"></span><br><span class="line">client := pb.NewUserServiceClient(conn)</span><br><span class="line"></span><br><span class="line">updateUser(client)</span><br><span class="line"><span class="comment">// find user without field mask</span></span><br><span class="line">findUser(client, <span class="literal">nil</span>)</span><br><span class="line"><span class="comment">// find user with field mask</span></span><br><span class="line">fieldMask := &amp;field_mask.FieldMask&#123;</span><br><span class="line">Paths: []<span class="type">string</span>&#123;<span class="string">&quot;id&quot;</span>, <span class="string">&quot;name&quot;</span>, <span class="string">&quot;gender&quot;</span>, <span class="string">&quot;email&quot;</span>&#125;,</span><br><span class="line">&#125;</span><br><span class="line">findUser(client, fieldMask)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">findUser</span><span class="params">(client pb.UserServiceClient, fieldMask *field_mask.FieldMask)</span></span> &#123;</span><br><span class="line">ctx, cancel := context.WithTimeout(context.Background(), time.Second)</span><br><span class="line"><span class="keyword">defer</span> cancel()</span><br><span class="line">req := &amp;pb.FindUserRequest&#123;</span><br><span class="line">Id:        <span class="number">1001</span>,</span><br><span class="line">FieldMask: fieldMask,</span><br><span class="line">&#125;</span><br><span class="line">reply, err := client.FindUser(ctx, req)</span><br><span class="line">log.Printf(<span class="string">&quot;find user reply: %v, fieldMask: %v, err: %v\n&quot;</span>, reply, fieldMask, err)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Server端代码：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(s *server)</span></span> FindUser(ctx context.Context, req *pb.FindUserRequest) (*pb.UserReply, <span class="type">error</span>) &#123;</span><br><span class="line">user := &amp;pb.User&#123;</span><br><span class="line">Id:       <span class="number">1001</span>,</span><br><span class="line">Name:     <span class="string">&quot;mary&quot;</span>,</span><br><span class="line">Gender:   <span class="string">&quot;female&quot;</span>,</span><br><span class="line">Birthday: <span class="string">&quot;1996-12-12&quot;</span>,</span><br><span class="line">Email:    &amp;wrappers.StringValue&#123;Value: <span class="string">&quot;xxx@xx.com&quot;</span>&#125;,</span><br><span class="line">Money:    &amp;wrappers.DoubleValue&#123;Value: <span class="number">12.34</span>&#125;,</span><br><span class="line">Size:     &amp;wrappers.Int32Value&#123;Value: <span class="number">12</span>&#125;,</span><br><span class="line">&#125;</span><br><span class="line">log.Printf(<span class="string">&quot;field mask path: %v\n&quot;</span>, req.GetFieldMask().GetPaths())</span><br><span class="line"><span class="comment">// filter user protobuf message by field mask</span></span><br><span class="line">fmutils.Filter(user, req.GetFieldMask().GetPaths())</span><br><span class="line"><span class="keyword">return</span> &amp;pb.UserReply&#123;</span><br><span class="line">User: user,</span><br><span class="line">&#125;, <span class="literal">nil</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Client端请求日志：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">2021/10/30 16:00:20 find user reply: user:&#123;id:1001  name:&quot;mary&quot;  gender:&quot;female&quot;  birthday:&quot;1996-12-12&quot;  email:&#123;value:&quot;xxx@xx.com&quot;&#125;  money:&#123;value:12.34&#125;  size:&#123;value:12&#125;&#125;, fieldMask: &lt;nil&gt;, err: &lt;nil&gt;</span><br><span class="line">2021/10/30 16:00:20 find user reply: user:&#123;id:1001  name:&quot;mary&quot;  gender:&quot;female&quot;  email:&#123;value:&quot;xxx@xx.com&quot;&#125;&#125;, fieldMask: paths:&quot;id&quot;  paths:&quot;name&quot;  paths:&quot;gender&quot;  paths:&quot;email&quot;, err: &lt;nil&gt;</span><br></pre></td></tr></table></figure><p>Server端日志：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">2021/10/30 16:00:20 field mask path: []</span><br><span class="line">2021/10/30 16:00:20 field mask path: [id name gender email]</span><br></pre></td></tr></table></figure><p>可以看到第一次FieldMask未赋值则User所有字段都返回，第二次请求则返回了指定字段。</p><h3 id="Wrapper-Message"><a href="#Wrapper-Message" class="headerlink" title="Wrapper Message"></a>Wrapper Message</h3><p>针对第二个问题，除了FieldMask外注意到上面的代码中User Message中email、money等部分字段使用了 wrapper message类型而非基础类型来定义，这样做的好处是可以清晰的区分出该字段是未被赋值还是被赋为零值。例如：请求中money这个字段通过moneyl&#x3D;&#x3D;0无法判断moeny这字段是未赋值还是赋为空。</p><p>例如：检查更新用户的接口中money字段必须赋值，否则报错</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(s *server)</span></span> validateUpdateUserRequest (ctx context.Context, req *pb.UpdateUserRequest) <span class="type">bool</span> &#123;</span><br><span class="line"><span class="keyword">if</span> req.GetUser().GetMoney() == <span class="literal">nil</span> &#123;</span><br><span class="line"><span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>当然也可以在protobuf中使用protoc-gen-validate等工具来限定money字段不为空</p><h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><p><a href="https://developers.google.cn/protocol-buffers/docs/reference/google.protobuf?hl=zh-cn#fieldmask">Protobuf FieldMask</a></p><p><a href="https://github.com/protocolbuffers/protobuf/blob/master/src/google/protobuf/wrappers.proto">Protobuf Wrappers</a></p><p><a href="https://github.com/mennanov/fieldmask-utils">Protobuf fieldmask util</a></p><p><a href="https://netflixtechblog.com/practical-api-design-at-netflix-part-1-using-protobuf-fieldmask-35cfdc606518">Netflix API Design</a></p>]]></content>
      
      
      <categories>
          
          <category> Go </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Go </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>ES模糊搜索</title>
      <link href="/2021/10/05/es-fuzzy-search/"/>
      <url>/2021/10/05/es-fuzzy-search/</url>
      
        <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>提到全文搜索ES是一款比较被人熟知的开源搜索引擎，很多公司会基于ES来做全文搜索相关的业务。例如，在做内容相关的业务时常会有类似的运营需求：根据某个关键词搜索出所有包含该关键词的内容。如果量级比较小使用SQL的<code>LIKE</code>语句便可以解决，如果量级比较大的话一般需要通过搜索引擎来处理，但中文搜索往往因为分词问题会影响到最终的搜索效果。不同的业务对搜索结果的准确率和召回率各有不同的侧重，在根据某关键词搜索出所有内容案例中更加重视召回率，在保证召回率的基础上尽可能的提升准确率。</p><h2 id="可用于模糊搜索的几种方式"><a href="#可用于模糊搜索的几种方式" class="headerlink" title="可用于模糊搜索的几种方式"></a>可用于模糊搜索的几种方式</h2><p>首选简单讲一下对于中文内容ES建立倒排索引的过程：一篇文章送入ES后，ES首先会对内容进行分词，每个词被称为一个term，每个term会建立一个倒排表，倒排表为包含该term的doc id以及该term在doc中的位置等信息的 list。其中分词是指将一个句子分成多个短语，例如：<code>人民大道路面积水</code> 可被分词为<code>人民</code>、<code>大道</code>、<code>路面</code>、<code>面积</code>、<code>积水</code>等。常见的ES分词器有：standard analyzer、IK、ngram分词器、结巴分词器等。分词效果的好坏会直接影响到最终的搜索效果，例如：根据关键词<code>东方木子</code>进行内容搜索，很有可能因为在建立索引时被分词为<code>东方</code>、<code>方木</code>、<code>木子</code>等而导致内容无法被召回</p><p>ES提供的搜索方式大致可分为：基于term的精准匹配搜索(Term-level queries)和基于分词的全文搜索(Full-text queries)</p><p>基于term的精准匹配搜索过程：将输入的query当做term不进行分词，直接查找倒排表中包含该term的doc，并将结果返回</p><p>基于分词的全文搜索过程：针对输入的query进行分词，按照得到的分词term结果在倒排表中查找包含这些term的doc，按照相关性进行打分，最终按照得分降序排序返回结果</p><h3 id="基于Term的几种模糊搜素方式"><a href="#基于Term的几种模糊搜素方式" class="headerlink" title="基于Term的几种模糊搜素方式"></a>基于Term的几种模糊搜素方式</h3><h4 id="Fuzzy搜索"><a href="#Fuzzy搜索" class="headerlink" title="Fuzzy搜索"></a>Fuzzy搜索</h4><p>官方文档描述：</p><blockquote><p>Returns documents that contain terms similar to the search term, as measured by a Levenshtein edit distance.</p></blockquote><p>该搜索方式返回所有包含的term与query相似的doc，term与query的相似性以Levenshtein编辑距离来度量。例如：输入<code>邓子棋</code>可以搜索出包含<code>邓紫棋</code>的文章，起到了一个纠错的作用。我们假设根据关键词搜索的场景下关键词几乎不会被输入错误，因此Fuzzy搜索并不能提升召回率反而会召回一些不应该召回的内容降低了搜索的准确率</p><h4 id="wildcard通配符匹配"><a href="#wildcard通配符匹配" class="headerlink" title="wildcard通配符匹配"></a>wildcard通配符匹配</h4><p>官方文档描述：</p><blockquote><p>Returns documents that contain terms matching a wildcard pattern</p></blockquote><p>通配符匹配搜索从query形式上看是最接近SQL中<code>LIKE</code>语句的搜索方式，在SQL中的<code> LIKE %东方木子%</code>可以在ES中写成</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">GET /myindex/article/_search</span><br><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;query&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;wildcard&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">            <span class="attr">&quot;content&quot;</span><span class="punctuation">:</span> <span class="string">&quot;*东方木子*&quot;</span> </span><br><span class="line">        <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>但从实际效果来看并非如此，就像官方文档上说的这种查询方式返回的是所包含的term中能匹配上该搜索条件的所有doc。还是以<code>东方木子</code>为例，如果在索引中分词为<code>东方</code>和<code>方木</code>则上面的查询语句没有办法匹配到该doc，除非写成<code>*东*方*木*子*</code>。</p><p>另外该搜索模式在某些场景下资源开销比较大会影响性能，官方 提示如下：</p><blockquote><p>Avoid beginning patterns with <code>*</code> or <code>?</code>. This can increase the iterations needed to find matching terms and slow search performance.</p></blockquote><p>中文含义是：避免以*或？开头的模式，这会增加查找匹配项所需的迭代次数并降低搜索性能，并且如果没有开启允许开销较大的查询设置则ES不会执行该查询：</p><blockquote><p>Wildcard queries will not be executed if <a href="https://www.elastic.co/guide/en/elasticsearch/reference/current/query-dsl.html#query-dsl-allow-expensive-queries"><code>search.allow_expensive_queries</code></a> is set to false.</p></blockquote><p>Regexp正则表达式搜索与Wildcard原理类似不再赘述</p><h3 id="基于分词的全文模糊搜索模式"><a href="#基于分词的全文模糊搜索模式" class="headerlink" title="基于分词的全文模糊搜索模式"></a>基于分词的全文模糊搜索模式</h3><h4 id="match搜索"><a href="#match搜索" class="headerlink" title="match搜索"></a>match搜索</h4><p>官方文档对该搜索方式的描述：</p><blockquote><p>Returns documents that match a provided text, number, date or boolean value. The provided text is analyzed before matching.</p></blockquote><p>match搜搜是最常见的ES搜索模式，它首先将query进行分词得到term集合，然后找到包含这些term的doc并进行相关性打分，最终返回结果。在根据关键词搜索内容的场景下该模式会出现召回率低的问题。其中一个主要原因是：相同的短语包含在文章内进行分词和当做query进行分词得到的结果有差异，因为所处的上下文环境不一样。例如：实际在doc中包含<code>东方木子</code>的语句可能为<code>这一房东方木子</code>，最终被分词为：<code>房东</code>、<code>方木</code>进而导致无法召回。（上述仅为举例，实际情况要看使用的分词器，例如IK分词器有ik_max_word和ik_smart两种模式。ik_max_word为最细粒度的分词方式，可能会切出<code>房东</code>、<code>东方</code>、<code>方木</code>、<code>木子</code>等多个term；ik_smart的分词粒度相对粗一些可能切出：<code>房东</code>、<code>方木</code>两个term）。相比于召回率问题更重要的是该模式下会将其他仅包含<code>东方</code>term的doc给召回，这些doc并不包含<code>东方木子</code>这个短语导致准确率下降。</p><h4 id="match-phrase短语匹配"><a href="#match-phrase短语匹配" class="headerlink" title="match_phrase短语匹配"></a>match_phrase短语匹配</h4><p>官方文档描述：</p><blockquote><p>Returns documents that contain terms matching a wildcard pattern</p></blockquote><p>该搜索模式下首先对query进行分词，然后根据分词后的term进行doc查找。与 match搜索不同的是match_phrase<strong>找到的是包含所有term且term间的位置与搜索词项相同的doc</strong></p><p>使用该搜索模式虽然也会遇到与match搜索类似的问题，但因为该搜索会考虑term间位置的关系因此准确率上会有一定的提升</p><h5 id="配合NGram分词提升召回率"><a href="#配合NGram分词提升召回率" class="headerlink" title="配合NGram分词提升召回率"></a>配合NGram分词提升召回率</h5><p>官方文档介绍：</p><blockquote><p>The <code>ngram</code> tokenizer first breaks text down into words whenever it encounters one of a list of specified characters, then it emits <a href="https://en.wikipedia.org/wiki/N-gram">N-grams</a> of each word of the specified length.</p></blockquote><p>以gram为单位可配置min_gram最小词元长度和max_gram最大词元长度。例如：</p><p>一个单词 quick，5 种长度下的 ngram</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">ngram length=1，q u i c k</span><br><span class="line">ngram length=2，qu ui ic ck</span><br><span class="line">ngram length=3，qui uic ick</span><br><span class="line">ngram length=4，quic uick</span><br><span class="line">ngram length=5，quick</span><br></pre></td></tr></table></figure><p>还有一种形式叫做edge ngram：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">首字母后进行 ngram</span><br><span class="line">q</span><br><span class="line">qu</span><br><span class="line">qui</span><br><span class="line">quic</span><br><span class="line">quick</span><br></pre></td></tr></table></figure><p>类似前缀索引的效果，可以应用于搜索suggest场景</p><p>NGram分词搭配使用match查询只要任何一个字匹配上就会被召回，错误率太高；搭配使用match_phrase查询既可以减少因对doc和query进行智能分词结果有差异导致的召回率低问题，同时加上match_phrase的slop偏移量参数配置可提升搜索的准确率</p><h5 id="混合使用match和match-phrase提高召回率和准确率"><a href="#混合使用match和match-phrase提高召回率和准确率" class="headerlink" title="混合使用match和match_phrase提高召回率和准确率"></a>混合使用match和match_phrase提高召回率和准确率</h5><p>单独使用match搜索存在召回率高但准确率低的问题，而单独使用match_phrase存在准确率高但召回率低的问题。如果二者混合一起使用可以一定程度上既提升召回率又提升准确率：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">GET /myindex/article/_search</span><br><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;query&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;bool&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;must&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">        <span class="punctuation">&#123;</span><span class="attr">&quot;match&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">          <span class="attr">&quot;content&quot;</span><span class="punctuation">:</span> <span class="string">&quot;东方木子&quot;</span></span><br><span class="line">        <span class="punctuation">&#125;</span><span class="punctuation">&#125;</span></span><br><span class="line">      <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;should&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">        <span class="punctuation">&#123;</span><span class="attr">&quot;match_phrase&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">          <span class="attr">&quot;content&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">            <span class="attr">&quot;query&quot;</span><span class="punctuation">:</span> <span class="string">&quot;东方木子&quot;</span><span class="punctuation">,</span></span><br><span class="line">             <span class="attr">&quot;slop&quot;</span><span class="punctuation">:</span> <span class="number">2</span></span><br><span class="line">          <span class="punctuation">&#125;</span></span><br><span class="line">        <span class="punctuation">&#125;</span><span class="punctuation">&#125;</span></span><br><span class="line">      <span class="punctuation">]</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>但这种混用的方式不一定适合搜索出所有包含某关键词内容的场景</p><h2 id="其他的方案"><a href="#其他的方案" class="headerlink" title="其他的方案"></a>其他的方案</h2><p>回到文章开始提到的场景：搜索出所有包含某关键词内容。如果我们就是要以超高的准确率找到包含某关键词的全部内容那这就变成了一个关键词匹配的场景，在这个场景下除了使用ES之外还有没有其他的方案呢？</p><p>这种场景实际转变为了字符串匹配的问题，字符串匹配问题业内已经有比较成熟的算法，那么我们可以使用MR或者Spark跑一个离线任务对每一篇文章进行字符串匹配计算将匹配上的文章返回，但这种方式不能满足请求即时响应。</p><p>如果要支持输入的关键词纠错查询或者支持?、*正则匹配呢？这种情况下如果用一个正则表达式去匹配全文内容会比较耗费资源，一种办法是先将doc按照标点符号进行断句，然后针对每一个句子进行正则匹配，这样做的前提假设是关键词不会有跨句出现的情况。</p><h2 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h2><p>以上即为在中文搜索中实现类似SQL <code>LIKE</code>模糊匹配的几种方式，中文搜索比英文搜索复杂的一个地方在于中文的分词问题，如果分词比较准确则能够很大的提升搜索效果，当然除了分词搜索本身还有许多难题要解决，例如query理解等，这不在本文讨论范围之内。</p><h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><p><a href="https://www.elastic.co/guide/en/elasticsearch/reference/current/index.html">Elastic Search官方文档</a></p>]]></content>
      
      
      <categories>
          
          <category> search </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 搜索 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>LSM Tree与SSTable：构建高效KV存储的基石</title>
      <link href="/2021/01/22/SSTable-and-LSMTree/"/>
      <url>/2021/01/22/SSTable-and-LSMTree/</url>
      
        <content type="html"><![CDATA[<p>数据存储方面除了被大家熟知的MySQL、PostgreSQL数据库之外，目前基于LSMTree数据模型的数据库也已经得到广泛的应用，尤其在比较看重写入吞吐量和写入性能的场景基于LSMTree的数据库具有良好的性能表现。我们简单看下SSTable和LSMTree的实现原理。</p><h3 id="从一个简单的数据库实现开始"><a href="#从一个简单的数据库实现开始" class="headerlink" title="从一个简单的数据库实现开始"></a>从一个简单的数据库实现开始</h3><p>假设我们要构建一个KV数据库，可以用一个shell脚本完成一个名字为myDB的简单实现：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_">#</span><span class="language-bash">! /bin/bash</span></span><br><span class="line"></span><br><span class="line">db_set() &#123;</span><br><span class="line">    echo &quot;$1,$2&quot; &gt;&gt; db.data</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">db_get() &#123;</span><br><span class="line">    grep &quot;^$1,&quot; db.data | sed -e &quot;s/^$1,//&quot; | tail -n 1</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>那么可以通过如下的方式来进行存储和获取数据：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_">$ </span><span class="language-bash">db_set 123 <span class="string">&#x27;&#123;&quot;name&quot;:&quot;tesla&quot;,&quot;attr&quot;:[&quot;made in china&quot;]&#125;&#x27;</span></span></span><br><span class="line"><span class="meta prompt_">$ </span><span class="language-bash">db_set 456 <span class="string">&#x27;&#123;&quot;name&quot;:&quot;audi&quot;,&quot;attr&quot;:[&quot;made in german&quot;]&#125;&#x27;</span></span></span><br><span class="line"><span class="meta prompt_">$ </span><span class="language-bash">db_get 123</span></span><br><span class="line">&#123;&quot;name&quot;:&quot;tesla&quot;,&quot;attr&quot;:[&quot;made in china&quot;]&#125;</span><br></pre></td></tr></table></figure><p>它的底层存储也很简单，在一个文本文件中每行包含一个kv对，key和value用逗号分隔。如果有新的值写入不会覆盖旧值，在获取数据时找到文件中该key最后一次的记录来获取最新的值。</p><p>通过这种在文件尾部追加的方式使得db_set性能很好，此处暂不考虑并发控制、错误处理、文件大小等问题。db_get每次都要扫描整个文件，随着文件中保存的数据增多性能会逐渐降低。</p><h3 id="引入索引和文件压缩"><a href="#引入索引和文件压缩" class="headerlink" title="引入索引和文件压缩"></a>引入索引和文件压缩</h3><p>在数据库中为了提高查询性能通常为某些字段添加索引，同样的思路我们可以为myDB添加一个哈希索引，哈希索引的key对应文件中的key，哈希索引的value为该key在文件中的offset。为简单起见这里使用HashMap来实现，每次启动myDB时都从数据库文件构建索引并保存在内存中。查找时通过索引找到该key对应的offset便可快速读取value，每次有新的数据写入数据库时都会更新HashMap中该key对应的最新offset。</p><p>随着数据的增多我们需要考虑数据文件膨胀的问题，控制文件大小的一个办法是对文件进行压缩，但是如果只有一个数据文件同时还要提供读写则没办对其压缩所以我们可以设定为当数据文件超过一定大小后就关闭该文件，新的数据写到另一个新的文件中。对于这些停止写入的文件后台有一个线程来对其进行合并和压缩：将两个文件的内容合并写入到另一个新文件中，写入过程中对相同的key只保留最近的value。合并过程中仍然可以用旧的文件进行读取，合并完成后读取请求切换到新的合并文件上，旧的文件即可删除。</p><p>每个文件在内存中都有对应的HashMap，查找数据时首先查找最新写入文件的HashMap如果没找到再找次新的HashMap，以此类推，数据文件的合并可以使得文件数量控制在一定的范围内，因此通常也不需要查找太多的HashMap。</p><p>其他方面的一些考虑：</p><p><strong>删除数据</strong></p><p>如果要删除某个kv，则需要数据文件中追加一条特殊的删除记录，改记录通常被称为墓碑，在合并数据文件时一旦发现墓碑标志则会丢弃这个已经删除的key</p><p><strong>崩溃恢复</strong></p><p>如果数据库发生崩溃再次重启的时候需要扫描全部的数据文件以构建索引，如果数据文件很大则耗时较长，可以将HashMap的快照存在磁盘中每次从快照文件中恢复索引</p><p>另外如果在数据写入时发生了崩溃则会导致数据丢失，通常的做法是在数据写入前先写入WAL文件中，崩溃时可从该文件进行数据恢复，也是目前数据库中常见的一种办法</p><p><strong>并发控制</strong></p><p>数据是先后顺序追加到文件中的所以通常可以只维护一个写线程，数据写入后便不可改变因此可以并发的有多个读取线程</p><p> 通过文件顺序追加的方式存储数据有以下几个好处：</p><ul><li>相比于新值覆盖旧值的方式，顺序追加将随机写入转变为顺序写入可以获取到更好的性能</li><li>对于文件顺序追加且不可变的方式，在发生崩溃时更容易进行数据恢复，不用过于担心某个数据存在部分新值部分旧值的问题</li><li>定期的合并压缩可以避免数据空洞和文件碎片化的问题</li></ul><p>至此我们已经构建了一个底层数据模型为基于文件顺序写入的简单KV数据库，可以满足一些场景下KV数据的存储和读取，但仍存在如下问题：</p><ul><li>如果数据量很大内存里放不下所有的HashMap，而如果将一部分HashMap放入磁盘中则会影响性能，因为它会带来许多磁盘的随机访问</li><li>哈希索引对于区间查询只能采用对区间内所有key挨个查询的方式，查询效率不高</li></ul><h3 id="有序的数据文件"><a href="#有序的数据文件" class="headerlink" title="有序的数据文件"></a>有序的数据文件</h3><p>为解决上面说的问题我们考虑将数据文件中KV的存储顺序改为按key排序的方式写入，这种格式被称为排序字符串表，简称SSTable，每个键在每个合并段文件里只能出现一次，SSTable有以下好处：</p><ul><li>合并文件将更加高效，可以同时读取多个文件采用归并排序的方式对文件进行合并</li><li>内存中的HashMap不在需要保存所有key的索引，例如对于Martin的查找，虽然索引中不存在该key，但是我们知道Mark和Matt的索引值并且Martin位于它俩之间那么我们只需要扫描Mark到Matt间的数据即可找到Martin。通过这种方式可以减少索引大小节省内存空间</li></ul><p>我们需要一种方式将按写入顺序保存的数据文件转变成按key排序的文件且要保证不降低写入的性能：</p><ul><li>在内存中维护一个有序的树状结构来存放数据，例如AVL树或者红黑树等，该结构通常被称为内存表(memtable)</li><li>当memtable超过一定大小时将其写入文件中，由于已经在内存中排好序因此按照顺序将其写入文件既可以保证写入的高效性又可以使得文件里的内容是按照key完成排序的</li><li>为防止崩溃时丢失数据，每次数据写入前先追加写入到WAL日志中，如果发送崩溃可重WAL中恢复memtable。该日志不需要按key排序，当memtable写入SSTable后该WAL日志删除</li></ul><p>当查找某个key时首先检查内存数据(memtable)，如果没有找到这个key，就会逆序的检查每个sstable文件，直到key 被找到。因为每个sstable都是有序的，单个文件的查找时间复杂度为O(logN)，总的查找时间复杂度为O(K log N), K为查找sstable的个数， N 为sstable平均大小。</p><h3 id="一些其他方面的性能优化"><a href="#一些其他方面的性能优化" class="headerlink" title="一些其他方面的性能优化"></a>一些其他方面的性能优化</h3><p>当查找的key不存在时myDB需要扫描所有的memtable和数据文件，为此可使用布隆过滤器来近似计算该key是否存在以提升查询性能</p><p>数据文件的合并和压缩方式不同也会影响性能，常见的合并压缩方式有：按层级压缩(LevelDB和RocksDB使用)、按大小分级压缩(HBase)使用。按层级压缩和按大小压缩的区别为：</p><ul><li>按层级压缩的每一层维护指定个数的文件且在一层中一个key只能存在于一个文件中，因此在一层查找一个key只需查找一个文件。注意：第一层不满足上述条件，一个key可以分布在多个文件中。</li><li>文件只会被合并到上一层的一个文件。当一层的文件数超过一定大小时，一个文件会被选出并合并到上一层</li></ul><figure class="image-bubble">                <div class="img-lightbox">                    <div class="overlay"></div>                    <img src="/img/leveldb.png" alt="LevelDB merge SSTable" title="">                </div>                <div class="image-caption">LevelDB merge SSTable</div>            </figure><p>这些策略的目的是为了减少合并操作对读写数据请求的影响同时节省磁盘空间，除了这些之外对性能的优化还有很多，例如通过KV分离来减少写放大、提升压缩率、提升索引效率等。</p><h3 id="与Btree对比"><a href="#与Btree对比" class="headerlink" title="与Btree对比"></a>与Btree对比</h3><p>基于SSTable的数据存储模型与我们熟知的Btree相比有以下不同：</p><ul><li>Btree查找速度相比SSTable更快一些</li><li>SSTable写入性能更高，吞吐量大</li><li>Btree是写时覆盖容易产生空洞和碎片</li><li>Compaction 机制会带来一定程度上的写放大</li><li>受后台合并线程的影响，在执行合并时SSTable读写操作性能会有所降低，因此SSTable读写响应时间不如Btree稳定</li></ul><h3 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h3><p>上述的描述便是LevelDB、RocksDB的主要思想，SSTable和memtable由Google首次在Bigtable论文中提出，HBase和Cassandra的设计也是受此影响，而在Lucene中term与doc list的映射关系也是存放在类似SSTable的文件中。这个memtable索引被称为以日志结构的合并树(Log Structed Merge Tree，简称LSM-Tree)，因此基于合并和压缩排序文件原理的存储引擎被称为LSM存储引擎。</p><h3 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h3><p><a href="http://www.benstopford.com/2015/02/14/log-structured-merge-trees/">Log Structured Merge Trees</a></p>]]></content>
      
      
      <categories>
          
          <category> 系统设计 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Architecture &amp; Design </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>探索Go的netpoller：netpoller网络模型与I/O多路复用技术</title>
      <link href="/2020/12/13/golang-netpoll/"/>
      <url>/2020/12/13/golang-netpoll/</url>
      
        <content type="html"><![CDATA[<p>使用Go进行网络编程是十分的高效和便捷的，在goroutine-per-connection这样的编程模式下开发者可以使用同步的模式来编写业务逻辑而无需关心网络的阻塞、异步等问题极大的降低了心智负担。同时Go基于 I&#x2F;O multiplexing 和 goroutine scheduler 使得这个网络模型在开发模式和接口层面保持简洁的同时也具备较高的性能可以满足绝大多数的场景。</p><h2 id="I-O模型"><a href="#I-O模型" class="headerlink" title="I&#x2F;O模型"></a>I&#x2F;O模型</h2><p>在网络编程的世界里存在5种IO模型：</p><ol><li>阻塞 I&#x2F;O (Blocking I&#x2F;O)  </li><li>非阻塞 I&#x2F;O (Nonblocking I&#x2F;O)</li><li>I&#x2F;O 多路复用 (I&#x2F;O multiplexing)</li><li>信号驱动 I&#x2F;O (Signal driven I&#x2F;O)</li><li>异步 I&#x2F;O (Asynchronous I&#x2F;O)</li></ol><p>当应用程序对一个socket发起一次网络读写操作时分为两个步骤：</p><ol><li>等待网络数据流到来网卡可读(读就绪)&#x2F;等待网卡可写(写就绪) ，将数据从网卡写入到内核&#x2F;读取内核数据写入网卡</li><li>从内核缓冲区复制数据到用户空间(读)&#x2F;从用户空间复制数据到内核缓冲区(写)</li></ol><p>同步IO与异步IO的主要区别在于执行第二步的时候应用程序是否需要同步等待。上述5种IO模型除了Asynchronous I&#x2F;O是异步之外，其余都为同步模型。</p><p>至于阻塞与非阻塞主要区别在于第一步数据未到达时当前进程是否会被阻塞住。</p><p>在阻塞模式下当应用程序调用<code>recvfrom</code>读取socket时，如果数据未到达应用程序会被阻塞直到数据完全准备好。</p><p>在非阻塞模式下当应用程序调用<code>recvfrom</code>读取socket时，如果数据未准备好则内核会立即返回一个EWOULDBLOCK，应用程序接到一个 EAGAIN error。非阻塞模式下也可以是同步IO：基于轮询，当返回EAGAIN时应用程序继续发起<code>recvfrom</code>调用直到数据准备好。</p><p>当数据准备好之后就可以将数据从内核空间的缓存复制到用户空间的缓存，在复制数据这一步应用程序还是阻塞的。</p><figure class="image-bubble">                <div class="img-lightbox">                    <div class="overlay"></div>                    <img src="/img/non-blockingIO.jpg" alt="Non-blocking I/O" title="">                </div>                <div class="image-caption">Non-blocking I/O</div>            </figure><h2 id="I-O多路复用"><a href="#I-O多路复用" class="headerlink" title="I&#x2F;O多路复用"></a>I&#x2F;O多路复用</h2><p>IO多路复用是指一个线程同时监听多个IO连接的事件，阻塞等待，当某个连接发生可读写IO事件时收到通知。所以复用的是线程而不是IO连接。常见的多路复用选择器有：select&#x2F;poll&#x2F;epoll、kqueue、iocp</p><p>简单讲一下select&#x2F;poll&#x2F;epoll：</p><h3 id="select-poll"><a href="#select-poll" class="headerlink" title="select&amp;poll"></a>select&amp;poll</h3><p>select主要存在以下几个缺点：</p><ul><li>每次调用select都需要使用copy_from_user把fd集合从用户态拷贝到内核态，当fd很多时这个开销会很大</li><li>每次调用select都需要在内核遍历传递进来的所有fd挨个检查fd的状态，随着fd数量的增长 I&#x2F;O 性能会线性下降</li><li>最大文件描述符数量限制，默认一次只能传入1024个fd，意味着一次只能监听1024个socket</li></ul><p>poll在本质上与select没有区别只是扩大了可传入的fd集合的大小</p><h3 id="epoll"><a href="#epoll" class="headerlink" title="epoll"></a>epoll</h3><p>相比于select，用户可以通过epoll_ctl调用将fd注册到epoll实例上，而epoll_wait则会阻塞监听所有的epoll实例上所有的fd的事件。他解决了select未解决的两个问题：</p><ol><li>通过epoll_ctl注册fd，一个fd只完成一次从用户态到内核态的拷贝而不需要每次调用时都拷贝一次，并且epoll使用红黑树存储所有的fd因此重复注册是没用的</li><li>当某个fd注册完成后会与对应的设备建立回调关系，当设备就绪触发中断后内核通过该回调函数将该fd添加到rdllist 双向就绪链表中。epoll_wait 就是去检查 rdllist 中是否有就绪的 fd，当 rdllist 为空时就会阻塞挂起当前调用epoll_wait进程，直到 rdllist 非空时进程才被唤醒并返回。此处epoll解决了select的第二个问题：不需要每次调用都遍历传进来的fd列表，从而不会因为随着fd数量的增多而性能下降。</li></ol><h2 id="netpoller"><a href="#netpoller" class="headerlink" title="netpoller"></a>netpoller</h2><p>首先对于epoll提供的三个调用接口Go对此都做了封装</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">#include &lt;sys/epoll.h&gt;  </span><br><span class="line"><span class="type">int</span> epoll_create(<span class="type">int</span> size);  </span><br><span class="line"><span class="type">int</span> epoll_ctl(<span class="type">int</span> epfd, <span class="type">int</span> op, <span class="type">int</span> fd, <span class="keyword">struct</span> epoll_event *event);  </span><br><span class="line"><span class="type">int</span> epoll_wait(<span class="type">int</span> epfd, <span class="keyword">struct</span> epoll_event * events, <span class="type">int</span> maxevents, <span class="type">int</span> timeout);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Go 对上面三个调用的封装</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">netpollinit</span><span class="params">()</span></span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">netpollopen</span><span class="params">(fd <span class="type">uintptr</span>, pd *pollDesc)</span></span> <span class="type">int32</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">netpoll</span><span class="params">(block <span class="type">bool</span>)</span></span> gList</span><br></pre></td></tr></table></figure><p>当调用<code>net.Listen</code>之后操作系统会创建一个socket同时返回与之对应的fd(被设置为非阻塞模式)，该fd用来初始化listener的netFD(go层面封装的网络文件描述符)，同时会调用<code>epoll_create</code>创建一个 epoll 实例 作为整个 runtime 的唯一 event-loop 使用。</p><p>当一个client连接server时，listener通过accept接受新的连接，该连接的fd被设置为非阻塞模式同时会起一个新的goroutine来处理新连接并将新连接对应的fd注册到epoll中。当goroutine调用<code>conn.Read</code>，<code>conn.Write</code>对连接进行读写操作遇到<code> EAGAIN</code>错误时会被 gopark 给park住进行休眠，让P去执行本地调度队列里的下一个可执行的G。这些被park住的goroutine会在goroutine的调度中调用<code>runtime.netpoll</code>被唤醒，然后调用 injectglist 把唤醒的 G 放入当前 P 本地调度队列或者全局调度队列去执行。</p><p>runtime.netpoll 的核心逻辑是： 根据入参 delay设置调用 epoll_wait 的 timeout 值，调用 epoll_wait 从 epoll 的 <code>eventpoll.rdllist</code>双向列表中获取IO就绪的fd列表，遍历epoll_wait 返回的fd列表， 根据调用<code>epoll_ctl</code>注册fd时封装的上下文信息组装可运行的 goroutine 并返回。(仔细看遍历fd列表的这个操作是不是又有点像select？)</p><p>调用<code>runtime.netpoll</code>的地方有两处：</p><ul><li>在调度器中执行<code>runtime.schedule()</code>，该方法中会执行<code>runtime.findrunable()</code>，在<code>runtime.findrunable()</code>中调用了<code>runtime.netpoll</code>获取待执行的goroutine</li><li>Go runtime 在程序启动的时候会创建一个独立的sysmon监控线程，sysmon 每 20us~10ms 运行一次，每次运行会检查距离上一次执行netpoll是否超过10ms，如果是则会调用一次<code>runtime.netpoll</code></li></ul><p><strong>敲黑板，划重点：</strong><br>上面过程中通过Listen和Accept返回的fd都被设置为非阻塞模式，原因是如果为阻塞模式则会使对应的G阻塞在system call上，此时与G对应的M也会被阻塞从而进入内核态，一旦进入内核态之后调度的控制权就不在go runtime手中也就无法借助go scheduler进行调度。在非阻塞模式下对应goroutine是被gopark给park住放入某个wait queue中，M可以继续执行下一个G。整个过程网络 I&#x2F;O 的控制权都牢牢掌握在 Go 自己的 runtime 里。</p><h2 id="one-more-thing"><a href="#one-more-thing" class="headerlink" title="one more thing"></a>one more thing</h2><p>在go1.14对timer进行了优化，其中一个优化点是将timer与netpoll结合。在go1.10之前由一个独立的timerproc通过小顶堆和futexsleep来管理定时任务，go1.10为了降低锁的竞争将其扩展为最多64个小顶堆和timerproc，但依然没有从本质上解决全局锁以及多次G、P和M间的切换导致的性能开销问题。</p><p>在go1.14中把存放定时事件的四叉堆放到P中，这样既很大程度上避免了全局锁又保证了G、P和M间的一致性避免相互之前多次切换，同时取消了timerproc协程转而使用netpoll来做就近时间的休眠等待：</p><ol><li>如果新添加的定时任务when小于netpoll的休眠等待时间<code>sched.pollUntil</code>就会激活netPoll的等待。也就是在<code>runtime.findrunable </code>里的最后会使用超时阻塞的方法调用<code>epoll_wait</code>，这样既监控了epoll实例红黑树上的fd，又可兼顾最近的定时任务</li><li>在每次<code>runtime.schedule</code>调度时在<code>runtime.findrunable </code>中都会通过<code>checkTimers </code>来查找可运行的定时任务</li></ol><h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><p><a href="https://morsmachine.dk/netpoller">The Go netpoller</a></p>]]></content>
      
      
      <categories>
          
          <category> Go </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Go </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>控制好微服务之间的连接数量</title>
      <link href="/2020/12/06/micro-service-subsetting/"/>
      <url>/2020/12/06/micro-service-subsetting/</url>
      
        <content type="html"><![CDATA[<p>在微服务的架构模式下服务间通过长连接进行通信，通常情况下每个客户端实例都会与每个服务实例建立连接，如果服务实例数达到几千几万个那么相互之间建立的连接数将指数级上升，这样带来的坏处是：</p><ol><li>客户端的连接池里的连接数变多，消耗的资源增加</li><li>对应的服务端维护的长连接增多，尤其在一个连接一个线程的模式下消耗的资源很可观</li><li>长连接定期的健康检查消耗一定的CPU</li></ol><p>为了避免过多的资源消耗客户端可以选择服务集群实例的某个子集来建立连接，这样可有效的减少资源消耗。这个子集的选择方案必须满足以下几个条件：</p><ol><li>子集的大小要足够客户端使用</li><li>后端服务的连接数要负载均衡</li><li>能够在服务重启、滚动升级、客户端和服务端进行集群大小调整时持续均衡，避免大量连接的重建、迁移或者各服务实例的连接数产生较大的波动</li></ol><p>接下来看看Google是如何解决这个问题的</p><h2 id="随机选择"><a href="#随机选择" class="headerlink" title="随机选择"></a>随机选择</h2><p>在确定好子集的大小后采用完全随机的方式为每个客户端选择固定大小的子集，但经过实验这种方式负载的效果非常差，要想达到比较均匀的负载则子集大小至少要在75%，这对于大规模部署的集群来说是不可接受的。</p><p>个人认为之所以完全随机表现的不够好是因为连接的建立次数是有限的，只在服务初始化时建立。这就好比抛硬币正反面出现的概率各占50%，但是如果我们只抛四次有可能出现三次正面一次反面，只有抛的次数足够多才能无限接近概率。而每次连接的建立就好比一次抛硬币，如果连接建立的次数足够多那负载会比较均衡，扩大子集的规模一定程度上增加了抛硬币的次数，如果每次请求都新建连接那么负载的效果会比较好但每次新建连接的成本太高，所以该方法不可靠。</p><h2 id="确定性算法"><a href="#确定性算法" class="headerlink" title="确定性算法"></a>确定性算法</h2><p>随后Google抛弃了完全随机的算法，采用了一种相对确定的算法：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">Subset</span><span class="params">(backends []<span class="type">string</span>, clientID, subsetSize <span class="type">int</span>)</span></span> []<span class="type">string</span> &#123;</span><br><span class="line"><span class="comment">// 计算一轮可满足几个客户端</span></span><br><span class="line">subsetCount := <span class="built_in">len</span>(backends)/subsetSize</span><br><span class="line"><span class="comment">// 计算本次客户端属于哪一轮，每一轮使用相同的随机列表</span></span><br><span class="line">round := clientID/subsetCount</span><br><span class="line"> r := rand.New(rand.NewSource(<span class="type">int64</span>(round)))</span><br><span class="line"> r.Shuffle(<span class="built_in">len</span>(backends), <span class="function"><span class="keyword">func</span><span class="params">(i, j <span class="type">int</span>)</span></span> &#123; backends[i], backends[j] = backends[j], backends[i] &#125;)</span><br><span class="line"> <span class="comment">// 计算客户端在本轮中的位置</span></span><br><span class="line"> subsetID := clientID % subsetCount</span><br><span class="line"> start := subsetID * subsetSize</span><br><span class="line"> <span class="keyword">return</span> backends[start: start+subsetSize]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>假设我们有13个后端实例，有10个客户端，子集的大小为3。那么每轮我们最多可以给4个客户端分配后端实例：<code>subsetCount = 12/3</code>，每一轮里一个后端实例只会被分配给一个且仅一个客户端，当客户端数量不够时有些实例没有被分配，但没有被分配的实例在每一轮中是随机的。总共有10个实例所以我们要分配三轮，每次产生的随机列表可能如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Round 0: [0, 6, 3, 5, 12, 1, 7, 11, 9, 2, 4, 8, 10]</span><br><span class="line"></span><br><span class="line">Round 1: [8, 11, 4, 12, 0, 5, 6, 10, 3, 2, 7, 9, 1]</span><br><span class="line"></span><br><span class="line">Round 2: [8, 3, 7, 2, 1, 4, 12, 9, 10, 6, 5, 0, 11]</span><br></pre></td></tr></table></figure><p>例如clientID为2每个client需要3个连接则roud&#x3D;2&#x2F;4&#x3D;0，subsetID&#x3D; 2%4 &#x3D; 2，那分配给该客户端的实例为[7, 11, 9]。注意，在最后一轮中只有2个客户端需要分配，所以剩下的两组后端实例是没有被分配连接的。</p><p>只要clientID是连续的则分配到每个后端的连接数也是均匀的</p><h3 id="滚动更新及扩容"><a href="#滚动更新及扩容" class="headerlink" title="滚动更新及扩容"></a>滚动更新及扩容</h3><h4 id="Client"><a href="#Client" class="headerlink" title="Client"></a>Client</h4><p>某个client的下线不会影响其他client的连接分布，所以当某个client下线时只会导致某些backend实例连接数比其他实例少1</p><p>client集群扩容时clientID从后面依次递增按照上述算法的描述连接依然是均匀的</p><h4 id="Server"><a href="#Server" class="headerlink" title="Server"></a>Server</h4><p>与client情况类似，当某个server实例下线时会导致跟这台实例有连接的部分client连接发生变化，但因每个server随机分配给不同的client从整体来看计算结果不会有大变化</p><p>server集群扩容时相当于每一轮能满足的client数变多，round变小连接依然是均匀的</p><h2 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h2><p>确定性算法为什么比完全随机要负载更均匀呢？确定性算法将客户端划分为多轮，在同一轮之中使用相同的随机序列使一轮之中连接的分配是均匀的，而每轮序列的不同又增加了一定的随机性</p><p>要使用该算法还需要考虑以下几个问题：</p><ol><li>clientID是需要连续的，这将增加外部依赖</li><li>如果clientID并非滚动升级那么极端情况下会挑选出一批client使得某台服务实例连接数为0</li><li>如果一次滚动升级的服务实例数较多囊括了某个client所选择的全部实例则会导致该client无连接可用</li></ol><h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><p>[1] <a href="https://sre.google/sre-book/load-balancing-datacenter/">https://sre.google/sre-book/load-balancing-datacenter/</a></p>]]></content>
      
      
      <categories>
          
          <category> 系统设计 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Architecture &amp; Design </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>微服务中的服务过载</title>
      <link href="/2020/12/05/micro-service-overload/"/>
      <url>/2020/12/05/micro-service-overload/</url>
      
        <content type="html"><![CDATA[<p>在微服务中某个服务的请求突然升高会造成服务的过载，服务一旦过载进入异常状态之后会对服务的上下游都造成一定的影响。当服务出现过载的时候常见的处理方法是调用方进行熔断，服务方进行降级、流量抛弃等。</p><h2 id="资源耗尽"><a href="#资源耗尽" class="headerlink" title="资源耗尽"></a>资源耗尽</h2><p>假设一个服务最多能处理1000QPS的请求，当QPS超过1000之后随着请求量的增多会发生什么：</p><ol><li>CPU不足以应对这么多的请求则所有请求的响应时间变慢</li><li>响应时间变慢进而导致积压的in-flight请求变多</li><li>in-flight请求增多会导致内存、活跃线程数(在每个请求一个线程的模式下)、文件描述符等资源的消耗增加</li><li>请求队列填满，所有请求都要排队进一步增加响应时间同时会使用更多的内存</li><li>如果跑的是带有垃圾回收的runtime程序内存使用率上升会导致GC触发的次数增多，进而导致CPU资源进一步减少</li><li>内存的过多消耗会导致内存中的缓存命中率降低，未命中缓存的请求发送给下游获取数据进而导致下游服务的连锁反应</li></ol><p>当发生过载的情况时往往会伴随着高延迟、高错误率或者低质量的响应发生</p><h2 id="应对过载"><a href="#应对过载" class="headerlink" title="应对过载"></a>应对过载</h2><h3 id="客户端"><a href="#客户端" class="headerlink" title="客户端"></a>客户端</h3><p>当服务发生过载错误率升高的时候对于调用方可以进行熔断容错处理，常见的是Netflix的Hystrix框架。当检测到错误率达到一定阈值之后调用方进行熔断，不再向服务方发送请求同时要对此进行容错处理，过一段时间之后再尝试向服务方发送一部分流量探测服务方是否正常，如果正常则恢复请求，如果不正常则继续熔断。</p><p>该熔断策略不好之处在于“一刀切”，熔断之后所有请求都不发送给服务方，调用方不能及时的感知到服务方的状态，动态调整的能力较弱。Google提出的一种自适应的机制可动态调整请求的发送量，客户端通过滑动窗口记录过去两分钟的请求数量(requests)和被服务端正常接受的请求数量(accepts)。</p><p>正常情况下这两个数值是相等的，当后端开始出现请求超时、返回错误等拒绝一部分请求时，accepts的值将降低，直到超过<code>requests = K*accepts</code>时客户端开始自行节流熔断，请求将以一定的概率被客户端直接拒绝，概率计算如下：$$ drop&#x3D;max(0, \frac {requests -K * accetps} {requests+1})$$ K通常设置为2，开始时requests&#x3D;accetps，drop值为0。当后端开始拒绝服务导致<code>requests &gt; K*accepts</code>此时客户端开始拒绝请求，requests会继续上升导致拒绝的概率变大。当后端服务恢复之后accepts值增大<br>且由于K的存在K*accepts的增速比requests快因此会逐渐降低drop值最终恢复正常的状态。</p><h3 id="服务端"><a href="#服务端" class="headerlink" title="服务端"></a>服务端</h3><p>服务端应对过载常见的做法有以下几种：</p><ol><li>流量抛弃，当请求速率超过线程处理速率时会产生排队，此时线程池和队列都会饱和，当出现队列饱和时可以尽早的拒绝请求。对于一些排队很久的请求可以不处理因为客户端可能已经发起重试了，对这种请求进行处理没有意义。如果请求有优先级可对某些低优先级的请求进行拒绝，进一步降低资源消耗</li><li>降级，通过降低响应的质量来减少资源的消耗，例如：推荐系统可以减少召回数量或者直接返回静态数据等</li><li>传递和判断请求的截止时间，在多层级的RPC请求中传递截止时间，如果已经超过截止时间则不再向下传递请求</li><li>为预防过载服务端针对不同的客户端设置一定配额的限流，防止某个客户端的流量突增导致整个服务不可用进而影响其他客户端</li></ol><h3 id="小心负载均衡和重试"><a href="#小心负载均衡和重试" class="headerlink" title="小心负载均衡和重试"></a>小心负载均衡和重试</h3><p>当某个集群实例发生过载而崩溃时负载均衡会把请求转发到其他的集群，这会导致其他的集群也相继过载从而导致整个服务过载，如同滚雪球一样整个服务进入循环崩溃且比较难以恢复，一旦某个实例恢复就会被流量立即打垮。</p><p>对于重试应注意以下几点：</p><ol><li>区分可重试与不可重试的错误处理，对于服务端明确返回的不可重试错误不应进行重试</li><li>限制请求重试次数，不能无限重试</li><li>客户端设置全局重试次数</li><li>在较长的调用链路中避免多级重试，进而放大重试次数</li><li>使用随机的、指数避让式的重试，避免同一时刻发起大量重试</li></ol><h2 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h2><p>服务一旦出现滚雪球式的过载往往对整个系统造成较大影响，需要在平时把容量估算、水位监控、限流、降级等工作做好，才能防患未然。</p>]]></content>
      
      
      <categories>
          
          <category> 系统设计 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Architecture &amp; Design </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Kubernetes使用DevicePlugin 管理 GPU</title>
      <link href="/2020/02/01/k8s-with-gpu/"/>
      <url>/2020/02/01/k8s-with-gpu/</url>
      
        <content type="html"><![CDATA[<h3 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h3><p>近几年随着大量数据的积累和算力的发展使AI有了更多的落地场景并再次成为人们口中的“明日之星”，这其中云计算是其背后的一个重要推手。而云计算领域Kubernetes作为云OS的事实标准也成为了关注的焦点，云原生和AI的快速发展使得用Kubernetes管理GPU的诉求也越来越多，使用Kubernetes管理GPU有什么好处呢？</p><p>首先，使用k8s管理GPU可以加速部署。使用容器技术可以将部署的过程固化以复用，减少准备部署环境所耗费的时间，目前许多AI框架都提供相关的容器镜像。</p><p>其次，提升资源使用率。当公司的GPU资源达到一定数量后通过k8s统一资源调度将GPU资源池化，进行分时复用，使得GPU可以用时申请，用完释放提升GPU使用效率降低成本。</p><p>最后，通过容器化可以将GPU资源进行隔离，有效的防止各任务之间的相互影响，提升系统的稳定性。</p><h3 id="Docker下如何使用GPU"><a href="#Docker下如何使用GPU" class="headerlink" title="Docker下如何使用GPU"></a>Docker下如何使用GPU</h3><p>首先我们看一下如何在Docker容器里使用GPU，要在Docker容器里使用GPU主要有两点：</p><ol><li>   GPU Device，在容器里能够找到 GPU设备，比如：&#x2F;dev&#x2F;nvidia0</li><li><pre><code>GPU驱动，在容器里能够找到对应的GPU驱动目录，比如：/usr/local/nvidia/*</code></pre></li></ol><p>以上两点可以通过配置docker启动参数来实现，GPU设备路径可以通过容器启动时Devices参数来配置，设备驱动目录可以通过容器启动时Volume参数来配置。</p><p>准备相关的Docker镜像有两种方式：</p><ul><li>下载官方镜像</li></ul><p>比如TensorFlow、PyTorch、Caffe等均提供了官方的镜像可在docker.hub上下载，简单便捷。</p><ul><li>基于Nvidia CUDA基础镜像进行构建</li></ul><p>如果因官方镜像无法满足需求而对其进行了改动那么就需要自己来构建镜像，在这种情况下最好是以NVIDIA 官方镜像为基础镜像来进行构建。例如PyTorch就是以nvidia cuda为基础镜像来构建的：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">FROM nvidia/cuda:10.1-cudnn7-devel-ubuntu16.04</span><br><span class="line">ARG PYTHON_VERSION=3.8</span><br><span class="line">ARG WITH_TORCHVISION=1</span><br><span class="line">RUN apt-get update &amp;&amp; apt-get install -y --no-install-recommends \</span><br><span class="line">         build-essential \</span><br><span class="line">         cmake \</span><br><span class="line">         git \</span><br><span class="line">         curl \</span><br><span class="line">         ca-certificates \</span><br><span class="line">         libjpeg-dev \</span><br><span class="line">         libpng-dev &amp;&amp; \</span><br><span class="line">     rm -rf /var/lib/apt/lists/*</span><br><span class="line">     ……</span><br></pre></td></tr></table></figure><h3 id="Nvidia-docker原理"><a href="#Nvidia-docker原理" class="headerlink" title="Nvidia-docker原理"></a>Nvidia-docker原理</h3><p>以上我们了解了在Docker中使用GPU的关键点，与在物理机中直接使用GPU的区别如下图：</p><figure class="image-bubble">                <div class="img-lightbox">                    <div class="overlay"></div>                    <img src="/img/docker-gpu.jpg" alt="gpu in docker" title="">                </div>                <div class="image-caption">gpu in docker</div>            </figure><p>要在物理机里运行一个AI程序，其运行环境如图左侧所示，首先底层是GPU的驱动，再往上是cuda lib，在cuda之上是基于TensorFlow、PyTorch等框架的应用，通常情况下AI应用框架对cuda库的版本有要求，如果AI应用框架版本变化后对应的cuda版本也要跟着变化，二者耦合较紧，而底层的GPU驱动则不会有大的变化。</p><p>如上图右侧所示，NVIDIA GPU容器化的方案是将GPU驱动安装到宿主机，将cuda lib及TensorFlow、PyTorch等打到镜像中，使用该镜像运行容器时将GPU驱动映射到容器中，这样就可以在一台宿主机上运行不同版本的cuda及对应的应用容器。</p><p>大家习惯使用 Nvidia-docker 来运行 GPU 容器，而 Nvidia-docker 的实际工作就是来自动化将宿主机的设备和 Nvidia 驱动库映射到容器中。</p><h3 id="k8s-Device-Plugin原理"><a href="#k8s-Device-Plugin原理" class="headerlink" title="k8s Device Plugin原理"></a>k8s Device Plugin原理</h3><p>当使用K8S时我们期望在pod的对象里声明需要的GPU资源后k8s在调度时即可为我们分配所需的GPU资源，如：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line">apiVersion: v1</span><br><span class="line">kind: Pod</span><br><span class="line">metadata:</span><br><span class="line">  name: cuda-vector-add</span><br><span class="line">spec:</span><br><span class="line">  restartPolicy: OnFailure</span><br><span class="line">  containers:</span><br><span class="line">    - name: cuda-vector-add</span><br><span class="line">      image: &quot;k8s.gcr.io/cuda-vector-add:v0.1&quot;</span><br><span class="line">      resources:</span><br><span class="line">        limits:</span><br><span class="line">          nvidia.com/gpu: 1</span><br></pre></td></tr></table></figure><p>在上述 Pod 的 limits 字段里，这个资源的名称是nvidia.com&#x2F;gpu，它的值是 1。即这个 Pod 声明了自己要使用一个 NVIDIA 类型的 GPU。这当中使用了一种叫作 Extended Resource特殊字段来传递所需GPU信息。</p><p>为了能够让kube-scheduler知道每台宿主机GPU资源可用量就需要宿主机能够向API Server汇报自己的该类型资源的可用量，每种资源的可用量实际表现为Node对象Status字段的内容：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">apiVersion: v1</span><br><span class="line">kind: Node</span><br><span class="line">...</span><br><span class="line">Status:</span><br><span class="line">  Capacity:</span><br><span class="line">   cpu:  2</span><br><span class="line">   memory:  2049008Ki</span><br><span class="line">   nvidia.com/gpu: 1</span><br></pre></td></tr></table></figure><p>那么如何为Node对象添加GPU资源的可用量呢？有两种方式：</p><ul><li>Extend Resources 属于 Node-level 的 api，完全可以独立于 Device Plugin 使用，只需通过一个 PACTH API 对 Node 对象进行 status 部分更新即可。这样在 Kubernetes 调度器中就能够记录这个节点的 GPU 类型，它所对应的资源数量是 1。</li></ul><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">$ curl --header &quot;Content-Type: application/json-patch+json&quot; \</span><br><span class="line">--request PATCH \</span><br><span class="line">--data &#x27;[&#123;&quot;op&quot;: &quot;add&quot;, &quot;path&quot;: &quot;/status/capacity/nvidia.com/gpu&quot;, &quot;value&quot;: &quot;1&quot;&#125;]&#x27; \</span><br><span class="line">http://localhost:8001/api/v1/nodes/&lt;your-node-name&gt;/status</span><br></pre></td></tr></table></figure><ul><li>使用Device Plugin，在k8s中所有硬件资源的管理都是通过Deveice Plugin插件来完成的，这当中当然也包括对Extended Resource汇报的逻辑，如下图所示：</li></ul><figure class="image-bubble">                <div class="img-lightbox">                    <div class="overlay"></div>                    <img src="/img/k8s-gpu.jpg" alt="gpu in k8s" title="">                </div>                <div class="image-caption">gpu in k8s</div>            </figure><p>Device Plugin的工作主要分为两部分：资源上报和资源使用时的调度。</p><h4 id="资源上报"><a href="#资源上报" class="headerlink" title="资源上报"></a>资源上报</h4><p>首先Device Plugin通过GRPC与kubelet的Device Plugin Manager进行通信并将自己注册给k8s，让kubelet知道三件事：我是谁，即该Device Plugin管理的设备是什么；我在哪，即Device Plugin所监听的socket所在位置，kubelet通过此来进行调用Device Plugin；交互协议，即API版本号；</p><p>然后Device Plugin启动一个GRPC Server让kubelet来访问。</p><p>第三步，kubelet与Device Plugin建立一个 ListAndWatch的长连接，用来发现设备 ID 以及设备的健康状态，Device Plugin定期向 kubelet 汇报该 Node 上 GPU 的列表。</p><p>第四步，kubelet 会将这些设备暴露到 Node 节点的状态中，在kubelet向 api-server发送心跳里以 Extended Resource 的方式，加上这些 GPU 的数量，比如nvidia.com&#x2F;gpu&#x3D;3。后续调度器可以根据这些信息进行调度。</p><p>kubelet 在向 api-server 进行汇报的时候，只会汇报该 GPU 对应的数量。而 kubelet 自身的 Device Plugin Manager 会对这个 GPU 的 ID 列表进行保存，并用来具体的设备分配。而对于 Kubernetes scheduler来说，它不知道这个 GPU 的 ID 列表，它只知道 GPU 的数量。</p><h4 id="资源调度使用"><a href="#资源调度使用" class="headerlink" title="资源调度使用"></a>资源调度使用</h4><p>当某个pod要使用GPU资源时只需要在api对象里声明所需资源的数量，接下来scheduler会在自己缓存里找到能够满足要求的node，然后将其GPU减掉相应的数量并完成node与pod的绑定。</p><p>当kubelet发现该pod被分配到自己的节点上时会从自己持有的GPU列表里为该pod分配一个GPU，此时kubelet向Device Plugin发起Allocate()请求，参数为分配给该pod的设备id。当 Device Plugin 收到 Allocate 请求之后根据 kubelet 传递过来的设备 ID找到这些设备对应的设备路径和驱动目录。这些信息正是 Device Plugin 周期性的从本机查询到的。比如，在 NVIDIA Device Plugin 的实现里它会定期访问 nvidia-docker 插件，从而获取到本机的 GPU 信息。</p><p>被分配 的GPU 对应的设备路径和驱动目录信息被返回给 kubelet 之后就完成了为一个容器分配 GPU 的操作。接下来，kubelet 会把这些信息追加在创建该容器所对应的 CRI（Container Runtime Interface）请求当中。当这个 CRI 请求发给 Docker 之后，Docker 为你创建出来的容器里就会出现这个 GPU 设备，并把它所需要的驱动目录挂载进去。这个过程与上面提到的通过docker启动参数挂载设备和驱动原理是一致的。</p><p>至此通过k8s为一个pod分配GPU的过程就完成了。</p><h3 id="Device-Pulgin的不足"><a href="#Device-Pulgin的不足" class="headerlink" title="Device Pulgin的不足"></a>Device Pulgin的不足</h3><p>虽然通过Device Plugin可以让k8s在调度时一定程度上满足Extended Resource的调度，但也只是基于数量调度，如果设备是异构的、硬件的属性比较复杂并且pod也关心硬件的某些属性，那么不能简单的通过数量来进行调度此时这个方案就不能满足需求，另外对于资源分配时quota限制粒度也较大。</p><p>针对以上问题目前有一种方案通过node label和node selector来解决该问题，但这种方式也只能通过将同一种类型的硬件挂载到同一个节点来解决对某种类型资源的分配，不能使其他更详细资源属性参与到调度中，比如：某个节点挂载了显存不同的NVIDIA Tesla K80，某个应用想要使用显存大于10G的GPU。</p><p>另外一种方案是通过CRD(CustomResourceDefinition)以及Scheduler Extender 机制来满足异构设备调度的需求，但多多少少都会涉及到改动k8s源码。</p><p>因此目前为止官方提供的Device Plugin可以用来进行设备的管理和资源分配但在调度方面由于向scheduler暴露的设备属性信息太少还无法满足复杂的调度需求。</p>]]></content>
      
      
      <categories>
          
          <category> Kubernetes </category>
          
      </categories>
      
      
        <tags>
            
            <tag> k8s </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>关于Kubernetes的思考</title>
      <link href="/2019/06/02/k8s-thinking/"/>
      <url>/2019/06/02/k8s-thinking/</url>
      
        <content type="html"><![CDATA[<p>随着这些年的发展Kubernetes已经成为容器编排调度的事实标准，它的出现将改变软件部署交付的方式而它本身的设计理念也十分值得深思和借鉴。</p><h3 id="声明式API和编程范式"><a href="#声明式API和编程范式" class="headerlink" title="声明式API和编程范式"></a>声明式API和编程范式</h3><p>当你开始尝试在k8s中部署一个pod时，它所给你带来的最直接的感受就是k8s的API是声明式而不是命令式的。如果是命令式的API，那么你就需要通过API来告诉k8s具体要执行的动作是什么，比如启动或停止容器。而声明式的API只需要我们使用声明式的配置，比如：json、yaml等，来指定我们想要的目标状态，同时k8s会获取当前资源状态，当目标状态和当前状态不一致时k8s会不断的调整资源的状态直到使其达到目标状态。这样做的好处是什么呢？</p><p>首先是可以简化API，通过声明式的API只需提供合适的Spec定义即可，用户通过修改Spec来通知系统他所期望的状态是什么。如果是命令式API则需要向用户暴露几十上百个接口用以应对不同资源的不同的操作，用户使用起来也会觉得头大。另外k8s提供一些用于特殊场景的api，比如：扩缩容、修改镜像等，但其底层依然是对Spec的修改。</p><p>再者是稳定，声明式和命令式分别代表着两种不同的动作触发模式：水平触发和边缘触发。举个例子：<br>比如一个开关，我们进行了开、关、开三次操作，对于命令式API这意味着要发三次命令，如果网络出现了问题最后一次开的命令丢失了则这个开关会一直处于错误的状态，所以如果是边缘触发则需要配合补偿机制。如果是声明式API即使网络出了问题系统停留在错误的状态，但是当网络恢复之后系统依然可以调整到所期望的状态。设想我们在对系统做扩缩容的操作时，如果每次告诉k8s要增加或减少一个实例，一但这个代表具体操作的命令发生了丢失那么系统的状态就乱了，更好的做法是告诉k8s我们所期望的实例个数是多少，即使这期间网络发生了问题或者控制器出了问题，等到网络或者出问题的组件恢复后k8s依然可以为我们把实例调整到我们所期望的个数。</p><p>第三是便于处理多写的操作，k8s API的客户端有多种，以kubectl为例它除了提供kubectl create、kubectl replace等操作外还提供了kubectl apply，kubectl apply 与 kubectl create、kubectl replace的不同之处在于前者是声明式的操作而后两者是命令式的操作。以Kubectl replace为例，它是使用新的yaml文件里的api对象来替换旧的对象而kubectl apply则是对原有的对象做patch操作。二者最大的区别是对于声明式请求一次可以处理多个写操作并且具备merge能力。Service Mesh是最近比较火的一个概念，其中一个代表型的项目叫Istio，它最主要的一个组件是Envoy。为了实现服务发现与治理的功能Istio需要为每个pod里安装一个Envoy容器，这过程中Istio通过k8s的Dynamic Admission Control功能所做的事情就是对新建的pod声明对象patch一个Envoy的容器配置，这样随后创建的pod中就注入了一个Envoy容器。</p><p>在声明式API基础上所形成的Kubernetes编程范式即：使用控制器监听Kubernetes里API对象的状态变化，为不同的事件注册相应的操作函数，当其状态发生变化时执行对应的函数进行相应的操作进而完成用户业务逻辑的编写。</p><h3 id="list-watch机制"><a href="#list-watch机制" class="headerlink" title="list-watch机制"></a>list-watch机制</h3><p>我们知道k8s中API对象的状态都存储在etcd当中，而所有跟etcd的交互都必须通过api server来进行。之前我们提到在声明式API的基础上控制器以及调度器等都需要监听API对象状态的变化，当API对象声明的状态发生变化后需要作出相应的动作。在这种需求下一般会引入MQ，而k8s为了降低系统本身对其他组件的依赖并没有使用任何的消息中间件也没有使用轮询的方式而是提供了一种list-watch的机制。该机制的list部分主要通过提供list接口来实现对api对象及其状态的查询，watch部分则是通过提供watch api来实现对资源变更事件的监听。watch实现的关键是http1.1中的<a href="https://zh.wikipedia.org/zh-hans/%E5%88%86%E5%9D%97%E4%BC%A0%E8%BE%93%E7%BC%96%E7%A0%81">Chunked transfer encoding(分块传输编码)</a></p><p>list和watch相互配合，通过list获取全量数据，通过watch可以获取增量数据。当某个时间点watch连接中断后会发生消息丢失的问题而此时通过调用list接口则可以解决该问题保证消息的可靠性。通过watch可以在保证消息实时性的同时降低系统的开销，同时事件中的resourceVersion标签可以保证消息的顺序性，当客户端并发处理同一个资源的事件时，可以对比 resourceVersion来保证资源最终的状态和最新的事件所期望的状态保持一致。</p><p>通过list-watch机制API Server相当于充当了消息总线的角色，k8s中的控制器、调度器、kubelet等其他组件只需通过该机制监听自己所关心的对象状态即可，相互之间并不知道彼此的存在。比如pod的调度，调度器通过监听发现有新的pod创建出来且尚未对其调度，调度器开始执行自己的调度逻辑为该pod选择node节点，然后调用API Server接口把要绑定的node信息写入pod对象的Node字段中即完成了调度。同时node节点上的kubelet组件通过订阅事件发现有一个pod调度到该节点上了，那么它就会通过API Server获取该pod对象的配置信息，根据pod的Spec完成部署。整个过程中kubelet并不知道scheduler的存在，通过这种机制对各组件的依赖关系进行了解耦。</p><h3 id="系统抽象"><a href="#系统抽象" class="headerlink" title="系统抽象"></a>系统抽象</h3><p>在系统抽象方面尤其在存储方面的抽象Kubernetes做的很精彩。</p><p>首先通过卷(Volume)来抽象出存储方式，卷描述的是底层的存储能力。但是底层的文件存储系统(NFS、Ceph等)一般都有相关人员来负责，如果仅仅有Volume那么k8s的使用者每次部署pod的时候需要指定存储系统的相关配置，对开发人员不友好。</p><p>于是k8s抽象出持久卷PV和和持久卷声明PVC的概念，系统管理员可以先配置PV映射到底层的存储系统，用户只需要创建PVC来关联PV，然后在创建Pod的时候引用PVC即可。PVC并不关注底层存储的具体细节，只关注容量需求和操作权限。PV这层抽象描述的是底层存储系统能提供出来的卷的资源，PVC这层抽象描述的是用户希望为Pod申请的存储资源请求。</p><p>但是总是需要系统管理员先创建好PV还是不方便，于是k8s又提供了StorageClass这层抽象，通过把PVC不直接关联到PV而是联到StorageClass，由StorageClass根据PVC的需求描述来动态创建PV并完成PVC与PV的绑定(包括动态绑定和延迟绑定)。下图引自《Kubernetes in Action》</p><figure class="image-bubble">                <div class="img-lightbox">                    <div class="overlay"></div>                    <img src="/img/k8s-volume.png" alt="The complete picture of dynamic provisioning of PersistentVolumes" title="">                </div>                <div class="image-caption">The complete picture of dynamic provisioning of PersistentVolumes</div>            </figure><h3 id="系统扩展"><a href="#系统扩展" class="headerlink" title="系统扩展"></a>系统扩展</h3><p>为了方便系统的扩展k8s提供了许多interface，比如CNI(Container Network Interface),CRI(Container Runtime Interface),CSI(Container Storage Interface)等。</p><p>除此之外k8s还提供了CRD(Custom Resource Definition),CR(Custom Resuorce)和Operator机制方便用户自定义资源以及编写对应的Operator。所谓的Operator可以理解为操作用户自定义资源的controller，社区提供的kube-builder和coreos提供的operator-framework可以帮助我们生成Operator的基础代码：初始化 Kubernetes客户端，建立Watch等。Operator甚至可以将应用集群当做一种资源，通过Operator可以完成集群的部署、高可用、故障恢复等，将运维经验代码化。比如：通过<a href="https://github.com/coreos/etcd-operator">etcd-opeator</a>完成etcd集群的搭建。</p><h3 id="面向k8s交付，甚至面向Operator交付"><a href="#面向k8s交付，甚至面向Operator交付" class="headerlink" title="面向k8s交付，甚至面向Operator交付"></a>面向k8s交付，甚至面向Operator交付</h3><p>随着技术的发展软件的交付方式也发生了几次迭代：</p><ul><li>交付源代码：最初根据客户现场的环境不同需要交付方带着源代码到客户现场进行现场编译、部署、运行</li><li>交付可执行文件：后来java提出了“Build once, Run everywhere”，以及Go交叉编译出的可执行二进制文件，使得我们可以直接交付可执行文件，但对于系统底层的依赖依然解决的不彻底</li><li>交付镜像：近几年Docker的盛行解决了因开发环境与运行环境的差异所带来的问题，提升了单体软件的交付效率，对于包含多个组件的复杂系统交付(比如：包含服务发现、负载均衡等组件的系统)仍然显得力不从心</li></ul><p>Kubernetes的出现解决了早些年PaaS系统想解决但没有解决好的问题，如果将来某一天客户现场的OS都是Kubernetes(听说工商银行一次招标的标书中明确写着要求Kubernetes技术栈)，对于ToB项目的交付是不是可以带着一堆镜像和yaml文件就可以完成交付，而这些yaml文件中很可能会包含用户自定义的资源和Operator。对于ToC的业务，目前大部分创业公司会选择将自己的服务跑在云上而不再是自己买服务器建IDC。目前来看没有哪一家云计算服务厂商是不支持k8s的，那么这就意味着如果你的服务是可以跑在k8s上的只需要很小的代价即可完成在不同的云厂商之间的切换。</p><h3 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h3><p>云原生（Cloud Native）这个词随着CNCF的崛起也变得火热起来，什么是云原生让人很难说清楚，但比较明确的是目前国内的云服务厂商开始尝试在正确合理的使用Kubernetes基础上构建新的PaaS平台，同时越来越多的公司开始借此机会来完成自身基础技术体系的转型升级，而在这当中Kubernetes也将会发挥越来越重要的作用。</p><p>最后，how about runing kubernetes on kubernetes</p>]]></content>
      
      
      <categories>
          
          <category> Kubernetes </category>
          
      </categories>
      
      
        <tags>
            
            <tag> k8s </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Go 中的nil channel</title>
      <link href="/2018/05/19/nil-channel/"/>
      <url>/2018/05/19/nil-channel/</url>
      
        <content type="html"><![CDATA[<h3 id="从一个问题开始"><a href="#从一个问题开始" class="headerlink" title="从一个问题开始"></a>从一个问题开始</h3><p>要求写一个函数，该函数将从两个channel里获取值并将获取到的值放到一个新的channel里，最后将新的channel返回:<br><code>func merge(a,b &lt;-chan int) &lt;-chan int</code></p><h3 id="一种思路"><a href="#一种思路" class="headerlink" title="一种思路"></a>一种思路</h3><p>首先有一个生成a,b这两个channel的函数：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">getChan</span><span class="params">(vs []<span class="type">int</span>)</span></span> &lt;-<span class="keyword">chan</span> <span class="type">int</span> &#123;</span><br><span class="line">c := <span class="built_in">make</span>(<span class="keyword">chan</span> <span class="type">int</span>)</span><br><span class="line"><span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span>&#123;</span><br><span class="line"><span class="keyword">for</span> _, v := <span class="keyword">range</span> vs&#123;</span><br><span class="line">c &lt;- v</span><br><span class="line">time.Sleep(time.Duration(rand.Intn(<span class="number">1000</span>)) * time.Millisecond)</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">close</span>(c)</span><br><span class="line">&#125;()</span><br><span class="line"><span class="keyword">return</span> c</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>接着来实现merge函数，需要注意的是何时关闭channel：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">merge</span><span class="params">(a, b &lt;-<span class="keyword">chan</span> <span class="type">int</span>)</span></span> &lt;-<span class="keyword">chan</span> <span class="type">int</span> &#123;</span><br><span class="line">c := <span class="built_in">make</span>(<span class="keyword">chan</span> <span class="type">int</span>)</span><br><span class="line"><span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line"><span class="keyword">defer</span> <span class="built_in">close</span>(c)</span><br><span class="line">aClosed, bClosed := <span class="literal">false</span>, <span class="literal">false</span></span><br><span class="line"><span class="keyword">for</span> !aClosed || !bClosed &#123;</span><br><span class="line"><span class="keyword">select</span> &#123;</span><br><span class="line"><span class="keyword">case</span> v, ok := &lt;-a:</span><br><span class="line"><span class="keyword">if</span> !ok &#123;</span><br><span class="line">fmt.Println(<span class="string">&quot;channel a is closed&quot;</span>)</span><br><span class="line">aClosed = <span class="literal">true</span></span><br><span class="line"><span class="keyword">continue</span></span><br><span class="line">&#125;</span><br><span class="line">c&lt;-v</span><br><span class="line"><span class="keyword">case</span> v, ok := &lt;-b:</span><br><span class="line"><span class="keyword">if</span> !ok &#123;</span><br><span class="line">fmt.Println(<span class="string">&quot;channel b is closed&quot;</span>)</span><br><span class="line">bClosed = <span class="literal">true</span></span><br><span class="line"><span class="keyword">continue</span></span><br><span class="line">&#125;</span><br><span class="line">c&lt;-v</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;()</span><br><span class="line"><span class="keyword">return</span> c</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>跑一下这个代码看到以下的结果：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"></span><br><span class="line">a := getChan([]<span class="type">int</span>&#123;<span class="number">1</span>, <span class="number">3</span>, <span class="number">5</span>, <span class="number">7</span>&#125;)</span><br><span class="line">b := getChan([]<span class="type">int</span>&#123;<span class="number">2</span>, <span class="number">4</span>, <span class="number">6</span>, <span class="number">8</span>&#125;)</span><br><span class="line">c := merge(a, b)</span><br><span class="line"><span class="keyword">for</span> v := <span class="keyword">range</span> c &#123;</span><br><span class="line">fmt.Println(v)</span><br><span class="line">&#125;</span><br><span class="line">fmt.Println(<span class="string">&quot;done&quot;</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">&gt;go run main.go</span><br><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">channel a is closed</span><br><span class="line">channel a is closed</span><br><span class="line">channel a is closed</span><br><span class="line">channel a is closed</span><br><span class="line">...</span><br><span class="line">channel b is closed</span><br><span class="line">done</span><br></pre></td></tr></table></figure><p>然后会发现在<code>channel b close</code>之前输出了好多<code>channel a is closed </code>，原因是在两个channel都没有数据到来前select语句会阻塞在那里，但是一旦channel a 关闭了select则不会阻塞所以在channel b 数据到来之前select会一直选择channel a，此时进入了忙等待的状态。</p><h3 id="另一种思路"><a href="#另一种思路" class="headerlink" title="另一种思路"></a>另一种思路</h3><p>如果一个channel close掉了，那么我们将这个channel置为nil看看效果会怎样。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">merge</span><span class="params">(a, b &lt;-<span class="keyword">chan</span> <span class="type">int</span>)</span></span> &lt;-<span class="keyword">chan</span> <span class="type">int</span> &#123;</span><br><span class="line">c := <span class="built_in">make</span>(<span class="keyword">chan</span> <span class="type">int</span>)</span><br><span class="line"><span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line"><span class="keyword">defer</span> <span class="built_in">close</span>(c)</span><br><span class="line"><span class="keyword">for</span> a!=<span class="literal">nil</span> || b!=<span class="literal">nil</span> &#123;</span><br><span class="line"><span class="keyword">select</span> &#123;</span><br><span class="line"><span class="keyword">case</span> v, ok := &lt;-a:</span><br><span class="line"><span class="keyword">if</span> !ok &#123;</span><br><span class="line">fmt.Println(<span class="string">&quot;channel a is closed&quot;</span>)</span><br><span class="line">a = <span class="literal">nil</span></span><br><span class="line"><span class="keyword">continue</span></span><br><span class="line">&#125;</span><br><span class="line">c &lt;- v</span><br><span class="line"><span class="keyword">case</span> v, ok := &lt;-b:</span><br><span class="line"><span class="keyword">if</span> !ok &#123;</span><br><span class="line">fmt.Println(<span class="string">&quot;channel b is closed&quot;</span>)</span><br><span class="line">b = <span class="literal">nil</span></span><br><span class="line"><span class="keyword">continue</span></span><br><span class="line">&#125;</span><br><span class="line">c &lt;- v</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;()</span><br><span class="line"><span class="keyword">return</span> c</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>执行main.go得到如下结果：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="number">1</span></span><br><span class="line"><span class="number">3</span></span><br><span class="line"><span class="number">2</span></span><br><span class="line"><span class="number">5</span></span><br><span class="line"><span class="number">4</span></span><br><span class="line"><span class="number">6</span></span><br><span class="line"><span class="number">7</span></span><br><span class="line"><span class="number">8</span></span><br><span class="line">channel a is closed</span><br><span class="line">channel b is closed</span><br><span class="line">done</span><br></pre></td></tr></table></figure><p>这一次没有再出现疯狂输出<code>channel a is closed</code>，原因是一个值为nil的channel有以下特性：</p><ul><li>&lt;-c 从channel中获取值的操作将一直被block</li><li>-&gt;c 向channel中放入值的操作将一直被block</li><li>close(c)关闭一个值为nil的channel将产生panic</li></ul><p>最后留一个问题，如何merge N 个channel，N是一个动态传入的参数。</p>]]></content>
      
      
      <categories>
          
          <category> Go </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>会话管理(Dialog Management)</title>
      <link href="/2018/04/01/DM/"/>
      <url>/2018/04/01/DM/</url>
      
        <content type="html"><![CDATA[<h2 id="基本概念"><a href="#基本概念" class="headerlink" title="基本概念"></a>基本概念</h2><p>会话管理(以下简称DM)是人机多轮对话系统的核心部分。它主要的功能包括：</p><ul><li>对话状态维护(dialog state tracking, DST)</li><li>维护更新会话状态。当前的会话状态依赖于之前的系统状态和之前的系统响应以及当前时刻的用户输入。</li><li>产生决策(dialog strategy)</li><li>根据DST中会话状态做出系统决策，决定下一步做什么。</li><li>与其他后端系统交互</li></ul><p>下图是一个经典的对话系统结构图，可以看到DM的位置，在NLU(Linguistic Analysis)之后在NLG(Text Generation)之前，简单来说DM控制着人机对话的过程和走向，根据对话上下文信息来决定此刻系统对用户的输入做出的响应。</p><figure class="image-bubble">                <div class="img-lightbox">                    <div class="overlay"></div>                    <img src="/img/DM.svg" alt="Architecture of dialog system" title="">                </div>                <div class="image-caption">Architecture of dialog system</div>            </figure><p>在展开描述DM之前先简单说一下NLU(Natural Language Understanding)。NLU主要的工作是分析用户说的话并确定这句话的意图(Intent)是什么。同一个意图通常会有多种表达方式，例如：“今天天气怎么样”、“今天会下雨吗？”、“我想查询一下天气”等等，这些都是在表达同一个意图：查询天气。意图不应该关心句子的某些具体细节，例如：“告诉我去车站怎么走”与“怎么去最近的酒店”所表达的意图都是查询路线，不同的是二者的目的地不同，所以意图应该跟这句话所触发的Action相对应。而至于具体的细节，例如目的地，被当做意图的slot来填充。</p><h2 id="实现方法"><a href="#实现方法" class="headerlink" title="实现方法"></a>实现方法</h2><p>DM的实现方式有多种，每一种都有自己的优势和劣势，下面简单介绍几种。</p><h3 id="Structure-based-Approaches"><a href="#Structure-based-Approaches" class="headerlink" title="Structure-based Approaches"></a>Structure-based Approaches</h3><p>本质上是一种关键词匹配的做法，例如ELIZA和AIML（人工智能标记语言），通常是通过捕捉用户输入语句的关键词和短语来进行回应，通过增加变量并按topic来组织规则等方法能够一定程度上支持简单的多轮对话。</p><h3 id="FSM-based-Approaches"><a href="#FSM-based-Approaches" class="headerlink" title="FSM-based Approaches"></a>FSM-based Approaches</h3><p>这种方法通常将对话建模成一棵树或者有限状态机。系统根据用户的输入在有限的状态集合内进行状态跳转并选择下一步输出，如果从开始节点走到了终止节点则任务就完成了。</p><p>该方法的特点是：</p><ul><li>提前设定好对话流程并由系统主导</li><li>建模简单，适用于简单任务</li><li>将用户的回答限定在有限的集合内</li><li>表达能力有限，灵活性不高</li></ul><h3 id="Frame-based-Approaches"><a href="#Frame-based-Approaches" class="headerlink" title="Frame-based Approaches"></a>Frame-based Approaches</h3><p>在以完成任务为导向的对话系统中，Frame-based的方法是一种较为合适的方法。该方法将对话建模成一个填槽(slot filling)的过程。槽是多轮对话过程中将初步用户意图转化为明确用户指令所需要补全的信息。一个槽与一件事情的处理中所需要获取的一种信息相对应。例如上文提到的：“告诉我去车站怎么走”，其中目的地是一个槽位，“车站”是该槽位所填充的值。槽位又可细分为，平行槽位、依赖槽位、单值槽位和多值槽位等。另外，为了引导用户填充槽位会有对应的澄清话术。例如：“目的地”对应的澄清话术是“您想从哪出发呢？”，“出发时间”对应的澄清话术是“您想什么时间出发呢？”<br>该方法的特点是：</p><ul><li>支持混合主导型系统，用户和系统都可以获取对话的主导权</li><li>输入相对灵活，用户回答可以包含一个或多个槽位信息</li><li>对槽位提取准确度的要求比较高</li><li>适用于相对复杂的多轮对话</li></ul><h3 id="Agenda-Frame-CMU-Communicator"><a href="#Agenda-Frame-CMU-Communicator" class="headerlink" title="Agenda + Frame(CMU Communicator)"></a>Agenda + Frame(CMU Communicator)</h3><p>Agenda + Frame(CMU Communicator)对Frame-based的方法进行了改进，增加了层次的概念支持话题切换、回退等操作。</p><p>该方法首先将会话构建成一棵树，这棵树是动态的，可以在会话过程中对树进行添加子树或挪动子树等操作。树上的每一个节点对应一个handler，handler有优先级，此外还构建了任务计划，该计划包含了话题的有序列表。</p><p>从左到右、深度优先遍历这颗树生成handler的顺序。当用户输入时，系统按照顺序调用每个 handler，每个 handler 尝试解释并回应用户输入。如果handler捕获到信息就把信息标记为consumed，这保证了一个 information item只能被一个handler消费。如果用户输入不被 handler处理，那么系统将会进入output pass，每个handler都有机会产生自己的 prompt。</p><p>handler可以通过返回码声明自己为当前对话焦点，这个handler就被提升到树的顶端。同时这里使用sub-tree promotion 的方法保留特定主题的上下文，handler被提升到兄弟节点中最左边的节点，父节点同样以此方式提升。</p><h2 id="DM设计"><a href="#DM设计" class="headerlink" title="DM设计"></a>DM设计</h2><p>刚开始要做DM的时候可能比较容易想到状态机、马尔科夫决策过程、end2end等方法，但实际操作起来去发现问题多多。</p><h3 id="模型化困难"><a href="#模型化困难" class="headerlink" title="模型化困难"></a>模型化困难</h3><ul><li>训练数据来源不容易找到及标准成本很高</li><li>在产品策略发生变化时需要重新标注语料</li><li>如何将与DM交互的其他service结果融入模型</li></ul><p>个人目前的做法是NLU是单独实现好的，那么DM要做的是多轮会话topic切换(场景打断与恢复)，基于context的语义信息筛选，多轮对话的容错，与其他service交互以及各下游服务结果的rank等。</p><p>人与人之间的对话是及其复杂的，想用一个模型来刻画描述这个过程十分困难，无论在理论研究还是在工程实践领域仍有大量的工作要做，相信将来人机对话的体验会越来越好。</p><p><strong>参考链接</strong></p><p><a href="https://www.inf.uni-hamburg.de/en/inst/ab/lt/publications/2016-schnelleetal-workshop-smartobjects.pdf">NLU vs. Dialog Management: To Whom am I Speaking?</a></p><p><a href="http://www.cs.cmu.edu/~xw/asru99-agenda.pdf">AN AGENDA-BASED DIALOG MANAGEMENT<br>ARCHITECTURE FOR SPOKEN LANGUAGE SYSTEMS</a></p><p><a href="https://tutorials.botsfloor.com/dialog-management-799c20a39aad">Dialog management</a></p>]]></content>
      
      
      <categories>
          
          <category> NLP </category>
          
      </categories>
      
      
        <tags>
            
            <tag> AI </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>关于机器人的一些思考</title>
      <link href="/2018/03/25/robot/"/>
      <url>/2018/03/25/robot/</url>
      
        <content type="html"><![CDATA[<p>前几天在猎豹在水立方开的机器人之夜发布会引起了人们的关注和讨论，在发布会上傅盛把机器人行业的几位大佬都给diss了一遍，RFC(Robot Funder Club)圈内人士也在讨论&#x2F;吐槽此事，猎豹可谓赚足了眼球。恰好我在做一些机器人有关的事情也接触了一些RFC的成员下面说一下我对机器人的一些思考。</p><p>机器人根据场景不同可以分为很多类型，比如：机械臂，服务机器人，陪伴机器人，商用机器人等。关于机械臂了解的不是很多，主要关注服务机器人，例如猎豹发布会里展示的迎宾机器人。要完成这样的一个机器人涉及到的技术有很多，从整体架构层面上来说我认为可以分为两部分：云端计算和本体控制。</p><p>云端计算主要包括语音识别、人脸识别、自然语言理解、知识图谱、多轮对话等。云端完成的工作就好比人对声音、文字、图像等输入信号的分析和思考，可以将其称之为机器人的大脑。</p><p>本体控制主要包括建图、导航、避障、音频采集、机械控制等。机器人本体一般基于ROS来开发，通过各种sensor采集到信号之后传送给云端(大脑)，云端经过分析处理后给本体下达指令，本体根据收到的指令来进行控制及输出，故将其称之为机器人的小脑。</p><p>目前机器人大脑部分技术比较成熟，比如语音识别、人脸识别都已经有很多落地的产品，NLP相关的技术近些年发展也比较迅速。相对来说本体控制相关的技术发展较为缓慢，例如导航部分，尽管有激光导航(实际上严格意义来说只用激光并不能导航)、UWB、vSLAM等技术，但在实际应用中仍需要踩很多坑。比如，由于激光的成本比较高我们想用基于计算机视觉的SLAM来解决室内导航的问题时发现，基于计算机视觉的SLAM本身就有很大缺陷，它对周围光线的强弱、物体位置的可变性等都有要求。还有语音的远场拾音，在比较嘈杂的环境下如何能够正确的拾音目前也是一个没有完全解决好的问题，这跟麦克风阵列的设计以及语音识别算法的调优都有关系。另外，如果声纹识别技术比较成熟的话那么机器人可以维持不同的上下文信息并同时跟多个人进行对话。类似的问题还有很多都值得我们去思考和解决。</p><p>实际上，机器人行业内、学术圈都知道目前技术水平距离媒体宣传的真正智能机器人还有巨大差距。机器人厂商活的也很辛苦，首先目前机器人的零配件的价格比较高，例如之前提到的激光雷达，一个4线的激光雷达动辄上万块，一个机器人的底盘也价格不菲再加上机器人外形磨具开模以及其他零配件等累计起来成本比较高，一个大型服务机器人不卖几万块连本钱都保不住，小型的家庭陪护型的机器人也要几千元。另外机器人的研发风险也比较高，除了工业机械臂外其他应用领域的机器人大家也都在探索，总的来说就是机器人研发周期长、成本高，所以目前迫切的需要一个可以降低研发成本的平台或解决方案，这需要整合硬件、软件、数据、服务等各个方面的资源，确实不容易。希望会有更多的公司来关注并加入机器人行业促进机器人行业的发展。</p><p>突然想到对于迎宾、服务查询办理等场景的服务机器人用全息投影+云端智能的方式或许能得到更好的效果，还记得生化危机里的Red Queue吗 :)</p>]]></content>
      
      
      <categories>
          
          <category> 此刻我在想什么 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>过剩的消费</title>
      <link href="/2018/01/28/consumption-upgrading/"/>
      <url>/2018/01/28/consumption-upgrading/</url>
      
        <content type="html"><![CDATA[<p>周末在收拾东西的时候发现桌子上、抽屉里不知不觉里多了好多东西，大部分是买回来就没怎么用过。最近几年流行一个词：消费升级，为什么要消费升级呢，那要从中国改革开放以来的快速发展说起。</p><p>近些年中国在近乎所有领域的制造方面碾压其他国家，好多从国外买回来的商品上也印有“made in china”的字样。这也导致了发达国家的实体制造业的萎缩，如果再遇上金融危机发达国家就会经济下滑，进而对中国订单的需求也跟着下滑导致中国产能过剩。为了使得经济能够继续前进只好选择拉动内需，让老百姓挺高消费水准和消费能力。</p><p>这几年市场也慢慢的形成了一种消费过剩的现象，简单来说就是买了好多从来没用过或者只用了一两次的东西。渐渐的出现一种现象：厂商们生产和制造廉价但质量不高的产品，然后用高大上的宣传手段把产品包装卖出去。比如走在大街上看到一些小饰品或者淘宝上发现一款看上去不错的电子设备，觉得挺有意思花几十或者一二百块钱买回去，买完之后用了几次就扔到一边再也没碰过。消费者对产品的体验基本上在挑选和购买阶段完成，哦，还有快递到了之后发个朋友圈。商家也意识到提升消费者的购物体验要比提升产品的使用体验更能吸引顾客，于是便想尽办法能够让用户愉悦的购物至于产品本身反而变得不那么重要。不知道这种现象还会持续多久。</p>]]></content>
      
      
      <categories>
          
          <category> 此刻我在想什么 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>写在2018年的第一天</title>
      <link href="/2018/01/01/2018-1-1/"/>
      <url>/2018/01/01/2018-1-1/</url>
      
        <content type="html"><![CDATA[<p>每年的第一天是极为普通的一天，但同时人们又为它赋予特别重要的意义。第一天意味着过去的结束也代表着新的开始，人们习惯性的在这个时候总结在刚过去的这一年有哪些收获，计划新的一年里要完成哪些目标。so, let’s do it.</p><p>大致数了一下，在刚过去的一年里读完了5本书，没有读完的有6本，要读还没有开始读的书有好几本，博客更新了18篇，这一年读书写字的量算是比较少的。去了长沙、成都、深圳、西宁、青海等几个地方，没有认识新朋友但多了几个新同事。</p><p>工作上最大的变动是约等于换了新的工作，有了更广阔的视野。切切实实的用Go语言写了一个不大不小的项目，从某种角度来说也算是得到了心理上的满足，毕竟关注Go有挺长时间了。在AI大火的这一年，学习和做了一些NLP相关的落地项目，也算是跟上时代的脚步不要被落下。</p><p>过去的一年能反映出互联网行业发展的关键词有很多：共享经济、feed流新闻、短视频、内容付费、无人驾驶等等。其中内容付费是一件有趣的事情，开启了中国网民获取内容的新篇章，在以前大家对于从网上获取盗版、免费的内容习以为常，现如今对自己感兴趣的内容进行付费阅读的方式渐渐被网民接收，同时也诞生了许多的内容平台，如得到、喜马拉雅、极客时间、混沌大学等，听说今日头条也将上线一款类似的APP。付费从某种角度来看促进了内容创作与消费这个生态的可持续发展，也在改变着人们的生活。我自己也关注订阅了几个专栏，从内容质量方面来说基本上还是有保障的。那为什么大家愿意为这些内容付费呢，一定程度上可能跟焦虑有关。</p><p>这一年经常被提到的一个词是：焦虑。无论是年轻人还是中年大叔都无时无刻不感到焦虑，我自己也一样。生活成本的增长速度远远高于收入水平的增长速度，社会的快速发展总让人觉得好像一不小心就会被淘汰。所以每个人都十分的忙碌，抓紧一切碎片化的时间来看新闻、阅读技术文章，但是碎片化阅读并不利于通过逻辑化的思考将所接收到的信息系统的组织起来并在将来需要的时候拿出来使用，每天都会见到新的新闻、新的技术，但是它跟昨天看到的新闻有什么联系我们并没有想的很清楚，碎片化阅读所带来的更多的是让人在不断碎片化的信息中被淹没而感到杂乱无章、无从下手，反而加剧了焦虑不安。所以如果真的对一个东西感兴趣那么系统化的学习是必不可少的，这个时候就体现出读书的重要性，好的书籍或内容专栏内容上都是脉络清晰、有始有终。通过一些书籍或付费内容可以比较系统全面的了解一种技术、一个领域。有人说焦虑&#x3D;未来的不确定性×事情的重要性×自己无能为力的程度，通过阅读可以增加对事物发展趋势的了解，降低不确定性的同时增加自己对其的掌控能力。未知的东西最容易让人产生恐惧害怕进而变的焦虑，当你看清一个事物的全貌和发展趋势时就不会感到焦躁心里也会踏实许多。所以，18年还是要多读书。</p><p>这一年还有一个比较大的变化是，从来不玩游戏的我玩上了王者荣耀，甚至有段时间痴迷于这个游戏，在这上面花费了不少时间。我发现这款游戏跟赌博有些类似：赢了还想赢，输了还想赢回来，如此下去直到手机没电为止，有时候充电器就在旁边插上电接着玩，简直难以自拔，新的一年不能再沉迷于此，否则太耽误事了。</p><p>这一年跑步没有坚持下来，天气暖和的时候还在小区里跑几步，最多的时候也只能跑5公里，天冷了之后直接就放弃了。相比于跑步熬夜反而变多了，一旦熬夜早上便会起得晚，起得晚就来不及吃早餐，如此恶性循环身体就会一天不如一天，这个得改。</p><p>17年初的时候定的目标是聚焦，一个人很难事事都做的好，聚焦在某个点上反而比较容易做成事情，当然前提是你见过许多东西知道自己想要什么。18年的目标是自律，少熬夜，少放纵自己，按计划行事。</p><p>17年最后一个晚上一个人去看了《再见前任3》，电影还不错，看完出来的时候刚好跨过了2017。岁月不饶人，愿我也未曾绕过岁月。再见2017！</p><p>最后奉上一首自己训练的神经网络作的诗，愿新的一年有新的收获，虽然我也不知道它到底想说啥：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">暖风迎新春，红花不可闻。</span><br><span class="line">不知天上去，不见一人行。</span><br><span class="line">何处见山上，何人无此身。</span><br><span class="line">不知天地外，不得一相看。</span><br><span class="line">一里一行客，何人无旧人。</span><br><span class="line">何人有山寺，一事不无心。</span><br><span class="line">白日多多处，孤舟不是君。</span><br><span class="line">不知人事少，不得不相逢。</span><br><span class="line"></span><br></pre></td></tr></table></figure><iframe frameborder="no" border="0" marginwidth="0" marginheight="0" width=330 height=86 src="//music.163.com/outchain/player?type=2&id=525572100&auto=1&height=66"></iframe>]]></content>
      
      
      <categories>
          
          <category> 此刻我在想什么 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>关于chatbot</title>
      <link href="/2017/12/16/chatbot/"/>
      <url>/2017/12/16/chatbot/</url>
      
        <content type="html"><![CDATA[<h3 id="什么是Chatbot"><a href="#什么是Chatbot" class="headerlink" title="什么是Chatbot"></a>什么是Chatbot</h3><p>Chatbot，宽泛的来讲叫做聊天机器人也有人叫他对话机器人，从16年开始Chatbot发展的比较火热成为大家关注的焦点，国外的有微软的小冰、Amazon的Alex，国内有百度的度秘等。如果说上述这些你还比较陌生的话，那么提到iPhone、iPad以及Mac等系统自带的Siri你应该就比较熟悉了。Chatbot的最大的优势在于他的对话式的交互方式，通过语言对话来交流已经发展成为人类的一种本能，通过像人与人之间交流的方式来与软件、机器等进行交互可以大大降低用户学习使用成本，这也是Chatbot这些年比较火热的一个原因。Chatbot常见的形态有：个人助手、家人陪伴、客服问答等。</p><h3 id="如何实现一个Chatbot"><a href="#如何实现一个Chatbot" class="headerlink" title="如何实现一个Chatbot"></a>如何实现一个Chatbot</h3><p>目前对于Chatbot的实现方案有两种：一种是基于IR，也就是基于检索的方式；另一种是基于机器学习生成式的模式。</p><h4 id="基于检索的实现方式"><a href="#基于检索的实现方式" class="headerlink" title="基于检索的实现方式"></a>基于检索的实现方式</h4><p>用检索的方式来实现Chatbot的前提是拥有较多的问答对来构建知识库，针对用户的输入在知识库中找到能够回答用户问题的答案。其基本流程是：<br><code>输入-&gt;明确用户意图-&gt;识别实体-&gt;retrieval-&gt;matching-&gt;ranking-&gt;response</code></p><p>其中matching和ranking阶段需要好的模型来保证最后输出的准确性。这种实现方式得到的Chatbot更偏向于问答场景的对话，当然了，从某种角度来看人与人之间的交流也可以看做是一问一答式的对话。问答类的对话对于有明确答案的事实类问题是比较容易做到的，难的是如何回答好“为什么”、“怎么做”这一类的问题。</p><p>值得一提的是，吴军博士在《智能时代》里提到在12年他回到Google后Google希望他能做一些跟机器智能相关的事情，经过一段时间的调查他们决定用Google强大的搜索引擎来尝试着回答一些比较复杂的问题。当时的做法是：首先根据网页来确定用户的哪些问题Google可以回答哪些问题Google回答不了，缩小要解决问题的范围，使问题得到解决变得可能。然后通过机器学习的方法来把问题和网页来做匹配，类似于上述的learn to matching的过程。最终通过NLP的技术来合成最后的答案。经过吴军博士及其团队的努力使得计算机能够回答30%的复杂问题，并通过评测计算机的回答跟人的回答已经相当接近甚至很难区分哪个是机器回答的哪个是人回答的，算是一定程度上通过了图灵测试。</p><h4 id="基于生成式的实现方式"><a href="#基于生成式的实现方式" class="headerlink" title="基于生成式的实现方式"></a>基于生成式的实现方式</h4><p>基于生成式的方式主要得益于近些年深度学习在NLP方面长足的发展，首先需要收集大量的对话、问答语料，然后将语料中的文字转成向量，在这些向量矩阵上做卷积以及GRU操作得到最终的答案，是一种sequence2sequence的方案。这种通过分析和学习语言元素的组成进而组合生成回答的方式相比基于IR的方式的好处是不需要构建和维护大规模的知识库，可以理解为通过不断的学习和训练的方法将知识库消化掉了，它比较灵活也更像是人的思考模式，同时它的缺点是结果不具有较强的可读性，有的时候会产生一些常人不能理解的句子，类似于出现之前报道的Facebook的两个机器人用人类看不懂的方式来对话的这种情况。</p><h3 id="Alexa和度秘是怎么做的"><a href="#Alexa和度秘是怎么做的" class="headerlink" title="Alexa和度秘是怎么做的"></a>Alexa和度秘是怎么做的</h3><p>严格意义上来说Alexa和度秘并非单纯只采用上述两种方案中的某一种。相对于宽泛的聊天而言，Alexa和度秘更偏向于task oriented的对话系统，聊天只是其中的一个功能，更多的是在一问一答的过程中完成用户的指令、向用户提供服务或者与用户闲聊。</p><p>为了更好的服务好用户，度秘将提供不同垂类服务的模块分别作为单独的bot，Alexa称之为skill。当用户与其交互时通过分析用户的意图调用不同的bot来满足用户的需求，这当中除了涉及到意图识别还涉及到多轮对话的管理和bot之间的调度。从大的层面上来看大家所采用的技术都差不多，细节上的差别则是针对各自产品面向的场景不同所采用的训练语料也有所不同。当然，如果用户问到了bot无法回答的问题度秘也会通过检索的方式来尝试回答(暂不确定同样的情况下Alexa是否也采用检索的方式来尝试回答)。所以从工业的角度出发一定是结合学术界的多种方法来完成现阶段的产品。过于宽泛的聊天并不好控制，最糟糕的就是出现Chatbot胡乱回答，驴唇不对马嘴甚至是南辕北辙的情况，缩小业务范围聚焦在某一个领域反而问题比较容易得到解决。</p><h3 id="未来"><a href="#未来" class="headerlink" title="未来"></a>未来</h3><p>目前深度学习和NLP并没有那么完美，Chatbot看起来也还是有点傻傻的甚至有些“人工智障”，但是对话的交互方式必将带来变革，通过对话式的交互方式我们再也不需要去教用户如何来使用我们的产品。有些人也可能觉得这是人工智能的泡沫，但不是还有那么一句话么：No beer no bubbles.</p>]]></content>
      
      
      <categories>
          
          <category> 此刻我在想什么 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> AI </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>python vitrualenv setuptools pip wheel failed with error code 1的问题</title>
      <link href="/2017/12/03/vitualenv-error/"/>
      <url>/2017/12/03/vitualenv-error/</url>
      
        <content type="html"><![CDATA[<p>在用vituralenv新建Python虚拟环境时遇到如下报错信息：<br><code>setuptools pip wheel failed with error code 1</code><br>看了一下virtualenv的版本是15.1.0。网上查了一下，发现遇到该问题的大部分版本都是V1.11，卸载重新用pip安装还是报同样的错，最后用easy_install安装V1.10.1的virtualenv：<code>easy_install &quot;virtualenv&lt;1.11&quot;</code>问题得以解决。</p>]]></content>
      
      
      <categories>
          
          <category> 胖虎的日常 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>闲聊NLP</title>
      <link href="/2017/11/26/something_about-nlp/"/>
      <url>/2017/11/26/something_about-nlp/</url>
      
        <content type="html"><![CDATA[<p>无论在工程界还是学术界自然语言处理(NLP)一直是一个比较热的话题，尤其随着近些年深度学习的发展给NLP也带来一些新思路。对于这个话题随便聊聊，想到哪写到哪。</p><h3 id="其实自然语言理解很难"><a href="#其实自然语言理解很难" class="headerlink" title="其实自然语言理解很难"></a>其实自然语言理解很难</h3><p>为什么说让机器真正的理解自然语言很难呢？</p><p>人类语言从出现至今经过上万年的演变，规则极其错综复杂，就连人类自己在学习一门外语的时候还经常发语法错误更不用说机器了。例如，一个句子由多个词语组成，通过各种组合能够得到更为复杂的句子并且如何理解一句话需要根据语境来判断，相同的词在不同的上下文中往往会产生不同的语义。同时，随着社会的发展不停的会有新词出现，退回5年前谁也不知道老铁还会双击666，哈哈。</p><h3 id="NLP做哪些事情"><a href="#NLP做哪些事情" class="headerlink" title="NLP做哪些事情"></a>NLP做哪些事情</h3><p>NLP的范围很广，包括但不限于：文本匹配、分类、实体预测等。具体应用有：分词、文本分类、语义分析、机器翻译、多轮对话，问答系统等等。</p><h3 id="特征工程"><a href="#特征工程" class="headerlink" title="特征工程"></a>特征工程</h3><p>特征工程在ML中十分耗时但又十分重要，同时又不具备通用性，同样是分类问题，在文本分类上好使的特征在商品分类上就完全不靠谱。而在NLP领域的特征工程主要包括：文本预处理、文本特征提取、文本表示。</p><h4 id="文本预处理"><a href="#文本预处理" class="headerlink" title="文本预处理"></a>文本预处理</h4><p>中文文本的预处理主要包括切词和去掉停用词。由于中文不像英文字母之间有空格分开，所以对于中文的切词需要用复杂的算法来解决，例如：最大匹配、基于统计信息的CRF及深度学习的一些方法。去停用词则是去掉文本中高频的连词介词等，这些词对于文本分析没有太多的价值，通常做法是维护一个停用词列表。</p><h4 id="文本表示"><a href="#文本表示" class="headerlink" title="文本表示"></a>文本表示</h4><p>计算机不能直接处理文字，但计算机可以处理数字。文本表示主要是将文本转换成计算机可以处理的方式。例如常用的词袋或者向量空间模型等，但最大的缺点是缺失上下文信息，词与词之间相互独立无法表征语义同时向量的维度和稀疏性都比较高。</p><h4 id="特征提取"><a href="#特征提取" class="headerlink" title="特征提取"></a>特征提取</h4><p>文本的表示依赖于特征的提取，主要包括特征选择和权重计算。特征选择一般是根据某个评价指标对文本进行评分排序选择评分较高的特征项，例如：词频、文档频率、互信息等。最经典的就是TF-IDF。</p><h3 id="深度学习在NLP中的应用"><a href="#深度学习在NLP中的应用" class="headerlink" title="深度学习在NLP中的应用"></a>深度学习在NLP中的应用</h3><p>深度学习在图像分类、语音识别的应用中取得了很大的成功。相比于传统的文本，表示图像和语音的数据是比较稠密的，而对于高度稀疏的one-hot文本向量深度学习不善于处理此类数据。所以要用深度学习解决NLP问题首先要解决的是文本表示问题。Distributed Representation，其基本思想是将每个词表达成 n 维稠密、连续的实数向量，他具有很强的特征表示能力。具有代表性的是word2vec，他有两种模式：CBOW和Skip-Gram。word2vec极大的推动了文本分析的发展，其提出的Hierarchical Softmax和Negative Sample两个方法很好的解决了耗时漫长的大规模计算问题。但word2vec通过语料中的上下文信息学习得到的词向量还不能表示真正的语义，例如通过word2vec得到的相似的词更多的是具有相似上下文的词，“好”与“坏”的相似度经常也很高。</p><p>解决了文本表示的问题，则可以通过CNN&#x2F;RNN来进一步解决自动特征提取进而完成文本分类等问题。</p><h3 id="关于文本分类及实体识别"><a href="#关于文本分类及实体识别" class="headerlink" title="关于文本分类及实体识别"></a>关于文本分类及实体识别</h3><p>文本分类和实体识别是NLP里一个老生常谈的话题，最早开始人们通过规则匹配的方式来做，到后来随着数据量的增长和机器性能的提升，运用统计学知识的机器学习方法开始渐渐成为处理此类问题的一个主要手段。但依旧避不开特征工程这个步骤。</p><p>例如在句子分类中，首先要把句子表示成TF-vector或者是WordCountVector进而利用LR或者SVM等分类器进行分类。而在NER中则更为复杂：当前词的前序词、后续词、是否大写、是否是Title、词的前缀和后缀，词性等等，这些都是大家在不断的摸索中发现的比较好的特征。</p><p>利用深度学习网络自动提取特征的技能可以帮助人们更好的完成之前的文本处理任务。例如：利用word2vec将词转成词向量，然后利用CNN进行卷积计算，在卷积的过程中不同size的卷积核可以自动获取n取值不同的n-gram信息，通过不同的卷积层捕捉局部相关性可以更好的来进行分类。详见：<a href="https://arxiv.org/abs/1408.5882">Yoon Kim发表的论文</a></p><h3 id="问答系统和多轮对话系统"><a href="#问答系统和多轮对话系统" class="headerlink" title="问答系统和多轮对话系统"></a>问答系统和多轮对话系统</h3><p>问答系统早期的做法是通过模板、规则匹配的方法，这种方法比较适合命令式的场景，例如家电控制等。</p><p>目前还有一种常用做法是基于检索的方法：将Q&amp;A索引起来，新来的question在索引上做一次检索，在候选集上用匹配模型去匹配，最后做一个排序，一般把第一个答案返回给用户。</p><p>还有一种方法是通过神经网络来对问题和答案做卷积，判断两句话里哪些局部的词语是互相对应的，最终判断问题和答案是否相关。</p><p>多轮对话可以看做是广义上的一个问答系统，目前开放式的多轮对话还是基于检索的方式来做，但还做的不够好。尤其对于一个闲聊的、开放式的对话目前还没有一个很好的模型来描述和刻画。</p><h3 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h3><p>目前看来在广义的NLP中，从语音到文字(语音识别)做的比较成功，从文字到语义及从语义到回答还有很长的路要走。人工智能的发展未来可期。</p>]]></content>
      
      
      <categories>
          
          <category> NLP </category>
          
      </categories>
      
      
        <tags>
            
            <tag> AI </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Go与TLS的那些事</title>
      <link href="/2017/10/21/TLS-Go/"/>
      <url>/2017/10/21/TLS-Go/</url>
      
        <content type="html"><![CDATA[<p>安全一直是一件很重要的事情，现在大部分正经公司的网站都已经跑在HTTPS上。所以这里分享一下如何用Go创建自签名证书并用自签名证书来实现一个支持HTTPS的服务。</p><h3 id="公私钥加解密"><a href="#公私钥加解密" class="headerlink" title="公私钥加解密"></a>公私钥加解密</h3><p>公私钥加密除了能保证内容的安全性以外还用来证明你自己是你自己，因为只有用你的私钥才能解密由你公钥加密的内容，公钥是对所有人公开，而私钥只有你自己知道。</p><p>在Go中有一个<code>crypto/rsa</code>包，提供了非对称加密的实现，首先我们生成一对公私钥：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// <span class="doctag">NOTE:</span> Use crypto/rand not math/rand</span></span><br><span class="line">privKey, err := rsa.GenerateKey(rand.Reader, <span class="number">2048</span>)</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">    log.Fatalf(<span class="string">&quot;generating random key: %v&quot;</span>, err)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>用公钥加密的内容只能用对应的私钥解开，主要原理是基于大质数的分解，有兴趣的可以查看相关资料，这里先按下不表。</p><p>用公钥加密：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">plainText := []<span class="type">byte</span>(<span class="string">&quot;Hi I&#x27;m Xiaoming&quot;</span>)</span><br><span class="line"><span class="comment">// use the public key to encrypt the message</span></span><br><span class="line">cipherText, err := rsa.EncryptPKCS1v15(rand.Reader, &amp;privKey.PublicKey, plainText)</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">    log.Fatalf(<span class="string">&quot;could not encrypt data: %v&quot;</span>, err)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这样我们就得到了密文<code>cipherText</code>，如果你打印密文的话将看到一串类似于乱码的内容。</p><p>用秘钥解密：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">decryptedText, err := rsa.DecryptPKCS1v15(<span class="literal">nil</span>, privKey, cipherText)</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">log.Fatalf(<span class="string">&quot;error decrypting cipher text: %v&quot;</span>, err)</span><br><span class="line">&#125;</span><br><span class="line">fmt.Printf(<span class="string">&quot;%s\n&quot;</span>, decryptedText)</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>那么如何证明你自己是你自己呢？如果我想要跟你通信首先要证明你的身份，我用你的公钥来加密一个内容发送给你，然后你用私钥解开后将内容返回给我，如果你能将我发送的内容正确无误的返回给我那就证明即将要跟我通信的人确实是你。</p><p>但是，如果我获取了公钥的时候被中间人攻击，我拿到的公钥不是你的而是中间人的怎么办呢？这样我会误认为中间人是你。这个时候就需要CA证书了。</p><h3 id="数字签名"><a href="#数字签名" class="headerlink" title="数字签名"></a>数字签名</h3><p>数字签名的作用是用来校验内容的正确性，保证内容没有被篡改过。</p><p>数字签名生成的过程是对要发送的内容来做hash得到一个指纹，然后用私钥对指纹进行计算得到签名。</p><p>验证签名的过程是，对接到内容做相同的hash得到一个指纹，用私钥对应的公钥来得到签名里的指纹信息，比对两个指纹，如果两个指纹能对上则证明内容没有被篡改过。</p><p>有人会问为什么不直接对发送的内容算签名而是要先得到一个hash值，首先hash计算得到的值比较短，对这个值进行签名计算比较快。另外RSA在计算签名时对签名内容的长度也有限制，如果直接将一本《红楼梦》的内容来计算签名那是很难想象的。</p><p>同样我们可以用<code>crypto/rsa</code>包来计算签名：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">plainText:=<span class="string">&quot;Hi, I&#x27;m Xiaoming&quot;</span></span><br><span class="line">h := sha1.New()</span><br><span class="line">h.Write([]<span class="type">byte</span>(plainText))</span><br><span class="line">digest := h.Sum(<span class="literal">nil</span>)</span><br><span class="line">fmt.Printf(<span class="string">&quot;The hash of my message is: %s\n&quot;</span>, <span class="type">string</span>(digest))</span><br><span class="line"></span><br><span class="line"><span class="comment">// generate a signature using the private key</span></span><br><span class="line">signature, err := rsa.SignPKCS1v15(rand.Reader, privKey, crypto.SHA1, digest)</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">    log.Fatalf(<span class="string">&quot;error creating signature: %v&quot;</span>, err)</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>验证签名：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">Verify</span><span class="params">(pub *rsa.PublicKey, data, signature []<span class="type">byte</span>)</span></span> <span class="type">error</span> &#123;</span><br><span class="line">h := sha1.New()</span><br><span class="line">h.Write(data)</span><br><span class="line">digest := h.Sum(<span class="literal">nil</span>)</span><br><span class="line"><span class="keyword">return</span> rsa.VerifyPKCS1v15(pub, crypto.SHA1, digest, signature)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>那么对于之前说的中间人替换了证书的情况，就可以用数字签名来解决。首先服务提供方需要去wellknown CA那里申请一张证书，这个证书的内容是服务提供方的相关信息和公钥，然后用CA的私钥对这些内容做一个签名附在证书里。在建立HTTPS连接的时候服务端需要向客户端提供他的证书，客户端首先验证证书的合法性：由wellknow CA签发，并且通过数字签名校验证书内容的正确性，没有问题后就可以认为证书的公钥确实是服务端的公钥而不是被中间人篡改的其他公钥。</p><h3 id="自签名证书-HTTPS服务"><a href="#自签名证书-HTTPS服务" class="headerlink" title="自签名证书&amp;HTTPS服务"></a>自签名证书&amp;HTTPS服务</h3><p>在开发的过程中我们可能需要先用一个自签名的证书来满足开发需求，或者我们要自建CA来为其他服务签发证书(k8s中可以用自签证书来做认证)这个时候就可以用<code>crypto/x509</code>来生成一张自签名的证书。</p><p>首先要创建生成证书的请求：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">CertTemplate</span><span class="params">()</span></span> (*x509.Certificate, <span class="type">error</span>) &#123;</span><br><span class="line">    serialNumberLimit := <span class="built_in">new</span>(big.Int).Lsh(big.NewInt(<span class="number">1</span>), <span class="number">128</span>)</span><br><span class="line">    serialNumber, err := rand.Int(rand.Reader, serialNumberLimit)</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nil</span>, errors.New(<span class="string">&quot;failed to generate serial number: &quot;</span> + err.Error())</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    tmpl := x509.Certificate&#123;</span><br><span class="line">        SerialNumber:          serialNumber,</span><br><span class="line">        Subject:               pkix.Name&#123;Organization: []<span class="type">string</span>&#123;<span class="string">&quot;Siglecool, Inc.&quot;</span>&#125;&#125;,</span><br><span class="line">        SignatureAlgorithm:    x509.SHA256WithRSA,</span><br><span class="line">        NotBefore:             time.Now(),</span><br><span class="line">        NotAfter:              time.Now().Add(time.Hour),         BasicConstraintsValid: <span class="literal">true</span>,</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> &amp;tmpl, <span class="literal">nil</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>接下来需要创建一对公私钥，并完善生成证书请求的内容：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">rootKey, err := rsa.GenerateKey(rand.Reader, <span class="number">2048</span>)</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">log.Fatalf(<span class="string">&quot;generating random key: %v&quot;</span>, err)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">rootCertTmpl, err := CertTemplate()</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">log.Fatalf(<span class="string">&quot;creating cert template: %v&quot;</span>, err)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// describe what the certificate will be used for</span></span><br><span class="line">rootCertTmpl.IsCA = <span class="literal">true</span></span><br><span class="line">rootCertTmpl.KeyUsage = x509.KeyUsageCertSign | x509.KeyUsageDigitalSignature</span><br><span class="line">rootCertTmpl.ExtKeyUsage = []x509.ExtKeyUsage&#123;x509.ExtKeyUsageServerAuth, x509.ExtKeyUsageClientAuth&#125;</span><br><span class="line">rootCertTmpl.IPAddresses = []net.IP&#123;net.ParseIP(<span class="string">&quot;127.0.0.1&quot;</span>)&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>接下来创建根证书：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">CreateCert</span><span class="params">(template, parent *x509.Certificate, pub <span class="keyword">interface</span>&#123;&#125;, parentPriv <span class="keyword">interface</span>&#123;&#125;)</span></span> (</span><br><span class="line">    cert *x509.Certificate, certPEM []<span class="type">byte</span>, err <span class="type">error</span>) &#123;</span><br><span class="line"></span><br><span class="line">    certDER, err := x509.CreateCertificate(rand.Reader, template, parent, pub, parentPriv)</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    cert, err = x509.ParseCertificate(certDER)</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line">    b := pem.Block&#123;Type: <span class="string">&quot;CERTIFICATE&quot;</span>, Bytes: certDER&#125;</span><br><span class="line">    certPEM = pem.EncodeToMemory(&amp;b)</span><br><span class="line">    <span class="keyword">return</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">rootCert, rootCertPEM, err := CreateCert(rootCertTmpl, rootCertTmpl, &amp;rootKey.PublicKey, rootKey)</span><br></pre></td></tr></table></figure><p>然后可以用根证书来签发httpserver的证书：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">servKey, err := rsa.GenerateKey(rand.Reader, <span class="number">2048</span>)</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">log.Fatalf(<span class="string">&quot;generating random key: %v&quot;</span>, err)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// create a template for the server</span></span><br><span class="line">servCertTmpl, err := CertTemplate()</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">log.Fatalf(<span class="string">&quot;creating cert template: %v&quot;</span>, err)</span><br><span class="line">&#125;</span><br><span class="line">servCertTmpl.KeyUsage = x509.KeyUsageDigitalSignature</span><br><span class="line">servCertTmpl.ExtKeyUsage = []x509.ExtKeyUsage&#123;x509.ExtKeyUsageServerAuth&#125;</span><br><span class="line">servCertTmpl.IPAddresses = []net.IP&#123;net.ParseIP(<span class="string">&quot;127.0.0.1&quot;</span>)&#125;</span><br><span class="line"></span><br><span class="line">_, servCertPEM, err := CreateCert(servCertTmpl, rootCert, &amp;servKey.PublicKey, rootKey)</span><br></pre></td></tr></table></figure><p>用httpserver证书来创建HTTPS服务：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">servKeyPEM := pem.EncodeToMemory(&amp;pem.Block&#123;</span><br><span class="line">Type: <span class="string">&quot;RSA PRIVATE KEY&quot;</span>, Bytes: x509.MarshalPKCS1PrivateKey(servKey),</span><br><span class="line">&#125;)</span><br><span class="line">servTLSCert, err := tls.X509KeyPair(servCertPEM, servKeyPEM)</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">log.Fatalf(<span class="string">&quot;invalid key pair: %v&quot;</span>, err)</span><br><span class="line">&#125;</span><br><span class="line">handler := <span class="function"><span class="keyword">func</span><span class="params">(w http.ResponseWriter, r *http.Request)</span></span> &#123; w.Write([]<span class="type">byte</span>(<span class="string">&quot;Hello World!&quot;</span>)) &#125;</span><br><span class="line">s := httptest.NewUnstartedServer(http.HandlerFunc(handler))</span><br><span class="line">s.TLS = &amp;tls.Config&#123;</span><br><span class="line">Certificates: []tls.Certificate&#123;servTLSCert&#125;,</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>这个时候请求这个server会报错：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">s.StartTLS()</span><br><span class="line">_, err = http.Get(s.URL)</span><br><span class="line">s.Close()</span><br><span class="line">fmt.Println(err)</span><br><span class="line"><span class="comment">// x509: certificate signed by unknown authority</span></span><br></pre></td></tr></table></figure><p>由于我们的证书不是知名CA签发的所以在client请求serve时在校验证书环节会报错，同时服务端也会提示：<code>http: TLS handshake error from 127.0.0.1:53844: remote error: bad certificate</code></p><p>为了让client信任server的证书，我们需要在client中用我们的root证书来替代系统的root证书，因为server的证书使用我们自己生成的root证书签发的，这样server证书就可以验证通过：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">certPool := x509.NewCertPool()</span><br><span class="line">certPool.AppendCertsFromPEM(rootCertPEM)</span><br><span class="line">client := &amp;http.Client&#123;</span><br><span class="line">Transport: &amp;http.Transport&#123;</span><br><span class="line">TLSClientConfig: &amp;tls.Config&#123;RootCAs: certPool&#125;,</span><br><span class="line">&#125;,</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">s.StartTLS()</span><br><span class="line">resp, err := client.Get(s.URL)</span><br><span class="line">s.Close()</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">log.Fatalf(<span class="string">&quot;could not make GET request: %v&quot;</span>, err)</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">defer</span> resp.Body.Close()</span><br><span class="line">body, err := ioutil.ReadAll(resp.Body)</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">log.Fatalf(<span class="string">&quot;could not response: %v&quot;</span>, err)</span><br><span class="line">&#125;</span><br><span class="line">fmt.Println(<span class="type">string</span>(body))</span><br></pre></td></tr></table></figure><p>这次我们就可以收到<code>Hello World</code>啦。</p>]]></content>
      
      
      <categories>
          
          <category> Go </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Go </tag>
            
            <tag> TLS </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>go 域名解析过慢</title>
      <link href="/2017/09/16/go-dns/"/>
      <url>/2017/09/16/go-dns/</url>
      
        <content type="html"><![CDATA[<p>这几天发现一个问题，用go的HttpClient向<code>某个URL</code>发送post请求超时，经过一番排查，发现主要是在DNS解析的时候很慢要5S左右。但是同样的环境用Python和Java发送请求可以很快的返回结果，&lt;1S。</p><p>首先看&#x2F;etc&#x2F;resolv.conf文件，默认配置了两个nameserver。strace执行过程发现在请求nameserver时有timeout，并且整个过程只请求了一个nameserver，同样的Python执行过程却没有这种情况。把resolv.conf中的option timeout从2设置为1之后，请求时间变为3S.如果增加一个nameserver：114.114.114.114，go的程序便可很快返回。</p><p>看了一下go关于域名解析的代码，觉得没毛病！ 可是为什么同样的环境下go跟Python的时间差这么多:( 而且目前只发现这个域名会出现这个问题。网上找了好久也没发现有人遇到这种问题。昨天跟leader讨论这个问题的时候，leader又提出用Java重写整个项目。心累，蓝瘦。</p>]]></content>
      
      
      <categories>
          
          <category> Go </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Go </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>go upgrade on fly</title>
      <link href="/2017/09/16/go-upgrade-on-fly/"/>
      <url>/2017/09/16/go-upgrade-on-fly/</url>
      
        <content type="html"><![CDATA[<p>作为一个服务端程序在修改配置时最好能够支持优雅的重启，这样避免给用户带来糟糕的体验。即使不能优雅重启，那么在停止服务做配置修改时，也要做到优雅的停止，而不是粗暴的kill掉进程，这种粗暴的做法会lost正在处理的请求，对用户也是不友好的。</p><h3 id="目标："><a href="#目标：" class="headerlink" title="目标："></a>目标：</h3><ol><li>不掐断当前处理中的连接</li><li>不中断socket的监听</li><li>新版本的程序能够接替老版本的程序</li></ol><h3 id="要做的事情："><a href="#要做的事情：" class="headerlink" title="要做的事情："></a>要做的事情：</h3><ol><li>启动新的进程并监听到对应的address</li><li>老进程停止接收新的连接</li><li>老进程完成ongoing请求</li><li>停止老进程</li></ol><h3 id="怎么做："><a href="#怎么做：" class="headerlink" title="怎么做："></a>怎么做：</h3><ol><li><p>通过Signal与进程交互，在程序中通过signal.Notify注册要接收的信号和接收信号的channel，向进程发送HUP signal通知其进行reload</p></li><li><p>go本身不支持端口复用，但是netListener支持从一个FD中开启监听。</p></li><li><p>通过exec.Command来执行新的binary，子进程继承父进程的句柄，但是句柄里存在CloseOnExec问题，也就是在执行exec的时候，所有的句柄都关闭了，好在netListener.File()返回一个dup(2)的FD，该FD没有设置FD_CLOEXEC，通过Cmd的ExtraFile来传递FD。</p></li><li><p>新启动的程序如何知道是从继承的FD监听还是新开一个socket？老进程在执行exec时设置一个环境变量，新进程通过该环境变量来进行判断</p></li><li><p>停止老进程对端口的监听，调用老进程的netListener.Close()。该方法会使得所有被block住的Accept()方法不在被block住并返回一个error。</p></li><li><p>等待老进程中ongoing的请求处理结束。这里使用sync.WaitGroup计数的方法，在Listener Accept()的时候Add，conn close时Done，在结束老进程时进行Wait()等待处理完所有的request。</p></li></ol><p>fork-exec的方式存在一个问题是，新exec出来的是一个新的进程，如果用Systemd进行进程管理的话，Systemd会认为其已经dead了。为了避免这种情况需要维护一份pid文件，告诉Systemd PIDFile路径，Systemd通过该文件来监控。</p><p>除了fork-exec的方式外可以用master-worker的方式，master负责监听signal，worker负责处理请求。如果要进行升级master fork出新的worker转移env, listener, args等。程序还是那个程序，但是负责处理请求的进程则换成了新的worker进程</p><h3 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h3><p><a href="https://grisha.org/blog/2014/06/03/graceful-restart-in-golang/">Graceful Restart in Golang</a></p><p><a href="http://blog.nella.org/zero-downtime-upgrades-of-tcp-servers-in-go/">Zero Downtime upgrades of TCP servers in Go</a></p>]]></content>
      
      
      <categories>
          
          <category> Go </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Go </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>只不过是一场生活</title>
      <link href="/2017/09/03/just-a-kind-life/"/>
      <url>/2017/09/03/just-a-kind-life/</url>
      
        <content type="html"><![CDATA[<p>九月，伴随着凉爽的秋风一同扑面而来的是一堆婚礼请柬。或许真的是到了年龄了，忽然之间身边的亲戚朋友都要结婚了。</p><p>生活真是让人捉摸不透，记忆里已经有些模糊的那些年那些事真要回味起来还是蛮下酒的。谁也没曾想到亲密会戛然而止，然后便相忘于江湖；谁也没有料到会说散就散，随后便听到了婚讯。听到B哥的这首歌的时候，往事忽然涌上心头，然而转过头青春早已散场，剩下我们站在那里泪流满面。</p><p>婚姻到底是一场生活，一场命运还是一场游戏？不得而知。</p><iframe frameborder="no" border="0" marginwidth="0" marginheight="0" width=330 height=86 src="//music.163.com/outchain/player?type=2&id=26508184&auto=1&height=66"></iframe></br><p>鬼知道接下来参加九月的婚礼需要顶着多大的压力。</p><p>走过八月，曾经的那个少年会继续走在九月的街头，他始终相信要找的答案会出现在下一个车站。</p><p>对他而言，或许这不只是一场生活，也或许这一切仅仅只是一场生活。</p><p>PS：比较喜欢这个版本: <a href="http://music.163.com/#/song?id=424477863">结婚 (2016 unplugged)</a>，尤其朱格乐的声音，但是版权问题无法生成外链播放器。</p>]]></content>
      
      
      <categories>
          
          <category> 此刻我在想什么 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>Go使用感受</title>
      <link href="/2017/08/29/golang-using-experience/"/>
      <url>/2017/08/29/golang-using-experience/</url>
      
        <content type="html"><![CDATA[<p>使用Go有一段时间了，说一下感受。</p><h3 id="语法方面"><a href="#语法方面" class="headerlink" title="语法方面"></a>语法方面</h3><ol><li>语法比较简洁，有点像Python，这一点比Java要好。</li><li>多返回值。这一点使得go语言程序不需要像用单返回值语言写的程序那样将多个返回值封装到一个对象里，一定程度上减少了代码量。</li><li>函数是一级公民，可以将函数作为参数来传递。</li><li>语言级别支持并发，减少程序员心智负担。</li><li>比较完善的工具包，比如net&#x2F;http，用简单的几行代码便可以实现一个http server服务。</li><li>……</li></ol><p>除了上述之外还有其他好多不错的地方，比如支持交叉编译，运行速度快，占用资源少等。下面说一下在开发过程中遇到的与其他语言开发习惯不太一样的地方。</p><h3 id="error处理"><a href="#error处理" class="headerlink" title="error处理"></a>error处理</h3><p>作为一门simple language，go对error的处理也十分的简单。我们知道在Java语言里使用try-catch-finally来处理错误和异常，而C语言则以返回错误码的方式来对错误作处理。go在这方面继承了C的风格，但与C不同的是go不是用整型来作为返回值而是用error这个interface类来作为返回值。<br>但是go的error处理方式从一开始就成为被人吐槽的点，有的人认为go的这种处理方式太古老了，使得处理错误的代码冗长且重复，例如：</p><figure class="highlight golang"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">err := func1()</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">    <span class="comment">//handle error...</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">err = func2()</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">    <span class="comment">//handle error...</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">err = func3()</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">    <span class="comment">//handle error...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Go 作者之一，Russ Cox对于这种观点进行过驳斥：当初选择返回值这种错误处理机制而不是try-catch这种机制，主要是考虑前者适用于大型软件，后者更适合小程序。当程序变大，try-catch会让错误处理更加冗长繁琐易出错。不过Russ Cox也承认Go的错误处理机制对于开发人员的确有一定的心智负担。</p><p>所以在开发过程中对于error有以下几个trick：</p><ul><li>checkError</li></ul><figure class="highlight golang"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">checkError</span><span class="params">(err <span class="type">error</span>)</span></span> &#123;</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        fmt.Println(<span class="string">&quot;Error is &quot;</span>, err)</span><br><span class="line">        <span class="built_in">panic</span>(err)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">echo</span><span class="params">()</span></span> &#123;</span><br><span class="line">    err := func1()</span><br><span class="line">    checkError(err)</span><br><span class="line"></span><br><span class="line">    err = func2()</span><br><span class="line">    checkError(err)</span><br><span class="line"></span><br><span class="line">    err = func3()</span><br><span class="line">    checkError(err)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>可以在需要的地方进行recover处理，防止由于某个panic导致了整个程序退出。当然了，在开发阶段也不要忘记“让其崩溃，crash is awesome!!!”的思想。</p><ul><li>Errors are values</li></ul><p>标准库里就用了这种思想，例如bufio的Writer：</p><figure class="highlight golang"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">b := bufio.NewWriter(fd)</span><br><span class="line">    b.Write(p0[a:b])</span><br><span class="line">    b.Write(p1[c:d])</span><br><span class="line">    b.Write(p2[e:f])</span><br><span class="line">    .....</span><br><span class="line">    <span class="keyword">if</span> b.Flush() != <span class="literal">nil</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> b.Flush()</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br></pre></td></tr></table></figure><figure class="highlight golang"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> Writer <span class="keyword">struct</span> &#123;</span><br><span class="line">    err <span class="type">error</span></span><br><span class="line">    buf []<span class="type">byte</span></span><br><span class="line">    n   <span class="type">int</span></span><br><span class="line">    wr  io.Writer</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(b *Writer)</span></span> Write(p []<span class="type">byte</span>) (nn <span class="type">int</span>, err <span class="type">error</span>) &#123;</span><br><span class="line">    <span class="keyword">for</span> <span class="built_in">len</span>(p) &gt; b.Available() &amp;&amp; b.err == <span class="literal">nil</span> &#123;</span><br><span class="line">        ... ...</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> b.err != <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> nn, b.err</span><br><span class="line">    &#125;</span><br><span class="line">    ......</span><br><span class="line">    <span class="keyword">return</span> nn, <span class="literal">nil</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>error作为一个状态值封装到了Writer里面，并且在Write方法入口处对Writer进行状态检查，如果error!&#x3D;nil则直接return。</p><ul><li>exported Error变量</li></ul><p>在开发过程中会遇到根据不同的error来进行不同的处理，所以一种做法就是exporte error:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">var (</span><br><span class="line">    ErrInvalid    = errors.New(&quot;invalid argument&quot;)</span><br><span class="line">    ErrPermission = errors.New(&quot;permission denied&quot;)</span><br><span class="line">    ErrExist      = errors.New(&quot;file already exists&quot;)</span><br><span class="line">    ErrNotExist   = errors.New(&quot;file does not exist&quot;)</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>这样就可以直接使用判断是否相等的方式来做处理：</p><figure class="highlight golang"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> err == os.ErrInvalid &#123;</span><br><span class="line">    <span class="comment">//handle invalid</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>或者使用type switch：</p><figure class="highlight golang"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">switch</span> t:=err.(<span class="keyword">type</span>)  &#123;</span><br><span class="line">    <span class="keyword">case</span> ErrInvalid:</span><br><span class="line">        <span class="comment">//handle invalid</span></span><br><span class="line">    <span class="keyword">case</span> ErrPermission:</span><br><span class="line">        <span class="comment">//handle no permission</span></span><br><span class="line">    ... ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li>自定义error</li></ul><p>通过自定义error可以增加更丰富的context，例如net包里，除了实现error interface的方法外还实现了Timeout，Temporary方法，可以通过type switch和type assertion来进行类型转换：</p><figure class="highlight golang"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">switch</span>  err := err.(<span class="keyword">type</span>) &#123;</span><br><span class="line">   <span class="keyword">case</span> *url.Error:</span><br><span class="line">  err2, ok := err.Err.(net.Error)</span><br><span class="line">  <span class="keyword">if</span> ok &amp;&amp; err2.Timeout() &#123;</span><br><span class="line">  <span class="comment">// handle timeout</span></span><br><span class="line">  &#125;</span><br><span class="line">  ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li>error OR bool?</li></ul><p>当函数失败的原因只有一个，所以返回值的类型应该为bool，而不是error。</p><ul><li>错误还是异常？</li></ul><p>什么是错误？什么是异常？什么时候使用错误？什么时候使用异常？错误和异常如何转换？<br>个人理解，错误是指出现的问题我们意料之中的，例如：文件打开错误，连接失败等。异常是出现了意料之外的错误，例如：空指针，下标越界等。对于错误程序可以进行自动处理，对于异常我们可以在上游进行recover，避免服务终止。</p><p>错误转异常，例如尝试请求某个URL，最多尝试三次，尝试三次的过程中请求失败是错误，尝试三次还不成功则失败就被提升为异常。</p><p>异常转错误，例如panic触发的异常被recover恢复后，对函数返回值中error类型变量进行赋值并返回，以此告诉上游本次调用发生了错误，上游程序走错误处理流程。</p><h3 id="依赖包管理"><a href="#依赖包管理" class="headerlink" title="依赖包管理"></a>依赖包管理</h3><p>go的包管理跟Java的maven不太一样，go使用GOPATH来管理依赖，无论是自己写的代码还是go get下来的代码都在一个GOPATH下面。这样就存在很大的问题：</p><ol><li>如果项目的依赖包发生了修改就可能会影响到自己的代码。</li><li>无法满足GOPATH下的两个工程分别依赖同一个包的不同版本。</li></ol><p>从go1.6版本开始正式引入了vendor机制，使得在编译时先从源码根目录下的vendor中寻找依赖，如果没找到再去GOPATH中寻找。这样可以解决上述的两个问题。但使用vendor还存在以下问题：</p><ol><li>缺少外部依赖包的版本信息，无法进行版本管理和升级。</li><li>无法使用指定版本的依赖包</li><li>无法列出项目所有依赖的外部包</li></ol><p>为了解决上述问题，社区里在vendor机制基础上开发了多个包管理工具，例如：govendor,godep,glide等。go也在开发官方的<a href="https://github.com/golang/dep">dep</a> 使用这些管理工具可以较好的解决上述问题。</p>]]></content>
      
      
      <categories>
          
          <category> Go </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Go </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>go语言HttpClient中的https证书</title>
      <link href="/2017/08/18/golang-https/"/>
      <url>/2017/08/18/golang-https/</url>
      
        <content type="html"><![CDATA[<p>关于HTTPS中SSL&#x2F;TLS具体原理就不具体说了，可以看一下阮一峰老师的<a href="http://www.ruanyifeng.com/blog/2014/09/illustration-ssl.html">文章</a></p><p>这里我们主要说一下HTTPS中的证书，HTTPS开始的握手阶段是基于非对称加密，也就是公钥加密的数据要用私钥才能解开，同理用私钥加密的数据用公钥才能解开。</p><p>然而有一肚子坏水的家伙可以进行中间人劫持然后用自己的公钥来替换要访问的服务器的公钥，这样本来要发给服务器的数据发给了中间人，信息就被他们给窃取走了，所以有了CA这个机构来负责管理和签发证书。CA会将申请者的一些信息和公钥用CA自己的私钥加密并附上CA的一些信息生成数字证书给申请者。client在向服务器发出加密请求的时候，服务器会将数字证书一起返回给client，client在受信任的根证书颁发机构列表里来查看解开数字证书的公钥是否在列表之内，如果这张数字证书不是由受信任的机构颁发的会发出警告，如果数字证书是可靠的，客户端就可以使用证书中的服务器公钥。</p><p>通过CA的数字签名来保证公钥的有效性，而黑客的公钥是很难通过CA的认证。CA证书具有层级结构，它建立了自上而下的信任链，下级CA信任上级CA，下级CA由上级CA颁发证书并认证。数字证书最常见的格式是X.509，这种公钥基础设施又称之为PKIX。在用HttpClient进行数字证书认证出错的时候会提示：<code>PKIX path building failed</code></p><p>我们知道浏览器保存了一个常用的CA证书列表，那么用golang的HttpClient请求HTTPS的服务时，它所受信任的证书列表在哪里呢？</p><p>查看golang的源码发现在目录<code>src/crypto/x509</code>下有针对各个操作系统获取根证书的实现，例如root_linux.go中记录了各个Linux发行版根证书的存放路径：</p><figure class="highlight golang"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Copyright 2015 The Go Authors. All rights reserved.</span></span><br><span class="line"><span class="comment">// Use of this source code is governed by a BSD-style</span></span><br><span class="line"><span class="comment">// license that can be found in the LICENSE file.</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">package</span> x509</span><br><span class="line"></span><br><span class="line"><span class="comment">// Possible certificate files; stop after finding one.</span></span><br><span class="line"><span class="keyword">var</span> certFiles = []<span class="type">string</span>&#123;</span><br><span class="line"><span class="string">&quot;/etc/ssl/certs/ca-certificates.crt&quot;</span>,                <span class="comment">// Debian/Ubuntu/Gentoo etc.</span></span><br><span class="line"><span class="string">&quot;/etc/pki/tls/certs/ca-bundle.crt&quot;</span>,                  <span class="comment">// Fedora/RHEL 6</span></span><br><span class="line"><span class="string">&quot;/etc/ssl/ca-bundle.pem&quot;</span>,                            <span class="comment">// OpenSUSE</span></span><br><span class="line"><span class="string">&quot;/etc/pki/tls/cacert.pem&quot;</span>,                           <span class="comment">// OpenELEC</span></span><br><span class="line"><span class="string">&quot;/etc/pki/ca-trust/extracted/pem/tls-ca-bundle.pem&quot;</span>, <span class="comment">// CentOS/RHEL 7</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>root_unix.go中写了类Unix系统的证书路径：</p><figure class="highlight golang"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Possible directories with certificate files; stop after successfully</span></span><br><span class="line"><span class="comment">// reading at least one file from a directory.</span></span><br><span class="line">  <span class="keyword">var</span> certDirectories = []<span class="type">string</span>&#123;</span><br><span class="line">  <span class="string">&quot;/etc/ssl/certs&quot;</span>,               <span class="comment">// SLES10/SLES11, https://golang.org/issue/12139</span></span><br><span class="line">  <span class="string">&quot;/system/etc/security/cacerts&quot;</span>, <span class="comment">// Android</span></span><br><span class="line">  &#125;</span><br><span class="line">  </span><br><span class="line"></span><br></pre></td></tr></table></figure><p>对应的还有root_darwin.go，root_windows.go等分别实现了MacOS及Windows平台下的根证书的获取。</p><p>另外，Java在JRE的安装目录下也有一份默认的可信任证书列表，这个列表一般是保存在 $JRE&#x2F;lib&#x2F;security&#x2F;cacerts 文件中，随着JDK版本的升级而更新。</p>]]></content>
      
      
      <categories>
          
          <category> Go </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Go </tag>
            
            <tag> https </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>搜索引擎系列(三)---用户行为分析</title>
      <link href="/2017/08/13/user-behavior/"/>
      <url>/2017/08/13/user-behavior/</url>
      
        <content type="html"><![CDATA[<p>搜索引擎主要是帮助用户找到想要的信息或者服务，所以这里面涉及到三件事情，理解用户搜索意图，理解资源，匹配用户意图和资源。在一开始的时候搜索引擎主要通过对文本的统计分析，如TF-IDF,BM25等，来满足二者之间的匹配。后来发现除了文本统计之外还可以通过对页面质量的分析，外链的分析，如PageRank，来提升对资源的理解改善搜索效果。再到后来发现通过分析用户在搜索时的行为可以更好的了解用户意图并以此来提升搜索结果的相关度。为什么呢？用户选择搜索结果的过程是一次人类智慧的体现，通过观察多个用户的这种行为并从中找出规律可以提升搜索结果的相关度，毕竟人还是要比机器更加有智慧，这也是典型的利用集体智慧的例子。</p><h3 id="用户行为有哪些"><a href="#用户行为有哪些" class="headerlink" title="用户行为有哪些"></a>用户行为有哪些</h3><p>用户在使用搜索引擎的过程中会有浏览，点击结果，翻页，变换query，点击相关搜索等行为，这些行为便是我们要记录和分析的行为。</p><h3 id="基础数据"><a href="#基础数据" class="headerlink" title="基础数据"></a>基础数据</h3><p>通过对用户行为的记录我们会得到展现日志，点击日志，搜索日志等数据。例如我们得到点击日志格式如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">时间 | 行为 | query | URL List</span><br><span class="line">time | Search | 薛之谦 | ...</span><br><span class="line">time | 点击第二条结果 | 薛之谦 | ...</span><br><span class="line">time | 点击相关搜索第二条 | 火星情报局 | ...</span><br><span class="line">time | 点击第一条结果 | 爱奇艺在线观看 | ...</span><br><span class="line">...</span><br></pre></td></tr></table></figure><p>将日志按照userID和时间戳排序就可以看到每个用户的一系列搜索行为。</p><h3 id="用户行为理解"><a href="#用户行为理解" class="headerlink" title="用户行为理解"></a>用户行为理解</h3><p>有了用户行为之后我们对用户行为进行一个简单的分析可以得出：<br>1.顺序浏览模式，大多数用户倾向于从前到后顺序浏览。<br>2.点击行为代表对结果投票，用户通过标题、摘要、飘红等信息对结果进行预判，感觉有兴趣可能会满足需求才会点击。<br>3.点击不一定能满足，通过点击进入落地页之后发现内容不满足需求，可能会重新搜索。<br>4.重视最后一次点击，在同一次搜索的多次点击中，最后一次点击对应的结果往往最好，其次是第一次点击的结果。<br>5.搜索行为的关联性，用户的后一次搜索(query变换，点击相关搜索等)往往是对之前搜索需求的细化或者拓展。</p><h3 id="用户行为分析"><a href="#用户行为分析" class="headerlink" title="用户行为分析"></a>用户行为分析</h3><p>考虑如下case：对于一个query，有两个结果A和B，A排在B前面，理论上说A会得到的点击数量比B要多，如果B比A的点击数多，那么说明B比A的相关性要好，需要调换二者的顺序。但是简单的通过点击数来衡量会存在如下的问题：</p><ol><li>点击质量，对于不同结果的点击权重应该不同。</li><li>位置偏见，排在前面的结果本身倾向于受到更多的点击，对后面的结果不公平。<br>所以，对每一个query-url对，根据点击结果在结果列表的位置和点击动作在点击序列中的位置给不同的打分：</li><li>搜索结果排序越靠后，对应的被点击的分值越高。</li><li>在点击序列中，越前面的点击分值越高，但最后一次点击的分值最高</li><li>如果本次搜索没有满足用户的需求，后续出现了点击相关搜素，变换query等情况，则对本次的打分做相应的惩罚。</li></ol><p>思考：这样的打分标准是不是就完美了？</p><p>通过对用户点击行为的分析，根据结果的整体点击率，前三条结果点击率，相关搜索点击等信息来对query整体满意度进行打分。对于在一个session中共现的query pair A和B，如果A的满意度低于B，可以选择B的结果列表中较好的结果插入到A的搜索结果列表中。同时也会引入新的问题，如果“范爷”和“大黑牛”经常同现，但搜索“范爷”的用户并不想看到跟大黑牛相关的新闻，如果将“大黑牛”的结果插入到“范爷”的结果列表中，反而使得搜索的满意度下降。所以在进行结果插入前可以先判断Query A与Query B文本相似度。</p><h3 id="调优结果排序"><a href="#调优结果排序" class="headerlink" title="调优结果排序"></a>调优结果排序</h3><p>有了点击打分之后如何来改善结果的排序呢(点击调权)？<br>例如可以建立一个神经网络模型，简单一点建立一个三层的神经网络，<br>第一层为输入层，每个节点为一个term<br>第二次为隐藏层，每个节点与一组term相连<br>第三层为输出层，每个节点代表一个URL，与隐藏层节点相连<br>每个URL的目标值是计算得到的点击分值，通过反向传播算法来训练更新神经网络中各个边的权重。对于之后的query可以通过该神经网络模型来计算结果的点击排序。</p><h3 id="冷门-VS-热门"><a href="#冷门-VS-热门" class="headerlink" title="冷门 VS 热门"></a>冷门 VS 热门</h3><p>对于热门query和URL可以很容易获得大量的数据支撑，但如果一个query-URL很冷门则获取到的点击数据也很少，则很难对其相关性做出有效的判断，并且如果一个query很冷门即使我们做的效果很好，但是这个query很少出现，投入产出比上也不划算。</p><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>通过对用户行为的分析可以更好的理解用户行为意图并利用用户的智慧来改善结果排序，此外，用户点击可以作为LTR的一个feature，提升LTR的准确度。</p>]]></content>
      
      
      <categories>
          
          <category> search </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 搜索 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>搜索引擎系列(二)---相关搜索</title>
      <link href="/2017/07/29/RelevantSearch/"/>
      <url>/2017/07/29/RelevantSearch/</url>
      
        <content type="html"><![CDATA[<h2 id="什么是相关搜索"><a href="#什么是相关搜索" class="headerlink" title="什么是相关搜索"></a>什么是相关搜索</h2><p>在使用搜索引擎的时候，输入一个关键词之后经常在页面底部或者右侧会出现相关搜索提示。例如搜索”万达”会提示“万达广场”，“万达影院”等。<br>相关搜索的作用是帮助用户更加快速的找到想要的东西，提升用户体验。当你通过一个关键词找不到想要的结果时相关搜索会帮到你，它会推荐你换一个关键词试试，所以它更像是搜索引擎中的一个推荐系统。搜索与推荐是不分家的，在搜索引擎中相关搜索的点击量还是很可观的。</p><h2 id="如何实现相关搜索"><a href="#如何实现相关搜索" class="headerlink" title="如何实现相关搜索"></a>如何实现相关搜索</h2><h3 id="数据思考"><a href="#数据思考" class="headerlink" title="数据思考"></a>数据思考</h3><p>相关搜索要做的事情是根据用户输入的query理解其搜索意图，为用户推荐更多与其意图相近的query。<br>那么如何来找到其他意图相近的query呢？首先我们要收集用户行为数据：搜索日志，展现日志，点击日志等。有了数据之后考虑以下情况：</p><ol><li>在我们使用搜索引擎的时候，输入一个关键词后发现没有我们想要的内容，这个时候我们一般会尝试再换一个关键词来搜索，例如，搜索“spark教程”之后发现不能满足你的需求，又输入“spark视频教程”来搜索，这个动作叫做<code>query变换</code>，后面出现的query就有可能是与前面query的语义相近词。</li><li>不同的用户有不同的搜索习惯，同一件事情，不同的用户也会用不同的词语来描述，如果不同query搜出来的结果相似，那么这些query也是相近的。</li><li>如果同一条结果出现在不同的query的搜索结果集中，那么这些query之间也是有联系的。尤其在不同query的结果集中，同一条结果都被点击了，那么更能表明这两个query是相近的了。</li><li>搜索了A query的多数用户也搜索了B query，那么A跟B存在一定的相似度。</li><li>根据query返回的网页内容相似度来推荐其他query。</li><li>……</li></ol><h3 id="算法实现"><a href="#算法实现" class="headerlink" title="算法实现"></a>算法实现</h3><p>有了以上的思考之后，接下来看一下如何利用用户行为数据实现相关搜索。</p><h4 id="1-根据query变换"><a href="#1-根据query变换" class="headerlink" title="1. 根据query变换"></a>1. 根据query变换</h4><p>根据之前的分析，在一次搜索行为中一个query之后的其他query可能是我们要找的词。那么首先如何定义<code>一次搜索行为</code>呢。通常在一定的时间跨度内，例如5分钟，用户的搜索行为定义为一次搜索行为，即一个session。搜索日志格式为：<br><code>UA querytime userID IP query result：A B C D E...</code><br>从搜索日志中将用户行为按照session进行分割合并，得到如下结果：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">userID：1 query：A afterwords:[B,C,D...]</span><br><span class="line">userID：2 query：C afterwords:[E,G,Q...]</span><br></pre></td></tr></table></figure><p>假如一个用户思维比较天马行空，上一秒搜索词为成龙，下一秒换成了人大代表（成龙做过人大代表），但并不能说明二者有很大的联系，所以要统计多个用户的搜索日志，并加入置信度等阈值过滤掉噪音。可得到如下结果：</p><p><code>spark教程：spark入门|spark基础教程|spark教程pdf</code></p><h4 id="2-协同过滤"><a href="#2-协同过滤" class="headerlink" title="2. 协同过滤"></a>2. 协同过滤</h4><p>通过日志我们可以得到如下的数据：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">user1 query:A,B,C,D</span><br><span class="line">user2 query:B,D,H,K</span><br><span class="line">...</span><br></pre></td></tr></table></figure><p>基于UserCF可以给搜索A query的用户推荐H query。</p><p>另外，我们还可以得到如下日志：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">query：A result：url1 url2 url3 ...</span><br><span class="line">query：B result：url2 url3 url4 ...</span><br><span class="line">...</span><br></pre></td></tr></table></figure><p>假设我们只取每个query结果的前十条，根据以上数据每个query可由一个由URL组成的向量来表示，每一维的向量值为URL的点展比，通过引入用户的点击行为数据，可以使得结果更加可靠。接下来就可以计算向量的相似度来进行相关推荐。<br>该方法本质也是一种CF：将query看做user，URL看做item，点击行为看做user like item的行为。可以用LFM等机器学习算法来得到相似度。</p><h4 id="3-词向量"><a href="#3-词向量" class="headerlink" title="3. 词向量"></a>3. 词向量</h4><p>有了上述的query得应的URL向量后可以用K-means方法来进行聚类，然后在同一个簇内进行相关搜索推荐。</p><p>另外可以根据词向量来进行相似度计算，首先从日志中得到如下数据：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">URL1: query1 query2 query3 ...</span><br><span class="line">URL2: query2 query4 query4 ...</span><br><span class="line">...</span><br></pre></td></tr></table></figure><p>每个URL可以由多个不同的query来召回，将其合并，一条记录可以看做是一个doc，每个doc由多个词(query)组成，用word2vec计算每个词的词向量，然后根据向量来计算相似度，进行相关搜索推荐。这个word2vec就比较厉害了，在得到的结果里，你搜范冰冰他会推荐给你李晨，你搜李晨除了给你推荐范冰冰之外还会推荐给你大黑牛、跑男等，有意思吧。</p><h2 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h2><p>相关搜索承担着扩展搜索，召回更多结果和帮助用户找到更准确的结果等责任。上述的算法部分算作是离线数据处理部分，处理完后可以将结果推到数据库中供在线部分查询使用。在线部分对用户输入的query进行分词，针对某些长尾词进行词干提取，然后去离线产生的数据中查询获取相关推荐。所以类似的系统设计一般都是重离线，轻在线，使得在线部分能够快速响应用户请求。另外，在线部分要记录用户的行为数据，将其给离线部分使用，形成一个闭环。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>相关搜索是搜索引擎中的一小部分，通过相关搜索可以提升用户搜索体验，并能够反馈给搜索引擎，提升搜索效果。所以，搜索和推荐是分不开的，当然啦，还有广告。上述只是写了一些思路，实际上做起来还有好多需要注意的地方。</p>]]></content>
      
      
      <categories>
          
          <category> search </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 搜索 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>7月盛夏</title>
      <link href="/2017/07/09/2017%E4%B8%8A%E5%8D%8A%E5%B9%B4/"/>
      <url>/2017/07/09/2017%E4%B8%8A%E5%8D%8A%E5%B9%B4/</url>
      
        <content type="html"><![CDATA[<p>转眼已到7月盛夏的季节，2017已经过了一半。前几天跟同事说，到了这个年龄，最让人感到恐惧的就是时间的飞逝。</p><p>所谓一年之计在于春，今年春天的时候回顾自己过去一年的工作与成长，感觉略有失望，跟leader沟通过后，在春末夏初的时候工作上做了新的调整。希望在下一个风口到来时自己不掉队并且能够做些事情。</p><p>比较幸运的是，在过去的一年多做过两个新项目，并且自己是其中一个项目的owner。虽然项目都不算大，但是这种从零到一的经验是宝贵的，尤其在大公司里。最大的一个体会是，即使是一名新人也不能期望别人给你过多的帮助，要学会自己去push。</p><p>闲暇的时候跟朋友聊天，问道：人活着的意义是什么？发现对于这个问题，不同年龄段的人都很难给出属于自己的那份答案。每天都疲于奔波，到了夜深人静的时候扪心自问却不知为何如此。前段时间去青海，在塔尔寺看到了那些虔诚的朝拜者，一步一跪，五体投地的进行朝拜，忽然感悟到人生就是一场修行，急不得。</p><p>后来我在想为什么在青海、西藏等地能够看到让人肃然起敬的信仰，为什么人们想要洗涤心灵的时候都是去青海湖、拉萨而不是去鸟巢和东方明珠塔。或许是因为在青海、西藏地处高原，远离纷扰，所以那里的人更淳朴、更虔诚。人待在大城市之所以会产生困惑和心烦意乱或许是因为周围的喧嚣使得自己过于着急的想要得到某些东西。</p><p>早上醒来看到书架上的书，其中有一半都只是读了一部分，或许自己犯了读书不多，想的太多的毛病。即使思考，也不要停下来思考。</p><p>2017的上半场让我想明白了一些事情，2017的下半场要努力做好全力以赴。</p>]]></content>
      
      
      <categories>
          
          <category> 此刻我在想什么 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 随笔 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Go多个pkg的单元测试覆盖率</title>
      <link href="/2017/06/11/golang-test/"/>
      <url>/2017/06/11/golang-test/</url>
      
        <content type="html"><![CDATA[<p>在go test命令后面添加 -cover参数开启测试覆盖率统计，其结果如下：<br><code>ok      models  0.012s  coverage: 71.4% of statements in models</code><br>-coverpkg 标记来指定要被统计的代码包之后，未被指定的代码则肯定不会被统计，即使是被直接测试的那个代码包。<code>go test -coverpkg=./... pkg2</code>可以跑pkg2下的所有单元测试及pkg2所用到的其他包的覆盖率情况。<br>但是由于go不支持<code>go test -coverpkg=./... ./...</code>如果我们有多个pkg，则无法一次性统计出所有的测试覆盖率和跑完全部单元测试。所以需要挨个跑完单元测试然自己来合并覆盖率的结果：</p><figure class="highlight golang"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">src</span><br><span class="line">├── pkg1</span><br><span class="line">│   ├── pkg11</span><br><span class="line">│   └── pkg12</span><br><span class="line">└── pkg2</span><br><span class="line">    ├── pkg21</span><br><span class="line">    └── pkg22</span><br><span class="line">    </span><br><span class="line"><span class="keyword">go</span> test -coverprofile=pkg1.cover.out -coverpkg=./... pkg1</span><br><span class="line"><span class="keyword">go</span> test -coverprofile=pkg1.cover.out -coverpkg=./... pkg2</span><br></pre></td></tr></table></figure><p>-coverprofile用来指定统计测试覆盖率信息的输出路径，其内容如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">mode: set</span><br><span class="line">models/bot.go:32.40,46.18 12 1</span><br><span class="line">models/bot.go:49.2,57.35 5 1</span><br><span class="line">...</span><br></pre></td></tr></table></figure><p>第一行是测试覆盖的mode，可取值为：set,count,atomic。剩下的行遵循以下的格式：<br>name.go:line.column,line.column numberOfStatements count<br>所以对每个pkg跑完单元测试后可以用以下的命令来合并：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">echo &quot;mode: set&quot; &gt; coverage.out &amp;&amp; cat *.cover.out | grep -v mode: | sort -r | \</span><br><span class="line">awk &#x27;&#123;if($1 != last) &#123;print $0;last=$1&#125;&#125;&#x27; &gt;&gt; coverage.out</span><br></pre></td></tr></table></figure><p>然后用go自带的工具来将其输出为HTML：<br><code>go tool cover -html=coverage.out -o cover.html</code><br>把整个流程串起来写成脚本如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br></pre></td><td class="code"><pre><span class="line">#!/bin/bash</span><br><span class="line"></span><br><span class="line">set -e</span><br><span class="line"></span><br><span class="line">profile=&quot;cover.out&quot;</span><br><span class="line">htmlfile=&quot;cover.html&quot;</span><br><span class="line">mergecover=&quot;merge_cover&quot;</span><br><span class="line">mode=&quot;set&quot;</span><br><span class="line"></span><br><span class="line">for package in $(go list ./...|grep -v src); do</span><br><span class="line">    coverfile=&quot;$(echo $package | tr / -).cover&quot;</span><br><span class="line">    go test -covermode=&quot;$mode&quot; -coverprofile=&quot;$coverfile&quot; -coverpkg=&quot;$package&quot; &quot;$package&quot;</span><br><span class="line">done</span><br><span class="line"></span><br><span class="line"># merge all profiles</span><br><span class="line">grep -h -v &quot;^mode:&quot; *.cover | sort &gt; $mergecover</span><br><span class="line"></span><br><span class="line"># aggregate duplicated code-block data</span><br><span class="line">echo &quot;mode: $mode&quot; &gt; $profile</span><br><span class="line">current=&quot;&quot;</span><br><span class="line">count=0</span><br><span class="line">while read line; do</span><br><span class="line">    block=$(echo $line | cut -d &#x27; &#x27; -f1-2)</span><br><span class="line">    num=$(echo $line | cut -d &#x27; &#x27; -f3)</span><br><span class="line">    if [ &quot;$current&quot; == &quot;&quot; ]; then</span><br><span class="line">        current=$block</span><br><span class="line">        count=$num</span><br><span class="line">    elif [ &quot;$block&quot; == &quot;$current&quot; ]; then</span><br><span class="line">        count=$(($count + $num))</span><br><span class="line">    else</span><br><span class="line">        echo $current $count &gt;&gt; $profile</span><br><span class="line">        current=$block</span><br><span class="line">        count=$num</span><br><span class="line">    fi</span><br><span class="line">done &lt; $mergecover</span><br><span class="line"></span><br><span class="line">if [ &quot;$current&quot; != &quot;&quot; ]; then</span><br><span class="line">    echo $current $count &gt;&gt; $profile</span><br><span class="line">fi</span><br><span class="line"></span><br><span class="line"># save result</span><br><span class="line">go tool cover -html=$profile -o $htmlfile</span><br><span class="line">go tool cover -func=$profile</span><br><span class="line"></span><br></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> Go </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Go </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>ssh远程执行nohup命令不退出</title>
      <link href="/2017/06/11/ssh-nohup/"/>
      <url>/2017/06/11/ssh-nohup/</url>
      
        <content type="html"><![CDATA[<h2 id="现象"><a href="#现象" class="headerlink" title="现象"></a>现象</h2><p>在本机执行<code>ssh target &quot;nohup sh test.sh &amp;&quot;</code>,ssh并没有立即结束退出，而是等着test.sh执行完才退出，如果提前断开ssh则执行失败。使用<code>nohup</code>和<code>&amp;</code>是想让test.sh在后台执行，并忽略<code>SIGHUP</code>信号，即使执行命令的console退出了，执行命令的进程也可以继续执行。而ssh远程执行nohup的命令不立即退出跟nohup没有太大的关系。将上面的命令换成下面的命令就会立即返回：<br><code>ssh target &quot;nohup sh test.sh &gt;/dev/null 2&amp;1 &amp;&quot;</code></p><h2 id="原因"><a href="#原因" class="headerlink" title="原因"></a>原因</h2><p>ssh在执行远程的命令时，首先在远程的机器上建立一个sshd的进程，然后由这个sshd进程启动一个bash进程来执行传过来的命令。bash进程的0，1，2三个fd通过管道与sshd的相应的文件描述符关联起来。如果远程命令是后台执行会发现其父进程变为1，输入fd重定向到了&#x2F;dev&#x2F;null，但是输出和错误的fd是管道，与sshd进程关联，所以这次启动的sshd进程不会结束，必须重定向输出和错误fd。</p><p>对于命令：<code>ssh target &quot;for i in 1 2 3;do sh ./test.sh;done&quot;</code> 在targe机器上执行<code>ps -ef | grep &#39;test\|sshd&#39;</code> 看到下面结果:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">work      14766     1  0 12:42 ?        00:00:00 /bin/bash .test.sh</span><br><span class="line">root      18185  2387  0 16:24 ?        00:00:00 sshd: work [priv] </span><br><span class="line">work      18377 18185  0 16:24 ?        00:00:00 sshd: work@pts/0  </span><br><span class="line">work      18406 18377  0 16:24 pts/0    00:00:00 bash -c <span class="keyword">for</span> i <span class="keyword">in</span> 1 2 3;<span class="keyword">do</span> sh ./test.sh;<span class="keyword">done</span></span><br><span class="line"></span><br></pre></td></tr></table></figure><p>由此可以看到进程的递进关系：sshd –&gt; bash -c –&gt; bash。所以本机执行<code>ssh target &quot;cmd&quot;</code>要能够立刻返回要满足：sshd进程没有要等着结束的子进程，这一点靠<code>&amp;</code>来保证。其次，没有其他正在执行的进程与它有管道关系，这点靠重定向来保证。因为重定向只对单个简单命令或单个复合命令有效，对于组合的命令需要放到{}中：<code>ssh target &quot;{ ./test.sh &amp;&amp; ./test.sh; } &gt;/dev/null 2&gt;&amp;1 &amp;&quot;</code>。</p><p>示例一：<br><code>ssh target &quot;for i in 1 2 3; do ./test.sh &gt;/dev/null 2&gt;&amp;1 0&lt;/dev/null; done &amp;&quot;</code><br>ssh不会立即退出，test.sh挨个执行，bash-c是后台执行但是1和2fd跟sshd有管道相连。</p><p>示例二：<br><code>ssh target &quot;for i in 1 2 3; do ./test.sh ; done &gt;/dev/null 2&gt;&amp;1 &amp;&quot;</code><br>ssh立即退出，test.sh挨个执行，执行test.sh的bash由bash-c启动，bash-c进行了重定向，所以子bash继承了重定向。</p><p>示例三：<br><code>ssh target &quot;for i in 1 2 3; do ./test.sh &amp; done &gt;/dev/null 2&gt;&amp;1 &amp;&quot;</code><br>与上面的示例不同的是这里的test.sh不是顺序执行而是近似的同时执行。</p>]]></content>
      
      
      <categories>
          
          <category> bash </category>
          
      </categories>
      
      
        <tags>
            
            <tag> ssh </tag>
            
            <tag> nohup </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>搜索引擎系列(一)---Rank的概率模型</title>
      <link href="/2017/05/13/RankModel/"/>
      <url>/2017/05/13/RankModel/</url>
      
        <content type="html"><![CDATA[<h2 id="Rank是干啥的"><a href="#Rank是干啥的" class="headerlink" title="Rank是干啥的"></a>Rank是干啥的</h2><p>Rank是搜索引擎中的精髓模块，Rank所做的事情就是根据用户的query对所有的doc(常见的如网页)进行打分排序。打分排序的依据是doc与query的相关性，而相关性怎么计算呢？把搜索这个动作可拆为两件事情：</p><ol><li>用户给出一个query</li><li>某个doc满足用户的搜索需求</li></ol><p>rank排序依据可以用一个条件概率来表示: $P(d_{i}|q)$其中q是用户给定的一个查询，$d_i$代表doc i满足用户的需求。这个条件概率的描述是：<strong>在给定一个用户查询时，一个特定的doc满足用户需求的概率</strong></p><p>rank要做的就是对所有的doc按照$P(d_i|q)$值的大小进行排序。根据贝叶斯公式可以得到：<br>$$P(d_i|q)&#x3D;\frac{P(q|d_i)P(d_i)}{P(q)}\tag{1}$$</p><p>这个公式告诉我们：</p><ol><li>不同的doc在同一个query查询下的值是可以比较的</li><li>$P(d_i)$是一个与查询无关的值，这个值可以理解为doc满足用户query的能力，可以在不同的doc间进行比较</li><li>$P(q|d_i)$可以理解为已知一个doc满足了用户的一些query，那么用户查询是q的概率是多少。就好比站长查看用户是从搜索引擎中的哪些query引到自己的网站。这个值反映的是一个doc对不同的query满足程度的比较，非doc间可比较。该值难以直接计算，它将一个已知query求条件概率的线上计算问题变成了一个已知doc求条件概率的线下计算问题。</li><li>$P(q)$是指一个特定查询出现的概率。该值较大时代表一个热门query，(1)式中的分子必须较大才能满足用户需求。如果该值较小，(1)式中分子无需太大也能满足用户的需求。</li></ol><h2 id="PageRank"><a href="#PageRank" class="headerlink" title="PageRank"></a>PageRank</h2><p>传统的TF*IDF算法，TF代表词频，IDF代表逆向文档频率，它主要思想为：</p><ol><li>某个词或短语在一篇文章中出现的次数越多，越相关。</li><li>整个文档集合中包含某个词的文档数量越少，这个词越重要。</li></ol><p>可见主要是针对(1)式中的$P(q|d_i)$，对$P(d_i)$考虑的比较少。而$P(d_i)$是一个查询无关的值，理解为在完全不知道用户查询的情况下，一个doc满足用户需求的情况，也可以理解为无查询推荐。它表示，即使在不知道用户想要什么，或者对用户的需求分析错的情况下，仍然可以尽量给出满足用户需求的doc。对于$P(d_i)$，google 利用一个随机浏览模型（即pagerank）进行计算，即认为这个值近似等于一个不断随着超链随机浏览的人看到网页i的概率。这个值的加入显著地提升了搜索质量。<br>由此可以窥见，推荐与搜索本来就是不分家的，有推荐的搜索才是真正的搜索，才体现出搜索的“情感”(query解析、排序、页面展示)，搜索引擎的难点(用户意图理解、有价值信息的展示)。<br>目前由于链接作弊等原因PageRank与真正想要值的差距已经越来越大了，渐渐的被其他算法所替代。</p><h2 id="页面因子"><a href="#页面因子" class="headerlink" title="页面因子"></a>页面因子</h2><p>我们认为对于一个doc先验的满足用户需求的概率由很多因素决定，如：页面质量、被其他页面链接数等。我们可以将其抽象为一系列页面因子组成的向量，对于一个doc w&#x3D;&lt; X1…Xn &gt;,令S为用户得到满足的事件。<br>$$p(d)&#x3D;p(s|w)&#x3D;p(s|x_1,x_2,…x_n)&#x3D;\frac{p(x_1..x_n|s)p(s)}{p(x_1..x_n)}$$<br>假设各页面因子是相互独立的<br>$$p(s|&lt; x_1,..x_n &gt;）&#x3D;\frac{p(x_1|s)p(x_2|s)…p(x_n|s)p(s)}{p(x_1)…p(x_n)})$$<br>其中p(xi|s)是对于满足用户需求的某一个页面因子的值，例如：用户点击数等。<br>在(1)式中如果p(di)&#x3D;0，无论其他值如何这个doc满足用户需求的概率都是0，这种doc就是属于垃圾doc，没必要参与rank。</p><h2 id="query分析"><a href="#query分析" class="headerlink" title="query分析"></a>query分析</h2><p>一个query可以被拆分为多个term，那么(1)式中p(q|di)的线下计算可以拆分为p(t|di)的计算。通过对query的分解变换将其转换为好计算的近似值，一个query可以分解为q&#x3D;&lt; t1..tn &gt;,分解方式可以为：</p><ol><li>完全不分解：$$p(q|d_i)&#x3D;p(&lt; t_1..t_n &gt;|d_i)\tag2$$</li><li>分解为相互独立的term：$$p(q|d_i)&#x3D;p(t_1|d_i)..p(t_n|d_i)$$</li><li>每个term都只跟前一个term相关:<br>$$p(q|di)&#x3D;p(t1|di)p(t2|di,t1)p(t3|di,t2)…p(tn|di,t_{n-1})$$</li><li>查询的term必须按照一定顺序出现</li></ol><h2 id="多样性"><a href="#多样性" class="headerlink" title="多样性"></a>多样性</h2><p>如果一个doc可以满足多个query，那么：$\sum_jp(q_j|d_i)&#x3D;1$ 实际情况中我们要根据query来分析用户的真正查询意图，同一个字符串可能对应多个用户需求。例如：对于“人人”这个查询，可能对应两种需求：人人网和人人车。假设其中对人人车的需求要高于对人人网的需求，(1)式中的排序会偏袒热门需求，因为对于冷门需求(1)式中对分子的权值要求比较低，如果不考虑“人人”对两种需求各自出现的概率那么满足“人人网”的doc在排序处于不利的位置。针对多样性，可以参考以下公式，其中$q_s$代表查询字符串：<br>$$p(d_i|q_s)&#x3D;p(q_1|q_s)p(d_i|q_1)+P(q_2|q_s)p(d_i|q_2)&#x3D;p(q_1|q_s)\frac{p(q_1|d_i)}{p(q_1)}+p(q_2|q_s)\frac{p(q_2|d_i)p(d_i)}{p(q_2)}$$</p><h2 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h2><p>在搜索引擎中可以引入其他的因素来扩展这个概率模型，比如引入时效性、权威性、引入地域等各种用户个性化信息。<br>另外目前各大搜索引擎希望在第一页(前10条)或者前3条结果中来满足用户需求，而不仅仅局限于某一个doc满足用户需求概率的最大化。<br>除了返回网页链接外，还可以通过增加返回结果类型的方式来满足用户的需求，例如：返回阿拉丁，框应用等。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>该模型解释了rank的排序依据以及如何将其分解为子目标进行计算，对于不好计算的变量，可以通过将其与好算的变量关联起来或者想办法近似的计算。近似计算就会用到一些假设，如果结果不对那么之前的假设就可能有问题。</p><p><strong>各种近似的计算都是对理论模型的逼近，理论模型存在的意义之一在于，它让我们的思考系统化，告诉我们在逼近什么，努力的方向在哪，离目标还有多远。想尽一切办法，越来越精确地逼近理论值，这就是进步。</strong></p>]]></content>
      
      
      <categories>
          
          <category> search </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 搜索 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>大数据与人工智能</title>
      <link href="/2017/04/22/BigData-And-AI/"/>
      <url>/2017/04/22/BigData-And-AI/</url>
      
        <content type="html"><![CDATA[<p>前些年大数据这个词很火，最近一两年AI又火了起来，现在出门不谈点AI 都不好意思跟人Say Hi。</p><p>为什么经历了两个冬天的AI在大数据火了之后迎来了新的春天？</p><h3 id="仿生学不是出路"><a href="#仿生学不是出路" class="headerlink" title="仿生学不是出路"></a>仿生学不是出路</h3><p>AI已经出现几十年了，而最近五年发展十分迅速，为什么？早期研究人工智能的学者认为要让机器获得智能，首先要了解人类是如何产生智能的，然后让计算机按照人的思路去做，要让机器像人一样思考才能获得智能。这种方法论被称为『鸟飞派』，看看鸟是怎么飞的，模仿鸟的动作人就能飞上天。而实际上飞机的发明依靠的是空气动力学而不是仿生学。</p><h3 id="数据-统计"><a href="#数据-统计" class="headerlink" title="数据+统计"></a>数据+统计</h3><p>后来人类又找到了另一条路子：数据+统计，即采用数据驱动和超级计算的方法。数据最大的作用在于承载信息。在远古时代尼罗河畔的土著居民通过观察天象来准确推测洪水到来和退去的时间，以此来判断一年农耕的时间和节气。但是由于能采集到数据量（例如音频、图像）有限或者计算能力不够无法处理大规模数据导致AI在早些年没有得到快速的发展。</p><p>近些年大数据技术得到快速发展，人类可以获得到越来越多的数据。这些数据无论在体量、多维度还是完备性都超过以往。在一些无法确定的因果关系中，数据所包含的信息可以帮助我们消除一些不确定性，甚至数据之间所呈现出的相关性可以取代原来的因果关系，帮助我们得到答案。例如：在图像识别方面，学术上常用的基准测试是一百万幅，所用图像最多的学术论文是1500万幅，而百度用来训练最先进的人脸识别系统所用的数据量为：两亿幅！可见，数据已经开始成为公司的一种资产。</p><p>大数据技术带来的另一个红利是对大规模数据的处理能力，近几年对GPU的使用是异构计算发展的一个典型代表，在此技术之上实现了能够利用巨大数据集的机器学习平台。</p><p>所以，大数据技术带来的两样东西：大规模数据集和高性能计算，促进了AI的发展。</p><h3 id="现在的AI"><a href="#现在的AI" class="headerlink" title="现在的AI"></a>现在的AI</h3><p>现在的AI能做什么？除了AlphaGo打败了李世石之外，在语音识别、图像识别、自动驾驶等方面也取得了较好的发展。Andrew Ng 指出AI进展最快的领域是人类能做到的领域，而人类难以做到的事情，目前AI也很难做到。原因有三：</p><ol><li>人类能做的，至少是可行的。</li><li>可以利用人类的经验数据作为培训样本</li><li>人类可以提供指导、校验，进而改善AI</li></ol><p>很多领域一旦超过人类自身的能力，AI发展也变得缓慢。由此引发的矛盾：<strong>如果AI一直在做人类可以做的事情，实际上是在跟人类进行竞争</strong>。</p><h3 id="未来的AI"><a href="#未来的AI" class="headerlink" title="未来的AI"></a>未来的AI</h3><p>在未来人工智能相关的技术和工具会如同水电一样，由专门的公司向全社会提供，各行各业的人们将用这个技术来改变自己所在的行业，就如同利用电力来改变制造业一样，由此将会带动整个人类社会的升级和变迁。</p>]]></content>
      
      
      <categories>
          
          <category> 此刻我在想什么 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> AI </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>躁动的季节，躁动的心</title>
      <link href="/2017/03/26/RestlessHeart/"/>
      <url>/2017/03/26/RestlessHeart/</url>
      
        <content type="html"><![CDATA[<p>春天来了，又到了<del>动物们</del>人们开始躁动的季节，正所谓『金三银四』，又到了换工作最疯狂的季节了，地铁站里贴满的猎聘、直聘BOSS等广告彰显出人们躁动的荷尔蒙。而我，在这个躁动的季节里也觉得格外焦虑。</p><p>为什么会感到焦虑呢？因为对自己的现状感到不满，想变得更好，可是又没有明确的路，摸索前行的过程中时常会感到迷茫。</p><p>回头审视了一下过去的一年，自己感觉并没有做出什么让自己满意的成绩，尽管组里的人都觉得我去年表现的不错，但是从自身成长的角度来说，提升不多，做的事情依靠之前的经验和能力就能够cover住。看了一下公司内外高级职位的招聘JD觉得自己离岗位要求还有一定的差距，这个时候我有点方了。。。</p><p>正常来说，一个人但凡有点上进心，如果发现自己在较长的一段时间里没有明显的提升就应该会感到焦虑。技术方面的成长，我个人比较倾向于Application-Oriented，所以如果所做的事情只是在重复之前的技能对于自身的提升几乎是毫无促进作用的。</p><p>可能是时候换个环境了。换工作是一件比较纠结的事情，想要找到跟自己match度比较高的岗位、业务、老大那是可遇而不可求的。同时还要考虑到薪酬、晋升、未来发展等问题。或许又要逼近下一个人生的十字路口了吧，至于怎么选，还没想好。</p>]]></content>
      
      
      <categories>
          
          <category> 此刻我在想什么 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 随笔 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>为什么更新频率降低了呢？</title>
      <link href="/2017/03/01/%E4%B8%BA%E4%BB%80%E4%B9%88%E6%9B%B4%E6%96%B0%E9%A2%91%E7%8E%87%E9%99%8D%E4%BD%8E%E4%BA%86%E5%91%A2/"/>
      <url>/2017/03/01/%E4%B8%BA%E4%BB%80%E4%B9%88%E6%9B%B4%E6%96%B0%E9%A2%91%E7%8E%87%E9%99%8D%E4%BD%8E%E4%BA%86%E5%91%A2/</url>
      
        <content type="html"><![CDATA[<h2 id="Status"><a href="#Status" class="headerlink" title="Status"></a>Status</h2><p>自从将博客从<a href="http://blog.163.com/xh_ding/">旧地址</a>搬过来之后更新速度明显下降了好多，在过去的2016年里总共也没写几篇，甚至连年度总结都省去了。为什么博文更新频率降低了呢？为什么呢！？</p><h2 id="Why"><a href="#Why" class="headerlink" title="Why"></a>Why</h2><p>为什么会这样呢？</p><p>1.<em>懒</em><br>过去的一年确实有些变懒了，除了写东西偷懒，在思维上也有些偷懒了，思考不够勤奋。<br>2.<em>不够聚焦</em><br>这也是老毛病了，总是思绪乱飞，经常被一些被热捧的技术吸引眼球，但又浅尝辄止，没有深入的学习。皮毛的东西不足道之。<br>3.<em>阅读量降低</em><br>跟之前的状态相比，每天专注的阅读量下降了不少，时间都去哪了呢？除了工作及聊天扯淡外，发现自己最近比较喜欢发呆，总期望在发呆中能想明白些什么事，可真要想起来思绪像一团找不到头的毛线团，比较乱。<br>4.<em>希望能够提升文章质量，持续输出高质量作品</em><br>这点跟第二点是有些关联的，不聚焦便也不能够产生高质量的内容。回看之前的博客内容，尤其<a href="http://blog.163.com/xh_ding/">旧地址</a>有许多是一些比较基础的知识介绍，并且文章的质量也并不是特别高，希望以后能不断的提高文章质量。</p><h2 id="Solution"><a href="#Solution" class="headerlink" title="Solution"></a>Solution</h2><p>反思一下，如何解决以上的问题呢？</p><p>1.<em>focus</em><br>年初的时候仔细想了一下，『聚焦』是今年核心主题，无论是工作还是生活。<br>人类科技文明经过千年的发展自身体系已十分的庞大，任你心再狂野，想要做到在多方面的精通乃至博古通今都是非常困难的，其中任何一个分支的深入学习所花费的时间都足以吞噬一个人的一生。<br>所以需要聚焦，把能量集中才有可能产生爆发。<br>2.<em>勤思考</em><br>不能用行动上的勤奋来掩盖思维上的懒惰。<br>3.<em>多阅读</em><br>多阅读，尤其是技术方面系统的学习某个领域的知识。另外，通过阅读也可以学习到别人是如何用通俗易懂的语句将一个问题描述清楚的。希望能把之前收藏的书单都能看完。时间从哪来呢？四个字：多读少浪。<br>4.<em>多练习</em><br>多联系写作。斯蒂芬·平克曾经说过：写作之难，在于把网状的思考，用树状的结构，体现在线性展开的语句里。多练习写作可以使思维逻辑得到极大的锻炼。在勤思考、多阅读的基础上定期的有产出，文章不急于一次就写好，写的过程多想想如何用简单的语言来讲清楚一件事情。<br><br>2017的第一个Q就剩最后一个月了，不能再这样蹉跎下去了。</p>]]></content>
      
      
      <categories>
          
          <category> 此刻我在想什么 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>分布式锁设计</title>
      <link href="/2016/12/19/%E5%88%86%E5%B8%83%E5%BC%8F%E9%94%81%E8%AE%BE%E8%AE%A1/"/>
      <url>/2016/12/19/%E5%88%86%E5%B8%83%E5%BC%8F%E9%94%81%E8%AE%BE%E8%AE%A1/</url>
      
        <content type="html"><![CDATA[<p>分布式环境下，多台机器上多个线程对同一份数据进行操作如果不做处理防范，就有可能出现类似『数据重复入库』、『商品超卖』等情况，在这种情况下需要分布式锁来对分布式环境下临界资源做互斥。</p><p>那么如何来做呢？</p><p>想想单机多线程的时候我们是如何保证互斥的，单机多线程的情况下所有的线程都去尝试获取同一把锁。换到分布式环境下我们把局限于一台机器内的锁提出来，其实就是利用一个大家都能访问的公共资源来实现分布式锁。这个公共资源可以是redis，可以是zookeeper，也可以是其他的某个资源。这里我们主要考虑用redis的setnx来实现。</p><p>设计一个分布式锁主要考虑以下几点：</p><h2 id="互斥"><a href="#互斥" class="headerlink" title="互斥"></a>互斥</h2><p>分布式系统中运行着多个节点，必须确保在同一时刻只能有一个节点的一个线程获得锁，这是最基本的。</p><h2 id="死锁"><a href="#死锁" class="headerlink" title="死锁"></a>死锁</h2><p>用Redis来实现分布式锁最简单的方式就是在实例里用setnx命令来创建一个键值，当一个节点想要释放锁时，它只需要删除这个键值即可。</p><p>上述方案看上去很美好，在分布式环境里，当一个节点获取到锁之后宕机了，或者因为网络不通无法执行释放锁的操作，则其他节点都无法申请到锁。</p><p>（1）锁的时效性<br>所以这里要加一个锁的时效性，避免单点故障造成死锁，但同时也要保证一旦一个节点获取到锁，在节点存活时不能被其他节点解锁。</p><p>（2）避免误删锁<br>Case：一个节点获取到锁开始执行业务，不幸被某个操作阻塞了很长时间，过了超时时间后自动释放了这个锁，过了一段时间这个节点执行完之后又尝试删除这个其实已经被其他节点拿到的锁。所以简单粗暴的直接删除有可能造成一个节点删除了其他节点的锁。<br>为了避免误删可以给锁加一个节点ID+线程ID作为签名的属性（用hset命令），这样每个锁就只能被获取到锁的节点删除。<br>另外还可以给每个获取锁的线程设定一个TimerTask，在线程执行任务的过程中定时的延长锁的时效时间，这样就不会因为执行时间超过了超时时间导致锁自动释放，也就是续租锁。但为了提升性能应尽量避免加锁时间太长，尽量减少锁的粒度，如下面一条所说。</p><h2 id="锁的性能"><a href="#锁的性能" class="headerlink" title="锁的性能"></a>锁的性能</h2><p>（1）加锁的事务或者操作尽量粒度小，减少其他节点申请锁的等待时间，提高处理效率和并发性。<br>（2）降低获取锁的频率，尽量减少Redis压力。让节点申请锁的时候有一个等待时间，而不是不停的循环尝试获取锁。<br>（3）持锁的客户端解锁后，要能通知到其他等待锁的节点，而不是隔一段时间尝试获取一次锁。</p><p>针对上面后两条，Redisson采用Semaphore及Redis的订阅&#x2F;发布消息来控制，通过订阅&#x2F;发布可以避免空转。<br>如果锁当前是被占用的，那么等待释放锁的消息，当锁释放并发布释放锁的消息后，信号量的 release() 方法会被调用，此时被信号量阻塞的等待队列中的一个线程就可以继续尝试获取锁了。<br>此处虽然Semaphore只是进程内部粒度的锁的，但是也可以一定程度减轻Redis节点的压力，因为每个节点上的请求数量少了，Redis的压力也就少了。</p><h2 id="锁重入"><a href="#锁重入" class="headerlink" title="锁重入"></a>锁重入</h2><p>可重入锁，即在已经获取锁的基础之上，再次获取当前的锁。在释放时，之前获取了N次，那么也需要相应的unlock N次，才表示最终锁被成功释放。<br>分布式锁是否需要设计为可重入的要根据具体的业务来判断。通过上文所说的锁的签名可以判断当前线程是否已经获得锁，如果已经获得则count增加。</p><h2 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h2><p>是否采用分布式锁及最终锁的实现方案还是要看具体的业务需求。<br>推荐看一下Redis官方文章：<a href="https://github.com/antirez/redis-doc/blob/master/topics/distlock.md">Distributed locks with Redis</a><br>中文翻译：<a href="http://ifeve.com/redis-lock">用Redis构建分布式锁</a><br>另外，<a href="https://github.com/redisson/redisson/wiki/Redisson%E9%A1%B9%E7%9B%AE%E4%BB%8B%E7%BB%8D">Redisson</a>实现了Redis官方提出的RedLock算法</p>]]></content>
      
      
      
        <tags>
            
            <tag> Distributed Locks </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>用户评论挖掘</title>
      <link href="/2016/12/04/%E7%94%A8%E6%88%B7%E8%AF%84%E8%AE%BA%E6%8C%96%E6%8E%98/"/>
      <url>/2016/12/04/%E7%94%A8%E6%88%B7%E8%AF%84%E8%AE%BA%E6%8C%96%E6%8E%98/</url>
      
        <content type="html"><![CDATA[<p>随着互联网的发展，越来越多的人选择在网上消费，并且越来越的证据表明商品的评论信息会影响到消费者的消费决定。评论挖掘的主要任务是从评论中了解到用户对产品的哪些功能、属性进行了怎样的评论，并抽取成简短有效的信息。</p><p>评论挖掘主要有以下几个子任务：</p><h2 id="识别、抽取产品特征"><a href="#识别、抽取产品特征" class="headerlink" title="识别、抽取产品特征"></a>识别、抽取产品特征</h2><p>产品特征分为显示特征和隐含特征。</p><ul><li>显示特征<br>显示特征是直接出现在产品的评论中，描述产品的性能或功能的名词或名词短语。</li><li>隐含特征<br>隐含特征没有在语句中直接进行描述，需要对句子进行语义理解才能得到，提取隐含特征需要自然语言的完全理解技术 。<br>特征词满足以下三个条件之一：<br>（1） 给定评论对象的一部分<br>（2） 给的评论对象的一个属性<br>（3）给定评论对象的一个部分的一个属性<br>特征提取分为人工标记和自动提取两种方法。人工标记一般由领域专家来完成。<br>自动提取过程：对语料进行词性标注，提取其中的名词短语，利用关联规则挖掘出频繁项，在频繁项候选集上做密实度修剪和冗余修剪，去掉无用的短语和合并相似的短语。</li></ul><h2 id="特征语意去重"><a href="#特征语意去重" class="headerlink" title="特征语意去重"></a>特征语意去重</h2><p>首先介绍一下什么是词向量和语言模型。<br>（1） 词向量就是用来将语言中的词进行数学化的一种方式。有了词向量之后就可以对词进行聚类、分类、计算相似度等等。<br>（2） 语言模型就是用来计算一个句子的概率的模型，简单来说就是判断一句话是不是正常人说出来的。常见的应用场景：机器翻译、语音识别得到若干候选之后，可以利用语言模型挑一个尽量靠谱的结果。</p><p>特征词去重采用聚类的方法对特征词进行聚类，归属于同一类的即为同义词。其中判断词语的相似度，采用计算特征词的词向量，然后计算向量间的相似度（例如计算向量夹角）的方法。</p><p>关于同义词聚类，Google推出的Word2Vec是一个不错的工具，关于Word2Vec的介绍请参考之前的博文：<a href="http://blog.163.com/xh_ding/blog/static/1939032892014312102457581">自然语言处理之Word2Vec</a> 。其中一个思路是：用word2vec的到词向量然后用kmeans进行同义词聚类。</p><h2 id="识别产品特征对应的观点词"><a href="#识别产品特征对应的观点词" class="headerlink" title="识别产品特征对应的观点词"></a>识别产品特征对应的观点词</h2><p>对评论语料进行词法和依存句法分析生成三元组：<br><code>&lt;Attribute, Subject, Value&gt;</code><br>Subject：产品<br>Attribute：特征<br>Value：特征观点</p><h2 id="分析评论的情感及强度"><a href="#分析评论的情感及强度" class="headerlink" title="分析评论的情感及强度"></a>分析评论的情感及强度</h2><p>把用户评论句子切分为只包含一个特征词的短句并将评论抽象概括为<code>&lt;Holder, Topic, Sentimentl&gt;</code>。<br>采用基于情感词典的方式计算短句的情感得分：</p><p>$$Score&#x3D;\sum_{ w_i \in V} \frac{S_w}{dis(w_i, f)}$$</p><p>$w_i$ 代表句子中的情感词</p><p>V代表情感词典中情感词集合</p><p>$S_{w_i}$代表$w_i$在情感词典中的极性值</p><p>$f$代表该句子中的特征词</p><p>$dis(w_i, f)$代表在句子中情感词$w_i$与特征词f之间的距离，距离越远情感词$w_i$对特征词f的影响越小，因此赋予的权重也越小。</p><p>在此基础之上应用否定词、转折词规则以及基于上下文情感的连词规则和蕴含连词规则。最终计算得到短句的情感倾向得分。</p><h2 id="后记"><a href="#后记" class="headerlink" title="后记"></a>后记</h2><p>以上是针对中文的用户评论挖掘的一个大致粗略的过程，也算是对之前一个回答的补充：<a href="https://www.zhihu.com/question/20905103/answer/16628186">淘宝的评论归纳是如何做到的？ - 慕希颜的回答 - 知乎 </a><br>在进行挖掘前的一些数据清洗工作（例如计算评论的质量，去掉无效、低质量评论）就不详细介绍了，如有不对的地方请指正，谢谢。</p>]]></content>
      
      
      <categories>
          
          <category> NLP </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 评论挖掘 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>计算机开机过程</title>
      <link href="/2016/12/02/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%82%B9%E4%BA%AE%E8%BF%87%E7%A8%8B/"/>
      <url>/2016/12/02/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%82%B9%E4%BA%AE%E8%BF%87%E7%A8%8B/</url>
      
        <content type="html"><![CDATA[<p>从按下计算机的电源按钮到出现欢迎页，这个过程都发生了什么？<br>我们知道计算机启动叫boot，重启叫reboot，这里的boot其实是bootstrap的缩写。计算机运行时其硬件交由软件来控制，所以计算机要启动必须要运行软件，但是计算机不启动就无法运行软件，这是一个鸡生蛋和蛋生鸡的过程。<br>有一句谚语：</p><blockquote><p>“pull oneself up by one’s bootstraps”</p></blockquote><p>意思是说拽着自己的鞋带把自己拉起来。这也充分反映了计算机的启动是一个矛盾的过程，时间久了就简称boot。</p><p>计算机启动的过程主要有四个阶段：</p><h2 id="一、BIOS"><a href="#一、BIOS" class="headerlink" title="一、BIOS"></a>一、BIOS</h2><p>内存包括随机存储器（RAM），只读存储器（ROM），以及高速缓存（CACHE）。开机程序被刷入ROM芯片中，按下电源键之后计算机首先去读取ROM，这段程序被称作基本的输入输出系统(Basic Input&#x2F;Output System)简称BIOS.</p><p>BIOS程序首先检查计算机硬件能否满足运行的基本条件，这叫做”硬件自检”（Power-On Self-Test），缩写为POST。如果硬件出现问题，主板会发出不同含义的蜂鸣，中止启动。</p><p>在BIOS的菜单中有一项『设定启动顺序』，这个启动顺序实际上是一个外部存储设备的排序，在BIOS完成硬件自检后按照这个启动顺序将控制权优先交给排在前面的设备。</p><h2 id="二、主引导记录-MBR"><a href="#二、主引导记录-MBR" class="headerlink" title="二、主引导记录(MBR)"></a>二、主引导记录(MBR)</h2><p>BIOS按照『启动顺序』把控制权交给排在第一个的存储设备。计算机读取该设备的第一个扇区，即前512个字节，并判断该设备是否能用于启动（512个字节的最后两位是不是0x55和0xAA），如果不能则将控制权交给启动顺序中的下一个设备。</p><p>这512个字节主要用来告诉计算机哪个分区是激活分区，并将控制权交给激活分区，被称为『主引导记录』(Master boot record)，简称MBR。MBR由三部分组成：<br>（1） 第1-446字节：调用操作系统的机器码。<br>（2） 第447-510字节：分区表（Partition table）。<br>（3） 第511-512字节：主引导记录签名（0x55和0xAA）。</p><p>一个硬盘可以分成多个分区，其中分区表中记录了硬盘的分区信息。不同的分区可以安装不同的操作系统，因此MBR需要知道将控制权交给哪个分区。</p><p>分区表只有64个字节，分为4项，所以一个硬盘最多只能有四个一级分区，即主分区。每一项的16个字节记录的内容为：<br>（1） 第1个字节：如果为0x80，就表示该主分区是激活分区，控制权要转交给这个分区。四个主分区里面只能有一个是激活的。<br>（2） 第2-4个字节：主分区第一个扇区的物理位置（柱面、磁头、扇区号等等）。<br>（3） 第5个字节：主分区类型。<br>（4） 第6-8个字节：主分区最后一个扇区的物理位置。<br>（5） 第9-12字节：该主分区第一个扇区的逻辑地址。<br>（6） 第13-16字节：主分区的扇区总数。</p><p>随着硬盘越来越大四个分区已经不够用了，但分区表只有四项，因此规定其中只有一项可以设置为扩展分区。所谓”扩展分区”，就是指这个区里面又分成多个区。这种分区里面的分区，就叫做”逻辑分区”（logical partition）。<br>扩展分区的第一个扇区叫做”扩展引导记录”（Extended boot record），里面也包含了一张分区表，记录着下一个逻辑分区的位置，以此类推，直到某个逻辑分区分区表只包含自己。</p><h2 id="三、硬盘启动"><a href="#三、硬盘启动" class="headerlink" title="三、硬盘启动"></a>三、硬盘启动</h2><p>此时将计算机的控制权交给硬盘的某个分区了。计算机会读取激活分区的第一个扇区，叫做”卷引导记录”（Volume boot record），VBR告诉计算机操作系统位于该分区的哪个位置，然后计算机加载OS。</p><p>特殊情况下，计算机读取”主引导记录”前面446字节的机器码之后，不再把控制权转交给某一个分区，而是运行事先安装的”启动管理器”（boot loader），由用户选择启动哪一个操作系统，目前常用的启动管理器是Grub。</p><h2 id="四、操作系统"><a href="#四、操作系统" class="headerlink" title="四、操作系统"></a>四、操作系统</h2><p>最后控制器交给操作系统，首先载入操作系统的内核，例如Linux，先载入&#x2F;boot下的kernel，然后运行&#x2F;sbin&#x2F;init，根据&#x2F;etc&#x2F;initab产生PID为1的init进程，他是所有进程的父进程。然后init进程加载各个模块，例如网络、XWindow等，直至运行&#x2F;bin&#x2F;login跳出登录界面等待用户输入用户名和密码。</p><p>至此，全过程结束。</p>]]></content>
      
      
      <categories>
          
          <category> OS </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>mysql的set names</title>
      <link href="/2016/11/13/mysql%E7%9A%84set%20names/"/>
      <url>/2016/11/13/mysql%E7%9A%84set%20names/</url>
      
        <content type="html"><![CDATA[<h2 id="为了Emoji"><a href="#为了Emoji" class="headerlink" title="为了Emoji"></a>为了Emoji</h2><p>最近写东西的时候需要支持Emoji表情，用MySQL作存储需要版本5.5.3+并且字符集设置为utf8mb4，由于是跟其他服务公用一个MySQL存储服务所以不能动MySQL的全局配置，在针对数据库和表设置完字符集设置后，应用程序连接数据库时指定default-character-set为utf8mb4会有报错提示（不知道是不是应用程序使用的MySQL驱动不支持的缘故），所以存储Emoji表情的时候还是会提示『Incorrect string value』，最后解决方案是在应用程序连接数据库的时候加上’set names utf8mb4’。<br>MySQL执行set names utf8mb4后等同于临时设置如下字符编码：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SET</span> character_set_client <span class="operator">=</span> utf8mb4;       </span><br><span class="line"></span><br><span class="line"><span class="keyword">SET</span> character_set_results <span class="operator">=</span> utf8mb4;      </span><br><span class="line"></span><br><span class="line"><span class="keyword">SET</span> character_set_connection <span class="operator">=</span> utf8mb4;  </span><br><span class="line"></span><br></pre></td></tr></table></figure><p>在对MySQL进行数据插入和查询的时候经过如下的过程：<br>插入：client→connection→server<br>查询：server→connection→results</p><p>当三者保持一致的时候就不会出现乱码等问题了。</p>]]></content>
      
      
      <categories>
          
          <category> Database </category>
          
      </categories>
      
      
        <tags>
            
            <tag> mysql </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>关于密码保护</title>
      <link href="/2016/08/28/%E5%85%B3%E4%BA%8E%E5%AF%86%E7%A0%81%E4%BF%9D%E6%8A%A4/"/>
      <url>/2016/08/28/%E5%85%B3%E4%BA%8E%E5%AF%86%E7%A0%81%E4%BF%9D%E6%8A%A4/</url>
      
        <content type="html"><![CDATA[<p>作为一名开发者，开发一个用户账户系统很可能就是你工作内容的一部分，其中比较重要的事情就是如何保护用户的密码，当系统被攻破、数据库被拖库时如何降低泄露用户原始密码的风险是一件值得深思的问题（还记得当年CSDN被拖库后爆出系统存储了用户的明文密码吗？）。比较行之有效的办法是对用户密码进行加盐哈希。</p><h2 id="为什么要对密码进行哈希"><a href="#为什么要对密码进行哈希" class="headerlink" title="为什么要对密码进行哈希"></a>为什么要对密码进行哈希</h2><p>首先我们希望即使被拖库后用户的明文密码也不会遭到泄露，以免使得坏蛋们可以拿着这些明文密码再去撞别家的库。<br>哈希算法是一个单向函数，它可以将任何大小的数据转化为定长的“指纹”，并且无法被反向计算。而且只要数据源改动了一点，哈希的结果也会完全不同。这样的特性使得它非常适合用于保存密码，因为我们期望加密后的密码无法被解密，但同时也能保证正确校验每个用户的密码。（这里的哈希算法跟数据结构里hashmap中的哈希算法可不是同一回事哦）</p><h2 id="单纯的哈希安全吗？"><a href="#单纯的哈希安全吗？" class="headerlink" title="单纯的哈希安全吗？"></a>单纯的哈希安全吗？</h2><p>在用户的密码上做一次加密哈希函数实际上并不是十分的安全，因为目前已经有办法可以快速的把密码从简单的哈希值里恢复出来。例如：字典攻击、暴力攻击等。</p><ul><li><p>字典攻击</p><p>在一个字典文件里包含了常见的短语、单词和常用密码等字符串以及其对应的哈希值，用它们和密码的哈希值做对比，如果相同则对应字典里的值就是密码。例如用查表的方式，将预先计算好的哈希值和对应的密码存放到一个用于快速查询的数据结构中，厉害一点的即使存储了上亿个哈希值每秒也能进行数百次查询。</p></li><li><p>暴力攻击</p><p>暴力攻击会尝试每一个在给定长度下各种字符的组合。这种方式需要进行大量的计算，也通常是破解哈希加密中效率最低的办法，但是它最终会找到正确的密码。但只要密码需要足够长，以至于遍历所有可能的字符串组合将耗费太长时间，使得攻击者放弃这种暴力的方法。</p></li><li><p>彩虹表</p><p> 彩虹表是一种在时间和空间的消耗上找寻平衡的破解技术。它和查表法很类似，但是为了使查询表占用的空间更小而牺牲了破解速度。因为它更小，于是我们可以在一定的空间内存储更多的哈希值，从而使攻击更加有效。</p></li></ul><h2 id="加盐"><a href="#加盐" class="headerlink" title="加盐"></a>加盐</h2><p>加盐是目前常用的一种让查表法和彩虹表都失效的计数。即在密码中混入一段“随机”的字符串再进行哈希加密，这个被字符串被称作盐值。由于随机化了哈希值，攻击者不能预先生成彩虹表，增加了攻击成本。另外每个用户都是用一个独一无二的随机盐使得反向查表也变得很难。</p><h2 id="加盐哈希的误区"><a href="#加盐哈希的误区" class="headerlink" title="加盐哈希的误区"></a>加盐哈希的误区</h2><ul><li><p>盐值重复</p><p>如果盐值被硬编码到代码里或者用一个固定的随机值，那攻击者只要把盐值用到每个猜测密码上仍然可以用反向查表法或者针对这个系统生成一份彩虹表。</p></li><li><p>盐值太短</p><p> 如果盐值太短，攻击者可以构造一个查询表包含所有可能的盐值。为了使攻击者无法构造包含所有可能盐值的查询表，盐值必须足够长。一个好的做法是使用和哈希函数输出的字符串等长的盐值。</p></li><li><p>盐值随机性不够</p><p> 有人喜欢使用用户名、注册时间作为盐值，这些都不具有符合密码学要求的随机性。盐值应该使用基于加密的伪随机数生成器，例如：java中的java.security.SecureRandom。其他语言：PHP、C&#x2F;C++、Python都有对应的实现。</p></li></ul><h2 id="关于组合哈希"><a href="#关于组合哈希" class="headerlink" title="关于组合哈希"></a>关于组合哈希</h2><p> 经常看到有人使用组合哈希函数，人们认为将哈希函数组合起来结果会更加安全，使用多种哈希函数会使得计算更慢，从而破解变慢。网上多见的组合有：</p><ul><li>md5(sha1(password))</li><li>md5(md5(salt) + md5(password))</li><li>sha1(sha1(password))</li><li>sha1(str_rot13(password + salt))</li><li>md5(sha1(md5(md5(password) + sha1(password)) + md5(password)))</li></ul><p><strong>但有人对此提出质疑：他们认为实际上这样做几乎没有好处，仅仅造成了函数之间互相影响的问题，甚至有时候会变得更加不安全。</strong></p><p>使用组合哈希函数是考虑到如果攻击者不知道系统用的哪种哈希函数就不会预先生成彩虹表，但是如密码学中著名的Kerckhoff准则所说，攻击者还是比较容易拿到源码的。</p><p> **Kerckhoff准则：**系统的保密性不依赖于加密体质或者算法的保密，而依赖于密钥。</p><h2 id="关于慢哈希"><a href="#关于慢哈希" class="headerlink" title="关于慢哈希"></a>关于慢哈希</h2><p>使用随机盐可以让攻击者无法使用查表和彩虹表的方式对大量hash进行破解，但是仍然不能阻止攻击者针对单个密码进行暴力和字典攻击，高端的显卡(GPUs)和一些定制的硬件每秒可以计算数十亿的hash。</p><p>为了避免针对单个密码的查表和暴力攻击，可以采用一种称为key扩展(key stretching)的技术。就是让hash的过程便得非常缓慢，即使使用高速GPU和特定的硬件，字典和暴力破解的速度也慢到没有实用价值，通过减慢hash的过程来防御攻击。</p><p>key扩展的实现是使用一种大量消耗cpu资源的hash函数。不要去使用自己创造的迭代hash函数，如果迭代不够多，是可以被高效的硬件快速并行计算出来的。要使用标准算法的hash函数，比如PBKDF2或者bcrypt。这类算法使用一个安全因子或迭代次数作为参数，这个值决定了哈希函数会有多慢。</p><p>同时应注意如果在web应用的服务器中做这类hash计算，密钥扩展也使得网站更容易遭受拒绝服务攻击（DoS）。当然也可以考虑在用户的浏览器运行hash函数，这样将计算量都分摊到了客户端，可以减轻服务器的压力，客户端的密钥扩展并不能免除服务端进行哈希加密的职责，服务器必须对客户端传来的哈希值再次进行哈希加密，就像看待一个普通密码一样，如果客户端不支持JS的时候，服务端应该接手计算。</p><h2 id="慢比较是怎么做的"><a href="#慢比较是怎么做的" class="headerlink" title="慢比较是怎么做的"></a>慢比较是怎么做的</h2><p>『慢比较』代码如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"> </span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="type">boolean</span> <span class="title function_">slowEquals</span><span class="params">(<span class="type">byte</span>[] a, <span class="type">byte</span>[] b)</span>&#123;</span><br><span class="line">    <span class="type">int</span> <span class="variable">diff</span> <span class="operator">=</span> a.length ^ b.length;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; a.length &amp;&amp; i &lt; b.length; i++) &#123;</span><br><span class="line">       diff |= a[i] ^ b[i];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> diff == <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>代码里使用异或来比较两个数字，将其应用到整数中每一位，当且仅当字节两个整数各位都相等，结果才是0。所以循环结束后diff是0的话只有一种可能，那就是循环前两个数组长度相等（a.length &#x3D;&#x3D; b.length），并且数组中每一个字节都相同（每次异或的结果都非0）。</p><p>使用异或而不是”&#x3D;&#x3D;”来作比较是因为”&#x3D;&#x3D;”通常会被编译成带有分支的语句，例如C语言中的“diff &amp;&#x3D; a &#x3D;&#x3D; b”可能会被编译为如下汇编语言：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">MOV EAX, [A]</span><br><span class="line">CMP [B], EAX</span><br><span class="line">JZ equal</span><br><span class="line">JMP done</span><br><span class="line">equal:</span><br><span class="line">AND [VALID], 1</span><br><span class="line">done:</span><br><span class="line">AND [VALID], 0</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>其中的分支导致代码运行的时间不固定，决定于两个整数相等的程度和CPU内部的跳转预测机制。<br>而C语言代码“diff |&#x3D;a ^ b”会被编译为下面的样子，它执行的时间和两个整数是什么样的情况无关。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">MOV EAX, [A]</span><br><span class="line">XOR EAX, [B]</span><br><span class="line">OR [DIFF], EAX</span><br><span class="line"></span><br></pre></td></tr></table></figure><h2 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h2><p>对密码进行哈希加密的手段，不是为了保护网站免受入侵，而是在入侵已经发生时保护数据库中的用户密码，以免使得危害继续扩散（如果用户在多个网站使用相同的密码，就容易被撞库），至少我们要对用户负责。</p>]]></content>
      
      
      <categories>
          
          <category> 加解密 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 密码 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>蓄水池抽样（Reservoir sampling）问题</title>
      <link href="/2016/08/20/Reservoir-Sampling/"/>
      <url>/2016/08/20/Reservoir-Sampling/</url>
      
        <content type="html"><![CDATA[<h3 id="蓄水池抽样"><a href="#蓄水池抽样" class="headerlink" title="蓄水池抽样"></a>蓄水池抽样</h3><p>** 问题：** 给出一个数据流，这个数据流的长度很大或者未知。并且对该数据流中数据只能访问一次。请写出一个随机选择算法，使得数据流中所有数据被选中的概率相等。</p><p><strong>思路：</strong> 解决方案就是蓄水池抽样（reservoir sampling）。主要思想就是保持一个集合作为蓄水池，依次遍历所有数据的时候以一定概率替换这个蓄水池中的数字。</p><p>对这个问题首先从最简单的情况出发：</p><ol><li><p>数据流只有一个数据。直接返回该数据，该数据返回的概率为1。</p></li><li><p>假设数据流里有两个数据。  读到了第一个数据，这次不能直接返回该数据，因为数据流没有结束。继续读取第二个数据，发现数据流结束了。因此只要保证以相同的概率返回第一个或者第二个数据就可以满足题目要求。因此我们生成一个0到1的随机数R,如果R小于0.5就返回第一个数据，如果R大于0.5，返回第二个数据。</p></li><li><p>接着分析有三个数据的数据流的情况。假设流中的数据命名为1、2、3。我们陆续收到了数据1、2和前面的例子一样，只能保存一个数据，所以必须淘汰1和2中的一个。首先按照二分之一的概率淘汰一个，例如淘汰了2。继续读取流中的数据3，发现数据流结束了，我们知道在长度为3的数据流中，如果返回数据3的概率为1&#x2F;3,那么才有可能保证选择的正确性。也就是说，目前我们手里有1,3两个数据，我们通过一次随机选择，以1&#x2F;3的概率留下数据3，以2&#x2F;3的概率留下数据1，那么数据1被最终留下的概率是：</p><p> 数据1被留下：（1&#x2F;2）*(2&#x2F;3) &#x3D; 1&#x2F;3</p><p> 数据2被留下概率：（1&#x2F;2）*(2&#x2F;3) &#x3D; 1&#x2F;3</p><p> 数据3被留下概率：1&#x2F;3</p></li></ol><p>因此结论如下：假设当前正要读取第n个数据，则以1&#x2F;n的概率留下该数据，否则留下前n-1个数据中的一个。以这种方法选择，所有数据流中数据被选择的概率一样。证明如下：</p><p>假设n-1时候成立，即前n-1个数据被返回的概率都是1&#x2F;n-1。<br>当前正在读取第n个数据，以1&#x2F;n的概率返回它。那么前n-1个数据中数据被返回的概率为返回n-1个数据中的一个且第n个数据不被选中，即：(1&#x2F;(n-1))*((n-1)&#x2F;n)&#x3D; 1&#x2F;n，假设成立。</p><p>**问题扩展：**给你一个长度为N的链表。N很大，但你不知道N有多大。你的任务是从这N个元素中随机取出k个元素。你只能遍历这个链表一次。你的算法必须保证取出的元素恰好有k个，且它们是完全随机的（出现概率均等）。<br>这次与上面唯一的不同是：总共需要取k个元素。</p><p>**方案：**以1的概率取前k个元素放到蓄水池中，从i&#x3D;k+1开始，以k&#x2F;i的概率取第i个元素，若被取，以均等的概率替换先前被取的k个元素。</p><p>**证明：**当i&#x3D;k+1时：第k+1个元素以k&#x2F;k+1概率被取，前k个元素被取的概率&#x3D;1 - 被第k+1个元素替换的概率&#x3D;1-k&#x2F;(k+1)*1&#x2F;k&#x3D;k&#x2F;(k+1) 符合条件。</p><p>设i&#x3D;p时符合条件，即前p个元素都以k&#x2F;p的概率被取。</p><p>则i&#x3D;p+1时：对第p+1个元素，被取概率为k&#x2F;(p+1)符合条件。 对于前p个元素，每个元素被取的概率&#x3D;被取并且没有被第p+1个元素替换的概率&#x3D; k&#x2F;p*((1-k&#x2F;(p+1))+k&#x2F;(p+1)*(1-1&#x2F;k))&#x3D;k&#x2F;p+1同样符合条件。</p><p>综上所述：得证。</p><h3 id="分布式蓄水池抽样"><a href="#分布式蓄水池抽样" class="headerlink" title="分布式蓄水池抽样"></a>分布式蓄水池抽样</h3><p>要进行容量为k的分布式蓄水池抽样，使用mapreduce 模拟sql中的ORDER BY RAND (随机抽取)。对于集合中的每一个元素，都产生一个0-1的随机数，之后选取随机值最大的前k个元素。这种方法在对大数据集进行分层抽样的时候非常管用。这里每一个分层都都是一些分类变量如性别，年龄，地理信息等的组合。注意如果输入的数据集分布极端的不均匀，那么抽样可能不能覆盖到所有的层级。</p><h3 id="加权分布式蓄水池抽样"><a href="#加权分布式蓄水池抽样" class="headerlink" title="加权分布式蓄水池抽样"></a>加权分布式蓄水池抽样</h3><p>**问题：**集合中的数据是有权重的，算法希望数据被抽样选中的概率和该数据的权重成比例。</p><p>**思路：**对于每个数据计算一个0-1的值R，并求r的n次方根作为该数据的新的R值。这里的n就是该数据的权重。最终算法返回前k个R值最高的数据然后返回。根据计算规则，权重越大的数据计算所得的R值越接近1，所以越有可能被返回。</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>基于词图的最大概率中文分词方法</title>
      <link href="/2016/07/31/%E5%9F%BA%E4%BA%8E%E8%AF%8D%E5%9B%BE%E7%9A%84%E6%9C%80%E5%A4%A7%E6%A6%82%E7%8E%87%E4%B8%AD%E6%96%87%E5%88%86%E8%AF%8D%E6%96%B9%E6%B3%95/"/>
      <url>/2016/07/31/%E5%9F%BA%E4%BA%8E%E8%AF%8D%E5%9B%BE%E7%9A%84%E6%9C%80%E5%A4%A7%E6%A6%82%E7%8E%87%E4%B8%AD%E6%96%87%E5%88%86%E8%AF%8D%E6%96%B9%E6%B3%95/</url>
      
        <content type="html"><![CDATA[<h2 id="分词方法分类"><a href="#分词方法分类" class="headerlink" title="分词方法分类"></a>分词方法分类</h2><p>中文分词大致分为三类：</p><ul><li>基于字符串匹配：最大正向匹配法、逆向最大匹配法、最少切分法、双向匹配法等</li><li>基于统计：基于词频度统计的分词方法</li><li>基于规则：基于知识理解，利用神经网络等分词方法</li></ul><h2 id="语言模型"><a href="#语言模型" class="headerlink" title="语言模型"></a>语言模型</h2><p>基于词图的最大概率分词方法源于概率统计语言模型。<br>从统计思想的角度来看，分词问题的输入是一个字串C&#x3D;C1,C2,……,Cn，输出是一个词串S&#x3D;W1,W2,……,Wm，其中m&lt;&#x3D;n。对于一个特定的字符串C，会有多个切分方案S对应，分词的任务就是在这些S中找出概率最大的一个切分方案，也就是对输入字符串切分出最有可能的词序列。</p><p>$$Seg(c)argmax_{S \epsilon G} P(S|C)&#x3D;argmax_{S \epsilon G}\frac{P(C|S)P(S)}{P(C)}<br>$$</p><p>例如对于输入『小红马上下来』，切分可能有：</p><ul><li>S1: 小红&#x2F;马上&#x2F;下来</li><li>S2: 小红马&#x2F;上&#x2F;下来</li><li>S3: 小红马&#x2F;上下&#x2F;来</li><li>……</li></ul><p>计算条件概率P(S1|C)，P(S2|C)和P(S3|C)，然后采用概率大的值对应的切分方案。根据贝叶斯公式：</p><p>$$P(S|C)&#x3D;\frac{P(C|S)P(S)}{P(C)}$$</p><p>其中P(C)是字符串在语料库中出现的概率为一固定值，从词串恢复到汉字串的概率只有唯一的一种方式，所以P(C|S)&#x3D;1。因此，比较P(S1|C)和P(S2|C)的大小变成比较P(S1)和P(S2)的大小。<br>概率语言模型分词的任务是：在全切分所得的所有结果中求某个切分方案S，使得P(S)最大。为了容易实现，假设每个词之间的概率是上下文无关的。（<strong>注意此处</strong>）</p><p>$$P(S)&#x3D;P(W_1,W_2,W_3 \ldots)&#x3D;P(W_1)*P(W_2)* \cdots*P(W_m)$$</p><p>计算任意一个词出现的概率如下：</p><p>$$P(W_i)&#x3D;\frac{W_i在语料库中出现的次数n_i}{语料库中总词数N}$$</p><h2 id="词典"><a href="#词典" class="headerlink" title="词典"></a>词典</h2><p>词典中存储了词的text，出现次数，词性等信息，结构如下：</p><table><thead><tr><th>词</th><th align="right">出现次数</th><th align="center">词性</th></tr></thead><tbody><tr><td>小红马</td><td align="right">113</td><td align="center">nr</td></tr><tr><td>巴塞尔</td><td align="right">113</td><td align="center">nr</td></tr><tr><td>巴塞爾</td><td align="right">113</td><td align="center">nr</td></tr><tr><td>文明史</td><td align="right">113</td><td align="center">nr</td></tr></tbody></table><p>为了能够快速查找字典采用双数组Trie树来存储，关于Trie树相关内容请自行百度。</p><h2 id="构建词图"><a href="#构建词图" class="headerlink" title="构建词图"></a>构建词图</h2><p>根据字典构建的trie树，扫描待分词句子生成DAG（有向无环图），就是对待分词句子，根据给定的词典进行查词典操作，找出所有可能的句子切分。<br>如果待切分的字符串有m个字符，考虑每个字符左边和右边的位置，则有m+1个点对应，点的编号从0到m。把候选词看成边，可以根据词典生成一个切分词图。例如『小红马上下来』形成的词图如下：</p><figure class="image-bubble">                <div class="img-lightbox">                    <div class="overlay"></div>                    <img src="/img/WordSeg.png" alt="WordSeg" title="">                </div>                <div class="image-caption">WordSeg</div>            </figure><p>路径1： 0-3-4-6  小红马&#x2F;上&#x2F;下来<br>路径2： 0-2-4-6  小红&#x2F;马上&#x2F;下来<br>……<br>切分词图中的边都是词典中的词，边的起点和终点分别是词的开始和结束位置。</p><h2 id="计算最大概率"><a href="#计算最大概率" class="headerlink" title="计算最大概率"></a>计算最大概率</h2><p>因为假设每个词之间的概率是上下文无关的，因此满足动态规划求解所要求的最优子结构性质和无后效性。求出值最大的P(Si)后，利用回溯的方法直接输出Si。图用邻接矩阵存储，伪代码如下：</p><figure class="highlight golang"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">type</span> Token <span class="keyword">struct</span> &#123;</span><br><span class="line"><span class="comment">//分词的字串</span></span><br><span class="line">text []<span class="type">byte</span></span><br><span class="line"><span class="comment">//该分词概率</span></span><br><span class="line">Prod <span class="type">float32</span></span><br><span class="line"><span class="comment">// 词性标注</span></span><br><span class="line">pos <span class="type">string</span></span><br><span class="line"><span class="comment">//分词开始节点的概率</span></span><br><span class="line">PredProd <span class="type">float32</span></span><br><span class="line">    </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">segmentWords(<span class="type">byte</span> []s) []Segment&#123;</span><br><span class="line">    <span class="keyword">for</span> i:=<span class="number">0</span>; i&lt;<span class="built_in">len</span>(s); i++ &#123;</span><br><span class="line">        <span class="comment">// 从Trie树中得到以当前字元开头的所有分词</span></span><br><span class="line">        tokens = dict.getPrev(i)</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">for</span> j:=<span class="number">0</span>; j&lt;<span class="built_in">len</span>(token); j++ &#123;</span><br><span class="line">            newProd := token[j].PrevProd + token[j].Prod</span><br><span class="line">            <span class="keyword">if</span>(newProd &gt; dis[token.end].prod) &#123;</span><br><span class="line">                dis[token.end].prod = newProd</span><br><span class="line">                dis[token.end].token = token[j]</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//从右向左扫描</span></span><br><span class="line">    numSegments := <span class="number">0</span></span><br><span class="line"><span class="keyword">for</span> index := <span class="built_in">len</span>(text) - <span class="number">1</span>; index &gt;= <span class="number">0</span>; &#123;</span><br><span class="line"></span><br><span class="line">location := index - <span class="built_in">len</span>(dis[index].token.text) + <span class="number">1</span></span><br><span class="line">numSegments++</span><br><span class="line">index = location - <span class="number">1</span></span><br><span class="line">&#125;</span><br><span class="line">output := <span class="built_in">make</span>([]Segment, numSeg)</span><br><span class="line">    <span class="keyword">for</span> index:=<span class="built_in">len</span>(s)<span class="number">-1</span>; index&gt;=<span class="number">0</span>; &#123;</span><br><span class="line">        location := index - <span class="built_in">len</span>(dis[index].token.text) + <span class="number">1</span></span><br><span class="line">numSegments--</span><br><span class="line">output[numSegments].text = dis[index].token.text</span><br><span class="line">index = location - <span class="number">1</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>从右向左扫描是因为汉语句子的重心经常落在后面，这种扫描方式能提高分词的准确性。</p><h2 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h2><p>该模型有一个前提条件：『假设每个词之间的概率是上下文无关的』，这一点是不严谨的，一句话当中相邻词之间上下文无关很难做到。</p><p>搜索引擎中的中文分词十分复杂，分词的过程中需要考虑：</p><ul><li>切分的正确性</li><li>粒度的合理性</li><li>切分的一致性</li><li>词条关系分析</li><li>词条属性分析  </li><li>歧义词处理</li><li>……</li></ul>]]></content>
      
      
      <categories>
          
          <category> NLP </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 分词 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>OpenSSL</title>
      <link href="/2016/06/19/openssl/"/>
      <url>/2016/06/19/openssl/</url>
      
        <content type="html"><![CDATA[<h3 id="SSL"><a href="#SSL" class="headerlink" title="SSL"></a>SSL</h3><hr><p>SSL 是一个缩写，代表的是 Secure Sockets Layer。它是支持在 Internet 上进行安全通信的标准，并且将数据密码术集成到了协议之中。数据在离开您的计算机之前就已经被加密，然后只有到达它预定的目标后才被解密。证书和密码学算法支持了这一切的运转，如果连接传输敏感信息，则应使用 SSL。</p><h3 id="OpenSSL"><a href="#OpenSSL" class="headerlink" title="OpenSSL"></a>OpenSSL</h3><hr><p>openssl是一个开源程序的套件、这个套件有三个部分组成：</p><ul><li>一是libcryto，这是一个具有通用功能的加密库，里面实现了众多的加密库；</li><li>二是libssl，这个是实现ssl机制的，它是用于实现TLS&#x2F;SSL的功能；</li><li>三是openssl，是个多功能命令行工具，它可以实现加密解密，甚至还可以当CA来用，可以让你创建证书、吊销证书。</li></ul><p>要获得关于如何使用 OpenSSL 命令行工具的资料，请参阅<a href="https://www.openssl.org/docs/manmaster/apps/openssl.html">官方手册</a></p><h3 id="密钥、证书请求、证书概要说明"><a href="#密钥、证书请求、证书概要说明" class="headerlink" title="密钥、证书请求、证书概要说明"></a>密钥、证书请求、证书概要说明</h3><hr><p>在申请证书过程中，涉及到密钥，证书请求，证书这几个概念，它们之间的联系是：</p><ul><li>首先，生成客户端的密钥，即客户端的公私钥对，且要保证私钥只有客户端自己拥有。</li><li>然后以客户端的密钥和客户端自身的信息(国家、机构、域名、邮箱等)为输入，生成证书请求文件。其中客户端的公钥和客户端信息是明文保存在证书请求文件中的，而客户端私钥的作用是对客户端公钥及客户端信息做签名，自身是不包含在证书请求中的。然后把证书请求文件发送给CA机构。</li><li>最后CA机构接收到客户端的证书请求文件后，首先校验其签名，然后审核客户端的信息，最后CA机构使用自己的私钥为证书请求文件签名，生成证书文件，下发给客户端。此证书就是客户端的身份证，来表明用户的身份。</li></ul><p>其中，证书签发机构CA，CA是被绝对信任的机构。<br>自签名证书就是自己给自己签发的证书，例如12306网站，它用的自签名证书，以为12306不是浏览器所信任的证书签发机构，所以浏览器会有提示证书有问题，将其的证书导入到电脑中，浏览器就不会报该错误。除非特别相信某个机构，否则不要在机器上随便导入证书，这种做法存在安全风险。</p><h3 id="OpenSSL使用"><a href="#OpenSSL使用" class="headerlink" title="OpenSSL使用"></a>OpenSSL使用</h3><hr><p>生成RSA密钥对。使用DES3加密，密钥使用密码保护，长度为1024，输出到rsaprivatekey.pem</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">openssl genrsa -des3 -out rsaprivatekey.pem -passout pass:123456 1024</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>openssl可以将这个文件中的公钥提取出来:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">openssl rsa -in rsaprivatekey.pem -pubout -out test_pub.key</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>当使用-new选取的时候，说明是要生成证书请求，当使用x509选项的时候，说明是要生成自签名证书。</p><p>生成证书请求文件</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">openssl req -new -key rsaprivatekey.pem -out rsaCertReq.csr</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>或者生成1024位RSA密钥，并生成证书请求文件</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">openssl req -new -newkey rsa:1024 -out rsaCertReq.csr -keyout RSA.pem -batch</span><br></pre></td></tr></table></figure><p>生成自签证书并指定过期时间</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">openssl x509 -req -days 3650 -in rsaCertReq.csr -signkey rsaprivatekey.pem -out rsaCert.crt</span><br></pre></td></tr></table></figure><p>或者</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">openssl req -new -x509 -key rsaprivatekey.pem -out rsaprivatekey.cert -days 1095</span><br></pre></td></tr></table></figure><p>证书里有对应的公钥和国家、地区等信息。验证证书：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">openssl verify rsaprivatekey.cert</span><br></pre></td></tr></table></figure><p>从证书中提取公钥：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">openssl x509 -in rsaprivatekey.cert  -noout -pubkey &gt; pubkey.pem</span><br></pre></td></tr></table></figure><p>生成pem结尾的私钥供Java使用:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">openssl pkcs8 -topk8 -inform PEM -outform DER -in ca.key.pem -out ca.private.der -nocrypt</span><br></pre></td></tr></table></figure><h3 id="举个栗子"><a href="#举个栗子" class="headerlink" title="举个栗子"></a>举个栗子</h3><hr><p>产生1024位RSA私匙，用3DES加密它，口令为123456。输出到文件rsaprivatekey_pass.pem：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">openssl genrsa -out rsaprivatekey_pass.pem -passout pass:123456 -des3 1024</span><br></pre></td></tr></table></figure><p>生成证书,days为有效天数，输出证书文件到rsaprivatekey.cert：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">openssl req -new -x509 -key rsaprivatekey_pass.pem -out rsaprivatekey.cert -days 1095 -passin pass:123456</span><br></pre></td></tr></table></figure><p>此证书文件中包含公钥，用于和对方进行公开的交换。</p><p>使用私钥对test.txt文本内容进行数字签名，输出到test.sign</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">openssl rsautl -sign -in test.txt -out test.sign -inkey rsaprivatekey_pass.pem -passin pass:123456</span><br></pre></td></tr></table></figure><p>使用公钥证书对数字签名进行验证，输出到test.vfy，此时test.vfy和test.txt的内容应完全一样:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">openssl rsautl -verify -in test.sig -out test.vfy -inkey rsaprivatekey.cert -certin</span><br><span class="line"></span><br></pre></td></tr></table></figure><h3 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h3><hr><p>这里只是简单介绍了openssl的一些用法，其他内容推荐查看官方文档。</p><p>关于数字签名的详细介绍，推荐[阮一峰的一篇日志][2]<br>[2]:<a href="http://www.ruanyifeng.com/blog/2011/08/what_is_a_digital_signature.html">http://www.ruanyifeng.com/blog/2011/08/what_is_a_digital_signature.html</a></p>]]></content>
      
      
      
        <tags>
            
            <tag> opnessl </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>AngularJS中ui-router如何传递参数</title>
      <link href="/2016/04/09/AngularJS%E4%B8%ADui-router%E5%A6%82%E4%BD%95%E4%BC%A0%E9%80%92%E5%8F%82%E6%95%B0/"/>
      <url>/2016/04/09/AngularJS%E4%B8%ADui-router%E5%A6%82%E4%BD%95%E4%BC%A0%E9%80%92%E5%8F%82%E6%95%B0/</url>
      
        <content type="html"><![CDATA[<p>ui-router是AngualrJS中常用的路由框架。其中ui-sref 一般使用在 a标签中，$state.go(‘someState’)一般使用在controller里面。这两个本质上是一样的东西，查看ui-sref的源码，ui-sref最后调用的还是$state.go()方法。</p><h3 id="如何传递参数"><a href="#如何传递参数" class="headerlink" title="如何传递参数"></a>如何传递参数</h3><p>首先定义路由状态：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">.<span class="title function_">state</span>(<span class="string">&#x27;monitorInstance&#x27;</span>, &#123;</span><br><span class="line">   <span class="attr">url</span>: <span class="string">&quot;/monitorInstance/:instanceId/:templateId&quot;</span>,</span><br><span class="line">   <span class="attr">views</span>: &#123;</span><br><span class="line">    <span class="string">&quot;appContent&quot;</span> :&#123;</span><br><span class="line">       <span class="attr">templateUrl</span>: baseUrl +<span class="string">&#x27;/tpl/monitorInstance.html&#x27;</span>,</span><br><span class="line">       <span class="attr">controller</span>: <span class="title class_">MonitorInstanceCtrl</span></span><br><span class="line">    &#125;</span><br><span class="line"> &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>ui-sref传参：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">&lt;a ui-sref=<span class="string">&quot;monitorInstance(&#123;instanceId: &#123;&#123;instance.id&#125;&#125;,  templateId: &#123;&#123;instance.template.id&#125;&#125; &#125;)&quot;</span>&gt;</span><br><span class="line">    <span class="language-xml"><span class="tag">&lt;<span class="name">span</span>&gt;</span>&#123;&#123;instance.name&#125;&#125;<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span></span><br><span class="line">&lt;/a&gt;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>$state.go传参：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$state.<span class="title function_">go</span>(<span class="string">&#x27;monitorInstance&#x27;</span>, &#123;<span class="attr">instanceId</span>: instance.<span class="property">id</span>, <span class="attr">templateId</span>:instance.<span class="property">template</span>.<span class="property">id</span>&#125;);</span><br></pre></td></tr></table></figure><p>接收参数：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">listInstanceCtrl</span>(<span class="params">$scope,$stateParams,listInstanceServices</span>) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>($stateParams.<span class="property">instanceId</span>);</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>($stateParams.<span class="property">templateId</span>);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> 前端 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> AngularJS </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Go语言的接口</title>
      <link href="/2015/12/02/Go%E8%AF%AD%E8%A8%80%E7%9A%84%E6%8E%A5%E5%8F%A3/"/>
      <url>/2015/12/02/Go%E8%AF%AD%E8%A8%80%E7%9A%84%E6%8E%A5%E5%8F%A3/</url>
      
        <content type="html"><![CDATA[<p>在计算机的世界里，同一类工具不同的实现所体现出背后的哲理也是不一样的，例如 Linux 与 windows，都是操作系统，但是如果用使用windows的习惯去操作Linux是玩不转的。编程语言也一样，对于面向对象不同的语言也同过不同的方式来实现。java不支持类的多重继承，但是可以通过接口的多重继承来弥补。Python干脆在语言层面上就不提供接口这样的特性，所以要想实现接口的效果可以采用继承只有方法空实现的父类并重写父类方法来达到目的。而Go语言提供了更加灵活和抽象的接口特性。</p><p>C++、Java使用的是“侵入式”的接口，即实现类需要显式的声明自己所实现的接口，而Go采用的是“非侵入式”的接口，从语言角度看，接口是一种类型，它指定一个方法集。只要类型T的公开方法完全满足接口I的要求，就可以把类型T的对象用在需要接口I的地方，所谓类型T的公开方法完全满足接口I的要求，也就是如果一个接口里的所有方法都被类型T实现了，那么我们就说该类型实现了该接口。这种做法的学名叫做Structural Typing，有人也把它看作是一种静态的Duck Typing。</p><h2 id="接口"><a href="#接口" class="headerlink" title="接口"></a>接口</h2><p>定义一个接口如下</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> Driver <span class="keyword">interface</span>&#123;</span><br><span class="line">    Drive()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>我们定义了一个接口Driver，该接口中声明了一个方法Drive，接下来我们看如何定义一个struct来实现该接口。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> Person sturct&#123;</span><br><span class="line">    Name stirng</span><br><span class="line">    Car  <span class="type">string</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(p *Person)</span></span> Drive() &#123;</span><br><span class="line">    fmt.Println(<span class="string">&quot;Drive my&quot;</span>, p.Car)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>定义了一个方法，该方法的Receiver是Person，并且该方法完全符合接口Driver的要求所以Person就实现了该接口。</p><p>下面我们定义一个方法用来接受任意一个实现Driver接口的类型的值或者指针。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">p := Person&#123;<span class="string">&quot;Grace&quot;</span>, <span class="string">&quot;BMW&quot;</span>&#125;</span><br><span class="line"></span><br><span class="line">DriveCar(p)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">DriveCar</span><span class="params">(d Driver)</span></span> &#123;</span><br><span class="line">d.Drive()</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//OutPut:</span></span><br><span class="line">cannot use p (<span class="keyword">type</span> Person) as <span class="keyword">type</span> Driver in argument to DriveCar:</span><br><span class="line">Person does not implement Driver (Drive method has pointer receiver)</span><br></pre></td></tr></table></figure><p>诶？为什么编译器不考虑值是实现该接口的类型？这就涉及到Go语言规范里的一些规则：</p><ul><li>类型 <code>*T</code>的可调用方法集包含接受者为 <code>*T</code> 或 <code>T</code> 的所有方法集</li></ul><p>这条规则是说如果用来调用特定接口方法的接口变量是一个指针类型，那么方法的接受者可以是值类型也可以是指针类型。显然刚才的例子不符合该规则，因为我们传入 <code>DriveCar</code> 函数的接口变量是一个值类型。</p><ul><li>类型 <code>T</code> 的可调用方法集包含接受者为 <code>T</code> 的所有方法</li></ul><p>这条规则是说如果用来调用特定接口方法的接口变量是一个值类型，那么方法的接受者必须也是值类型该方法才可以被调用。显然上面的例子也不符合这条规则，因为我们 <code>Drive</code> 方法的接受者是一个指针类型。<br>由此可以得出以下结论：</p><ul><li>类型 <code>T</code>的可调用方法集不包含接受者为 <code>*T</code> 的方法</li></ul><p>所以我们只需将Person的地址传给DriveCar即可</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">p := &amp;Person&#123;<span class="string">&quot;Grace&quot;</span>, <span class="string">&quot;BMW&quot;</span>&#125;</span><br><span class="line"></span><br><span class="line">DriveCar(p)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">DriveCar</span><span class="params">(d Driver)</span></span> &#123;</span><br><span class="line">d.Drive()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>类型的指针同样可以调用接受者是值的方法。Go调整和解引用指针使得调用可以被执行。注意，当接受者不是一个指针时，该方法操作对应接受者的值的副本(意思就是即使你使用了指针调用函数，但是函数的接受者是值类型，所以函数内部操作还是对副本的操作，而不是指针操作）</p><h2 id="内嵌类型"><a href="#内嵌类型" class="headerlink" title="内嵌类型"></a>内嵌类型</h2><p>struct中的一个属性为另一个struct即为内嵌，定义一个新类型然后潜入Person：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> Manager <span class="keyword">struct</span> &#123;</span><br><span class="line">Person</span><br><span class="line">Age <span class="type">int</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这种方式是组合而不是集成，现在修改调用方法：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"></span><br><span class="line">m := &amp;Manager&#123;</span><br><span class="line"></span><br><span class="line">Person: Person&#123;</span><br><span class="line">Name: <span class="string">&quot;xiaoming&quot;</span>,</span><br><span class="line">Car:  <span class="string">&quot;BMW&quot;</span>,</span><br><span class="line">&#125;,</span><br><span class="line">Age: <span class="number">22</span>,</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">DriveCar(m)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//OutPut:</span></span><br><span class="line">Drive my BMW</span><br></pre></td></tr></table></figure><p>解释如下</p><blockquote><p>当我们嵌入一个类型，这个类型的方法就变成了外部类型的方法，但是当它被调用时，方法的接受者是内部类型(嵌入类型)，而非外部类型。— Effective Go</p></blockquote><p>我们可以用 <code>Manager</code> 类型的一个指针来调用 <code>DriveCar</code> 函数。现在 <code>Manager</code> 类型也通过来自嵌入的 <code>Person</code> 类型的方法提升实现了该接口。</p><p>Go 语言中内部类型方法集提升的规则有如下三条：</p><p>给定一个结构体类型<code>S</code>和一个命名为<code>T</code>的类型，方法提升像下面规定的这样被包含在结构体方法集中：</p><ul><li>如果 <code>S</code> 包含一个匿名字段<code>T</code>，<code>S</code>和<code>*S</code>的方法集都包含接受者为T的方法提升。</li></ul><p>意思是当嵌入一个类型时嵌入类型的接受者为值类型的方法将被提升，可以被外部类型的值和指针调用。</p><ul><li>对于 <code>*S</code> 类型的方法集包含接受者为 <code>*T</code> 的方法提升</li></ul><p>这条规则说的是当外部类型使用指针调用内部类型的方法时，只有接受者为指针类型的内部类型方法集将被提升。</p><ul><li>如果 <code>S</code> 包含一个匿名字段 <code>*T</code>，<code>S</code> 和 <code>*S</code> 的方法集都包含接受者为 <code>T</code> 或者 <code>*T</code> 的方法提升</li></ul><p>这条规则说的是嵌入类型的接受者为值类型或指针类型的方法将被提升，可以被外部类型的值或者指针调用。</p><p>所以如果外部类型包含了符合要求的接口实现，它将会被使用。否则，通过方法提升，任何内部类型的接口实现可以直接被外部类型使用。</p>]]></content>
      
      
      <categories>
          
          <category> Go </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Go </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>数据库表空间</title>
      <link href="/2015/08/16/%E6%95%B0%E6%8D%AE%E5%BA%93%E8%A1%A8%E7%A9%BA%E9%97%B4/"/>
      <url>/2015/08/16/%E6%95%B0%E6%8D%AE%E5%BA%93%E8%A1%A8%E7%A9%BA%E9%97%B4/</url>
      
        <content type="html"><![CDATA[<p>数据库表空间允许数据库管理员定义存储数据库对象的文件在文件系统中的位置。一旦创建了表空间，当创建数据库时就可以引用这个表空间。</p><p>通过表空间来控制数据库的磁盘存储位置有一下好处：<br>1.如果数据库初始安装所在的分区或卷耗尽了空间，并且已经无法扩展，可以在另外的分区上面创建和使用一个新的表空间，直到系统重新被配置。<br>2.表空间可以允许管理员根据已知的数据库对象使用模式优化系统性能。比如，将频繁使用的index放在快速高可用的固态硬盘上;将存储很少使用或者对性能要求不高的数据放在廉价低速的磁盘上。</p><p>下面以PostgresSQL为例讲一下表空间的使用。</p><h3 id="创建表空间"><a href="#创建表空间" class="headerlink" title="创建表空间"></a>创建表空间</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> TABLESPACE tablespace_name [ OWNER user_name ] LOCATION <span class="string">&#x27;directory&#x27;</span>;</span><br></pre></td></tr></table></figure><p>参数<br>*tablespace_name 表空间的名字，不能以pg_开头，这是为系统表空间保留的。<br>*user_name 表空间所有者的名字，如果省略，缺省为执行命令的用户。只有数据库超级用户才可以创建表空间<br>*directory 表空间使用的路径，目录必须是空的，并且owner为PostgreSQL操作系统用户。通常情况PostgreSQL默认安装的系统用户为postgres。目录必须是一个绝对路径。</p><p>注意<br>*只有支持符号链接的系统才能创建表空间<br>*CREATE TABLESPACE不能在一个事务块中执行</p><p>示例</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> TABLESPACE ts_mydbspace OWNER testuser LOCATION <span class="string">&#x27;/var/data/postgres&#x27;</span>;</span><br></pre></td></tr></table></figure><h3 id="使用表空间"><a href="#使用表空间" class="headerlink" title="使用表空间"></a>使用表空间</h3><p>只有数据库超级用户才可以创建表空间，但是创建之后，普通的数据库用户就可以使用它了，只要用户有相应的CREATE权限。比如：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> DATABASE testdb TABLESPACE ts_mydbspace;</span><br></pre></td></tr></table></figure><p>设置缺省的表空间参数</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SET</span> default_tablespace <span class="operator">=</span> ts_mydbspace;</span><br><span class="line"><span class="keyword">CREATE</span> DATABASE testdb;</span><br></pre></td></tr></table></figure><p>数据库创建时指定的表空间为其默认表空间，如果没有指定，则其默认表空间与生成该数据库的模板数据库的默认表空间是同一个。</p><h3 id="查看表空间"><a href="#查看表空间" class="headerlink" title="查看表空间"></a>查看表空间</h3><p>当PostgeSQL初始化时，自动创建两个表空间。pg_global用于存储共享的系统目录信息。pg_default是模板数据库template0和template1默认的数据库表空间，也是其他数据库默认的表空间。</p><p>也就是说在执行initdb时，pg的后端（无需经过SQL查询编译）以bootstrap模式执行，在bootstrap模式下，从零开始创建数据库模板template1、创建系统视图、系统表、template0和postgres数据库。template0和postgres数据库都是从template1创建的，这3个都是pg的系统数据库，其中template1在initdb完成之后是可以由用户修改的，template0则始终提供一个未被修改的干净模板。postgres数据库提供一个初始的可供用户连接的数据库。</p><p>由于CREATE DATABASE dbname语句并没有指明数据库模板，所以系统将默认克隆template1数据库，得到新的数据库dbname。（By default, the new database will be created by cloning the standard system database template1）.而template1数据库的默认表空间是pg_default，这个表空间是在数据库初始化时创建的，所以所有template1中的对象将被同步克隆到新的数据库中，新的数据库的表空间也是pg_default。相对完整的语法如下:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> DATABASE dbname OWNER username TEMPLATE template1 TABLESPACE tablespacename;</span><br></pre></td></tr></table></figure><p>查看表空间：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">postgres<span class="operator">=</span># <span class="keyword">select</span> oid, spcname <span class="keyword">from</span> pg_tablespace;</span><br><span class="line"> oid  <span class="operator">|</span>  spcname   </span><br><span class="line"><span class="comment">------+------------</span></span><br><span class="line"> <span class="number">1663</span> <span class="operator">|</span> pg_default</span><br><span class="line"> <span class="number">1664</span> <span class="operator">|</span> pg_global</span><br><span class="line">(<span class="number">2</span> <span class="keyword">rows</span>)</span><br></pre></td></tr></table></figure><p>或者</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">postgres<span class="operator">=</span># \db</span><br><span class="line">       List <span class="keyword">of</span> tablespaces</span><br><span class="line">    Name    <span class="operator">|</span>  Owner   <span class="operator">|</span> Location </span><br><span class="line"><span class="comment">------------+----------+----------</span></span><br><span class="line"> pg_default <span class="operator">|</span> postgres <span class="operator">|</span> </span><br><span class="line"> pg_global  <span class="operator">|</span> postgres <span class="operator">|</span> </span><br><span class="line">(<span class="number">2</span> <span class="keyword">rows</span>)</span><br><span class="line"></span><br></pre></td></tr></table></figure><h3 id="PG数据库的文件目录"><a href="#PG数据库的文件目录" class="headerlink" title="PG数据库的文件目录"></a>PG数据库的文件目录</h3><p>PGDATA下的目录，最基本的是base和global。base中放了每个数据的文件，每个数据库会在base目录下有一个以该数据库oid（object id，pg具有面向对象数据库的一些特性，其中的数据库、视图、数据表，甚至某些元组都被作为数据对象管理，每个数据对象有一个唯一oid）命名的子目录。其中命名为1的子目录是pg中的模板数据库template1，pg中的数据库都会以template1作为模板，也就是说template1中的内容会被复制到所有的数据库中。global目录下存放了该实例的共享系统表，如pg_database（存放该实例中所有数据库的元信息）。</p><p>可以通过 查看 pg_database 这张表查看每一个 数据库的 oid</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">postgres<span class="operator">=</span># <span class="keyword">select</span> oid , datname <span class="keyword">from</span> pg_database;</span><br><span class="line">  oid  <span class="operator">|</span>  datname  </span><br><span class="line"><span class="comment">-------+-----------</span></span><br><span class="line">     <span class="number">1</span> <span class="operator">|</span> template1</span><br><span class="line"> <span class="number">12067</span> <span class="operator">|</span> template0</span><br><span class="line"> <span class="number">12072</span> <span class="operator">|</span> postgres</span><br><span class="line"> <span class="number">16385</span> <span class="operator">|</span> testdb</span><br><span class="line">(<span class="number">4</span> <span class="keyword">rows</span>)</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>每一张表的数据（大部分）又是放在 base&#x2F;(dboid)&#x2F;(relfilenode) 这个文件里面，连接testdb数据库：<code>\c testdb</code>，查看该数据库表信息：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">testdb<span class="operator">=</span># <span class="keyword">select</span> relname, relowner, relfilenode <span class="keyword">from</span> pg_class <span class="keyword">where</span> relowner <span class="operator">=</span> <span class="number">16384</span>;</span><br><span class="line">      relname       <span class="operator">|</span> relowner <span class="operator">|</span> relfilenode </span><br><span class="line"><span class="comment">--------------------+----------+-------------</span></span><br><span class="line"> book               <span class="operator">|</span>    <span class="number">16384</span> <span class="operator">|</span>       <span class="number">16402</span></span><br><span class="line"> book_pkey          <span class="operator">|</span>    <span class="number">16384</span> <span class="operator">|</span>       <span class="number">16405</span></span><br><span class="line"> hibernate_sequence <span class="operator">|</span>    <span class="number">16384</span> <span class="operator">|</span>       <span class="number">16407</span></span><br><span class="line">(<span class="number">3</span> <span class="keyword">rows</span>)</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>其中16384是数据库用户的oid。由此可见book这张表的数据存放在<code>base/16385/16402</code>文件中。也可以用 pg_relation_filepath 这个函数查询：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">testdb<span class="operator">=</span># <span class="keyword">select</span> pg_relation_filepath(<span class="string">&#x27;book&#x27;</span>);</span><br><span class="line"> pg_relation_filepath </span><br><span class="line"><span class="comment">----------------------</span></span><br><span class="line"> base<span class="operator">/</span><span class="number">16385</span><span class="operator">/</span><span class="number">16402</span></span><br><span class="line">(<span class="number">1</span> <span class="type">row</span>)</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>数据库中的每张数据表的索引和数据都存放同一个文件中。每张表除了存放数据和索引的文件外，还会有一个_fsm文件(free space map)，其中存放了数据表文件中空闲空间的信息，还有一个_vm文件(visibility map)，标记了数据表文件中哪些文件块没有失效的元组。</p><p>对于数据表文件，pg采取段页式管理方式，每个段的默认大小是1GB。每个段都是一个文件，也就是说，默认配置下，pg中的一个数据表超过1GB时会分为多个1GB大小的文件，第一个段文件名依然是filenode number，第二个段文件是filenode number.1，依此类推。如 24589 24589.1 24589.2。</p><p>对于数据表中的大对象，pg会将其存放在另外的一个TOAST表中，因为大对象不适合与其他属性一起按行存放在段页中。</p>]]></content>
      
      
      <categories>
          
          <category> Database </category>
          
      </categories>
      
      
        <tags>
            
            <tag> PostgreSQL </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>数据库的schema</title>
      <link href="/2015/08/16/%E6%95%B0%E6%8D%AE%E5%BA%93%E7%9A%84schema/"/>
      <url>/2015/08/16/%E6%95%B0%E6%8D%AE%E5%BA%93%E7%9A%84schema/</url>
      
        <content type="html"><![CDATA[<p>schema是对数据库逻辑的分割，schema隶属于数据库。一个数据库可以包含多个schema。</p><p>schema可以包含多种命名对象，例如：数据类型、函数等。不同的schema中可以包含相同的对象名而不会冲突。</p><p>使用schema的原因主要有：</p><ol><li>允许多个用户使用同一个数据库而互不干扰</li><li>将数据库对象进行逻辑分组，便于管理</li><li>第三方应用放在单独的schema，不与其他对象发生冲突</li></ol><p>下面以PostgresSQL为例说一下schema的使用</p><h3 id="创建schema"><a href="#创建schema" class="headerlink" title="创建schema"></a>创建schema</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> SCHEMA schema_name [ <span class="keyword">AUTHORIZATION</span> user_name ] [ schema_element [ ... ] ]</span><br><span class="line"></span><br></pre></td></tr></table></figure><ul><li>schema_name<br> 将要创建的schema的名字，在当前数据库中schema的名字不能冲突。如果忽略此参数，则以当前数据库用户的名字命名新创建的schema。schema的名字不能以pg_开头，这是系统保留的名字。</li><li>user_name<br> 拥有新创建schema的角色名字，如果不指定则为执行当前命令的数据库用户。</li><li>schema_element<br> 创建schema名字空间下其他对象的SQL语句。与创建schema完毕后执行单独的SQL来创建对象是一样的，除了如果指定AUTHORIZATION,那么新创建的对象都有指定的角色拥有</li></ul><p>每个新建数据库包含一个默认的public模式，数据库中没有指定的schema对象都归属于public schema</p><h3 id="查询schema"><a href="#查询schema" class="headerlink" title="查询schema"></a>查询schema</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">postgres<span class="operator">-</span># \dn</span><br><span class="line">  List <span class="keyword">of</span> schemas</span><br><span class="line">  Name  <span class="operator">|</span>  Owner   </span><br><span class="line"><span class="comment">--------+----------</span></span><br><span class="line"> public <span class="operator">|</span> postgres</span><br><span class="line">(<span class="number">1</span> <span class="type">row</span>)</span><br><span class="line"></span><br></pre></td></tr></table></figure><h3 id="删除schema"><a href="#删除schema" class="headerlink" title="删除schema"></a>删除schema</h3><p>如果schema已经为空对象，可以用一下语句删除schema</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">DROP</span> SCHEMA myschema;</span><br></pre></td></tr></table></figure><h3 id="schema搜索路径"><a href="#schema搜索路径" class="headerlink" title="schema搜索路径"></a>schema搜索路径</h3><p>全限定的名字写起来十分冗长，系统通过一个搜索路径来决定到底使用的是哪一张表，搜索路径是schema的一个列表。搜索路径中第一个匹配的表即是要访问的表。如果搜索路径中没有匹配，会报告一个错误，即使在数据库的其他schema中有相匹配的表。</p><p>查看当前额搜索路径：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">postgres<span class="operator">=</span># <span class="keyword">SHOW</span> search_path;</span><br><span class="line">  search_path   </span><br><span class="line"><span class="comment">----------------</span></span><br><span class="line"> &quot;$user&quot;,public</span><br><span class="line">(<span class="number">1</span> <span class="type">row</span>)</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>默认情况下，搜索路径的第一个schema是与当前用户同名的schema,第二个则是public schema。</p><p>可以这样设置搜索路径：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SET</span> search_path <span class="keyword">TO</span> myschema,public;</span><br></pre></td></tr></table></figure><p>**官方建议是这样的：**在管理员创建一个具体数据库后，应该为所有可以连接到该数据库的用户分别创建一个与用户名相同的模式，然后，将search_path设置为”$user”，这样，任何当某个用户连接上来后，会默认将查找或者定义的对象都定位到与之同名的模式中。这是一个好的设计架构。</p><p>默认情况下，用户不能访问不属于他的schema中的任何对象。要允许访问，schema的拥有者必须授予用户在这个schema上的USAGE权限。不同访问权限需要不同的授权。</p><h3 id="System-Catalog-Schema"><a href="#System-Catalog-Schema" class="headerlink" title="System Catalog Schema"></a>System Catalog Schema</h3><ul><li>如果不创建任何schema，则所有用户隐式的使用public schema。这等同于不使用schema。这在数据库中只有一个或者极少的用户时推荐使用。</li><li>可以为每一个用户创建一个与其用户名相同的schema。如果每个用户有一个与其名字相同的单独的schema，则默认他们只能访问自己所属的schema。使用这种schema范式，可以撤销掉对public schema的访问许可，甚至把public schema直接移除，这样每个用户就真正的限定在了他们自己的schema里</li></ul>]]></content>
      
      
      <categories>
          
          <category> Database </category>
          
      </categories>
      
      
        <tags>
            
            <tag> PostgreSQL </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>关于前后端分离的一些想法</title>
      <link href="/2015/07/30/%E5%85%B3%E4%BA%8E%E5%89%8D%E5%90%8E%E7%AB%AF%E5%88%86%E7%A6%BB%E7%9A%84%E4%B8%80%E4%BA%9B%E6%83%B3%E6%B3%95/"/>
      <url>/2015/07/30/%E5%85%B3%E4%BA%8E%E5%89%8D%E5%90%8E%E7%AB%AF%E5%88%86%E7%A6%BB%E7%9A%84%E4%B8%80%E4%BA%9B%E6%83%B3%E6%B3%95/</url>
      
        <content type="html"><![CDATA[<h3 id="为什么要前端分离？"><a href="#为什么要前端分离？" class="headerlink" title="为什么要前端分离？"></a>为什么要前端分离？</h3><p>　　伴随着软件行业的快速发展，其所涉及到的方方面面的技术也越来越细分和专业化。从各公司的招聘信息便可见一斑，例如：前端工程师、算法工程师、java工程师等。</p><p>　　除此之外，当下的Web系统开发模式跟以往已经有了很大的不同，早期的Web项目是一个封闭的项目，用户从浏览器里看到的页面直到后台数据库都是在一个项目里集成。而现在Web系统的规模越来越大，其架构也发生了变化，大型的Web系统可能会是一个分布式、开放式的系统。数据的请求可能不会直接请求数据库，而是访问的缓存服务器，这些基本都是服务端的变化。如果依旧按照之前前后端耦合度很高的项目结构来构建系统，因其后端服务的复杂度的提升必然带来了Web前端的复杂度的提升。</p><p>　　B端和S端从技术体系角度而言异构性很大，不适于同一个体系，将前后端放到一起很难做到专业分工，如果项目开发过程中管控不到位，就有可能会影响到整个项目的开发质量。因此Web前端从系统架构的角度也需要更加专业的管控，管控的作用之一就是前后端进行分离，降低前端对服务端的依耐性，做到专业化分工，提高项目的质量和开发效率。</p><p>　　各类用于web开发的语言都有自己的MVC框架，例如java就有Struts、SpringMVC等MVC框架。像这样的框架实际上做了太多的浏览器就可以完成的工作，例如：页面渲染。这也就限制了web前端技术的深入运用，很多的前端优化技术和提升用户体验的技术就很难派上用场。尽管MVC中的View层是想把界面开发工作专业化，让界面设计人员能专心于界面开发，但是传统的MVC框架下的View层的本质却是一个服务端技术。例如，java的web开发中的jsp，全称为Java Server Pages.其根本是一个简化的Servlet设计，它是java里动态网页的技术标准，这就说明jsp虽然看起来像html，其实它并不是真正的html，它需要被java的web容器进行解析转化为浏览器可以解析的html页面，然后通过网络传输到浏览器后，浏览器才能正确的展示这个jsp页面。我们使用它时候就是让web前端技术被服务端技术所绑架，这也就是为什么每个招聘web前端工程师的岗位都要问你是否会java，php语言的源头。随着互联网的发展，我们需要web前端更高的专业化，而不希望web前端工程师被服务端技术束缚的更多而限制了自身能力的发展，这就导致前后端分离技术的出现。</p><h3 id="什么是前后端分离？"><a href="#什么是前后端分离？" class="headerlink" title="什么是前后端分离？"></a>什么是前后端分离？</h3><p>　　所谓的前后端分离，其主要关注的重点就是页面的渲染工作。之前后端渲染好页面交给前端来显示。前后端分离后拼装html及页面渲染的事情完全交由前端来负责。后端只负责数据的处理。</p><h3 id="如何做到前后端分离？"><a href="#如何做到前后端分离？" class="headerlink" title="如何做到前后端分离？"></a>如何做到前后端分离？</h3><p>　　前面提到服务器端抢了许多前端做的事，我们需要的不是一个服务器端的MVC框架也不是一个前端的MVC框架，我们要的是一个总体的MVC，是一个传统web项目中MVC模式的进一步演进。控制层是前端和服务端的交互边界。因此要前后端解耦就要划清控制层的边界。我们看一下控制层主要做什么工作：</p><ol><li>控制路由，找到具体的模型层处理请求</li><li>http报文信息格式转换</li><li>参与页面渲染工作</li></ol><p>　　在MVC模式里控制层作用是调度而不是写业务逻辑的地方，控制层是前端和服务端通讯的桥梁，其实控制层是参入了前端的工作任务，既然控制层不做具体的业务逻辑实现并且涉及到了页面渲染，那么将控制层归为前端的一部分是合理的。<br>   要实现前后分离要做到以下几点：</p><h4 id="1-前端静态化"><a href="#1-前端静态化" class="headerlink" title="1. 前端静态化"></a>1. 前端静态化</h4><p>　　前端有且仅有静态内容，只有HTML&#x2F;CSS&#x2F;JS. 其内容来自于完全静态的资源而不需要任何后台技术进行动态化组装。前端内容的运行环境和引擎完全基于浏览器本身。</p><h4 id="2-后端数据化"><a href="#2-后端数据化" class="headerlink" title="2.后端数据化"></a>2.后端数据化</h4><p>　　后端可以用任何语言、技术和平台实现，但它们必须遵循一个原则：只提供数据，不提供任何和界面表现有关的内容。后台提供的数据可以用于任何其他客户端无论是本地化程序还是web程序、移动端程序。前后端通过后端实现符合RESTful风格的接口和交互Json数据来进行交互。</p><h4 id="3-工程-架构分离化"><a href="#3-工程-架构分离化" class="headerlink" title="3.工程&#x2F;架构分离化"></a>3.工程&#x2F;架构分离化</h4><p>　　前后端分离的终极目标应该是前端和服务端是完全独立的项目，由于前台是纯静态内容，大型构架方面可以考虑使用CDN。后端的RESTful Api方面可以考虑负载均衡。而数据,业务实现等可以考虑数据库优化和分布式缓存等。</p><hr><p>　　随着前端开发技术难度的越来越高，前端工程师也成为一个独立的技术岗位。而nodejs的出现更加促进了前后端的分离，node.js是出现在前端技术和服务端技术鸿沟之上的一座桥梁，为前后端两套不同技术体系进行真正意义的解耦提供了无限的可能性。前后端的分离就是不要让前端工程师被一些不可控的外在因素所影响（例如：前后端的耦合性），最后导致前端不能专心致志做出更加好的作品。所以，前后端分离是让前后端更加专业化一条必由之路。</p><p>　　前后分离以后，前后端的集成、部署、调试、服务间的相互调用都是我们要考虑的问题。</p><h2 id="所以，问题来了：前后端分离了，然后呢？"><a href="#所以，问题来了：前后端分离了，然后呢？" class="headerlink" title="所以，问题来了：前后端分离了，然后呢？"></a>所以，问题来了：前后端分离了，然后呢？</h2>]]></content>
      
      
      <categories>
          
          <category> 系统设计 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Architecture &amp; Design </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Hello SingleX</title>
      <link href="/2015/06/27/HelloSingle/"/>
      <url>/2015/06/27/HelloSingle/</url>
      
        <content type="html"><![CDATA[<p><strong>用Hexo搭建博客完毕，以后会将博客慢慢迁移至此。</strong><br><strong>感谢GitHub，感谢Hexo！</strong><br><strong>Hello SingleX!</strong></p>]]></content>
      
      
      <categories>
          
          <category> archives </category>
          
      </categories>
      
      
        <tags>
            
            <tag> single </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
